---
phase: 9
plan: 09-01
wave: 1
depends_on: [08-01]
requirements: [RND-02, PAL-01]
files_modified:
  - apps/workspaces/src/components/projectPage/EditableHeading.tsx
  - apps/workspaces/src/lib/projectPage/registry.tsx
  - apps/workspaces/src/components/projectPage/LivePageCanvas.tsx
  - apps/workspaces/src/lib/projectPage/slashMenu.ts
autonomous: true
---

# Phase 9 — Page Edit-Mode + Slash Derivation → Registry

**Goal:** the edit switch and the slash-menu derivation are registry-driven; contentEditable/slash UX unchanged. (RND-02, PAL-01)

## Tasks
- **09-01-01** Extract `EditableHeading` from `LivePageCanvas.tsx` into `components/projectPage/EditableHeading.tsx` (cycle avoidance — registry references it).
- **09-01-02** Add `editor` entries to the 4 edit kinds in `registry.tsx` (rich-text/list → `EditableRichText` with `slashItems`/`onSlashSelect` from `PageCtx`; heading → `EditableHeading`; kanban → `KanbanBoardView` w/ onUpdate). Extend the `entry()` helper with an optional `editor`. Reorder entries to canonical `PAGE_BLOCK_REGISTRY` order so `list()` preserves palette order.
- **09-01-03** Replace `BlockEditor`'s switch in `LivePageCanvas.tsx` with `pageBlockRegistry.renderEditor(block, { projectId, clientId, slashItems, onSlashSelect }, onUpdate)`. Leave `handleSlashSelect`, `isContentItem`, `PhantomFirstBlock`, `SortableBlockShell`, `DragOverlay`, `EditableRichText` untouched.
- **09-01-04** `slashMenu.ts` `buildSlashMenuItems` iterates `pageBlockRegistry.list()` (skip heading/list — hand-expanded), preserving `EXTRA_KEYWORDS` and variant expansion.

## Verification
- `npx tsc --noEmit` → 0; `npx next build` → 0, all 29 static pages generated (SSR eval confirms the registry↔EditableRichText↔slashMenu import cycle resolves).
- No `switch (block.kind)` in `LivePageCanvas.tsx`.

## must_haves
- No edit-mode switch in `LivePageCanvas`; slash menu derived from the registry.
- `/` still transforms empty content blocks in place; heading/list variants intact; identical `create` output; no persisted-JSON change.
