---
phase: 10
plan: 10-01
wave: 1
depends_on: [07-01]
requirements: [MPG-01, MPG-02]
files_modified:
  - apps/workspaces/src/components/miniProgram/miniProgramBlockViews.tsx
  - apps/workspaces/src/lib/miniProgram/registry.tsx
  - apps/workspaces/src/components/miniProgram/BlockView.tsx
autonomous: true
---

# Phase 10 — Mini Program onto the Engine

**Goal:** `BlockView` dispatches through the same `WorkspaceBlockRegistry` class; existing programs + manifest plugins unaffected. (MPG-01, MPG-02)

## Tasks
- **10-01-01** Extract the 11 player-side view components + helpers out of `BlockView.tsx` into `components/miniProgram/miniProgramBlockViews.tsx` (exported) — cycle avoidance.
- **10-01-02** New `lib/miniProgram/registry.tsx`: `MiniProgramCtx` (empty; views use `useRuntime()`); `keyOf = b => b.kind === 'plugin' ? b.pluginId : b.kind`; `renderManifest = block => <DynamicBlockView block={block} />`; static entries as `Record<Exclude<BlockKind,'plugin'>, …>` reusing `blockDef()` metadata + `create()` (code/columns/transform → `Invisible` renderer returning null).
- **10-01-03** Reduce `BlockView.tsx` to `return <>{miniProgramBlockRegistry.render(block, {})}</>`. Plugin blocks miss the static lookup → `renderManifest` → `DynamicBlockView` (→ `getPlugin`).

## Verification
- `npx tsc --noEmit` → 0; `npx next build` → 0, 29/29 static pages.
- No `switch (block.kind)` in `BlockView.tsx`.

## must_haves
- No switch in `BlockView`; both registries are instances of `WorkspaceBlockRegistry`.
- Manifest plugins render via `DynamicBlockView`; v2 program round-trip + page embed intact; no persisted-JSON change.
