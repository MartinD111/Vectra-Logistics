---
phase: 02-crm-dashboard-navigation-client-detail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/022_client_pages.sql
  - apps/api/src/domains/crm/crm.types.ts
  - apps/api/src/domains/crm/dto/create-client-page.dto.ts
  - apps/api/src/domains/crm/dto/update-client-page.dto.ts
  - apps/api/src/domains/crm/crm.repository.ts
  - apps/api/src/domains/crm/crm.service.ts
  - apps/api/src/domains/crm/crm.controller.ts
  - apps/api/src/domains/crm/crm.routes.ts
autonomous: true
requirements: [DET-01, DET-02, DET-03, DET-04]
must_haves:
  truths:
    - "A client_pages row can be fetched or created (get-or-create) for a given client, scoped to the requester's company"
    - "Requesting a client page for the same client twice never creates two rows"
    - "The client detail timeline endpoint returns a merged, date-sorted feed of emails, invoices, and KPI results for a client, even when all three sources are empty"
  artifacts:
    - path: "database/migrations/022_client_pages.sql"
      provides: "client_pages table, unique on client_id, mirrors project_pages minus hierarchy columns"
      contains: "CREATE TABLE IF NOT EXISTS client_pages"
    - path: "apps/api/src/domains/crm/crm.repository.ts"
      provides: "findClientPage, createClientPage, updateClientPage, getClientTimeline data-source queries"
      contains: "findClientPage"
    - path: "apps/api/src/domains/crm/crm.routes.ts"
      provides: "GET/POST /clients/:id/page, PATCH /client-pages/:pageId, GET /clients/:id/timeline"
      contains: "clients/:id/page"
  key_links:
    - from: "apps/api/src/domains/crm/crm.controller.ts"
      to: "apps/api/src/domains/crm/crm.service.ts"
      via: "getOrCreateClientPage / updateClientPage / getClientTimeline calls"
      pattern: "crmService\\.(getOrCreateClientPage|updateClientPage|getClientTimeline)"
    - from: "apps/api/src/domains/crm/crm.repository.ts"
      to: "database"
      via: "parameterized queries scoped by company_id"
      pattern: "WHERE .*company_id"
---

<objective>
Extend the existing `crm` API domain (established in Phase 1) with everything the client detail page needs on the backend: a new `client_pages` table (Notion-style block-canvas storage keyed on `client_id`, one row per client), CRUD endpoints for it with dedupe-safe get-or-create semantics, and a new timeline aggregation endpoint that merges `email_messages`, `invoices`, and `kpi_results` for a client into a single date-sorted feed.

Purpose: Give the frontend (Plan 03) a working data layer for DET-01 (client settings persistence — already covered by existing `clients` PATCH), DET-02 (current situation / emails — already stubbed from Phase 1), DET-03 (unified timeline), and DET-04 (create-or-open a client's detail page without duplicates).
Output: `022_client_pages.sql` migration applied to the dev DB; `crm` domain extended with `client_pages` CRUD + `getClientTimeline`; all new routes mounted under `/api/v1/crm`, inheriting existing `authenticateToken` middleware.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-CONTEXT.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-RESEARCH.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-PATTERNS.md

@database/migrations/009_project_pages.sql
@database/migrations/021_crm_extensions.sql
@apps/api/src/domains/crm/crm.types.ts
@apps/api/src/domains/crm/crm.repository.ts
@apps/api/src/domains/crm/crm.service.ts
@apps/api/src/domains/crm/crm.controller.ts
@apps/api/src/domains/crm/crm.routes.ts
@apps/api/src/domains/projects/projects.repository.ts
@apps/api/src/domains/projects/dto/page.dto.ts
@apps/api/src/domains/billing/billing.repository.ts
</context>

<interfaces>
<!-- Existing crm.types.ts contracts the executor must respect (do not rename): -->

From apps/api/src/domains/crm/crm.types.ts:
```typescript
export interface ClientRecord { id: string; company_id: string; name: string; country: string; vat_id: string | null; email: string | null; credit_limit: number; outstanding_balance: number; default_rate_eur: number | null; notes: string | null; address: string | null; responsible_employee_id: string | null; created_at: Date; updated_at: Date; }
```

From apps/api/src/domains/billing/billing.repository.ts (InvoiceRecord shape, for the timeline's invoice source — read-only, do not modify billing domain):
```typescript
export interface InvoiceRecord { id: string; company_id: string; client_id: string | null; number: string; description: string; amount_total: number; status: string; issued_at: string; /* ...other billing-only fields */ }
```
`invoices` table columns confirmed: `id, company_id, client_id, pod_request_id, number, description, amount_net, vat_treatment, vat_rate, vat_amount, amount_total, vat_note, currency, status, pod_url, issued_at, due_at, approved_at, paid_at, created_at, updated_at`.

`email_messages` table columns (migration 021, already exists): `id, company_id, client_id, outlook_id, sender_email, recipient_emails[], subject, body_preview, full_body, received_at, is_draft, synced_at, created_at`.

`kpi_results` table: has nullable `user_id`, nullable `client_id` (added in 021), `CHECK (user_id IS NOT NULL OR client_id IS NOT NULL)`. Do not assume any columns beyond what 021 confirms; query only `id, client_id, rule_name, actual_value, target_value, status, period_start` style columns consistent with the existing `useKpiSummary`/`kpi_results` reads elsewhere in the codebase — if uncertain of an exact column name, inspect `apps/api/src/domains/kpi/kpi.repository.ts` before writing the query.

Existing `crm.repository.ts` ON CONFLICT upsert pattern (mirror exactly, source: same file `upsertProjectLink`):
```typescript
async upsertProjectLink(companyId: string, d: {...}): Promise<ClientProjectLinkRecord> {
  const { rows } = await db.query<ClientProjectLinkRecord>(
    `INSERT INTO client_project_links (...) VALUES (...) ON CONFLICT (client_id, project_id) DO UPDATE SET ... RETURNING *`, [...]);
  return numProjectLink(rows[0]);
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: client_pages migration + repository/types/dto layer</name>
  <files>database/migrations/022_client_pages.sql, apps/api/src/domains/crm/crm.types.ts, apps/api/src/domains/crm/dto/create-client-page.dto.ts, apps/api/src/domains/crm/dto/update-client-page.dto.ts, apps/api/src/domains/crm/crm.repository.ts</files>
  <read_first>database/migrations/009_project_pages.sql, database/migrations/021_crm_extensions.sql, apps/api/src/domains/crm/crm.types.ts, apps/api/src/domains/crm/crm.repository.ts, apps/api/src/domains/projects/dto/page.dto.ts, apps/api/src/domains/projects/projects.repository.ts</read_first>
  <action>
Create `database/migrations/022_client_pages.sql` (idempotent, header comment describing purpose and "apply after 021"). Table `client_pages`: `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE`, `client_id UUID NOT NULL REFERENCES clients(id) ON DELETE CASCADE`, `title TEXT NOT NULL DEFAULT 'Untitled'`, `icon TEXT`, `config JSONB NOT NULL DEFAULT '{"version":1,"blocks":[]}'`, `cover_image_url TEXT`, `header_settings JSONB NOT NULL DEFAULT '{}'`, `created_by UUID REFERENCES users(id) ON DELETE SET NULL`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`. Add `CREATE UNIQUE INDEX IF NOT EXISTS client_pages_client_uniq ON client_pages (client_id)` (D-06: one page per client) and `CREATE INDEX IF NOT EXISTS client_pages_company_idx ON client_pages (company_id)`. Do NOT add `parent_page_id`, `is_default`, or `sort_order` columns (no hierarchy per D-05/D-06).

Run the migration against the dev DB using the same mechanism used for migration 021 (check `database/migrations/` directory or `docker-compose.yml`/README for the exact run command — e.g. `psql` against the running Postgres container, or an existing npm script such as `npm run migrate`). Confirm via a `\d client_pages` or equivalent query that the table exists before moving on.

In `apps/api/src/domains/crm/crm.types.ts`, add `export interface ClientPageRecord { id: string; company_id: string; client_id: string; title: string; icon: string | null; config: Record<string, unknown>; cover_image_url: string | null; header_settings: Record<string, unknown>; created_by: string | null; created_at: Date; updated_at: Date; }`. Also add `export interface ClientTimelineEntry { type: 'email' | 'invoice' | 'kpi'; id: string; occurred_at: string; summary: string; }` for the Task 2 timeline endpoint.

Create `apps/api/src/domains/crm/dto/create-client-page.dto.ts` mirroring `apps/api/src/domains/projects/dto/page.dto.ts`'s `CreatePageSchema` exactly but WITHOUT `is_default`/`parent_page_id` fields: `title` (`z.string().min(1).max(120).optional()`), `icon` (`z.string().max(16).nullable().optional()`), `config` (same envelope-only `PageConfigSchema` — `z.object({ version: z.number().int(), blocks: z.array(z.unknown()) }).catchall(z.unknown())`), `cover_image_url` (`z.string().max(2048).nullable().optional()`), `header_settings` (same envelope `HeaderSettingsSchema` as page.dto.ts). Export `CreateClientPageSchema` and `export type CreateClientPageDto = z.infer<typeof CreateClientPageSchema>`.

Create `apps/api/src/domains/crm/dto/update-client-page.dto.ts` with the same shape as create (all fields already optional in create, so `UpdateClientPageSchema` can be `CreateClientPageSchema` directly re-exported or a `.partial()` — since all fields in create are already optional, define `export const UpdateClientPageSchema = CreateClientPageSchema;` and `export type UpdateClientPageDto = z.infer<typeof UpdateClientPageSchema>`).

In `apps/api/src/domains/crm/crm.repository.ts`, add three methods to the `CrmRepository` class:
- `findClientPage(clientId: string, companyId: string): Promise<ClientPageRecord | null>` — `SELECT * FROM client_pages WHERE client_id = $1 AND company_id = $2`.
- `createClientPage(companyId: string, clientId: string, createdBy: string | null, data: { title?: string; icon?: string | null; config?: Record<string, unknown> }): Promise<ClientPageRecord>` — `INSERT INTO client_pages (company_id, client_id, title, icon, config, created_by) VALUES ($1,$2,$3,$4,$5,$6) ON CONFLICT (client_id) DO UPDATE SET updated_at = NOW() RETURNING *` (dedupe race safety per D-08 — the `ON CONFLICT` clause guarantees a single row is ever returned even under concurrent calls; see Pitfall 2 in RESEARCH.md).
- `updateClientPage(pageId: string, companyId: string, data: { title?: string; icon?: string | null; config?: Record<string, unknown>; cover_image_url?: string | null; header_settings?: Record<string, unknown> }): Promise<ClientPageRecord | null>` — follow the nullable-field `CASE WHEN $n::boolean THEN ... ELSE ... END` pattern from `projects.repository.ts`'s `updatePage` (lines 221-244) for `icon`/`cover_image_url`; use `COALESCE` for `title`/`config`/`header_settings`. Scope by `WHERE id = $1 AND company_id = $2`.

Import `ClientPageRecord` at the top of `crm.repository.ts`.
  </action>
  <acceptance_criteria>
    <source>database/migrations/022_client_pages.sql contains "CREATE TABLE IF NOT EXISTS client_pages" and "CREATE UNIQUE INDEX IF NOT EXISTS client_pages_client_uniq ON client_pages (client_id)"</source>
    <source>database/migrations/022_client_pages.sql does NOT contain "parent_page_id" or "is_default" or "sort_order"</source>
    <source>apps/api/src/domains/crm/crm.types.ts exports "ClientPageRecord" and "ClientTimelineEntry"</source>
    <source>apps/api/src/domains/crm/crm.repository.ts contains "findClientPage", "createClientPage", "updateClientPage", and "ON CONFLICT (client_id)"</source>
    <source>apps/api/src/domains/crm/dto/create-client-page.dto.ts exports "CreateClientPageSchema" and does not include "is_default" or "parent_page_id" fields</source>
    <behavior>Migration runs cleanly against the dev Postgres instance with no errors, and re-running it a second time also succeeds (idempotent)</behavior>
  </acceptance_criteria>
  <verify>
    <automated>psql or equivalent: table `client_pages` exists with a unique index on `client_id` — run the migration file twice against the dev DB and confirm no error on the second run</automated>
  </verify>
  <done>client_pages table exists in the dev database with the correct shape; crm.repository.ts has three new methods compiling against ClientPageRecord; DTO files created following the page.dto.ts envelope-only convention.</done>
</task>

<task type="auto">
  <name>Task 2: crm.service.ts get-or-create + timeline aggregation, controller, routes</name>
  <files>apps/api/src/domains/crm/crm.service.ts, apps/api/src/domains/crm/crm.controller.ts, apps/api/src/domains/crm/crm.routes.ts</files>
  <read_first>apps/api/src/domains/crm/crm.service.ts, apps/api/src/domains/crm/crm.controller.ts, apps/api/src/domains/crm/crm.routes.ts, apps/api/src/core/errors/AppError.ts, apps/api/src/core/errors/asyncHandler.ts, apps/api/src/domains/kpi/kpi.repository.ts</read_first>
  <action>
In `apps/api/src/domains/crm/crm.service.ts`, add:
- `async getOrCreateClientPage(clientId: string, companyId: string, createdBy: string | null): Promise<ClientPageRecord>` — calls `this.getClient(clientId, companyId)` first (404s if the client isn't in this company), then `crmRepository.findClientPage`, returning it if found, otherwise `crmRepository.createClientPage(companyId, clientId, createdBy, {})`.
- `async updateClientPage(pageId: string, companyId: string, body: unknown): Promise<ClientPageRecord>` — parse `body` with `UpdateClientPageSchema.safeParse`, throw `AppError(400, parsed.error.issues[0].message)` on failure, call `crmRepository.updateClientPage(pageId, companyId, parsed.data)`, throw `AppError(404, 'Client page not found')` if the repository returns null.
- `async getClientTimeline(clientId: string, companyId: string): Promise<ClientTimelineEntry[]>` — calls `this.getClient(clientId, companyId)` for the ownership check, then queries three sources directly via `db` (import `db` from `../../core/db` at the top of `crm.service.ts`, matching the convention already used in `crm.repository.ts`) OR — preferred, to keep the service layer free of raw SQL — add three small repository methods (`listClientEmails`, `listClientInvoices`, `listClientKpiResults`) to `crm.repository.ts` instead and call those from the service. Choose the repository-method approach for consistency with the rest of the domain's layering (controller → service → repository, repository owns all SQL). Each repository method takes `(clientId: string, companyId: string)` and returns rows scoped by `WHERE client_id = $1 AND company_id = $2`, limited to the most recent 50 rows ordered by their date column descending: `email_messages` → `received_at`; `invoices` → `issued_at`; `kpi_results` → whatever timestamp column `apps/api/src/domains/kpi/kpi.repository.ts` uses for period/computed date (inspect that file first — do not guess the column name). Map each source into `ClientTimelineEntry` shape (`type`, `id`, `occurred_at`, `summary`) in the service layer: emails → `summary: `Email: ${subject}``, invoices → `summary: `Invoice ${number} — €${amount_total}``, kpi → `summary: `${rule_name}: ${status}``. Merge the three arrays and sort by `occurred_at` descending before returning. Empty inputs must produce an empty array without throwing (Pitfall 3 in RESEARCH.md — zero KPI rows today is correct, not a bug).

In `apps/api/src/domains/crm/crm.controller.ts`, add three handlers following the existing `asyncHandler` + `requireCompany` pattern:
- `getClientPage = asyncHandler(async (req, res) => { res.status(200).json({ page: await crmService.getOrCreateClientPage(req.params.id, requireCompany(req), req.user?.id ?? null) }); })`
- `updateClientPage = asyncHandler(async (req, res) => { res.status(200).json({ page: await crmService.updateClientPage(req.params.pageId, requireCompany(req), req.body) }); })`
- `getClientTimeline = asyncHandler(async (req, res) => { res.status(200).json({ timeline: await crmService.getClientTimeline(req.params.id, requireCompany(req)) }); })`

In `apps/api/src/domains/crm/crm.routes.ts`, import the three new controller functions and register routes: `router.get('/clients/:id/page', getClientPage)`, `router.post('/clients/:id/page', getClientPage)` (POST and GET both resolve to the same get-or-create handler, since D-07/D-08 require idempotent "get or create" semantics regardless of HTTP verb used by the frontend's create-vs-fetch call sites), `router.patch('/client-pages/:pageId', updateClientPage)`, `router.get('/clients/:id/timeline', getClientTimeline)`. Place these route registrations near the existing `/clients/:id/projects` block, before the final `export default router` line. `authenticateToken` is already applied globally via `router.use(authenticateToken)` at the top of the file — do not add per-route auth middleware.
  </action>
  <acceptance_criteria>
    <source>apps/api/src/domains/crm/crm.service.ts contains "getOrCreateClientPage", "updateClientPage", and "getClientTimeline"</source>
    <source>apps/api/src/domains/crm/crm.controller.ts contains "getClientPage", "updateClientPage", "getClientTimeline" each wrapped in "asyncHandler"</source>
    <source>apps/api/src/domains/crm/crm.routes.ts contains "clients/:id/page", "client-pages/:pageId", and "clients/:id/timeline"</source>
    <behavior>GET /api/v1/crm/clients/:id/page called twice in a row for the same client returns the same page id both times (no duplicate row created)</behavior>
    <behavior>GET /api/v1/crm/clients/:id/timeline for a client with zero emails, zero invoices, and zero kpi_results returns an empty array with HTTP 200 (not an error)</behavior>
  </acceptance_criteria>
  <verify>
    <automated>Manual: start the API dev server, curl -X GET http://localhost:PORT/api/v1/crm/clients/{seeded-client-id}/page with a valid bearer token twice, confirm identical `page.id` in both responses; curl the /timeline endpoint and confirm `{"timeline":[]}` for a client with no synced data</automated>
  </verify>
  <done>All three new endpoints respond correctly through the full controller→service→repository chain; getOrCreateClientPage is idempotent under repeated calls; getClientTimeline never throws on empty data sources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → crm API domain | Authenticated JWT bearer requests; company_id derived from token, never from request body/params |
| client_pages / timeline queries → Postgres | Parameterized queries only, no string interpolation of user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege / Information Disclosure | crm.repository.ts findClientPage/updateClientPage/timeline queries | mitigate | Every new query filters `WHERE ... AND company_id = $N` exactly like existing `findClient` — never trust `client_id`/`page_id` alone |
| T-02-02 | Tampering | create-client-page.dto.ts / update-client-page.dto.ts | mitigate | Envelope-only Zod validation on `config`/`header_settings` (frontend owns block semantics), matching `page.dto.ts` exactly — rejects malformed non-object payloads before they reach the repository |
| T-02-03 | Denial of Service (race) | createClientPage insert path | mitigate | `ON CONFLICT (client_id) DO UPDATE` on the unique index guarantees exactly one row per client even under concurrent create requests (D-08) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — confirmed via RESEARCH.md Package Legitimacy Audit (not applicable, zero new dependencies) |
</threat_model>

<verification>
1. `022_client_pages.sql` applies cleanly twice in a row against the dev DB (idempotency check).
2. `GET /api/v1/crm/clients/:id/page` called repeatedly for the same client_id never creates more than one `client_pages` row (verify via `SELECT COUNT(*) FROM client_pages WHERE client_id = $1` after 2+ calls — must equal 1).
3. `GET /api/v1/crm/clients/:id/timeline` returns `{"timeline": []}` for a client with no emails/invoices/kpi_results, and returns a date-descending merged array once seed data exists in any of the three source tables.
4. `PATCH /api/v1/crm/client-pages/:pageId` persists `config`/`title`/`icon` updates and returns the updated row.
</verification>

<success_criteria>
- client_pages table exists, unique on client_id, mirrors project_pages minus hierarchy columns.
- crm domain exposes get-or-create client page, update client page, and unified timeline endpoints, all scoped by company_id.
- No duplicate client_pages rows possible under concurrent "New client page" clicks (satisfies D-08 ahead of Plan 04's frontend entry point).
</success_criteria>

<output>
Create `.planning/phases/02-crm-dashboard-navigation-client-detail/02-01-SUMMARY.md` when done
</output>
