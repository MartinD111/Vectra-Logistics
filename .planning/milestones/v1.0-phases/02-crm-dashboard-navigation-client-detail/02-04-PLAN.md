---
phase: 02-crm-dashboard-navigation-client-detail
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - apps/workspaces/src/components/projectPage/NewClientPageModal.tsx
  - apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx
autonomous: true
requirements: [DET-04]
must_haves:
  truths:
    - "User can create a new client's detail page directly from the project page creator, not only from the CRM dashboard"
    - "Picking an already-paged client from the creator opens the existing detail page instead of creating a duplicate"
  artifacts:
    - path: "apps/workspaces/src/components/projectPage/NewClientPageModal.tsx"
      provides: "Picker modal: search existing client or quick-create, then get-or-create the client's detail page and open it in a new tab"
      min_lines: 40
  key_links:
    - from: "apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx"
      to: "apps/workspaces/src/components/projectPage/NewClientPageModal.tsx"
      via: "\"New client page\" trigger button opens the modal"
      pattern: "NewClientPageModal"
    - from: "apps/workspaces/src/components/projectPage/NewClientPageModal.tsx"
      to: "apps/workspaces/src/lib/hooks/useCrm.ts"
      via: "useClientPage(clientId)/getClientPage get-or-create call, then window.open the result"
      pattern: "getClientPage|useClientPage"
---

<objective>
Add the missing DET-04 entry point: a "New client page" trigger next to the existing "+ Sub-page" button on the project page, opening a small picker modal (search existing clients, or quick-create a new one), which then gets-or-creates that client's `client_pages` row (via Plan 01's dedupe-safe backend endpoint) and opens it in a new tab. No existing "add page" menu component is being extended — this is genuinely new UI, per RESEARCH.md's explicit correction of the original CONTEXT.md assumption.

Purpose: Satisfies DET-04 — users must be able to create a client's detail page from the Notion-like project page creator, not only from the CRM dashboard (Plan 02).
Output: `NewClientPageModal.tsx` (search + quick-create + open-or-create flow); one new button wired into the existing project page's toolbar next to "+ Sub-page".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-CONTEXT.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-RESEARCH.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-UI-SPEC.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-PATTERNS.md

@apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx
@apps/workspaces/src/lib/hooks/useCrm.ts
@apps/workspaces/src/lib/api/crm.api.ts
</context>

<interfaces>
<!-- Hooks this plan consumes, all already built in Plan 01 (backend) / Plan 03 (frontend) -->

From apps/workspaces/src/lib/hooks/useCrm.ts (existing, Phase 1):
```typescript
export function useClients(): UseQueryResult<CrmClient[]>
export function useCreateClient(): UseMutationResult<CrmClient, unknown, CreateClientInput>
```

From apps/workspaces/src/lib/hooks/useCrm.ts (added in Plan 03):
```typescript
export function useClientPage(clientId: string): UseQueryResult<ClientPage>   // GET, get-or-create semantics server-side
```
Note: the backend's `GET /clients/:id/page` route (Plan 01) already performs get-or-create — calling `crmApi.getClientPage(clientId)` directly (via `useClientPage` or an ad-hoc `crmApi.getClientPage(clientId)` promise call) is sufficient to satisfy D-07/D-08; no separate "create" call is needed from the frontend.

Existing "+ Sub-page" button style to copy visually (source: apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx, current toolbar row):
```tsx
<button onClick={...} className="inline-flex items-center gap-2 px-4 py-2 rounded-xl bg-white dark:bg-dark-card border border-gray-200 dark:border-dark-border text-gray-900 dark:text-white text-sm font-semibold hover:bg-gray-50 disabled:opacity-60">
  <Plus className="w-4 h-4" /> Sub-page
</button>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: NewClientPageModal — search/quick-create picker with get-or-create-then-open flow</name>
  <files>apps/workspaces/src/components/projectPage/NewClientPageModal.tsx</files>
  <read_first>apps/workspaces/src/components/projectPage/CrmClientsBlock.tsx, apps/workspaces/src/lib/hooks/useCrm.ts, apps/workspaces/src/lib/api/crm.api.ts</read_first>
  <action>
Create `apps/workspaces/src/components/projectPage/NewClientPageModal.tsx` — a new modal component, no existing analog (confirmed in RESEARCH.md/PATTERNS.md: no "add page" menu component exists to extend). Props: `{ open: boolean; onClose: () => void }`.

Modal heading per Copywriting Contract: "Choose a client". Body: a search input (reuse `saas-input` styling) filtering `useClients()`'s result client-side by name substring (same client-side filter approach as Plan 02's dashboard, per RESEARCH.md's Alternative A2). Render matching clients as a selectable list (name + country badge, reusing the same small badge style as elsewhere in this codebase). Below the list, if the search text doesn't match any existing client (or always, as a persistent fallback), show a "Create new client" button/row per the Copywriting Contract — clicking it reveals the same minimal quick-create field set as `AddClientModal` from Plan 02 (name, country, VAT ID, credit limit, default rate) using `useCreateClient()`.

Selection flow (D-07/D-08 — get-or-create, no duplicates):
1. User selects an existing client from the list, OR quick-creates a new one (in which case, on `useCreateClient()`'s success callback, treat the newly created client as "selected").
2. On selection, call `crmApi.getClientPage(selectedClientId)` directly (import `crmApi` from `@/lib/api/crm.api`; this is the same get-or-create endpoint `useClientPage` wraps — calling the API function directly here avoids needing a mounted query for a one-shot action) and await the result.
3. On success, call `window.open(\`/records/${selectedClientId}\`, '_blank', 'noopener,noreferrer')` (same same-app new-tab pattern as Plan 02's dashboard row click — NOT `crossAppUrl`) and then `onClose()`.
4. Show a loading indicator on the selected row/button while the get-or-create call is in flight; on failure, show a small inline error message and allow retry (do not close the modal automatically on failure).

Modal shell styling: centered overlay (`fixed inset-0 z-40 bg-black/40 flex items-center justify-center`), `saas-card` surface `max-w-md w-full mx-4`, close via backdrop click or an `X` icon button, matching `AddClientModal`'s shell conventions from Plan 02 for visual consistency (both are new modals introduced in this phase).
  </action>
  <acceptance_criteria>
    <source>apps/workspaces/src/components/projectPage/NewClientPageModal.tsx contains "Choose a client" and "Create new client"</source>
    <source>apps/workspaces/src/components/projectPage/NewClientPageModal.tsx contains "getClientPage" and "window.open" and does not import "crossAppUrl"</source>
    <source>apps/workspaces/src/components/projectPage/NewClientPageModal.tsx imports "useClients" and "useCreateClient" from "@/lib/hooks/useCrm"</source>
    <behavior>Selecting an existing client and confirming opens /records/{thatClientId} in a new tab</behavior>
    <behavior>Quick-creating a new client (name only, other fields optional/defaulted) then selecting it opens its freshly-created detail page in a new tab</behavior>
    <behavior>Selecting the same already-paged client twice in two separate modal sessions opens the same detail page both times (verified by the underlying getClientPage call being idempotent per Plan 01's backend contract — no duplicate client_pages row is created)</behavior>
  </acceptance_criteria>
  <verify>
    <automated>MISSING — no test framework in repo. Manual: npm run dev, open any project page, trigger the (Task 2) "New client page" button, search for an existing client, select it, confirm a new tab opens at /records/{id}; repeat for the same client and confirm no duplicate client_pages row is created (spot-check via a DB query: SELECT COUNT(*) FROM client_pages WHERE client_id = $1 should remain 1)</automated>
  </verify>
  <done>NewClientPageModal supports search-existing + quick-create-new, and both paths end in a single get-or-create call followed by opening the resulting detail page in a new tab, with no duplicate-page risk.</done>
</task>

<task type="auto">
  <name>Task 2: Wire "New client page" trigger into the project page toolbar</name>
  <files>apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx</files>
  <read_first>apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx</read_first>
  <action>
In `apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx`, add a `newClientPageOpen` boolean state (`useState(false)`). In the existing toolbar row (the `flex items-center gap-2` div currently containing the "Saved"/spinner indicator and the "+ Sub-page" button), add a new button immediately before or after the "+ Sub-page" button: visual style copied from the "+ Sub-page" button exactly (`inline-flex items-center gap-2 px-4 py-2 rounded-xl bg-white dark:bg-dark-card border border-gray-200 dark:border-dark-border text-gray-900 dark:text-white text-sm font-semibold hover:bg-gray-50`), icon `Building2` or `UserPlus` from `lucide-react` (Claude's discretion per CONTEXT.md — pick whichever reads more clearly as "client", `Building2` is already used elsewhere in this codebase for clients so prefer it for visual consistency), label "New client page" (per Copywriting Contract). `onClick={() => setNewClientPageOpen(true)}`.

Import `NewClientPageModal` from `./NewClientPageModal` (relative to `components/projectPage/`, matching existing sibling-component import conventions in this file) — actual import path is `@/components/projectPage/NewClientPageModal` per this codebase's alias convention. Render `<NewClientPageModal open={newClientPageOpen} onClose={() => setNewClientPageOpen(false)} />` near the end of the component's JSX tree, alongside any other modal-like conditionally-rendered elements.

Do not alter any existing sub-page creation logic (`addSubPageUnder`, `createSubPage`) — this is a purely additive second entry point living alongside it, not a replacement.
  </action>
  <acceptance_criteria>
    <source>apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx contains "New client page" and imports "NewClientPageModal"</source>
    <source>apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx still contains "Sub-page" and "addSubPageUnder" unchanged</source>
    <behavior>Visiting any existing project page shows both the "+ Sub-page" button and the new "New client page" button in the toolbar; clicking the latter opens the NewClientPageModal</behavior>
  </acceptance_criteria>
  <verify>
    <automated>MISSING — no test framework in repo. Manual: npm run dev, open any project page, confirm both toolbar buttons render and the new one opens the modal; confirm existing "+ Sub-page" behavior is unaffected</automated>
  </verify>
  <done>"New client page" button is live on every project page's toolbar, opens the picker modal, and does not interfere with existing sub-page creation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (project page toolbar) → crm API get-or-create client page endpoint | Authenticated JWT bearer; company_id enforced server-side (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Denial of Service (duplicate-page race) | NewClientPageModal double-click / double-submit | mitigate | Relies on Plan 01's `ON CONFLICT (client_id) DO UPDATE` unique-constraint guarantee at the database layer — even if the frontend fires two get-or-create calls in quick succession, only one `client_pages` row can ever exist per client |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
1. "New client page" button appears on every project page's toolbar, styled consistently with "+ Sub-page".
2. Selecting an existing client from the picker opens that client's detail page in a new tab.
3. Quick-creating a new client from the picker, then selecting it, creates exactly one `client_pages` row and opens it in a new tab.
4. Re-triggering the flow for an already-paged client opens the same page — no duplicate `client_pages` row is ever created (D-08).
5. Existing "+ Sub-page" / sub-page creation behavior is unaffected by this plan's changes.
</verification>

<success_criteria>
DET-04 fully satisfied: users can create (or re-open) a client's detail page from the Notion-like project page creator, not only from the CRM dashboard, with no duplicate-page risk under repeated or concurrent selection.
</success_criteria>

<output>
Create `.planning/phases/02-crm-dashboard-navigation-client-detail/02-04-SUMMARY.md` when done
</output>
