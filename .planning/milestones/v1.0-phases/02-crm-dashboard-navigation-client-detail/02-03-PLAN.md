---
phase: 02-crm-dashboard-navigation-client-detail
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/workspaces/src/lib/api/crm.api.ts
  - apps/workspaces/src/lib/hooks/useCrm.ts
  - apps/workspaces/src/lib/projectPage/blocks.ts
  - apps/workspaces/src/components/projectPage/LivePageCanvas.tsx
  - apps/workspaces/src/components/projectPage/PageBlockView.tsx
  - apps/workspaces/src/components/projectPage/ClientCurrentSituationBlock.tsx
  - apps/workspaces/src/components/projectPage/ClientTimelineBlock.tsx
  - apps/workspaces/src/app/records/[clientId]/page.tsx
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, DET-01, DET-02, DET-03]
must_haves:
  truths:
    - "User can open a client's detail page at /records/[clientId] showing a sidebar with address, notes, and responsible employee"
    - "User can inline-edit address and notes with autosave on blur/debounce, and pick a responsible employee from the full team list"
    - "The detail page's 'current situation' section renders a visible empty state ('No emails synced yet') since no emails exist until Phase 5"
    - "The detail page's timeline section renders a single unified chronological feed (or its empty state 'No activity yet') merging emails, invoices, and KPI results"
  artifacts:
    - path: "apps/workspaces/src/app/records/[clientId]/page.tsx"
      provides: "Client detail page: sidebar (address/notes/responsible employee) + block canvas (current-situation, timeline)"
      min_lines: 60
    - path: "apps/workspaces/src/components/projectPage/ClientCurrentSituationBlock.tsx"
      provides: "Current situation block renderer with empty-state per D-11"
      contains: "No emails synced yet"
    - path: "apps/workspaces/src/components/projectPage/ClientTimelineBlock.tsx"
      provides: "Unified timeline block renderer, icon-tagged by entry type, per D-12"
      contains: "No activity yet"
  key_links:
    - from: "apps/workspaces/src/app/records/[clientId]/page.tsx"
      to: "apps/workspaces/src/lib/hooks/useCrm.ts"
      via: "useClientPage(clientId) get-or-create fetch + useUpdateClientPage for canvas autosave"
      pattern: "useClientPage\\(|useUpdateClientPage\\("
    - from: "apps/workspaces/src/components/projectPage/PageBlockView.tsx"
      to: "apps/workspaces/src/components/projectPage/ClientCurrentSituationBlock.tsx"
      via: "dispatch case 'client-current-situation'"
      pattern: "client-current-situation"
    - from: "apps/workspaces/src/app/records/[clientId]/page.tsx"
      to: "apps/workspaces/src/lib/hooks/useCrm.ts"
      via: "useUpdateClient for sidebar field autosave (address/notes/responsible_employee_id)"
      pattern: "useUpdateClient\\(\\)"
---

<objective>
Build the client detail page itself: a two-column layout (D-09) with a persistent sidebar for inline-editable client fields (address, notes, responsible employee — CLI-01/02/03, DET-01) and a main block canvas reusing `LivePageCanvas`/`PageBlockView` for two new block kinds — "current situation" (last 10 emails, DET-02) and "timeline" (unified emails+invoices+KPI feed, DET-03). Both new blocks render correctly with a visible empty state today (no email data exists until Phase 5).

Purpose: This is the core "client home" experience the roadmap phase goal describes — a full page per client that opens in a new tab from the dashboard (Plan 02) and is reachable from the project page creator (Plan 04).
Output: `/records/[clientId]` route; extended `crm.api.ts`/`useCrm.ts` with client-page and timeline hooks; two new `PageBlockKind` union members with working renderers; additive `clientId` prop threaded through `LivePageCanvas`/`PageBlockView` without touching any existing project-page block kind's behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-CONTEXT.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-RESEARCH.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-UI-SPEC.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-PATTERNS.md

@apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx
@apps/workspaces/src/components/projectPage/PageHeader.tsx
@apps/workspaces/src/components/projectPage/LivePageCanvas.tsx
@apps/workspaces/src/components/projectPage/PageBlockView.tsx
@apps/workspaces/src/lib/projectPage/blocks.ts
@apps/workspaces/src/lib/hooks/useCrm.ts
@apps/workspaces/src/lib/api/crm.api.ts
@apps/workspaces/src/lib/hooks/useTeam.ts
@apps/workspaces/src/lib/hooks/useProjectPages.ts
</context>

<interfaces>
<!-- Backend contracts this plan consumes, built in 02-01-PLAN.md -->

New crm domain endpoints (apps/api/src/domains/crm/crm.routes.ts, from Plan 01):
```
GET/POST /api/v1/crm/clients/:id/page   -> { page: ClientPageRecord }  (get-or-create)
PATCH    /api/v1/crm/client-pages/:pageId -> { page: ClientPageRecord }
GET      /api/v1/crm/clients/:id/timeline -> { timeline: ClientTimelineEntry[] }
```

ClientPageRecord shape (apps/api/src/domains/crm/crm.types.ts, from Plan 01):
```typescript
export interface ClientPageRecord { id: string; company_id: string; client_id: string; title: string; icon: string | null; config: Record<string, unknown>; cover_image_url: string | null; header_settings: Record<string, unknown>; created_by: string | null; created_at: Date; updated_at: Date; }
export interface ClientTimelineEntry { type: 'email' | 'invoice' | 'kpi'; id: string; occurred_at: string; summary: string; }
```

Existing hooks reused as-is (no changes):
```typescript
// apps/workspaces/src/lib/hooks/useCrm.ts
export function useClient(id: string): UseQueryResult<CrmClient>
export function useUpdateClient(): UseMutationResult<CrmClient, unknown, { id: string; data: Partial<CreateClientInput> }>
// apps/workspaces/src/lib/hooks/useTeam.ts
export function useTeam(): UseQueryResult<TeamMember[]>  // flat list, no role filter — matches D-13
```

Debounced-autosave pattern to copy verbatim (source: apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx lines 44-87) — seed-once state, 1500ms debounce timer, unmount-flush effect.

Uncontrolled-contentEditable inline-edit pattern to adapt (source: apps/workspaces/src/components/projectPage/PageHeader.tsx lines 215-239, `EditableTitle`) — sync only while unfocused, commit on blur; D-10 additionally wants a ~800ms debounce-while-typing variant, not just blur-commit.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend crm.api.ts + useCrm.ts with client-page and timeline hooks; register two new block kinds</name>
  <files>apps/workspaces/src/lib/api/crm.api.ts, apps/workspaces/src/lib/hooks/useCrm.ts, apps/workspaces/src/lib/projectPage/blocks.ts</files>
  <read_first>apps/workspaces/src/lib/api/crm.api.ts, apps/workspaces/src/lib/hooks/useCrm.ts, apps/workspaces/src/lib/hooks/useProjectPages.ts, apps/workspaces/src/lib/projectPage/blocks.ts</read_first>
  <action>
In apps/workspaces/src/lib/api/crm.api.ts, add a ClientPage interface mirroring the backend ClientPageRecord (id, company_id, client_id, title, icon, config, cover_image_url, header_settings, created_at, updated_at as strings for dates) and a ClientTimelineEntry interface (type: 'email' | 'invoice' | 'kpi', id, occurred_at, summary). Add to the crmApi object: getClientPage(clientId) doing a GET to BASE/clients/{clientId}/page returning page; updateClientPage(pageId, data) doing a PATCH to BASE/client-pages/{pageId} with a Partial of title/icon/config/cover_image_url/header_settings, returning page; getClientTimeline(clientId) doing a GET to BASE/clients/{clientId}/timeline returning timeline. Reuse the existing getClientEmails method already present from Phase 1 as-is (its current return type is unknown[] — leave unchanged unless it blocks Task 2, in which case narrow it to an inline shape matching { id, sender_email, subject, received_at }).

In apps/workspaces/src/lib/hooks/useCrm.ts, add clientPage(clientId) and clientTimeline(clientId) key factories to the qk object, each returning a tuple keyed on the id. Add four hooks: useClientPage(clientId) wrapping crmApi.getClientPage via useQuery, enabled on company_id and clientId; useUpdateClientPage(pageId, clientId) wrapping crmApi.updateClientPage via useMutation, on success calling qc.setQueryData(qk.clientPage(clientId), page); useClientTimeline(clientId) wrapping crmApi.getClientTimeline via useQuery; useClientEmails(clientId) wrapping the existing crmApi.getClientEmails via useQuery (new hook, existing API method).

In apps/workspaces/src/lib/projectPage/blocks.ts, add two members to the PageBlockKind union: 'client-current-situation' and 'client-timeline', in a new comment-delimited section directly after the existing union list — keep every existing member unchanged. Add two new interfaces extending PageBlockBase: ClientCurrentSituationBlock (kind: 'client-current-situation', optional title) and ClientTimelineBlock (kind: 'client-timeline', optional title). Add both to the PageBlock union type. Register both in PAGE_BLOCK_REGISTRY with group 'widget', available true, icon 'Mail' and 'Clock' respectively, titles "Current situation" / "Timeline", descriptions "Last 10 emails sent to this client." / "Unified feed of emails, invoices, and KPI activity for this client.", and create factories producing { id: uid(), kind, span: 'full', title: <same title> }.

Do NOT repoint the existing 'activity-timeline' kind at client data (Pitfall 1 in RESEARCH.md — it is keyed on project_id via useProjectActivity and has no email/invoice/KPI merge concept). These are genuinely new kinds.
  </action>
  <acceptance_criteria>
    <source>apps/workspaces/src/lib/api/crm.api.ts exports "ClientPage", "ClientTimelineEntry", and crmApi contains "getClientPage", "updateClientPage", "getClientTimeline"</source>
    <source>apps/workspaces/src/lib/hooks/useCrm.ts exports "useClientPage", "useUpdateClientPage", "useClientTimeline", "useClientEmails"</source>
    <source>apps/workspaces/src/lib/projectPage/blocks.ts PageBlockKind union contains "'client-current-situation'" and "'client-timeline'"</source>
    <source>apps/workspaces/src/lib/projectPage/blocks.ts PAGE_BLOCK_REGISTRY contains two entries with kind "client-current-situation" and "client-timeline"</source>
    <behavior>TypeScript compiles with no new type errors introduced in blocks.ts, crm.api.ts, or useCrm.ts</behavior>
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces &amp;&amp; npx tsc --noEmit (or equivalent existing type-check script) — no new errors in the three modified files</automated>
  </verify>
  <done>Two new block kinds are registered end-to-end (type union, interface, registry entry); crm.api.ts/useCrm.ts expose typed hooks matching Plan 01's backend contract exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Additive clientId prop on LivePageCanvas/PageBlockView + two new block renderers</name>
  <files>apps/workspaces/src/components/projectPage/LivePageCanvas.tsx, apps/workspaces/src/components/projectPage/PageBlockView.tsx, apps/workspaces/src/components/projectPage/ClientCurrentSituationBlock.tsx, apps/workspaces/src/components/projectPage/ClientTimelineBlock.tsx</files>
  <read_first>apps/workspaces/src/components/projectPage/LivePageCanvas.tsx, apps/workspaces/src/components/projectPage/PageBlockView.tsx</read_first>
  <action>
In apps/workspaces/src/components/projectPage/LivePageCanvas.tsx, change the component's prop type from requiring projectId: string to an additive form where both projectId and clientId are optional strings (projectId?: string, clientId?: string), keeping config and onChange unchanged. Thread clientId down alongside the existing projectId prop everywhere projectId is currently passed internally — to SortableBlockShell, BlockEditor, PageBlockView, and SettingsPopover — adding clientId?: string to each of those internal components' prop signatures without removing projectId from any of them. All existing project-page block kinds continue reading only projectId and ignore clientId; the 2 new client block kinds read only clientId and ignore projectId. Existing call sites that only pass projectId (e.g. the project page.tsx) continue to compile unchanged since clientId is optional.

In apps/workspaces/src/components/projectPage/PageBlockView.tsx, widen the exported PageBlockView's projectId prop to optional (projectId?: string) and add an optional clientId?: string prop alongside it. Add two new dispatch cases to the kind switch: 'client-current-situation' renders ClientCurrentSituationBlockView passing block and clientId (non-null asserted, since this case only fires from the client canvas where clientId is always supplied); 'client-timeline' renders ClientTimelineBlockView the same way. Import both view components from their new files. Every existing case in the switch remains unchanged.

Create apps/workspaces/src/components/projectPage/ClientCurrentSituationBlock.tsx — export ClientCurrentSituationBlockView taking { block: ClientCurrentSituationBlock; clientId: string }. Use useClientEmails(clientId) (from Task 1). Structure per UI-SPEC "Current situation" contract and the ActivityTimelineView structural template (PageBlockView.tsx lines 294-318): saas-card !p-4 wrapper, optional block.title heading, loading state (Loader2 spinner + "Loading…"), and — since the stub always currently returns [] (Phase 1 behavior, Phase 5 fills it in) — an empty state that is ALWAYS rendered when the array is empty: centered Mail icon (lucide-react, text-gray-300), heading text "No emails synced yet", body text "Sent-mail history will appear here once email sync is connected (coming in a later phase)." exactly matching the Copywriting Contract. Do not hide this section behind any conditional that could omit it entirely (D-11).

Create apps/workspaces/src/components/projectPage/ClientTimelineBlock.tsx — export ClientTimelineBlockView taking { block: ClientTimelineBlock; clientId: string }. Use useClientTimeline(clientId) (from Task 1). Structure: saas-card !p-4 wrapper, optional block.title heading, loading state, and for non-empty results render a single reverse-chronological list (space-y-1.5 max-h-80 overflow-y-auto, matching ActivityTimelineView's scroll container) where each entry shows an icon badge by entry.type (Mail for 'email', Receipt for 'invoice', BarChart3 for 'kpi', all from lucide-react), a text-xs text-gray-400 timestamp (formatted from entry.occurred_at), and entry.summary as a one-line description. No filter tabs (D-12 — single unified feed). Empty state (array length 0): centered icon + heading "No activity yet" + body "Emails, invoices, and other client activity will appear here as they happen." exactly matching Copywriting Contract, same visual treatment (centered, py-8) as the current-situation empty state.
  </action>
  <acceptance_criteria>
    <source>apps/workspaces/src/components/projectPage/LivePageCanvas.tsx signature contains "clientId?: string" and "projectId?: string"</source>
    <source>apps/workspaces/src/components/projectPage/PageBlockView.tsx contains "case 'client-current-situation'" and "case 'client-timeline'"</source>
    <source>apps/workspaces/src/components/projectPage/ClientCurrentSituationBlock.tsx contains "No emails synced yet" and "useClientEmails"</source>
    <source>apps/workspaces/src/components/projectPage/ClientTimelineBlock.tsx contains "No activity yet" and "useClientTimeline"</source>
    <behavior>Rendering an existing project page (any project's default page) with LivePageCanvas continues to work exactly as before — no regression from the additive prop change</behavior>
    <behavior>A client-current-situation block renders its empty state when useClientEmails returns []</behavior>
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces &amp;&amp; npx tsc --noEmit — no new type errors; manual: open any existing project page in dev server, confirm it still renders all its blocks correctly (regression check on the additive prop change)</automated>
  </verify>
  <done>LivePageCanvas/PageBlockView accept an additive optional clientId prop with zero behavior change for existing project-page block kinds; both new client block kinds render correctly, including their mandated empty states.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Client detail page — sidebar (inline edit) + canvas</name>
  <files>apps/workspaces/src/app/records/[clientId]/page.tsx</files>
  <read_first>apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx, apps/workspaces/src/components/projectPage/PageHeader.tsx, apps/workspaces/src/lib/hooks/useTeam.ts, apps/workspaces/src/lib/hooks/useCrm.ts</read_first>
  <action>
Create apps/workspaces/src/app/records/[clientId]/page.tsx ('use client') — new route, does not exist yet. Read clientId from useParams.

Data hooks: useClient(clientId) for the client record; useClientPage(clientId) for the get-or-create page row (Plan 01's endpoint creates the row on first view); useUpdateClient() for sidebar field mutations; useUpdateClientPage(page?.id ?? '', clientId) for canvas config autosave; useTeam() for the responsible-employee picker's options list.

Block-canvas config state: copy the debounced-autosave pattern verbatim from apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx lines 44-87 (seed-once via isPageConfig(page.config) fallback to emptyPageConfig(), 1500ms debounce timer via useEffect, unmount-flush effect) — but call the client-page update mutation instead of the project-page one. If the seeded config's blocks array is empty at first-ever load (a brand-new client_pages row defaults to {"version":1,"blocks":[]}), seed it once with a default two-block layout: a 'client-current-situation' block (span full, title "Current situation") followed by a 'client-timeline' block (span full, title "Timeline"), each with a fresh uid(). This seeding must happen exactly once on first non-empty page load, not on every render — guard it with the same seeded boolean pattern already used for the debounce seed.

Layout per D-09/UI-SPEC: two-column flex/grid — left sidebar (w-80, ~320px fixed width, saas-card or bordered panel styling, dark:bg-slate-800) containing: a client name heading (text-xl font-semibold per UI-SPEC Heading role — a simple heading only, NOT a full PageHeader cover/icon reuse, per RESEARCH.md Open Question 1's recommendation), and three inline click-to-edit fields — address, notes, responsible employee.

Address and notes fields (D-10): render as static text by default with a subtle hover affordance (hover:bg-gray-50 dark:hover:bg-slate-800/60 rounded-lg on the container), minimum min-h-[48px] click target (UI-SPEC spacing scale). On click, transform into an inline input (address) or textarea (notes) in place. Adapt the uncontrolled-contentEditable pattern from PageHeader.tsx's EditableTitle (sync only while unfocused, commit on blur) but ALSO add an ~800ms debounce-while-typing autosave (per D-10's explicit requirement, beyond blur-only): track a local live-edit value in state, run a setTimeout/clearTimeout pattern mirroring the canvas's save-timer, call useUpdateClient's mutate with { id: clientId, data: { address: value } } (or { notes: value }) after the debounce fires, and also flush on blur. Show a small inline spinner transiently while the mutation is pending, and on save-failure show inline small red text "Couldn't save — try again" per the Copywriting Contract (non-blocking, does not interrupt editing).

Responsible-employee field (D-13): render the selected value as plain text (team member's first_name + last_name, looked up from the team list by client.responsible_employee_id, or "Unassigned" if null) with a chevron-down affordance on hover; clicking opens a simple dropdown/listbox of all team members (flat list, no role grouping, per D-13) — selecting one immediately calls useUpdateClient's mutate with { id: clientId, data: { responsible_employee_id: selectedId } } (no debounce needed for a discrete select, unlike the free-text fields).

Main area (right of sidebar, flex-1): render LivePageCanvas passing config, clientId (the additive prop from Task 2), and onChange — do NOT pass a projectId prop here.

Loading state: while the client or page query is loading, show a centered spinner + "Loading…" for the whole page. Error state: if the client fails to load (404 — cross-tenant IDOR protection from RESEARCH.md Security Domain, or an invalid id), show the Copywriting Contract's error copy: heading "Couldn't load this client." + body "Check your connection and try again, or return to the CRM dashboard." + a link back to /records.
  </action>
  <acceptance_criteria>
    <source>apps/workspaces/src/app/records/[clientId]/page.tsx contains "useClient(", "useClientPage(", "useUpdateClientPage(", "useUpdateClient(", "useTeam("</source>
    <source>apps/workspaces/src/app/records/[clientId]/page.tsx contains "LivePageCanvas" and "clientId={clientId}" and does not pass a "projectId=" prop to LivePageCanvas</source>
    <source>apps/workspaces/src/app/records/[clientId]/page.tsx contains "Couldn't load this client." and "Couldn't save" text for the inline error state</source>
    <behavior>Visiting /records/{validClientId} shows a sidebar with the client's address/notes/responsible-employee fields and a main canvas with current-situation and timeline blocks (seeded automatically on first visit)</behavior>
    <behavior>Editing the address field, waiting ~800ms without further typing, persists the change via useUpdateClient without requiring an explicit save action</behavior>
    <behavior>Visiting /records/{invalidOrForeignClientId} shows the "Couldn't load this client." error state instead of crashing</behavior>
  </acceptance_criteria>
  <verify>
    <automated>MISSING — no test framework in repo. Manual: npm run dev in apps/workspaces, visit /records/{seeded-client-id}, confirm sidebar renders with editable fields, confirm canvas shows current-situation ("No emails synced yet") and timeline ("No activity yet" unless invoices/kpi seeded) blocks by default, confirm address edit autosaves after ~800ms pause, confirm an invalid client id shows the error state</automated>
  </verify>
  <done>/records/[clientId] renders a working two-column detail page: sidebar fields persist via inline click-to-edit with autosave, and the canvas shows the two new client-scoped blocks with correct empty-state copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (/records/[clientId]) → crm API (client, client-page, timeline endpoints) | Authenticated JWT bearer; company_id enforced server-side (Plan 01), never trusted from the URL param alone |
| Notes/address free-text fields → rendered in sidebar | Rendered as React-controlled plain text (not dangerouslySetInnerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Elevation of Privilege (IDOR) | /records/[clientId] page's client/page/timeline fetches | mitigate | All three underlying queries are scoped server-side by company_id (Plan 01); the frontend simply surfaces the resulting 404 as the "Couldn't load this client." error state rather than assuming success |
| T-02-07 | Tampering (XSS) | Notes/address inline-edit fields | mitigate | Rendered as plain text via React's default escaping (not dangerouslySetInnerHTML) — no DOMPurify needed since these fields are never rendered as HTML, per RESEARCH.md's recommendation to confirm render mode and prefer plain-text |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
1. `/records/[clientId]` loads for a valid client and shows the sidebar (address/notes/responsible employee) plus the block canvas.
2. The canvas auto-seeds a `client-current-situation` + `client-timeline` block pair on first visit for a brand-new client_pages row, and does not re-seed on subsequent visits (config persists via updateClientPage).
3. Address/notes edits autosave (~800ms debounce + blur-flush) without any explicit "Save" button.
4. Responsible-employee picker lists the full team (no role filter) and persists immediately on selection.
5. Current-situation and timeline blocks render their exact mandated empty-state copy when no data exists, and never disappear/hide.
6. An invalid or cross-tenant client id shows the error state, not a crash.
7. Existing project pages continue to render all their existing block kinds unchanged (regression check on the additive `clientId` prop).
</verification>

<success_criteria>
CLI-01/02/03 and DET-01/02/03 fully satisfied. D-09 through D-13 implemented exactly as decided in CONTEXT.md. No regression to any existing project-page block kind from the additive prop threading.
</success_criteria>

<output>
Create `.planning/phases/02-crm-dashboard-navigation-client-detail/02-03-SUMMARY.md` when done
</output>
