---
phase: 23-record-detail-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workspaces/src/lib/api/records.api.ts
  - apps/workspaces/src/lib/hooks/useRecords.ts
  - apps/workspaces/src/components/records/PropertyField.tsx
autonomous: true
requirements: [CARD-02, CARD-03]

must_haves:
  truths:
    - "A frontend module exists that can fetch and mutate collections/records via the existing /api/v1/records/* backend without any new backend endpoints"
    - "A single component renders the correct native input for every one of the 12 CollectionPropertyDef types (text, number, date, select, multi-select, checkbox, person, url, email, phone, files, relation), bound to a record's props value — files/relation use a free-text tag-input editor (StringArrayChips), not an unhandled default branch"
    - "Text-type fields (text/url/email/phone) commit on blur with an 800ms debounce-while-typing; discrete-type fields (checkbox/select/date/person/multi-select) commit immediately on change; files/relation (freeform string[] tag lists) also commit immediately on add/remove"
  artifacts:
    - path: "apps/workspaces/src/lib/api/records.api.ts"
      provides: "CollectionPropertyDef/DataCollection/CollectionRecord types + recordsApi (getCollection/updateCollection/getRecord/updateRecord/createRecord)"
      exports: ["recordsApi", "CollectionPropertyDef", "DataCollection", "CollectionRecord"]
    - path: "apps/workspaces/src/lib/hooks/useRecords.ts"
      provides: "React Query hooks wrapping recordsApi"
      exports: ["useCollection", "useRecord", "useUpdateCollectionSchema", "useUpdateRecord", "useCreateRecord"]
    - path: "apps/workspaces/src/components/records/PropertyField.tsx"
      provides: "Type-switch property editor covering all 12 property types, including the new multi-select chip-toggle sub-component (D-02) and the new files/relation free-text tag-input sub-component (StringArrayChips)"
      exports: ["PropertyField"]
  key_links:
    - from: "apps/workspaces/src/lib/hooks/useRecords.ts"
      to: "apps/workspaces/src/lib/api/records.api.ts"
      via: "import { recordsApi, ... } from '@/lib/api/records.api'"
      pattern: "from '@/lib/api/records\\.api'"
    - from: "apps/workspaces/src/lib/api/records.api.ts"
      to: "apps/api/src/domains/records/records.routes.ts"
      via: "apiFetch calls to /api/v1/records/collections/:id and /api/v1/records/records/:id"
      pattern: "/api/v1/records"
---

<objective>
Build the frontend data layer and the schema-driven property editor component that the record detail page (Plan 02) will compose. This plan produces the CONTRACTS (API client types, hooks, and the type-switch `PropertyField` component) that Plan 02 consumes — no page route or panel layout yet.

Purpose: Phase 23 is a pure frontend consumer of the already-shipped Phase 22 `/api/v1/records/*` domain (verified in `22-VERIFICATION.md`, 102/102 API tests passing). This plan establishes the typed client + hooks + per-property-type editor UI so Plan 02 can compose them into the actual detail page without re-deriving any of these contracts.
Output: `records.api.ts` (typed HTTP client), `useRecords.ts` (React Query hooks), `PropertyField.tsx` (type-switch editor for all 12 property types, including the new D-02 multi-select chip-toggle and a new files/relation tag-input editor).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-record-detail-page/23-CONTEXT.md
@.planning/phases/23-record-detail-page/23-RESEARCH.md
@.planning/phases/23-record-detail-page/23-PATTERNS.md
@.planning/phases/23-record-detail-page/23-UI-SPEC.md

<interfaces>
<!-- Backend contract this plan's API client must match exactly. Executor should
     use these directly — no backend exploration needed. -->

From apps/api/src/domains/records/records.types.ts (backend row shapes — JSON dates arrive as strings on the frontend):
```typescript
export interface CollectionPropertyDef {
  id: string;
  name: string;
  type: 'text' | 'number' | 'date' | 'select' | 'multi-select' | 'checkbox'
    | 'person' | 'url' | 'email' | 'phone' | 'files' | 'relation';
  [key: string]: unknown; // catchall — e.g. options: {id,label}[] for select/multi-select (UI-owned convention, A3)
}
export interface DataCollectionRow {
  id: string; company_id: string; project_id: string | null; name: string;
  schema: CollectionPropertyDef[]; created_by: string | null; created_at: Date; updated_at: Date;
}
export interface CollectionRecordRow {
  id: string; company_id: string; collection_id: string; parent_record_id: string | null;
  props: Record<string, unknown>; body: Record<string, unknown>; sort_order: number;
  created_by: string | null; created_at: Date; updated_at: Date;
}
```

From apps/api/src/domains/records/records.routes.ts (exact paths, all already mounted and auth-gated):
```
GET/POST   /api/v1/records/collections
GET/PATCH  /api/v1/records/collections/:id
GET/POST   /api/v1/records/collections/:id/records
GET/PATCH  /api/v1/records/records/:id
GET        /api/v1/records/records/:id/children
```

From apps/api/src/domains/records/records.service.ts (`validatePropValue` switch — client must send correctly-typed JS values or the PATCH 400s):
```
text/url/email/phone/select/person/date -> typeof value === 'string'
number -> typeof value === 'number'
checkbox -> typeof value === 'boolean'
multi-select/files/relation -> Array.isArray(value) && value.every(v => typeof v === 'string')
```

From apps/workspaces/src/lib/api/client.ts (re-export, import from here not the package directly):
```typescript
export { apiFetch, ApiError } from '@vectra/api-client';
```

From apps/workspaces/src/lib/api/crm.api.ts / lib/hooks/useCrm.ts (the exact module shape + hook conventions to follow verbatim — object-of-functions API module, `qk` query-key factory, `enabled: !!user?.company_id && !!id` guard):
See `23-RESEARCH.md` Code Examples section and `23-PATTERNS.md` records.api.ts / useRecords.ts sections for the full verbatim shape to clone.

From apps/workspaces/src/lib/hooks/useTeam.ts (person picker data source, Plan 02 will use this too but PropertyField's person case needs the shape now):
```typescript
export function useTeam(): UseQueryResult<TeamMember[]>; // TeamMember: { id, first_name, last_name, ... }
```

From apps/workspaces/src/components/miniProgram/DynamicBlockSettings.tsx (canonical type-switch structure to clone — see full file, 87 lines):
Switch on a type field, one case per input kind, `saas-input`/`label-xs` classes, `<span className="label-xs">{f.label}</span>` label pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Records API client (records.api.ts)</name>
  <files>apps/workspaces/src/lib/api/records.api.ts</files>
  <read_first>
    apps/workspaces/src/lib/api/crm.api.ts
    apps/workspaces/src/lib/api/client.ts
    apps/api/src/domains/records/records.types.ts
    apps/api/src/domains/records/records.routes.ts
  </read_first>
  <action>
    Create `apps/workspaces/src/lib/api/records.api.ts` following `crm.api.ts`'s exact module shape (import `apiFetch` from `./client`; export interfaces; export a single `recordsApi` object of functions). Define and export `CollectionPropertyDef` (id, name, type union of the 12 types listed in the interfaces block, plus `options?: { id: string; label: string }[]` and index signature `[key: string]: unknown` per A3), `DataCollection` (id, company_id, project_id: string|null, name, schema: CollectionPropertyDef[], created_by: string|null, created_at: string, updated_at: string — dates as `string`, not `Date`, since this is JSON over HTTP), and `CollectionRecord` (id, company_id, collection_id, parent_record_id: string|null, props: Record&lt;string, unknown&gt;, body: Record&lt;string, unknown&gt;, sort_order: number, created_by: string|null, created_at: string, updated_at: string). Set `const BASE = '/api/v1/records'`. Implement `recordsApi` with: `getCollection(id)` → `GET ${BASE}/collections/${id}` unwrapping `{ collection }`; `updateCollection(id, data: { schema?: CollectionPropertyDef[]; name?: string })` → `PATCH ${BASE}/collections/${id}` unwrapping `{ collection }`; `getRecord(id)` → `GET ${BASE}/records/${id}` unwrapping `{ record }`; `updateRecord(id, data: { props?: Record&lt;string, unknown&gt;; body?: Record&lt;string, unknown&gt; })` → `PATCH ${BASE}/records/${id}` unwrapping `{ record }`; `createRecord(collectionId: string, data: { props?: Record&lt;string, unknown&gt;; body?: Record&lt;string, unknown&gt; })` → `POST ${BASE}/collections/${collectionId}/records` unwrapping `{ record }` (needed by Plan 02's testability verification, since no board/list UI exists yet to create records). Every `apiFetch` call follows `crm.api.ts`'s exact call signature (`apiFetch<T>(url).then(r => r.x)` for GET, `apiFetch<T>(url, 'METHOD', data).then(r => r.x)` for PATCH/POST).
  </action>
  <acceptance_criteria>
    - `grep -c "export interface CollectionPropertyDef" apps/workspaces/src/lib/api/records.api.ts` returns 1
    - `grep -c "export const recordsApi" apps/workspaces/src/lib/api/records.api.ts` returns 1
    - `grep -c "getCollection\|updateCollection\|getRecord\|updateRecord\|createRecord" apps/workspaces/src/lib/api/records.api.ts` returns at least 5
    - `grep -c "'/api/v1/records" apps/workspaces/src/lib/api/records.api.ts` returns at least 1 (BASE constant literal)
    - File has no `any` type usage (project convention, strict mode)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <done>records.api.ts exports CollectionPropertyDef/DataCollection/CollectionRecord types and a recordsApi object with getCollection/updateCollection/getRecord/updateRecord/createRecord, matching the backend routes exactly.</done>
</task>

<task type="auto">
  <name>Task 2: React Query hooks (useRecords.ts)</name>
  <files>apps/workspaces/src/lib/hooks/useRecords.ts</files>
  <read_first>
    apps/workspaces/src/lib/hooks/useCrm.ts
    apps/workspaces/src/lib/api/records.api.ts
    apps/workspaces/src/context/AuthContext.tsx
  </read_first>
  <action>
    Create `apps/workspaces/src/lib/hooks/useRecords.ts` starting with `'use client';`. Define a `qk` query-key factory object with `collection: (id: string) => ['records-collection', id] as const` and `record: (id: string) => ['records-record', id] as const`. Export `useCollection(id: string)` — `useQuery` with `queryKey: qk.collection(id)`, `queryFn: () => recordsApi.getCollection(id)`, `enabled: !!user?.company_id && !!id` (via `useAuth()`). Export `useRecord(id: string)` — same shape, `qk.record(id)`, `recordsApi.getRecord`. Export `useUpdateCollectionSchema(id: string)` — `useMutation` with `mutationFn: (schema: CollectionPropertyDef[]) => recordsApi.updateCollection(id, { schema })`, `onSuccess: (collection) => qc.setQueryData(qk.collection(id), collection)` (cache-write, NOT invalidateQueries — this is what lets other already-open records of the same collection pick up the new schema per CARD-03, per `useUpdateClientPage`'s precedent in `useCrm.ts`). Export `useUpdateRecord(id: string)` — `useMutation` with `mutationFn: (data: { props?: Record&lt;string, unknown&gt;; body?: Record&lt;string, unknown&gt; }) => recordsApi.updateRecord(id, data)`, `onSuccess: (record) => qc.setQueryData(qk.record(id), record)`. Export `useCreateRecord(collectionId: string)` — `useMutation` with `mutationFn: (data: { props?: Record&lt;string, unknown&gt;; body?: Record&lt;string, unknown&gt; }) => recordsApi.createRecord(collectionId, data)` (no onSuccess cache write needed — this hook exists solely for manual test-data creation per Plan 02's verification instructions, no consuming UI needs its cache entry).
  </action>
  <acceptance_criteria>
    - `grep -c "export function useCollection\|export function useRecord\|export function useUpdateCollectionSchema\|export function useUpdateRecord\|export function useCreateRecord" apps/workspaces/src/lib/hooks/useRecords.ts` returns 5
    - `grep -c "setQueryData" apps/workspaces/src/lib/hooks/useRecords.ts` returns at least 2 (useUpdateCollectionSchema and useUpdateRecord both cache-write, not invalidate)
    - `grep -c "enabled: !!user?.company_id" apps/workspaces/src/lib/hooks/useRecords.ts` returns 2 (useCollection and useRecord)
    - `npx tsc --noEmit -p apps/workspaces/tsconfig.json` exits 0 (or `npm --prefix apps/workspaces run build` if no standalone tsconfig gate)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <done>useRecords.ts exports useCollection/useRecord/useUpdateCollectionSchema/useUpdateRecord/useCreateRecord, all following useCrm.ts's query-key + enabled-guard + cache-write conventions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Schema-driven property editor (PropertyField.tsx)</name>
  <files>apps/workspaces/src/components/records/PropertyField.tsx</files>
  <read_first>
    apps/workspaces/src/components/miniProgram/DynamicBlockSettings.tsx
    apps/workspaces/src/app/records/[clientId]/page.tsx
    apps/workspaces/src/lib/hooks/useTeam.ts
    apps/workspaces/src/lib/api/records.api.ts
  </read_first>
  <action>
    Create `apps/workspaces/src/components/records/PropertyField.tsx` starting with `'use client';`. Export `PropertyField({ property, value, onCommit }: { property: CollectionPropertyDef; value: unknown; onCommit: (value: unknown) => void })`. Import `CollectionPropertyDef` from `@/lib/api/records.api` and `useTeam` from `@/lib/hooks/useTeam`. Implement a `switch (property.type)` with one case per type, covering all 12 types with NO default/unhandled branch, following `DynamicBlockSettings.tsx`'s structural pattern but the input-type-to-class mapping from `records/[clientId]/page.tsx`'s `saas-input !py-2 text-sm mt-1` convention: `checkbox` → native `&lt;input type="checkbox"&gt;`, `className="rounded border-gray-300 text-primary-600"`, commits immediately via `onCommit(e.target.checked)`; `date` → native `&lt;input type="date"&gt;`, `saas-input !py-2 text-sm mt-1`, commits immediately via `onCommit(e.target.value)`; `select` → native `&lt;select&gt;` populated from `property.options ?? []` (each `{id, label}`), `&lt;option value=""&gt;—&lt;/option&gt;` first, commits immediately via `onCommit(e.target.value)`; `person` → clone `EmployeeOverrideField`'s dropdown-popover pattern from `records/[clientId]/page.tsx` (lines ~538-608): `useTeam()` for the member list, `fixed inset-0 z-20` backdrop-close, "Unassigned" as the first option (value `''`), commits immediately on pick, no debounce; `multi-select` → build a NEW small chip-toggle sub-component (`MultiSelectChips`, defined in the same file, no analog exists per D-02/PATTERNS.md "No Analog Found"): render one `&lt;button&gt;` pill per `property.options` entry, `rounded-full text-xs font-semibold px-3 py-1` — selected: `bg-primary-600 text-white`, unselected: `border border-gray-300 text-gray-600 dark:border-slate-600 dark:text-gray-300`; value is `string[]` of selected option ids; clicking toggles membership and commits immediately (`onCommit(nextArray)`), no debounce; `files`/`relation` → both are `string[]` per `validatePropValue` (same array shape as `multi-select`) but represent freeform values with no predefined `property.options` list (files = file references, relation = linked record ids — a real file-upload widget or record-relation picker is explicitly out of scope this phase); render a lightweight tag-input editor via a NEW `StringArrayChips` sub-component (defined in the same file, distinct from `MultiSelectChips` — a free-text tag list, not a fixed-option toggle): a text input + Enter-to-add (or an inline "Add" button) appends the trimmed non-empty string to the array, existing entries render as removable chips reusing the same pill classes as `MultiSelectChips` (`rounded-full text-xs font-semibold px-3 py-1 bg-primary-600 text-white`) plus an inline "×" remove control, committing immediately on every add/remove (`onCommit(nextArray)`), no debounce; `text`/`url`/`email`/`phone`/`number` → plain `&lt;input type={htmlTypeFor(property.type)}&gt;` (htmlTypeFor: `number`→`'number'`, `url`→`'url'`, `email`→`'email'`, else `'text'`) with an internal `draft` state + `lastCommittedRef` + `setTimeout`-based 800ms debounce-while-typing, committing on blur AND on the debounce firing — clone the `handleChange`/`flush` structure from `InlineTextField` in `records/[clientId]/page.tsx` (lines 672-758), adapted to call `onCommit` instead of a `mutate` call directly (the debounce/blur-commit logic lives inside `PropertyField`, but `onCommit` is the prop passed in by `PropertyPanel` in Plan 02 — do not call `recordsApi`/`useUpdateRecord` directly from this component, keep it a pure controlled/semi-controlled input). Every case uses the `label-xs`-equivalent styling only where the parent renders the label — `PropertyField` itself renders ONLY the input, not the label row (label row is `PropertyPanel`'s responsibility in Plan 02, per PATTERNS.md's row structure: label above, `PropertyField` below).
  </action>
  <acceptance_criteria>
    - `grep -c "export function PropertyField\|export const PropertyField" apps/workspaces/src/components/records/PropertyField.tsx` returns 1
    - `grep -c "case 'checkbox'\|case 'date'\|case 'select'\|case 'person'\|case 'multi-select'\|case 'number'\|case 'text'\|case 'url'\|case 'email'\|case 'phone'\|case 'files'\|case 'relation'" apps/workspaces/src/components/records/PropertyField.tsx` returns 12 (switch explicitly covers all 12 CollectionPropertyDef types — no default/unhandled branch for files/relation)
    - `grep -c "MultiSelectChips\|bg-primary-600 text-white" apps/workspaces/src/components/records/PropertyField.tsx` returns at least 1 (new chip-toggle sub-component present for multi-select)
    - `grep -c "StringArrayChips" apps/workspaces/src/components/records/PropertyField.tsx` returns at least 1 (new free-text tag-input sub-component present for files/relation, distinct from MultiSelectChips)
    - `grep -c "useTeam" apps/workspaces/src/components/records/PropertyField.tsx` returns at least 1
    - `npx tsc --noEmit -p apps/workspaces/tsconfig.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <done>PropertyField renders a type-appropriate, correctly-committing input for every one of the 12 CollectionPropertyDef types — including the new multi-chip-toggle component (MultiSelectChips) for multi-select per D-02 and the new free-text tag-input component (StringArrayChips) for files/relation — with no default/unhandled branch for any type.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → `/api/v1/records/*` | Authenticated (JWT bearer) requests from this new frontend client to the existing, already-hardened Phase 22 backend domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | `records.api.ts` prop-value payloads | accept | Server-side `validatePropValue` (already shipped, Phase 22) is the authoritative type gate — this plan's client sends correctly-typed JS values as a UX correctness measure, not as the security boundary; duplicating validation client-side would risk drift per RESEARCH.md's "Don't Hand-Roll" guidance |
| T-23-02 | Information Disclosure | `useCollection`/`useRecord` hooks | accept | `company_id` scoping is enforced server-side (`recordsRepository.findRecord`/`findCollection`, verified in `22-VERIFICATION.md`) — this plan's hooks pass through whatever the authenticated session's company returns; no cross-tenant ID guessing surface is introduced by the client code itself |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan (confirmed in RESEARCH.md "Installation: None") — no supply-chain surface introduced |
</threat_model>

<verification>
1. `npx tsc --noEmit -p apps/workspaces/tsconfig.json` (or `npm --prefix apps/workspaces run build` if no standalone tsconfig) exits 0 with all three new files in place.
2. Manual: import `recordsApi`, `useCollection`, `useRecord`, and `PropertyField` from a scratch component (or the browser console via React DevTools) and confirm no import errors / no circular dependency warnings.
3. `npm --prefix apps/api test` still passes (no backend files touched this plan — sanity check only).
</verification>

<success_criteria>
- `records.api.ts`, `useRecords.ts`, and `PropertyField.tsx` exist, export the contracts listed in `must_haves.artifacts`, and type-check cleanly.
- `PropertyField` handles all 12 `CollectionPropertyDef` types with the correct commit-timing (immediate vs. 800ms-debounced) per UI-SPEC, with no default/unhandled branch for files/relation.
- No backend files modified; no new npm packages added.
</success_criteria>

<output>
Create `.planning/phases/23-record-detail-page/23-01-SUMMARY.md` when done
</output>
</output>
