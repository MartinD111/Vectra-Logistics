---
phase: 22-records-views-data-model
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - apps/api/src/domains/records/records.repository.ts
  - apps/api/src/domains/records/records.repository.test.ts
autonomous: true
requirements: [REC-01, REC-02, REC-03, REC-04]

must_haves:
  truths:
    - "records.repository.createCollectionWithDefaultView atomically inserts one data_collections row and one default type='table' collection_views row inside a single BEGIN/COMMIT transaction (D-03)"
    - "records.repository.listChildren returns only collection_records rows where parent_record_id = :id AND company_id = :companyId, ordered by sort_order then created_at (REC-04)"
    - "Every repository method that writes schema/props/body/config passes the value through JSON.stringify(...) as a query parameter, never string-concatenated into SQL text"
  artifacts:
    - path: "apps/api/src/domains/records/records.repository.ts"
      provides: "CRUD data access for data_collections, collection_records, collection_views, all company_id-scoped"
      exports: ["recordsRepository"]
    - path: "apps/api/src/domains/records/records.repository.test.ts"
      provides: "Unit tests covering company_id scoping and parent/child query behavior via mocked db.query"
      min_lines: 40
  key_links:
    - from: "apps/api/src/domains/records/records.repository.ts"
      to: "apps/api/src/core/db (db.query / db.connect)"
      via: "import { db } from '../../core/db'"
      pattern: "import \\{ db \\} from '\\.\\./\\.\\./core/db'"
    - from: "apps/api/src/domains/records/records.repository.ts"
      to: "apps/api/src/domains/records/records.types.ts"
      via: "import { DataCollectionRow, CollectionRecordRow, CollectionViewRow } from './records.types'"
      pattern: "import.*from '\\./records\\.types'"
---

<objective>
Implement the data-access layer for the Records + Views domain: parameterized, company_id-scoped CRUD over `data_collections`, `collection_records`, and `collection_views`, plus the D-03 atomic collection+default-view creation and the REC-04 parent/child query. Test the repository against a mocked `db.query`/`db.connect`, since no live Postgres is available in this environment.

Purpose: Isolate all raw SQL behind a single, tested data-access module so the service layer (Plan 22-03) never touches SQL directly.
Output: `records.repository.ts` (exports `recordsRepository`), `records.repository.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-records-views-data-model/22-RESEARCH.md
@.planning/phases/22-records-views-data-model/22-PATTERNS.md
@.planning/phases/22-records-views-data-model/22-VALIDATION.md
</context>

<interfaces>
<!-- records.types.ts contracts from Plan 22-01 (already on disk), + core/db and crm.repository.ts patterns. -->

From apps/api/src/domains/records/records.types.ts (Plan 22-01 output — read this file, do not re-derive):
```typescript
export interface DataCollectionRow { id: string; company_id: string; project_id: string | null; name: string; schema: CollectionPropertyDef[]; created_by: string | null; created_at: Date; updated_at: Date; }
export interface CollectionRecordRow { id: string; company_id: string; collection_id: string; parent_record_id: string | null; props: Record<string, unknown>; body: Record<string, unknown>; sort_order: number; created_by: string | null; created_at: Date; updated_at: Date; }
export interface CollectionViewRow { id: string; company_id: string; collection_id: string; name: string; type: string; config: Record<string, unknown>; sort_order: number; created_at: Date; }
```

From apps/api/src/core/db/index.ts (verified this session):
```typescript
import { Pool } from 'pg';
const db = new Pool({ connectionString: process.env.DATABASE_URL });
export { db };
```
`db` is a raw `pg.Pool` — no transaction helper exists anywhere in the codebase (verified via grep: zero domain repositories use `db.connect()`). Use `db.connect()` to check out a `PoolClient` for the D-03 transaction; `db.query()` for every other single-statement method.

Dynamic-SET partial-update pattern (from crm.repository.ts `updateClient`, apply to `updateCollection`/`updateRecord`/`updateView`):
```typescript
const sets: string[] = [];
const params: unknown[] = [id, companyId];
for (const [col, val] of Object.entries(patch)) {
  if (val === undefined) continue;
  params.push(val);
  sets.push(`${col} = $${params.length}`);
}
if (sets.length === 0) return this.findX(id, companyId);
sets.push('updated_at = NOW()');
const { rows } = await db.query<XRow>(`UPDATE x SET ${sets.join(', ')} WHERE id = $1 AND company_id = $2 RETURNING *`, params);
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write records.repository.test.ts (RED)</name>
  <files>apps/api/src/domains/records/records.repository.test.ts</files>
  <read_first>
    apps/api/src/domains/kpi/evaluators/creditRisk.evaluator.test.ts
    apps/api/src/core/db/index.ts
  </read_first>
  <behavior>
    - Test 1: `createCollectionWithDefaultView` — mock `db.connect` to return a fake client whose `.query` records every call; assert the sequence is `BEGIN`, then an INSERT into `data_collections` (SQL text contains `INSERT INTO data_collections`), then an INSERT into `collection_views` with `type` = `'table'` (SQL text contains `INSERT INTO collection_views` and the params or SQL literal include `'table'`), then `COMMIT`; assert `client.release()` is called.
    - Test 2: `createCollectionWithDefaultView` rollback — mock the second `client.query` call (the `collection_views` insert) to throw; assert `client.query` was called with `'ROLLBACK'` and the thrown error propagates.
    - Test 3: `listChildren(parentRecordId, companyId)` — mock `db.query` to capture the SQL string and params; assert the SQL text contains `parent_record_id = $1` and `company_id = $2`, and params equal `[parentRecordId, companyId]` in that order.
    - Test 4: `createRecord` — mock `db.query`; assert `props` and `body` are passed to `db.query`'s params array as JSON strings (`JSON.stringify` output), not as raw objects, and that the SQL text never contains inlined JSON (no string concatenation of the payload into the query text).
    - Test 5: `findCollection(id, companyId)` (or equivalent single-row getter) — mock `db.query` to return `{ rows: [] }`; assert the method returns `null` (not throwing) when no row matches.
  </behavior>
  <action>
    Create `apps/api/src/domains/records/records.repository.test.ts` using `node:test`'s `test`/`mock`/`afterEach` (mirroring `creditRisk.evaluator.test.ts`'s import style: `import { test, mock, afterEach } from 'node:test'; import assert from 'node:assert/strict';`). Mock `db.connect` and `db.query` via `mock.method(db, 'connect', async () => fakeClient)` and `mock.method(db, 'query', async (sql, params) => ({ rows: [...] }))`, importing `db` from `../../core/db` and `recordsRepository` from `./records.repository`. This file will fail to import `recordsRepository` until Task 2 creates it — that import failure is the expected RED state. Call `mock.restoreAll()` in `afterEach`.
  </action>
  <verify>
    <automated>node --require ts-node/register --test src/domains/records/records.repository.test.ts 2>&1 | grep -q 'fail\|Error' && echo "RED confirmed (expected failure before Task 2)"</automated>
  </verify>
  <done>Test file exists with 5 tests covering D-03 transaction sequencing/rollback, REC-04 scoping, and JSONB parameterized-write safety; fails to run because records.repository.ts does not yet exist (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement records.repository.ts (GREEN)</name>
  <files>apps/api/src/domains/records/records.repository.ts</files>
  <read_first>
    apps/api/src/domains/crm/crm.repository.ts
    apps/api/src/domains/records/records.types.ts
    apps/api/src/domains/records/records.repository.test.ts
  </read_first>
  <action>
    Create `apps/api/src/domains/records/records.repository.ts` exporting a singleton `recordsRepository` object (matching `crmRepository`'s export style — instance, not class export) with these methods, all filtering `company_id = $N` on every SELECT/UPDATE/DELETE (Pitfall 1 — `collection_views` needs its own `company_id` filter, not just the `collection_id` join):

    Collections: `createCollectionWithDefaultView(companyId, { name, schema, createdBy })` — uses `db.connect()` for a `BEGIN`/INSERT `data_collections`/INSERT `collection_views` (name `'Table'`, type `'table'`, config `'{}'`)/`COMMIT` transaction with `ROLLBACK` in a `catch` and `client.release()` in `finally`, per the D-03 sketch in 22-PATTERNS.md; `findCollection(id, companyId)`; `listCollections(companyId)`; `updateCollection(id, companyId, patch)` using the dynamic-SET pattern from the Interfaces block (JSON.stringify `schema` if present in patch).

    Records: `createRecord(companyId, { collectionId, parentRecordId, props, body, createdBy })` — INSERT with `JSON.stringify(props ?? {})` and `JSON.stringify(body ?? { version: 1, blocks: [] })` as parameters; `findRecord(id, companyId)`; `listRecords(collectionId, companyId)` ordered by `sort_order ASC, created_at ASC`; `updateRecord(id, companyId, patch)` using the dynamic-SET pattern (JSON.stringify `props`/`body` if present); `listChildren(parentRecordId, companyId)` — exact query from the Interfaces/RESEARCH.md Code Examples section (`WHERE parent_record_id = $1 AND company_id = $2 ORDER BY sort_order ASC, created_at ASC`).

    Views: `createView(companyId, { collectionId, name, type, config })` — INSERT with `JSON.stringify(config ?? {})`; `findView(id, companyId)`; `listViews(collectionId, companyId)`; `updateView(id, companyId, patch)` using the dynamic-SET pattern.

    Use `db.query<RowType>(...)` with the exact row interfaces from `records.types.ts` as the generic parameter on every call. Never string-concatenate `props`/`body`/`schema`/`config` into SQL text — always `JSON.stringify(...)` as a bound parameter (ASVS V5 / SQLi mitigation, per 22-RESEARCH.md's Security Domain and 22-PATTERNS.md's "JSONB write safety" shared pattern).
  </action>
  <acceptance_criteria>
    - `recordsRepository` is exported and has methods: createCollectionWithDefaultView, findCollection, listCollections, updateCollection, createRecord, findRecord, listRecords, updateRecord, listChildren, createView, findView, listViews, updateView
    - Every method's SQL text includes `company_id = $` (or `company_id)` on INSERT) — no method omits company scoping
    - `createCollectionWithDefaultView` uses `db.connect()`, `BEGIN`, `COMMIT`, `ROLLBACK` in a catch block, and `client.release()` in a finally block
    - `listChildren` query text contains `parent_record_id = $1 AND company_id = $2` and orders by `sort_order ASC, created_at ASC`
    - `createRecord`/`updateRecord`/`createCollectionWithDefaultView`/`updateCollection`/`createView`/`updateView` pass `JSON.stringify(...)` for any JSONB column value, never a raw object or string-concatenated literal
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix apps/api test -- src/domains/records/records.repository.test.ts</automated>
  </verify>
  <done>All 5 tests in records.repository.test.ts pass (GREEN); npx tsc --noEmit -p apps/api/tsconfig.json exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service layer → repository → PostgreSQL | Repository is the last stop before raw SQL execution; every query parameter here is trusted only if bound, never interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Tampering | records.repository.ts (all INSERT/UPDATE methods) | mitigate | Parameterized queries (`$1, $2, ...`) via `pg`; `JSON.stringify(...)` always passed as a bound parameter, verified by Task 1's Test 4 asserting no inlined JSON in SQL text |
| T-22-05 | Elevation of Privilege / Information Disclosure | records.repository.ts (all SELECT/UPDATE/DELETE) | mitigate | Every query filters `company_id = $N` on all three tables, including `collection_views` (which has its own `company_id` column, not just a `collection_id` FK join) — Pitfall 1 from 22-RESEARCH.md, verified structurally in acceptance criteria |
| T-22-06 | Denial of Service | createCollectionWithDefaultView (transaction) | accept | Transaction scope is two INSERTs, bounded and fast; no user-controlled loop or unbounded query inside the transaction |
</threat_model>

<verification>
Per-task: `node --require ts-node/register --test src/domains/records/records.repository.test.ts`. Per-wave: `npm --prefix apps/api test`. Both must pass before this plan is considered complete.
</verification>

<success_criteria>
- records.repository.test.ts exists with 5 tests, all passing after Task 2
- records.repository.ts exports recordsRepository with all 13 methods listed above
- Every method is company_id-scoped; D-03 transaction is atomic with rollback on failure
- `npm --prefix apps/api test -- src/domains/records/records.repository.test.ts` exits 0
</success_criteria>

<output>
Create `.planning/phases/22-records-views-data-model/22-02-SUMMARY.md` when done
</output>
