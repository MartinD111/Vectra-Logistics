---
phase: 25-view-ux-parity
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx
  - apps/workspaces/src/components/projectPage/board/ColumnAggregation.tsx
  - apps/workspaces/src/components/projectPage/board/BoardCard.tsx
  - apps/workspaces/src/components/projectPage/board/BoardColumn.tsx
  - apps/workspaces/src/components/projectPage/BoardBlock.tsx
autonomous: true
requirements: [VIEWX-02, VIEWX-03]

must_haves:
  truths:
    - "User can choose which properties preview on a card face"
    - "User can see column aggregations (count, and sum/avg for a chosen number property) on a board"
    - "D-03: view settings entry point opens a checklist of schema properties toggling card-face display, persisted to view.config.cardProperties[]"
    - "D-04: column footer shows count always available, plus sum/avg for a user-chosen number property"
  artifacts:
    - path: "apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx"
      provides: "'...' popover: card-face property checklist + view-wide aggregation type/property picker"
      contains: "View settings"
    - path: "apps/workspaces/src/components/projectPage/board/ColumnAggregation.tsx"
      provides: "Per-column footer rendering count/sum/avg via aggregateColumn"
      contains: "aggregateColumn"
    - path: "apps/workspaces/src/components/projectPage/board/BoardCard.tsx"
      provides: "Renders view.config.cardProperties[] read-only below the title"
      contains: "cardProperties"
  key_links:
    - from: "apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx"
      to: "apps/workspaces/src/lib/hooks/useRecords.ts"
      via: "useUpdateView(view.id).mutate({ config: { ...view.config, cardProperties/columnAggregation } })"
      pattern: "useUpdateView\\(view\\.id\\)"
    - from: "apps/workspaces/src/components/projectPage/board/BoardColumn.tsx"
      to: "apps/workspaces/src/components/projectPage/board/ColumnAggregation.tsx"
      via: "footer render call passing column.cards + view.config.columnAggregation"
      pattern: "<ColumnAggregation"
---

<objective>
Ship VIEWX-02 and VIEWX-03: a view-settings entry point (D-03, the "More options" / three-dot menu) that lets the user toggle which schema properties render on a card's face, and a column aggregation footer (D-04) showing count always plus a user-chosen sum/avg of a number property -- both persisted to `view.config.cardProperties[]` / `view.config.columnAggregation` via the `useUpdateView` hook from Plan 25-01.

Purpose: Turns the board's cards from title-only into configurable at-a-glance previews, and turns each column into a real aggregate summary, matching Notion-parity board behavior.
Output: `ViewSettingsMenu.tsx` (new), `ColumnAggregation.tsx` (new), `BoardCard.tsx` extended to render card-face properties read-only, `BoardColumn.tsx` extended with an aggregation footer, `BoardBlock.tsx` extended to mount `ViewSettingsMenu` in its toolbar row and thread `view.config` down to `BoardColumn`/`BoardCard`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-view-ux-parity/25-CONTEXT.md
@.planning/phases/25-view-ux-parity/25-RESEARCH.md
@.planning/phases/25-view-ux-parity/25-PATTERNS.md
@.planning/phases/25-view-ux-parity/25-UI-SPEC.md
@.planning/phases/25-view-ux-parity/25-01-SUMMARY.md
@.planning/phases/25-view-ux-parity/25-02-SUMMARY.md
</context>

<interfaces>
Contracts from Plan 25-01/25-02 this plan consumes.

From apps/workspaces/src/lib/projectPage/viewFilters.ts (Plan 25-01):
- `export interface AggregationConfig { type: 'count' | 'sum' | 'avg'; propId?: string }`
- `export function aggregateColumn(records: CollectionRecord[], agg: AggregationConfig): number;`

From apps/workspaces/src/lib/hooks/useRecords.ts (Plan 25-01):
- `export function useUpdateView(id: string): UseMutationResult<CollectionView, unknown, UpdateViewInput>;`

From apps/workspaces/src/components/records/PropertyField.tsx (existing, checkbox branch to model the checklist toggle on, NOT to reuse directly -- CONTEXT.md D-03: "no existing analog for a property-picker checklist scoped to a view", build a plain checklist instead):
- `case 'checkbox': return <input type="checkbox" checked={!!value} onChange={(e) => onCommit(e.target.checked)} className="rounded border-gray-300 text-primary-600" />;`

Current `apps/workspaces/src/components/projectPage/board/BoardCard.tsx` title-only render (lines 89-101, to extend):
- Root element: `<div ref={setNodeRef} {...attributes} {...listeners} onClick={...} style={...} className="rounded-md ...">`
- Title span: `<span className={title ? 'text-gray-800 dark:text-gray-200' : 'text-gray-400 italic'}>{title || 'Untitled'}</span>`

Current `apps/workspaces/src/components/projectPage/board/BoardColumn.tsx` count badge (line 102, existing "count" precedent to extend into a full aggregation footer):
- `<span className="text-[10px] text-gray-400 flex-shrink-0">{column.cards.length}</span>`
- Mount point for new footer: after the `</SortableContext>` closing tag (line 139-140), before the "+ New" button (line 142).

`BoardColumn` current props signature (to extend with two new optional props):
- `export function BoardColumn({ column, titlePropId, collectionId, collection, groupByPropId }: { column: BoardColumnData; titlePropId: string; collectionId: string; collection: DataCollection; groupByPropId: string; }): JSX.Element;`

`BoardCard` current props signature (to extend with one new optional prop):
- `export function BoardCard({ record, titlePropId, collectionId, autoFocusEdit = false, onExitEdit }: { record: CollectionRecord; titlePropId: string; collectionId: string; autoFocusEdit?: boolean; onExitEdit?: () => void; }): JSX.Element;`
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build ViewSettingsMenu.tsx (card-face checklist + aggregation picker)</name>
  <files>apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx</files>
  <read_first>
    apps/workspaces/src/components/records/PropertyField.tsx (lines 111-146 popover shell; lines 28-36 checkbox pattern to model, not reuse directly)
    apps/workspaces/src/components/records/PropertyPanel.tsx (lines 46-55 -- schema-iteration loop pattern for listing collection.schema entries)
    apps/workspaces/src/lib/projectPage/viewFilters.ts (Plan 25-01 -- AggregationConfig type)
    .planning/phases/25-view-ux-parity/25-UI-SPEC.md (Interaction Contract rows "View settings" (D-03) and "Column aggregation footer" (D-04) -- exact copy/behavior: icon-only MoreHorizontal trigger, aria-label="View settings", 32px touch target, instant persist on toggle)
  </read_first>
  <action>
    Create apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx, a 'use client' component accepting `{ collection: DataCollection; view: CollectionView }`. Internally call `useUpdateView(view.id)`.

    Render an icon-only trigger button using `MoreHorizontal` from lucide-react, className including `min-h-[32px] min-w-[32px]`, and `aria-label="View settings"` (no visible text label, per UI-SPEC). On click, toggle local `useState<boolean>` open/close, following the PersonField popover shell exactly (`fixed inset-0 z-20` backdrop + `absolute ... top-full mt-1 z-30` panel, right-aligned since this trigger sits at the end of the toolbar row -- use `right-0` instead of `left-0 right-0` for the panel's horizontal anchor).

    Inside the panel, render two sections:

    1. "Card properties" heading (12px semibold uppercase per UI-SPEC Label tier) followed by one row per `collection.schema` entry, each a label wrapping a checkbox: checked = `cardProperties.includes(property.id)`, where `cardProperties = (view.config.cardProperties as string[]) ?? []`. Toggling a checkbox computes the next array (`includes(id) ? filter out id : append id`) and calls `useUpdateView(view.id).mutate({ config: { ...view.config, cardProperties: next } })` immediately (instant persist, no save button, per UI-SPEC).

    2. "Column aggregation" heading followed by a select of `count`/`sum`/`avg` (labels: "Count"/"Sum"/"Average") bound to `(view.config.columnAggregation as AggregationConfig | undefined)?.type ?? 'count'`, and -- only when the selected type is sum or avg -- a second select listing `collection.schema.filter((p) => p.type === 'number')` (value = property.id, label = property.name) bound to `(view.config.columnAggregation as AggregationConfig | undefined)?.propId`. Both selects call `useUpdateView(view.id).mutate({ config: { ...view.config, columnAggregation: { type: nextType, propId: nextPropId } } })` on change (Pitfall 3 mitigation from 25-RESEARCH.md -- only number-typed properties are offered for sum/avg).

    Export as `export function ViewSettingsMenu(...)`.
  </action>
  <acceptance_criteria>
    - Component exports ViewSettingsMenu accepting `{ collection: DataCollection; view: CollectionView }`
    - Trigger button uses MoreHorizontal icon, has aria-label="View settings", and no visible text (accessible name comes entirely from the aria-label)
    - Card-properties checklist renders one row per collection.schema entry with a checkbox reflecting view.config.cardProperties membership; toggling calls useUpdateView(view.id).mutate immediately
    - Aggregation type select offers exactly count/sum/avg; the property select for sum/avg is populated ONLY from collection.schema.filter(p => p.type === 'number') (verify by reading the filter expression in the file)
    - cd apps/workspaces and npx tsc --noEmit reports no new errors in this file
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep "ViewSettingsMenu.tsx" || echo "no errors in ViewSettingsMenu.tsx"</automated>
  </verify>
  <done>ViewSettingsMenu.tsx exists, compiles, and implements the card-face checklist and aggregation picker per D-03/D-04 and the UI-SPEC's exact copy/behavior contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build ColumnAggregation.tsx and extend BoardCard.tsx with card-face properties</name>
  <files>apps/workspaces/src/components/projectPage/board/ColumnAggregation.tsx, apps/workspaces/src/components/projectPage/board/BoardCard.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/board/BoardCard.tsx (full file, current state)
    apps/workspaces/src/components/records/PropertyPanel.tsx (lines 46-55 -- pattern for rendering a property list with PropertyField below a title)
    apps/workspaces/src/lib/projectPage/viewFilters.ts (Plan 25-01 -- aggregateColumn, AggregationConfig)
    apps/workspaces/src/lib/api/records.api.ts (CollectionPropertyDef, CollectionRecord shapes)
    .planning/phases/25-view-ux-parity/25-CONTEXT.md (card-face rendering is read-only; click-through to record detail remains the edit path)
  </read_first>
  <action>
    Create apps/workspaces/src/components/projectPage/board/ColumnAggregation.tsx exporting `export function ColumnAggregation({ records, schema, aggregation }: { records: CollectionRecord[]; schema: CollectionPropertyDef[]; aggregation: AggregationConfig | undefined }): JSX.Element`. Compute `const agg = aggregation ?? { type: 'count' as const };` then `const value = aggregateColumn(records, agg);`. Render a small footer `<div>` (12px, `text-gray-400`, matching BoardColumn's existing `text-[10px]`/`text-xs` footer text conventions) showing `Count: {value}` when `agg.type === 'count'`, or `{agg.type === 'sum' ? 'Sum' : 'Avg'}: {formatted value}` when sum/avg -- format via `value.toLocaleString()` if the property lookup (`schema.find(p => p.id === agg.propId)`) resolves, otherwise fall back to `String(value)`. If `agg.type` is sum/avg but `agg.propId` is unset, render nothing (no property chosen yet).

    Extend BoardCard.tsx: add a new prop `cardProperties?: CollectionPropertyDef[]` and `record` already available. Below the existing title `<span>` (inside the same non-editing return branch, after the title span, still inside the same root `<div>`), add: `{(cardProperties ?? []).length > 0 && (<div className="mt-1.5 space-y-1">{cardProperties!.map((property) => (<div key={property.id} className="text-xs text-gray-500 dark:text-gray-400 truncate">{formatCardPropertyValue(record.props[property.id], property)}</div>))}</div>)}`. Add a local helper `formatCardPropertyValue(value: unknown, property: CollectionPropertyDef): string` that renders a READ-ONLY display string per property type (do not use PropertyField's onCommit-driven controls here, per CONTEXT.md's explicit "click-through to record detail remains the edit path" note): `checkbox` -> `value ? 'Yes' : 'No'`; `select`/`person` -> resolve label from `property.options`/leave as raw id string if no options match (person label resolution can stay a raw id here, out of scope for a lightweight card-face preview); `multi-select`/`files`/`relation` -> `Array.isArray(value) ? value.join(', ') : ''`; everything else (`text`/`number`/`date`/`url`/`email`/`phone`) -> `value == null ? '' : String(value)`. The card's existing `onClick` (opens `/collections/:id/records/:id` in a new tab) remains the sole edit path -- do not wire any `onCommit`/mutation into these new card-face elements.
  </action>
  <acceptance_criteria>
    - ColumnAggregation.tsx exports ColumnAggregation, calls aggregateColumn, and renders "Count: N" for count type and "Sum: X"/"Avg: X" for sum/avg types
    - ColumnAggregation renders nothing when agg.type is sum/avg and agg.propId is unset
    - BoardCard.tsx accepts an optional cardProperties prop and renders each property's value below the title as plain read-only text (no input/select/checkbox elements introduced for card-face properties)
    - BoardCard's existing onClick (opening the record detail page in a new tab) is unchanged and remains the only interactive element wired to card-face data
    - cd apps/workspaces and npx tsc --noEmit reports no new errors in either file
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep -E "ColumnAggregation.tsx|BoardCard.tsx" || echo "no errors in touched files"</automated>
  </verify>
  <done>ColumnAggregation.tsx exists and computes count/sum/avg correctly; BoardCard.tsx renders cardProperties read-only below the title without introducing any new inline-edit surface.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire ViewSettingsMenu + aggregation footer + cardProperties through BoardColumn and BoardBlock</name>
  <files>apps/workspaces/src/components/projectPage/board/BoardColumn.tsx, apps/workspaces/src/components/projectPage/BoardBlock.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/board/BoardColumn.tsx (full file, current state)
    apps/workspaces/src/components/projectPage/BoardBlock.tsx (full file, current state after Plan 25-02's edits)
    apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx (this plan's Task 1 output)
    apps/workspaces/src/components/projectPage/board/ColumnAggregation.tsx (this plan's Task 2 output)
  </read_first>
  <action>
    In BoardColumn.tsx: add two new optional props to the component signature: `cardProperties?: CollectionPropertyDef[]` and `aggregation?: AggregationConfig`. Import `CollectionPropertyDef` from `@/lib/api/records.api`, `AggregationConfig` from `@/lib/projectPage/viewFilters`, and `ColumnAggregation` from `./ColumnAggregation`. Pass `cardProperties` through to each `<BoardCard ... cardProperties={cardProperties} />` inside the existing `column.cards.map(...)` loop. After the `</SortableContext>`-closing `</div>` (the div at line 124-127 wrapping the droppable region) and before the "+ New" button, insert `<ColumnAggregation records={column.cards} schema={collection.schema} aggregation={aggregation} />`.

    In BoardBlock.tsx: import `ViewSettingsMenu` from `./board/ViewSettingsMenu`. Mount it in the same header row as `FilterSortToolbar` (from Plan 25-02): `<div className="flex items-center gap-2 mb-3"><FilterSortToolbar collection={collection} view={view} /><ViewSettingsMenu collection={collection} view={view} /></div>` (use `ml-auto` on `ViewSettingsMenu`'s wrapping element or `justify-between` on the row so it right-aligns per the UI-SPEC's toolbar layout). Compute `const cardProperties = collection.schema.filter((p) => ((view.config.cardProperties as string[]) ?? []).includes(p.id));` and `const aggregation = view.config.columnAggregation as AggregationConfig | undefined;` before the `columns.map(...)` render, then pass both down: `<BoardColumn key={col.id} column={col} titlePropId={titlePropId} collectionId={block.collectionId as string} collection={collection} groupByPropId={groupByPropId} cardProperties={cardProperties} aggregation={aggregation} />`.
  </action>
  <acceptance_criteria>
    - BoardColumn.tsx accepts cardProperties and aggregation props, forwards cardProperties to every BoardCard it renders, and renders ColumnAggregation once per column footer
    - BoardBlock.tsx renders ViewSettingsMenu alongside FilterSortToolbar in its header row
    - BoardBlock.tsx computes cardProperties from view.config.cardProperties (resolved to full CollectionPropertyDef objects, not raw ids) and passes it to every BoardColumn
    - BoardBlock.tsx passes view.config.columnAggregation through to every BoardColumn as the aggregation prop
    - cd apps/workspaces and npx tsc --noEmit reports no new errors in either file
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep -E "BoardColumn.tsx|BoardBlock.tsx" || echo "no errors in touched files"</automated>
  </verify>
  <done>ViewSettingsMenu is mounted in the board toolbar, toggling a card-face property immediately updates every card in every column, and every column shows an aggregation footer reflecting the view-wide count/sum/avg setting -- verified by manual/live smoke test (no FE test runner available per 25-RESEARCH.md).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> PATCH /api/v1/records/views/:id | cardProperties/columnAggregation config writes flow through the existing authenticated, company-scoped endpoint via useUpdateView (built Plan 25-01) -- no new backend surface |
| Card-face property rendering | Read-only display of already-fetched, already-authorized record prop values -- no new data-access path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-25-05 | Tampering | ViewSettingsMenu config writes | accept | Same existing PATCH /views/:id endpoint, already gated by authenticateToken + company_id scoping in records.repository.ts -- no new validation gap |
| T-25-06 | Information Disclosure | Card-face property values (BoardCard) | accept | Renders values the user already has access to via the existing company-scoped records list fetch (useRecords) -- no new field exposed beyond what the record detail page already shows |
| T-25-SC | Tampering | npm installs | accept | Zero new packages this plan |
</threat_model>

<verification>
- cd apps/workspaces and npx tsc --noEmit passes with no new errors in the five touched/created files.
- Manual/live smoke test: open the "..." view settings menu, toggle a property on, confirm it appears read-only below every card's title in every column; set aggregation to "Sum" + a number property, confirm each column footer shows a correct sum matching its cards' values.
</verification>

<success_criteria>
- VIEWX-02 satisfied: user can choose which properties preview on a card face.
- VIEWX-03 satisfied: user can see column aggregations (count, and sum/avg for a chosen number property) on a board.
</success_criteria>

<output>
Create .planning/phases/25-view-ux-parity/25-03-SUMMARY.md when done
</output>
