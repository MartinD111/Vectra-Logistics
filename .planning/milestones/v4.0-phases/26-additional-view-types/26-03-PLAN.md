---
phase: 26-additional-view-types
plan: 03
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx
autonomous: true
requirements: [VIEW-03]

must_haves:
  truths:
    - "User can view a collection as a calendar, with records plotted by a chosen date property (VIEW-03, D-01)"
    - "Records with no value set for the chosen date property render in an 'Unscheduled' tray rather than being silently hidden (D-02)"
    - "Scope is a single month view with prev/next navigation; clicking an empty day cell creates a new record with that date pre-set and enters inline-editable title state (D-03)"
    - "Calendar/Timeline prev/next nav buttons carry accessible labels (aria-label='Previous month'/'Next month')"
  artifacts:
    - path: "apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx"
      provides: "Month-grid calendar view with Unscheduled tray, prev/next nav, and click-to-create"
      contains: "Unscheduled"
  key_links:
    - from: "apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx"
      to: "apps/workspaces/src/lib/hooks/useRecords.ts"
      via: "useCreateRecord for click-to-create, useUpdateAnyRecord for inline title commit"
      pattern: "useCreateRecord\\("
    - from: "apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx"
      to: "apps/workspaces/src/components/projectPage/board/BoardCard.tsx"
      via: "import { formatCardPropertyValue }"
      pattern: "formatCardPropertyValue"
---

<objective>
Ship VIEW-03: a Calendar view renderer for the `collection-view` block — a month grid plotting records by a user-chosen `date`-type property (D-01), an "Unscheduled" tray for date-less records so nothing disappears silently (D-02), and click-an-empty-day-to-create with inline-editable title entry mirroring Phase 24's D-07 pattern (D-03).

Purpose: Calendar is the most interactive of the four new view types (the only one with a create-record affordance), so it gets its own dedicated plan rather than being bundled with List/Gallery.
Output: `CollectionCalendarView.tsx` (new). Not wired into `BoardBlock.tsx` yet — that happens in Plan 26-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-additional-view-types/26-CONTEXT.md
@.planning/phases/26-additional-view-types/26-PATTERNS.md
@.planning/phases/26-additional-view-types/26-UI-SPEC.md
@.planning/phases/26-additional-view-types/26-01-SUMMARY.md
</context>

<interfaces>
From `apps/workspaces/src/components/projectPage/board/BoardCard.tsx` (Plan 26-01):
```typescript
export function formatCardPropertyValue(value: unknown, property: CollectionPropertyDef, personNames: Map<string, string>): string;
```
BoardCard's inline-editable-title state machine (lines 79-134, to replicate for calendar chips created via click-to-create) — `editing`/`draft` state, `debounceTimer`/`lastCommittedRef`, `flush`/`handleChange`/`commitAndExit`, all keyed off `autoFocusEdit`/`onExitEdit` props.

From `apps/workspaces/src/lib/hooks/useRecords.ts` (existing, unmodified):
```typescript
export function useCreateRecord(collectionId: string): UseMutationResult<CollectionRecord, unknown, CreateRecordInput>;
export function useUpdateAnyRecord(collectionId: string): UseMutationResult<CollectionRecord, unknown, { id: string; data: UpdateRecordInput }>;
```

From `apps/workspaces/src/components/records/PropertyField.tsx` (line 41, confirms date-value format): `date`-type property values are plain strings in `YYYY-MM-DD` format (HTML `<input type="date">` convention) — construct day keys with local-date components (`getFullYear`/`getMonth`/`getDate`, zero-padded), NOT `toISOString().slice(0,10)`, which is UTC-based and can shift the displayed day near midnight in non-UTC timezones.

From `apps/workspaces/src/components/projectPage/collectionTable/CollectionTableView.tsx` (existing, unmodified — empty-state copy precedent).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build the month grid, Unscheduled tray, and read-only record plotting</name>
  <files>apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/board/BoardCard.tsx (lines 137-159 — non-editing chip body layout + exported `formatCardPropertyValue`; line 142 — click-through pattern)
    apps/workspaces/src/components/projectPage/collectionTable/CollectionTableView.tsx (empty-state shell to reuse)
    apps/workspaces/src/lib/hooks/useTeam.ts (`useTeam` signature)
    .planning/phases/26-additional-view-types/26-CONTEXT.md (D-01, D-02, D-03)
    .planning/phases/26-additional-view-types/26-UI-SPEC.md (Interaction Contract rows "Calendar date-property picker (D-01)", "Calendar grid (D-03)", "Unscheduled tray (D-02)" — exact grid dimensions, spacing, color, and copy)
  </read_first>
  <action>
    Create `apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx`, a `'use client'` component exporting `CollectionCalendarView({ collection, records, collectionId, titlePropId, calendarDateProperty, cardProperties }: { collection: DataCollection; records: CollectionRecord[]; collectionId: string; titlePropId: string; calendarDateProperty: string; cardProperties: CollectionPropertyDef[] })`. Import `formatCardPropertyValue` from `'../board/BoardCard'` and `useTeam`, building `personNames` the same way other view renderers do.

    If `!calendarDateProperty` (unset), render the shared empty-state shell with heading `"Choose a date property to plot this calendar"` (Body tier).

    Otherwise, maintain local `const [currentMonth, setCurrentMonth] = useState(() => new Date())` (not persisted to `view.config` — D-03 scopes to local prev/next nav only, unpersisted across sessions). Compute the visible month's day cells (weekday-aligned grid, Sunday-first) and a `formatDateKey(date: Date): string` helper using local `getFullYear`/`getMonth() + 1`/`getDate()`, zero-padded, joined with `-` (matching the `YYYY-MM-DD` `date`-input convention — do not use `toISOString()`).

    Partition `records` into `scheduled` (grouped by `formatDateKey` of their `props[calendarDateProperty]` value, when truthy) and `unscheduled` (`props[calendarDateProperty]` falsy).

    Render: weekday header row (`Sun`–`Sat`, Label tier, `uppercase tracking-wider`); month nav header with `ChevronLeft`/`ChevronRight` icon buttons (`min-h-[32px] min-w-[32px]`, `aria-label="Previous month"` / `aria-label="Next month"` respectively — required for accessibility) flanking a month label (e.g. `currentMonth.toLocaleString('default', { month: 'long', year: 'numeric' })`, Heading tier) — clicking these adjusts `currentMonth` by ±1 month; day-cell grid (`min-h-[96px]` per cell, `border border-gray-200 dark:border-slate-700`) — today's cell shows its day number inside a `primary-600`-filled circular pill (`inline-flex items-center justify-center w-6 h-6 rounded-full bg-primary-600 text-white`), other cells show a plain day number. Inside each cell, render one read-only chip per scheduled record for that day: title + first `cardProperties[0]` value (truncated), `onClick` opens `window.open(\`/collections/${collectionId}/records/${record.id}\`, '_blank', 'noopener,noreferrer')`.

    Layout: `<div className="flex flex-col lg:flex-row gap-6">` — the month grid first (`flex-1`), then the Unscheduled tray (`w-64 flex-shrink-0 bg-gray-50 dark:bg-slate-800/60 rounded-lg p-3`) with heading `"Unscheduled"` (Heading tier) and the same read-only chip rendering for `unscheduled` records; when `unscheduled.length === 0`, show `"All records have a date"` (`text-gray-400 italic`, Body tier).
  </action>
  <acceptance_criteria>
    - `grep -c "export function CollectionCalendarView" apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx` returns exactly 1, with props including `calendarDateProperty: string`
    - `grep -c 'aria-label="Previous month"' apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx` returns >= 1 and `grep -c 'aria-label="Next month"'` returns >= 1
    - `grep -c "Unscheduled" apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx` returns >= 1 and `grep -c "All records have a date"` returns >= 1
    - `grep -c "Choose a date property to plot this calendar" apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx` returns >= 1
    - `grep -c "min-h-\[96px\]" apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx` returns >= 1 (day-cell height)
    - `grep -c "bg-primary-600" apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx` returns >= 1 (today's-date pill)
    - `grep -c "toISOString" apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx` returns 0 (local-date day-key helper used instead, avoiding the UTC-shift bug)
    - `cd apps/workspaces && npx tsc --noEmit` reports no new errors in this file
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep "CollectionCalendarView.tsx" || echo "no errors in CollectionCalendarView.tsx"</automated>
  </verify>
  <done>CollectionCalendarView.tsx renders a month grid with today's-date highlight, prev/next accessible nav, and an Unscheduled tray for date-less records; compiles cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add click-to-create with inline-editable title (D-03)</name>
  <files>apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx (this plan's Task 1 output — the file being extended)
    apps/workspaces/src/components/projectPage/board/BoardCard.tsx (lines 79-134 — the exact debounce/flush/commitAndExit inline-title-edit state machine to replicate for calendar chips; lines 111-134 — the editing-mode JSX to replicate)
    apps/workspaces/src/lib/hooks/useRecords.ts (`useCreateRecord`, `useUpdateAnyRecord` signatures)
    .planning/phases/26-additional-view-types/26-CONTEXT.md (D-03 — mirrors Phase 24's D-07 inline "+ New" pattern: title enters inline-editable state, no auto-navigation to record detail)
  </read_first>
  <action>
    In `CollectionCalendarView.tsx`, add `const createRecord = useCreateRecord(collectionId);` and `const updateRecord = useUpdateAnyRecord(collectionId);` inside the component. Add local state `const [autoFocusId, setAutoFocusId] = useState<string | null>(null)` to track which chip should render in inline-edit mode.

    Give each day cell an `onClick` handler on the cell's empty background (not on individual chips — chip `onClick` handlers must call `e.stopPropagation()` so clicking an existing chip does not also trigger cell-level creation) that computes that cell's `formatDateKey(date)` and calls `createRecord.mutateAsync({ props: { [titlePropId]: '', [calendarDateProperty]: dayIso } }).then((created) => setAutoFocusId(created.id)).catch((err) => console.error('Failed to create calendar record:', err))`.

    Extract or inline a chip sub-component (`CalendarChip`, module-private to this file) that replicates `BoardCard.tsx`'s `editing`/`draft`/`debounceTimer`/`lastCommittedRef`/`flush`/`handleChange`/`commitAndExit` state machine verbatim (do not reimplement from scratch — copy the logic shape), driven by an `autoFocusEdit={record.id === autoFocusId}` prop and `onExitEdit={() => setAutoFocusId(null)}`. When `editing` is true for a chip, render the same `autoFocus` `<input>` with `onBlur`/`onKeyDown` (Enter commits, Escape reverts without deleting the record) as `BoardCard.tsx` lines 118-132, calling `updateRecord.mutate({ id: record.id, data: { props: { ...record.props, [titlePropId]: next } } })` on commit (debounced, 800ms, matching `BoardCard`'s `flush` timing). When not editing, render the existing read-only chip body from Task 1, with `onClick={(e) => { e.stopPropagation(); window.open(...); }}`.

    Apply this `CalendarChip` to both the day-cell-plotted chips and the Unscheduled tray's chips (both need the same inline-edit-on-create behavior, since a newly created record could in principle land in either, though in practice a day-cell click always sets `calendarDateProperty`, so only day-cell chips receive `autoFocusEdit=true` in normal flow — Unscheduled tray chips reuse `CalendarChip` for consistency but will never be the `autoFocusId` target from this click path).
  </action>
  <acceptance_criteria>
    - `grep -c "useCreateRecord" apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx` returns >= 1
    - `grep -c "stopPropagation" apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx` returns >= 1 (chip clicks don't also trigger cell-level create)
    - `grep -c "autoFocusId" apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx` returns >= 2 (state + prop wiring)
    - `grep -c "props: { \[titlePropId\]: '', \[calendarDateProperty\]: " apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx` returns >= 1, OR an equivalent object literal constructing both keys together for the create call
    - New records created via day-cell click do NOT navigate to the record detail page (no `window.open` call inside the create-handler path, only inside the chip's non-editing click handler)
    - `cd apps/workspaces && npx tsc --noEmit` reports no new errors in this file
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep "CollectionCalendarView.tsx" || echo "no errors in CollectionCalendarView.tsx"</automated>
  </verify>
  <done>Clicking an empty day cell creates a record pre-set to that day's date and immediately enters inline-editable title state, matching Phase 24's D-07 pattern; existing chips remain click-through only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> `POST /api/v1/records/collections/:id/records` | Calendar's click-to-create flows through the existing authenticated, company-scoped `useCreateRecord` mutation — same endpoint Board's "+ New" column control already uses, no new backend surface |
| Browser -> `PATCH /api/v1/records/records/:id` | Inline title commit flows through the existing `useUpdateAnyRecord` mutation, same as `BoardCard.tsx` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-26-05 | Tampering | Calendar click-to-create `props` payload | accept | Payload only ever sets `titlePropId` (empty string) and `calendarDateProperty` (a locally computed `YYYY-MM-DD` string derived from the rendered grid, never user-supplied free text) — same trust level as Board's existing "+ New" column create call |
| T-26-06 | Denial of Service | Repeated day-cell clicks creating many records | accept | Same risk profile as Board's existing "+ New" inline-create button (no rate limiting added or removed by this plan); out of scope per CONTEXT.md, no throttling requirement raised |
| T-26-SC | Tampering | npm installs | accept | Zero new packages this plan |
</threat_model>

<verification>
- `cd apps/workspaces && npx tsc --noEmit` passes with no new errors.
- Manual/live smoke test (once wired in Plan 26-05): choose a date property via ViewSettingsMenu, confirm existing records with that property set plot on the correct day; confirm records without it appear in the Unscheduled tray; click an empty day cell, confirm a new record is created pre-set to that date and enters inline-editable title mode; type a title and blur, confirm it persists on refresh.
</verification>

<success_criteria>
- VIEW-03 satisfied: user can view a collection as a calendar plotted by a chosen date property, with an Unscheduled tray for date-less records and click-to-create on empty days.
</success_criteria>

<output>
Create `.planning/phases/26-additional-view-types/26-03-SUMMARY.md` when done
</output>
