---
phase: 33-tree-based-sidebar-ui-read-navigate
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workspaces/src/lib/api/folders.api.ts
  - apps/workspaces/src/lib/hooks/useFolders.ts
  - apps/workspaces/src/components/tree/treeFilters.ts
  - apps/workspaces/src/components/tree/treeFilters.test.ts
  - apps/workspaces/src/components/tree/treeNodeUrl.ts
  - apps/workspaces/src/lib/hooks/useExpandedTreeNodes.ts
autonomous: true
requirements: [TREEUI-01, TREEUI-02, TREEUI-03, TREEUI-05]

must_haves:
  truths:
    - "Calling useFullTree() fetches GET /api/v1/folders/tree/full and returns a cached TreeNode[] via React Query, sharing one network request across all consumers"
    - "pruneTree() removes archived data_collection/project_page nodes and module-disabled nodes, and recursively drops folders left with zero surviving children"
    - "D-03: filtering happens entirely client-side over the already-fetched useFullTree() response — Phase 32's GET /folders/tree/full endpoint is not touched"
    - "Expand/collapse state read/write is namespaced per signed-in user id in localStorage, so switching users does not leak expand state"
  artifacts:
    - path: "apps/workspaces/src/lib/api/folders.api.ts"
      provides: "TreeNode interface + foldersApi.getFullTree() fetch function"
      contains: "getFullTree"
    - path: "apps/workspaces/src/lib/hooks/useFolders.ts"
      provides: "useFullTree() React Query hook, separate query key from useFolderTree()"
      contains: "useFullTree"
    - path: "apps/workspaces/src/components/tree/treeFilters.ts"
      provides: "pruneTree() and isArchived() pure functions"
      contains: "export function pruneTree"
    - path: "apps/workspaces/src/components/tree/treeNodeUrl.ts"
      provides: "node_type -> route mapping (single source of truth)"
      contains: "export function treeNodeUrl"
    - path: "apps/workspaces/src/lib/hooks/useExpandedTreeNodes.ts"
      provides: "localStorage-backed expand/collapse state hook, namespaced by user id"
      contains: "export function useExpandedTreeNodes"
  key_links:
    - from: "apps/workspaces/src/lib/hooks/useFolders.ts"
      to: "apps/workspaces/src/lib/api/folders.api.ts"
      via: "foldersApi.getFullTree queryFn"
      pattern: "queryFn:\\s*foldersApi\\.getFullTree"
    - from: "apps/workspaces/src/components/tree/treeFilters.ts"
      to: "apps/workspaces/src/lib/api/folders.api.ts"
      via: "TreeNode type import"
      pattern: "import type \\{ TreeNode \\} from '@/lib/api/folders.api'"
---

<objective>
Build the data-layer foundation for the tree-based sidebar: a new `getFullTree()` API function + `useFullTree()` hook targeting Phase 32's already-shipped `GET /folders/tree/full`, plus three dependency-free pure utilities (module/archive pruning, node-type-to-route mapping, per-user expand/collapse persistence) that all downstream tree/breadcrumb components will consume unchanged.

Purpose: Establish the contracts (TreeNode type, query key, filter/route/persistence functions) other plans in this phase build against, so Wave 2 plans can implement rendering without re-deriving these decisions.
Output: `foldersApi.getFullTree`, `useFullTree()`, `pruneTree()`/`isArchived()`, `treeNodeUrl()`, `useExpandedTreeNodes()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-tree-based-sidebar-ui-read-navigate/33-CONTEXT.md
@.planning/phases/33-tree-based-sidebar-ui-read-navigate/33-RESEARCH.md
@.planning/phases/33-tree-based-sidebar-ui-read-navigate/33-PATTERNS.md

<interfaces>
<!-- Backend TreeNode shape this plan's client type must mirror exactly. -->
<!-- Source: apps/api/src/domains/folders/folders.types.ts:31-38 -->
```typescript
export interface TreeNode {
  node_type: 'folder' | 'project' | 'program' | 'data_collection' | 'project_page';
  id: string;
  company_id: string;
  name: string;
  children: TreeNode[];
  raw: Folder | Project | Program | ProjectPage | DataCollectionRow;
}
```
Response envelope confirmed at `apps/api/src/domains/folders/folders.controller.ts:10-12`: `res.json({ tree: await foldersService.getFullTree(...) })` — the client must unwrap `r.tree`, not `r.folders` or `r.nodes`.

<!-- Existing client-side pattern to extend, not replace. -->
<!-- Source: apps/workspaces/src/lib/api/folders.api.ts (full file, 31 lines) -->
```typescript
import { apiFetch } from './client';

export interface Folder { id: string; company_id: string; parent_id: string | null; name: string; icon: string | null; color: string | null; sort_order: number; created_at: string; updated_at: string; }
export interface FolderTree extends Folder { children: FolderTree[]; }

const BASE = '/api/v1/folders';

export const foldersApi = {
  tree: () => apiFetch<{ folders: FolderTree[] }>(BASE).then((r) => r.folders),
  get: (id: string) => apiFetch<{ folder: Folder }>(`${BASE}/${id}`).then((r) => r.folder),
  create: (data) => apiFetch<{ folder: Folder }>(BASE, 'POST', data).then((r) => r.folder),
  update: (id, data) => apiFetch<{ folder: Folder }>(`${BASE}/${id}`, 'PATCH', data).then((r) => r.folder),
  move: (id, parentId) => apiFetch<{ folder: Folder }>(`${BASE}/${id}/move`, 'PATCH', { parent_id: parentId }).then((r) => r.folder),
  remove: (id) => apiFetch<void>(`${BASE}/${id}`, 'DELETE'),
};
```

<!-- Existing hook pattern to extend, not replace. -->
<!-- Source: apps/workspaces/src/lib/hooks/useFolders.ts:1-19 -->
```typescript
const qk = { folders: ['folders'] as const };

export function useFolderTree() {
  const { user } = useAuth();
  return useQuery({
    queryKey: qk.folders,
    queryFn: foldersApi.tree,
    enabled: !!user?.company_id,
    staleTime: 1000 * 60,
  });
}
```

<!-- AuthUser shape confirming user.id (not just user.company_id) is available. -->
<!-- Source: packages/types/src/index.ts:11-21 -->
```typescript
export interface AuthUser {
  id: string;
  email: string;
  first_name: string;
  last_name: string;
  role: UserRole;
  company_id: string | null;
  is_verified: boolean;
  avatar_url: string | null;
  subscription: 'active' | 'inactive' | 'none';
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add TreeNode type + getFullTree() to folders.api.ts, add useFullTree() hook to useFolders.ts</name>
  <files>apps/workspaces/src/lib/api/folders.api.ts, apps/workspaces/src/lib/hooks/useFolders.ts</files>
  <read_first>
    apps/workspaces/src/lib/api/folders.api.ts
    apps/workspaces/src/lib/hooks/useFolders.ts
    apps/api/src/domains/folders/folders.types.ts
    apps/api/src/domains/folders/folders.controller.ts
  </read_first>
  <action>
    In `folders.api.ts`: add and export a `TreeNode` interface matching the backend shape exactly — `node_type: 'folder' | 'project' | 'program' | 'data_collection' | 'project_page'`, `id: string`, `company_id: string`, `name: string`, `children: TreeNode[]`, `raw: Record<string, unknown>` (keep `raw` as `Record<string, unknown>` here; call sites narrow it per node_type as needed — do not attempt to import the backend's `Folder`/`Project`/`Program`/`ProjectPage`/`DataCollectionRow` types into the frontend). Do NOT reuse or extend the existing `FolderTree` interface — this is a distinct type for a distinct endpoint. Add `getFullTree: () => apiFetch<{ tree: TreeNode[] }>(`${BASE}/tree/full`).then((r) => r.tree)` as a new entry inside the existing `foldersApi` object, alongside (not replacing) `tree`, `get`, `create`, `update`, `move`, `remove`.
    In `useFolders.ts`: add a new query key `fullTree: ['folders', 'tree', 'full'] as const` inside the existing `qk` object (do not rename or remove `folders: ['folders'] as const`, which `useFolderTree()`/`Navbar.tsx` still depend on). Add `export function useFullTree()` following the exact `useFolderTree()` shape: `useQuery({ queryKey: qk.fullTree, queryFn: foldersApi.getFullTree, enabled: !!user?.company_id, staleTime: 1000 * 60 })`. Import `TreeNode` type re-export is not required in this file; `foldersApi.getFullTree`'s return type flows through automatically.
  </action>
  <acceptance_criteria>
    - `folders.api.ts` exports `interface TreeNode` with exactly the 6 fields listed above
    - `folders.api.ts`'s `foldersApi` object has a `getFullTree` entry that calls `apiFetch` against `` `${BASE}/tree/full` `` and unwraps `.tree` (not `.folders` or `.nodes`)
    - `foldersApi.tree`, `.get`, `.create`, `.update`, `.move`, `.remove` are all still present and unchanged
    - `useFolders.ts` exports `function useFullTree()` using query key `['folders', 'tree', 'full']`, distinct from `qk.folders` (`['folders']`)
    - `useFolderTree()` is unchanged (still uses `qk.folders` and `foldersApi.tree`)
  </acceptance_criteria>
  <verify>
    <automated>cd "apps/workspaces" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>TypeScript compiles cleanly; `TreeNode`, `foldersApi.getFullTree`, and `useFullTree()` exist and are importable without touching `FolderTree`/`useFolderTree`/`Navbar.tsx`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create treeFilters.ts (pruneTree/isArchived) + treeFilters.test.ts, and treeNodeUrl.ts (routing table)</name>
  <files>apps/workspaces/src/components/tree/treeFilters.ts, apps/workspaces/src/components/tree/treeFilters.test.ts, apps/workspaces/src/components/tree/treeNodeUrl.ts</files>
  <read_first>
    apps/workspaces/src/lib/api/folders.api.ts
    apps/workspaces/src/components/layout/WorkspaceSidebar.tsx
    apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx
    apps/workspaces/src/components/shared/treeFindPath.test.ts
  </read_first>
  <behavior>
    - Test 1: Given a folder node whose only child is a `data_collection` node with `raw.archived_at` set to a non-null timestamp, `pruneTree` returns `[]` (archived leaf removed, then the now-empty folder is removed too).
    - Test 2: Given a `data_collection` node with `archived_at: null` and `enabledModules` not containing `'records'`, `pruneTree` excludes the node (module-gated regardless of archive state).
    - Test 3: Given a `folder` node (non-archivable node_type) with an arbitrary `raw` shape and at least one surviving non-archived, module-enabled child, `pruneTree` keeps the folder and its surviving child.
    - Test 4: Given a `project` node (never module-gated, per D-04 — absent from `MODULE_KEY`) with `enabledModules` as an empty `Set`, `pruneTree` still returns the project node (module gate never applies to `folder`/`project`).
  </behavior>
  <action>
    Create `apps/workspaces/src/components/tree/treeFilters.ts`. Import `type { TreeNode } from '@/lib/api/folders.api'`. Define `const MODULE_KEY: Partial<Record<TreeNode['node_type'], string>> = { data_collection: 'records', project_page: 'records', program: 'programs' }` (per D-04 — `folder` and `project` are intentionally absent, never gated). Define `function isArchived(node: TreeNode): boolean` returning `false` for any `node_type` other than `'data_collection'`/`'project_page'`, and for those two types, reading `(node.raw as { archived_at: string | null }).archived_at != null` (per D-12/RESEARCH.md's verified gap: `folders`/`projects`/`programs` are pre-filtered server-side, `data_collections`/`project_pages` are not). Define `export function pruneTree(nodes: TreeNode[], enabledModules: Set<string>): TreeNode[]` (D-03 — filtering happens entirely client-side over the already-fetched `GET /folders/tree/full` response, same approach as today's `ITEMS` filtering against `workspace.enabled_modules`; Phase 32's API is not touched) using `reduce` to build a new array: skip archived nodes, skip nodes whose `MODULE_KEY[node.node_type]` is set and not in `enabledModules`, recursively prune `children`, and if `node.node_type === 'folder'` and it originally had children but the pruned `children` array is empty, drop the folder too (D-01/D-02 recursive prune with empty-folder bubbling). Never mutate the input array or its nodes — always return new objects/arrays.
    Create `apps/workspaces/src/components/tree/treeFilters.test.ts` mirroring the plain-Node, no-new-dependency smoke-script pattern already used in `apps/workspaces/src/components/shared/treeFindPath.test.ts` (top-level code, Node's built-in `assert` module, throws a descriptive `Error` on any mismatch, executed via `npx --yes tsx`). Exercise all 4 behaviors listed above by constructing minimal `TreeNode` fixtures inline (do not import a fixture file — none exists yet).
    Create `apps/workspaces/src/components/tree/treeNodeUrl.ts`. Import `type { TreeNode } from '@/lib/api/folders.api'`. Define `export function treeNodeUrl(node: TreeNode): string | null` with a `switch (node.node_type)` returning: `'project'` -> `` `/projects/${node.id}` ``, `'program'` -> `` `/programs/${node.id}` ``, `'project_page'` -> `` `/projects/${(node.raw as { project_id: string }).project_id}/pages/${node.id}` ``, `'data_collection'` -> `` `/collections/${node.id}` ``, `'folder'` -> `null` (folders have no detail page — per RESEARCH.md Open Question 2 recommendation, folder rows toggle expand/collapse only).
  </action>
  <acceptance_criteria>
    - `treeFilters.ts` exports `pruneTree(nodes, enabledModules)` returning a new array (input array reference is not returned/mutated)
    - Given a folder node whose only child is a `data_collection` node with `archived_at` set, `pruneTree` returns `[]` for that branch (archived leaf removed, then empty folder removed)
    - Given a `data_collection` node with `MODULE_KEY.data_collection = 'records'` and `enabledModules` not containing `'records'`, the node is excluded regardless of `archived_at`
    - Given a `folder` node with `archived_at` in its `raw` (folders don't carry an `archived_at` gate in this function — `isArchived` returns `false` for any node_type other than `data_collection`/`project_page`), the folder itself is never excluded by `isArchived`
    - `treeFilters.test.ts` exercises all 4 documented behaviors and exits non-zero / throws on any failed assertion
    - `treeNodeUrl.ts` exports `treeNodeUrl(node)` returning `null` for `node_type: 'folder'` and a non-null string for all four other node types
  </acceptance_criteria>
  <verify>
    <automated>cd "apps/workspaces" && npx tsc --noEmit -p tsconfig.json && npx --yes tsx src/components/tree/treeFilters.test.ts</automated>
  </verify>
  <done>Both files exist, export the specified pure functions, compile cleanly, contain no React/JSX/side effects, and `treeFilters.test.ts` passes when run via `npx tsx`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create useExpandedTreeNodes.ts (per-user localStorage expand/collapse persistence)</name>
  <files>apps/workspaces/src/lib/hooks/useExpandedTreeNodes.ts</files>
  <read_first>
    apps/workspaces/src/context/AuthContext.tsx
    apps/cmr/src/app/page.tsx
  </read_first>
  <action>
    Create `apps/workspaces/src/lib/hooks/useExpandedTreeNodes.ts` as a `'use client'` module. Import `useState, useCallback` from `'react'` and `useAuth` from `'@/context/AuthContext'`. Define `function storageKey(userId: string | undefined) { return \`vectra_tree_expanded_${userId ?? 'anon'}\`; }`. Export `function useExpandedTreeNodes()` that: reads `user` from `useAuth()`, computes `key = storageKey(user?.id)`, initializes `expanded` state as a `Set<string>` via `useState(() => { if (typeof window === 'undefined') return new Set(); try { const raw = window.localStorage.getItem(key); return raw ? new Set(JSON.parse(raw) as string[]) : new Set(); } catch { return new Set(); } })`, and exposes a `toggle(id: string)` callback (via `useCallback`, deps `[key]`) that flips membership of `id` in a copied `Set`, calls `window.localStorage.setItem(key, JSON.stringify([...next]))` inside a try/catch (swallow quota/SSR errors), and returns `{ expanded, toggle }`. Namespacing by `user?.id` is mandatory — do not use a single global storage key (multi-user SSO app, unlike `apps/cmr`'s single-tenant assumption).
  </action>
  <acceptance_criteria>
    - `useExpandedTreeNodes()` returns an object with `expanded: Set<string>` and `toggle: (id: string) => void`
    - The localStorage key used is `` `vectra_tree_expanded_${user?.id ?? 'anon'}` `` — verifiable by reading the file for the `storageKey` function and its call sites
    - `toggle` both updates in-memory state and calls `window.localStorage.setItem` with the namespaced key
    - No `window`/`localStorage` access happens outside a `typeof window === 'undefined'` guard or try/catch (SSR-safe)
  </acceptance_criteria>
  <verify>
    <automated>cd "apps/workspaces" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>File exists, exports `useExpandedTreeNodes`, compiles cleanly, and namespaces storage by the signed-in user's id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser localStorage -> React state | Locally-stored expand/collapse node-id set, read back into UI state on mount |
| Client -> GET /folders/tree/full | Already-authenticated request to an existing, unmodified Phase 32 endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | `useExpandedTreeNodes` localStorage key | mitigate | Namespace the storage key by `user?.id` (Task 3) so a second user on a shared browser profile cannot see the first user's expanded folder-id set on mount |
| T-33-02 | Information Disclosure | `getFullTree`/`useFullTree` | accept | Not a new risk — `GET /folders/tree/full` already scopes all underlying queries by `company_id` server-side (Phase 32, unmodified); this plan only adds a client-side fetch wrapper, no new query logic |
| T-33-SC | Tampering | npm installs | accept | This plan installs no new packages — all work composes `@tanstack/react-query`, `lucide-react`, and built-in `localStorage`, already present in `apps/workspaces/package.json` |
</threat_model>

<verification>
Run `cd "apps/workspaces" && npx tsc --noEmit -p tsconfig.json` after all three tasks — must exit 0 with no type errors introduced by the new files or the two modified files.
Manually confirm in a Node REPL or scratch script (optional, not blocking) that `pruneTree` behaves per the acceptance criteria scenarios above before Wave 2 consumes it.
</verification>

<success_criteria>
- `foldersApi.getFullTree()` and `useFullTree()` exist, target `/folders/tree/full`, and share one query key across future consumers
- `pruneTree`/`isArchived`/`treeNodeUrl` are pure, dependency-free, and exported for Wave 2 plans to import unchanged
- `useExpandedTreeNodes()` persists expand/collapse state per signed-in user
- No existing file (`useFolderTree`, `Navbar.tsx`, `FolderTree` type) is modified or broken
</success_criteria>

<output>
Create `.planning/phases/33-tree-based-sidebar-ui-read-navigate/33.1-SUMMARY.md` when done
</output>
