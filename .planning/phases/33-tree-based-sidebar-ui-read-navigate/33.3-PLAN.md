---
phase: 33-tree-based-sidebar-ui-read-navigate
plan: 3
type: execute
wave: 2
depends_on: ["33.1"]
files_modified:
  - apps/workspaces/src/components/shared/treeFindPath.ts
  - apps/workspaces/src/components/shared/treeFindPath.test.ts
  - apps/workspaces/src/components/shared/Breadcrumbs.tsx
autonomous: true
requirements: [TREEUI-04, TREEUI-05]

must_haves:
  truths:
    - "Given a node_type + id present in the cached full tree, Breadcrumbs renders the ancestor chain from root to that node as clickable links, with the final segment as non-link current-page text"
    - "D-05: breadcrumb ancestor segments are clickable links, each navigating to that node via its canonical ID-keyed route"
    - "Breadcrumbs derives the ancestor path by walking the already-fetched useFullTree() cache — no new backend endpoint is added"
    - "Breadcrumb links navigate via existing ID-keyed routes (treeNodeUrl), never a new tree-relative route scheme"
  artifacts:
    - path: "apps/workspaces/src/components/shared/treeFindPath.ts"
      provides: "Pure DFS findPath(nodes, targetId) -> TreeNode[] | null function"
      contains: "export function findPath"
    - path: "apps/workspaces/src/components/shared/Breadcrumbs.tsx"
      provides: "Breadcrumbs component: walks cached tree via findPath, renders ChevronRight-separated clickable ancestor chain"
      contains: "export default function Breadcrumbs"
  key_links:
    - from: "apps/workspaces/src/components/shared/Breadcrumbs.tsx"
      to: "apps/workspaces/src/components/shared/treeFindPath.ts"
      via: "findPath() call"
      pattern: "findPath\\("
    - from: "apps/workspaces/src/components/shared/Breadcrumbs.tsx"
      to: "apps/workspaces/src/lib/hooks/useFolders.ts"
      via: "useFullTree() call (shared React Query cache)"
      pattern: "useFullTree\\(\\)"
---

<objective>
Build a reusable `Breadcrumbs` component that derives a node's ancestor chain by walking the already-cached `GET /folders/tree/full` result client-side (D-08 — no new backend endpoint), rendering each ancestor as a clickable link (D-05 — each ancestor segment navigates to that node via its canonical ID-keyed route; matches the "navigation only, canonical URLs are ID-keyed" framing from TREEUI-05) with the current node as trailing non-link text.

Purpose: Deliver TREEUI-04 (breadcrumbs reflect the live tree ancestor path) as a single, reusable component that Wave 3 wires into three existing detail pages, and TREEUI-05 (breadcrumb navigation stays on existing canonical routes).
Output: `treeFindPath.ts` (pure, unit-testable DFS) and `Breadcrumbs.tsx` (presentation + data wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-tree-based-sidebar-ui-read-navigate/33-CONTEXT.md
@.planning/phases/33-tree-based-sidebar-ui-read-navigate/33-UI-SPEC.md
@.planning/phases/33-tree-based-sidebar-ui-read-navigate/33-PATTERNS.md
@.planning/phases/33-tree-based-sidebar-ui-read-navigate/33.1-SUMMARY.md

<interfaces>
<!-- Contracts from Wave 1 (33.1) this plan consumes directly. -->

From apps/workspaces/src/lib/api/folders.api.ts:
```typescript
export interface TreeNode {
  node_type: 'folder' | 'project' | 'program' | 'data_collection' | 'project_page';
  id: string;
  company_id: string;
  name: string;
  children: TreeNode[];
  raw: Record<string, unknown>;
}
```

From apps/workspaces/src/lib/hooks/useFolders.ts:
```typescript
export function useFullTree(): UseQueryResult<TreeNode[]>; // shared cache, queryKey ['folders','tree','full']
```

From apps/workspaces/src/components/tree/treeNodeUrl.ts:
```typescript
export function treeNodeUrl(node: TreeNode): string | null;
```

<!-- Existing single-level "ancestor nav" idiom to generalize, not copy verbatim. -->
<!-- Source: apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx:108-121 -->
```typescript
<div className="flex items-center gap-1 text-sm text-gray-500 flex-wrap">
  <Link href={`/projects/${projectId}`} className="inline-flex items-center gap-1 hover:text-gray-700 dark:hover:text-gray-200">
    <ArrowLeft className="w-4 h-4" /> Project
  </Link>
  {parent && (
    <>
      <span className="text-gray-300">/</span>
      <Link href={`/projects/${projectId}/pages/${parent.id}`} className="hover:text-gray-700 dark:hover:text-gray-200">
        {parent.title}
      </Link>
    </>
  )}
</div>
```
This phase's `Breadcrumbs` replaces this idiom with a generic, tree-driven version — separator becomes `ChevronRight` (not `/`), and segments come from `findPath` over the cached tree, not manually threaded props.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create treeFindPath.ts — pure DFS ancestor-path finder</name>
  <files>apps/workspaces/src/components/shared/treeFindPath.ts, apps/workspaces/src/components/shared/treeFindPath.test.ts</files>
  <read_first>
    apps/workspaces/src/lib/api/folders.api.ts
  </read_first>
  <behavior>
    - Test 1: Given a tree with a single root node matching `targetId`, `findPath` returns `[rootNode]`.
    - Test 2: Given a tree where `targetId` is nested 3 levels deep (root -> child -> grandchild), `findPath` returns `[root, child, grandchild]` in that order.
    - Test 3: Given a tree where no node matches `targetId`, `findPath` returns `null`.
    - Test 4: Given an empty `nodes` array, `findPath` returns `null`.
  </behavior>
  <action>
    Create `apps/workspaces/src/components/shared/treeFindPath.ts`. Import `type { TreeNode } from '@/lib/api/folders.api'`. Export `function findPath(nodes: TreeNode[], targetId: string, trail: TreeNode[] = []): TreeNode[] | null` implementing depth-first search: for each `node` in `nodes`, build `nextTrail = [...trail, node]`; if `node.id === targetId` return `nextTrail`; otherwise recurse into `findPath(node.children, targetId, nextTrail)` and return the result if non-null; after the loop, return `null`. No mutation of input. Since no test runner is configured anywhere in `apps/workspaces` or the wider monorepo (confirmed in 33-RESEARCH.md/33-VALIDATION.md), do NOT introduce a new test framework for this single file — instead create `treeFindPath.test.ts` as a plain, dependency-free, runnable-via-`node`-with-`ts-node`-equivalent smoke script: write it as a `.ts` file whose top-level code calls `findPath` with the four scenarios above and `throw`s a descriptive `Error` on any mismatch (using Node's built-in `assert` module — no new package). This keeps the four behaviors verified via a real automated command (`npx ts-node` is not installed either; instead use `npx tsc --noEmit` for type-correctness plus a `node -e` smoke invocation compiled via a one-off `tsc` transpile, OR simplest: run the assertions through the existing Next.js/TypeScript toolchain is unnecessary — use plain Node.js by writing `treeFindPath.test.ts` in a way it can be executed with `node --experimental-strip-types` if available, falling back to documenting the exact manual command in the task's verify block). Prioritize correctness of `findPath` itself; the test file's execution mechanism is secondary and may be marked for manual `ts-node`-less verification if the exact runnable command cannot be confirmed at plan time — the executor should try `npx tsx apps/workspaces/src/components/shared/treeFindPath.test.ts` (using the `tsx` package via `npx`, which does not require installation as a persistent devDependency, only a transient `npx` invocation — this matches the "no persistent new packages" constraint since `npx` does not modify `package.json`/lockfiles) and record actual output in the task's completion notes.
  </action>
  <acceptance_criteria>
    - `treeFindPath.ts` exports `findPath(nodes, targetId, trail?)` matching the signature above
    - `findPath` returns `null` (not `undefined`, not throwing) when `targetId` is not found or `nodes` is empty
    - `findPath` returns an array whose last element's `id === targetId` and whose earlier elements are the correct ancestor chain in root-to-target order
    - `treeFindPath.test.ts` exercises all 4 behaviors listed above and exits non-zero / throws on any failed assertion
  </acceptance_criteria>
  <verify>
    <automated>cd "apps/workspaces" && npx tsc --noEmit -p tsconfig.json && npx --yes tsx src/components/shared/treeFindPath.test.ts</automated>
  </verify>
  <done>`findPath` compiles, is pure/side-effect-free, and all 4 documented behaviors pass when the test script is executed via `npx tsx` (transient, no persistent devDependency added).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Breadcrumbs.tsx — ancestor-chain rendering component</name>
  <files>apps/workspaces/src/components/shared/Breadcrumbs.tsx</files>
  <read_first>
    apps/workspaces/src/components/shared/treeFindPath.ts
    apps/workspaces/src/lib/hooks/useFolders.ts
    apps/workspaces/src/components/tree/treeNodeUrl.ts
    apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx
    .planning/phases/33-tree-based-sidebar-ui-read-navigate/33-UI-SPEC.md
  </read_first>
  <action>
    Create `apps/workspaces/src/components/shared/Breadcrumbs.tsx` as a `'use client'` component. Props: `{ nodeType: TreeNode['node_type']; id: string; trailingLabel?: string }` (import `TreeNode` from `@/lib/api/folders.api`; `trailingLabel` is an optional override for the final segment's text — needed by Wave 3's record-detail page, where the record itself is not a tree node, so its `data_collection` parent's `findPath` result is used for ancestors and `trailingLabel` supplies the record's own display name manually). Call `const { data: tree } = useFullTree()`. If `tree` is falsy (still loading), render `null` (per UI-SPEC: no skeleton, no placeholder — matches the "shared cache already in flight from AppShell mount" rationale). Otherwise call `const path = findPath(tree, id)`. If `path` is `null` (node not found — e.g. deep link to a node outside the tenant's tree, or a transient cache miss), render `null` (fail silently rather than showing a broken breadcrumb; the page's own not-found handling already covers this case). Otherwise render a `<nav className="flex flex-wrap items-center gap-1.5 text-sm text-gray-500 dark:text-gray-400 mb-4">` containing: for each ancestor `node` in `path` except the last, a `Link href={treeNodeUrl(node) ?? '#'}` with classes `hover:text-primary-600 dark:hover:text-primary-400` wrapping `{node.name}`, followed by a `<ChevronRight className="w-3.5 h-3.5 text-gray-300 dark:text-gray-600" />` separator (import `ChevronRight` from `lucide-react`); for the last segment, render plain `<span className="font-semibold text-gray-900 dark:text-white">{trailingLabel ?? path[path.length - 1].name}</span>` with no link styling and no trailing separator (per UI-SPEC's "current segment: plain node name, semibold, no link styling, no trailing separator").
  </action>
  <acceptance_criteria>
    - `Breadcrumbs.tsx` default-exports a component accepting `{ nodeType, id, trailingLabel? }`
    - Renders `null` while `useFullTree()` data is not yet loaded (no skeleton)
    - Renders `null` when `findPath` returns `null` for the given `id`
    - All ancestor segments except the last are wrapped in `Link href={treeNodeUrl(node) ?? '#'}`
    - The last segment is plain `<span>` text (semibold), never a `Link`
    - `ChevronRight` (not a `/` character) separates segments, styled `w-3.5 h-3.5 text-gray-300 dark:text-gray-600`
    - Container uses `flex flex-wrap items-center gap-1.5` (no ellipsis-collapse logic, per D-07/UI-SPEC)
  </acceptance_criteria>
  <verify>
    <automated>cd "apps/workspaces" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Component compiles and matches every state (loading, not-found, populated) and every styling rule from UI-SPEC's Interaction Contract for breadcrumbs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client render of already-tenant-scoped tree data | `Breadcrumbs` only walks the already-fetched, already-tenant-scoped `useFullTree()` cache; no new data-fetching boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05 | Elevation of Privilege | Breadcrumb ancestor links | accept | `treeNodeUrl()` only produces existing, already-authorization-checked ID-keyed routes — identical reasoning to T-33-04 in 33.2-PLAN.md |
| T-33-06 | Information Disclosure | `findPath` returning `null` on not-found | accept | Silently rendering nothing (rather than an error) avoids leaking whether a given id exists in a tenant's tree to a user who reached this component via a stale/foreign deep link; the underlying page's own data fetch (e.g. `useRecord`) is the actual authorization boundary, unchanged by this phase |
| T-33-SC | Tampering | npm installs | accept | This plan uses `npx tsx` transiently for one test script (no `package.json`/lockfile change, no persistent devDependency); no other packages installed |
</threat_model>

<verification>
Run `cd "apps/workspaces" && npx tsc --noEmit -p tsconfig.json` after both tasks.
Run `npx --yes tsx src/components/shared/treeFindPath.test.ts` from `apps/workspaces` and confirm no thrown assertion errors.
</verification>

<success_criteria>
- `findPath` is verified via 4 automated behavior assertions (TREEUI-04's only unit-testable surface)
- `Breadcrumbs` renders a clickable ancestor chain ending in a plain current-segment label, sourced entirely from the shared `useFullTree()` cache (TREEUI-04, TREEUI-05)
- No new backend endpoint, no new persistent npm dependency
</success_criteria>

<output>
Create `.planning/phases/33-tree-based-sidebar-ui-read-navigate/33.3-SUMMARY.md` when done
</output>
