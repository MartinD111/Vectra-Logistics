---
phase: 33-tree-based-sidebar-ui-read-navigate
plan: 4
type: execute
wave: 3
depends_on: ["33.3"]
files_modified:
  - apps/workspaces/src/app/projects/[id]/page.tsx
  - apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx
  - apps/workspaces/src/app/collections/[collectionId]/records/[recordId]/page.tsx
autonomous: true
requirements: [TREEUI-04, TREEUI-05]

must_haves:
  truths:
    - "The project detail page, project page detail view, and record detail page each render live, tree-derived breadcrumbs above their existing content"
    - "Existing single-level back-links on the project and project-page detail views are replaced by the new Breadcrumbs component, not left duplicated alongside it"
    - "The record detail page (which previously had no back-navigation at all) now shows a breadcrumb chain ending in the record's own display name"
    - "No canonical route changes — all three pages keep their existing URL params and data-fetching hooks unchanged"
  artifacts:
    - path: "apps/workspaces/src/app/projects/[id]/page.tsx"
      provides: "Breadcrumbs replacing the '← Projects' back-link"
      contains: "<Breadcrumbs"
    - path: "apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx"
      provides: "Breadcrumbs replacing the two-level 'Project / parent' ancestor block"
      contains: "<Breadcrumbs"
    - path: "apps/workspaces/src/app/collections/[collectionId]/records/[recordId]/page.tsx"
      provides: "New Breadcrumbs insertion (collection ancestors + record's own title as trailing segment)"
      contains: "<Breadcrumbs"
  key_links:
    - from: "apps/workspaces/src/app/projects/[id]/page.tsx"
      to: "apps/workspaces/src/components/shared/Breadcrumbs.tsx"
      via: "JSX render with nodeType='project'"
      pattern: "<Breadcrumbs nodeType=\"project\""
    - from: "apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx"
      to: "apps/workspaces/src/components/shared/Breadcrumbs.tsx"
      via: "JSX render with nodeType='project_page'"
      pattern: "<Breadcrumbs nodeType=\"project_page\""
    - from: "apps/workspaces/src/app/collections/[collectionId]/records/[recordId]/page.tsx"
      to: "apps/workspaces/src/components/shared/Breadcrumbs.tsx"
      via: "JSX render with nodeType='data_collection' and trailingLabel=<record title>"
      pattern: "<Breadcrumbs nodeType=\"data_collection\""
---

<objective>
Wire the new `Breadcrumbs` component (33.3) into the three existing detail pages named in RESEARCH.md/PATTERNS.md, replacing the two pages' current single-level back-links and adding breadcrumb navigation to the record detail page where none exists today.

Purpose: Complete TREEUI-04 (breadcrumbs on any page/record/project reflect the live tree ancestor path) end-to-end, and confirm TREEUI-05 (no canonical URL/routing changes) by construction — this plan only inserts a component, it never modifies a route file's params or data hooks.
Output: Three modified page files, each rendering `<Breadcrumbs />` above their existing content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-tree-based-sidebar-ui-read-navigate/33-CONTEXT.md
@.planning/phases/33-tree-based-sidebar-ui-read-navigate/33-UI-SPEC.md
@.planning/phases/33-tree-based-sidebar-ui-read-navigate/33-PATTERNS.md
@.planning/phases/33-tree-based-sidebar-ui-read-navigate/33.3-SUMMARY.md

<interfaces>
From apps/workspaces/src/components/shared/Breadcrumbs.tsx:
```typescript
export default function Breadcrumbs(props: {
  nodeType: TreeNode['node_type'];
  id: string;
  trailingLabel?: string;
}): JSX.Element | null;
```

<!-- Current state of the three target files (exact lines to replace/insert). -->

apps/workspaces/src/app/projects/[id]/page.tsx (lines 116-119, inside `<div className="max-w-5xl mx-auto px-4 lg:px-8 py-8">`, immediately before the title block at line 121):
```typescript
<Link href="/projects" className="inline-flex items-center gap-1 text-sm text-gray-500 hover:text-gray-700 dark:hover:text-gray-200 mb-4">
  <ArrowLeft className="w-4 h-4" /> Projects
</Link>
```

apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx (lines 108-121, the whole `<div className="flex items-center justify-between gap-3 mb-4 flex-wrap">...</div>` ancestor block's LEFT half only — the right half with the Saved/Loading/buttons stays):
```typescript
<div className="flex items-center gap-1 text-sm text-gray-500 flex-wrap">
  <Link href={`/projects/${projectId}`} className="inline-flex items-center gap-1 hover:text-gray-700 dark:hover:text-gray-200">
    <ArrowLeft className="w-4 h-4" /> Project
  </Link>
  {parent && (
    <>
      <span className="text-gray-300">/</span>
      <Link href={`/projects/${projectId}/pages/${parent.id}`} className="hover:text-gray-700 dark:hover:text-gray-200">
        {parent.title}
      </Link>
    </>
  )}
</div>
```

apps/workspaces/src/app/collections/[collectionId]/records/[recordId]/page.tsx (full file, 115 lines) — no existing `Link`/`ArrowLeft`/back-nav of any kind. Top of returned JSX:
```typescript
return (
  <div className="min-h-[calc(100vh-64px)] bg-gray-50/50 dark:bg-dark-bg">
    <div className="max-w-6xl mx-auto px-4 lg:px-8 py-8 flex gap-6 items-start">
      <PropertyPanel collection={collection} record={record} onUpdateRecord={updateRecord} onUpdateSchema={updateSchema} />
      <div className="flex-1 min-w-0">
        ...
```
`collection.schema[0]` is the "title property" convention (confirmed live at `apps/workspaces/src/components/records/PropertyPanel.tsx:29,36` — `const titleProperty = collection.schema[0]`), and the record's display value lives at `record.props[titleProperty.id]` (a `string` when present, per `PropertyPanel.tsx:36`'s type guard). This is the value to pass as `Breadcrumbs`' `trailingLabel`, falling back to `'Untitled'` when absent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace back-links with Breadcrumbs in the two project-scoped detail pages</name>
  <files>apps/workspaces/src/app/projects/[id]/page.tsx, apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx</files>
  <read_first>
    apps/workspaces/src/app/projects/[id]/page.tsx
    apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx
    apps/workspaces/src/components/shared/Breadcrumbs.tsx
  </read_first>
  <action>
    In `apps/workspaces/src/app/projects/[id]/page.tsx`: add `import Breadcrumbs from '@/components/shared/Breadcrumbs';`. Replace the existing `<Link href="/projects" ...><ArrowLeft .../> Projects</Link>` block (currently at line ~117-119, inside the `max-w-5xl mx-auto px-4 lg:px-8 py-8` container, directly before the title/kpi block) with `<Breadcrumbs nodeType="project" id={id} />`. Remove the now-unused `ArrowLeft` import only if it is not referenced elsewhere in the file (grep the file first — `ArrowLeft` may still be used elsewhere; if it is, keep the import).
    In `apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx`: add `import Breadcrumbs from '@/components/shared/Breadcrumbs';`. Replace only the LEFT half of the existing `<div className="flex items-center justify-between gap-3 mb-4 flex-wrap">` block — i.e. the inner `<div className="flex items-center gap-1 text-sm text-gray-500 flex-wrap">...</div>` containing the `Project` link and conditional `parent.title` link — with `<Breadcrumbs nodeType="project_page" id={pageId} />`. Keep the outer `flex items-center justify-between gap-3 mb-4 flex-wrap` wrapper and the RIGHT half (Saved indicator, "New client page" button, "Sub-page" button) completely unchanged — `Breadcrumbs` becomes the new left child of that same flex row. The `parent` variable (computed via `page?.parent_page_id ? (allPages ?? []).find(...) : null`) becomes unused for breadcrumb purposes; leave its declaration in place only if referenced elsewhere in the file (grep first), otherwise remove it along with any now-dead `ArrowLeft` import (grep for other uses before removing).
  </action>
  <acceptance_criteria>
    - `projects/[id]/page.tsx` renders `<Breadcrumbs nodeType="project" id={id} />` in place of the old `/projects` back-link, at the same position in the JSX
    - `projects/[id]/pages/[pageId]/page.tsx` renders `<Breadcrumbs nodeType="project_page" id={pageId} />` as the left child of the existing `justify-between` flex row; the right-side Saved/button controls are unchanged
    - Neither file has a leftover, now-dead `Link`/`ArrowLeft` back-link duplicating what `Breadcrumbs` now renders
    - No import is removed if it is still referenced elsewhere in the same file (verified by grep before removal)
  </acceptance_criteria>
  <verify>
    <automated>cd "apps/workspaces" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Both pages compile cleanly and render `Breadcrumbs` in place of their old single-level back-link, with no duplicate navigation UI.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insert Breadcrumbs into the record detail page (no prior back-navigation)</name>
  <files>apps/workspaces/src/app/collections/[collectionId]/records/[recordId]/page.tsx</files>
  <read_first>
    apps/workspaces/src/app/collections/[collectionId]/records/[recordId]/page.tsx
    apps/workspaces/src/components/records/PropertyPanel.tsx
    apps/workspaces/src/components/shared/Breadcrumbs.tsx
  </read_first>
  <action>
    In `apps/workspaces/src/app/collections/[collectionId]/records/[recordId]/page.tsx`: add `import Breadcrumbs from '@/components/shared/Breadcrumbs';`. Records are not tree nodes (`TreeNode['node_type']` has no `'record'` variant per `folders.types.ts`), so the breadcrumb must resolve ancestors via the record's parent `data_collection` (using `collectionId` from `useParams`) and supply the record's own display title as the trailing segment via the `trailingLabel` prop. Compute `const titleProperty = collection?.schema[0]` and `const recordTitle = titleProperty && typeof record?.props[titleProperty.id] === 'string' ? (record.props[titleProperty.id] as string) : 'Untitled'` (mirrors `PropertyPanel.tsx`'s existing title-resolution convention exactly). Insert `{collection && <Breadcrumbs nodeType="data_collection" id={collectionId} trailingLabel={recordTitle} />}` as the first child inside the returned `<div className="max-w-6xl mx-auto px-4 lg:px-8 py-8 flex gap-6 items-start">` wrapper — since that wrapper is a flex row containing `PropertyPanel` and the canvas side-by-side, wrap the whole return in an outer `<div className="min-h-[calc(100vh-64px)] bg-gray-50/50 dark:bg-dark-bg"><div className="max-w-6xl mx-auto px-4 lg:px-8 pt-8">{collection && <Breadcrumbs .../>}</div><div className="max-w-6xl mx-auto px-4 lg:px-8 pb-8 flex gap-6 items-start">...</div></div>` structure (breadcrumb row above the two-column content, not inside the flex row itself) — i.e. split the existing single `py-8` container into a `pt-8` breadcrumb-only container and a `pb-8` flex-content container, both sharing the same `max-w-6xl mx-auto px-4 lg:px-8` horizontal rhythm so nothing shifts horizontally, only a breadcrumb row is added above.
  </action>
  <acceptance_criteria>
    - The record detail page renders `<Breadcrumbs nodeType="data_collection" id={collectionId} trailingLabel={recordTitle} />` above the existing `PropertyPanel`/canvas flex row, guarded by `collection &&` so it never renders before the collection has loaded
    - `recordTitle` is computed from `collection.schema[0]` and `record.props[titleProperty.id]`, matching `PropertyPanel.tsx`'s existing title convention, with `'Untitled'` fallback
    - The existing `max-w-6xl mx-auto px-4 lg:px-8` horizontal container rhythm is preserved (no horizontal shift to `PropertyPanel`/canvas content)
    - `PropertyPanel`, `LivePageCanvas`, and all existing autosave/effect logic in this file are unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd "apps/workspaces" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Record detail page compiles cleanly and renders a breadcrumb chain (collection ancestors + record title) above its existing two-column layout, with no regression to autosave or property-panel behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Page-level render of Breadcrumbs | Purely presentational insertion; no new data boundary — `Breadcrumbs` itself already only reads the shared, tenant-scoped `useFullTree()` cache (established in 33.3-PLAN.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07 | Information Disclosure | Record-detail breadcrumb trailing label | accept | `recordTitle` is derived from data the page already fetched and is already authorized to display (`record.props`) — no new field or endpoint is read |
| T-33-SC | Tampering | npm installs | accept | This plan installs no new packages |
</threat_model>

<verification>
Run `cd "apps/workspaces" && npx tsc --noEmit -p tsconfig.json` after both tasks.
Manual QA (per 33-VALIDATION.md): navigate to a project via the sidebar tree and via a cold deep link; confirm the breadcrumb chain matches in both cases. Navigate to a nested project page (with a parent page) and confirm two ancestor links plus the current page name render. Navigate to a record inside a collection and confirm the collection breadcrumb plus the record's own title (from its first schema property) render correctly, with no back-navigation left over.
</verification>

<success_criteria>
- All three detail views named in RESEARCH.md/PATTERNS.md render live, tree-derived breadcrumbs (TREEUI-04)
- No route, param, or data-hook changes in any of the three files — only presentational insertion (TREEUI-05)
- Old single-level back-links are fully replaced, not duplicated
</success_criteria>

<output>
Create `.planning/phases/33-tree-based-sidebar-ui-read-navigate/33.4-SUMMARY.md` when done
</output>
