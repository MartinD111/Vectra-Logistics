---
phase: 33-tree-based-sidebar-ui-read-navigate
plan: 4
subsystem: ui
tags: [react, nextjs, breadcrumbs, tree, navigation]

# Dependency graph
requires:
  - phase: 33-tree-based-sidebar-ui-read-navigate (33.3)
    provides: "Breadcrumbs component (nodeType/id/trailingLabel props), findPath ancestor-path resolution over the cached full tree"
provides:
  - "Live, tree-derived breadcrumb navigation on the project detail page, project page detail view, and record detail page"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Detail pages consume the shared Breadcrumbs component instead of hand-rolled single-level back-links, keeping ancestor-chain logic in one place"
    - "Pages for node types not tracked in the tree (e.g. records) pass trailingLabel to override the current-segment label while still resolving ancestors from a related tree node id (the parent data_collection)"

key-files:
  created: []
  modified:
    - apps/workspaces/src/app/projects/[id]/page.tsx
    - apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx
    - apps/workspaces/src/app/collections/[collectionId]/records/[recordId]/page.tsx

key-decisions:
  - "Followed plan exactly for Breadcrumbs prop wiring (nodeType/id/trailingLabel) on all three pages"
  - "Removed now-dead ArrowLeft imports and the unused `parent` lookup variable in the two project-scoped pages once their manual back-link/ancestor JSX was replaced"
  - "Split the record detail page's single py-8 container into a pt-8 breadcrumb-only row and a pb-8 flex-content row so the breadcrumb sits above the two-column layout without shifting horizontal alignment"

requirements-completed: [TREEUI-04, TREEUI-05]

# Metrics
duration: 10min
completed: 2026-07-20
---

# Phase 33 Plan 4: Wire Breadcrumbs into Detail Pages Summary

**Replaced the project and project-page detail views' single-level back-links with the shared `Breadcrumbs` component, and added breadcrumb navigation (with record-title trailing label) to the record detail page, which previously had none.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-07-20T10:00:00Z
- **Completed:** 2026-07-20T10:10:00Z
- **Tasks:** 2 completed
- **Files modified:** 3

## Accomplishments
- `projects/[id]/page.tsx` renders `<Breadcrumbs nodeType="project" id={id} />` in place of the old `← Projects` link
- `projects/[id]/pages/[pageId]/page.tsx` renders `<Breadcrumbs nodeType="project_page" id={pageId} />` as the left child of the existing `justify-between` header row, replacing the manual two-level `Project / parent-page` block
- `collections/[collectionId]/records/[recordId]/page.tsx` now renders `<Breadcrumbs nodeType="data_collection" id={collectionId} trailingLabel={recordTitle} />` above the existing `PropertyPanel`/canvas layout — the record itself is not a tree node, so ancestors resolve via the parent collection and the record's own display title (from `collection.schema[0]` / `record.props[titleProperty.id]`, mirroring `PropertyPanel.tsx`'s title convention) is passed as the trailing label
- No canonical route, param, or data-fetching hook changes in any of the three files — purely presentational insertion (confirms TREEUI-05 by construction)

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace back-links with Breadcrumbs in the two project-scoped detail pages** - `7fd1cc1` (feat)
2. **Task 2: Insert Breadcrumbs into the record detail page (no prior back-navigation)** - `a366be8` (feat)

**Plan metadata:** committed alongside this SUMMARY.

## Files Created/Modified
- `apps/workspaces/src/app/projects/[id]/page.tsx` - `← Projects` back-link replaced with `<Breadcrumbs nodeType="project" id={id} />`; dropped now-unused `ArrowLeft` import
- `apps/workspaces/src/app/projects/[id]/pages/[pageId]/page.tsx` - Two-level `Project / parent` ancestor block replaced with `<Breadcrumbs nodeType="project_page" id={pageId} />`; dropped now-unused `ArrowLeft` import and the `parent` lookup variable (right-side Saved/button controls untouched)
- `apps/workspaces/src/app/collections/[collectionId]/records/[recordId]/page.tsx` - Added `recordTitle` computation (`collection.schema[0]` + `record.props[titleProperty.id]`, `'Untitled'` fallback) and a breadcrumb row above the existing two-column `PropertyPanel`/canvas layout, splitting the outer container into a `pt-8` breadcrumb row and `pb-8` content row sharing the same horizontal rhythm

## Decisions Made
- Followed plan exactly for prop wiring, JSX placement, and container restructuring on the record detail page
- Removed dead imports/variables only after confirming via grep they were not referenced elsewhere in the same file

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None. `npx tsc --noEmit -p tsconfig.json` passed cleanly after each task.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All three detail views named in RESEARCH.md/PATTERNS.md now render live, tree-derived breadcrumbs (TREEUI-04 complete end-to-end)
- No route/param/data-hook changes in any of the three files — TREEUI-05 confirmed by construction
- Manual QA (per 33-VALIDATION.md) still recommended: navigate to a project via sidebar tree and via cold deep link to confirm breadcrumb parity; navigate to a nested project page with a parent to confirm two ancestor links render; navigate to a record inside a collection to confirm the collection breadcrumb plus record title render with no leftover back-navigation
- No blockers for future phases

---
*Phase: 33-tree-based-sidebar-ui-read-navigate*
*Completed: 2026-07-20*

## Self-Check: PASSED

All three modified files verified present on disk; both task commit hashes (7fd1cc1, a366be8) verified present in git log.
