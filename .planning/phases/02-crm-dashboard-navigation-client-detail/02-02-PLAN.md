---
phase: 02-crm-dashboard-navigation-client-detail
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workspaces/src/components/layout/WorkspaceSidebar.tsx
  - apps/workspaces/src/app/records/page.tsx
  - apps/workspaces/src/components/projectPage/AddClientModal.tsx
autonomous: true
requirements: [NAV-01, NAV-02, CLI-01, CLI-02, CLI-03]
must_haves:
  truths:
    - "User sees 'CRM' (not 'Records') in the left sidebar, still linking to /records"
    - "User can browse a full-page table of all clients at /records with name, country, credit-status badge, and responsible employee columns"
    - "User can search clients by name and toggle an 'over limit only' filter on the dashboard"
    - "User can add a single new client via a modal without leaving the dashboard"
    - "Clicking a client row opens that client's detail page in a new browser tab"
  artifacts:
    - path: "apps/workspaces/src/app/records/page.tsx"
      provides: "CRM dashboard: full-page client table with search, over-limit filter, add-client modal, new-tab row navigation"
      min_lines: 60
    - path: "apps/workspaces/src/components/projectPage/AddClientModal.tsx"
      provides: "Shared add-client form modal (name, country, VAT ID, credit limit, default rate)"
      min_lines: 30
  key_links:
    - from: "apps/workspaces/src/app/records/page.tsx"
      to: "apps/workspaces/src/lib/hooks/useCrm.ts"
      via: "useClients() data fetch"
      pattern: "useClients\\(\\)"
    - from: "apps/workspaces/src/app/records/page.tsx"
      to: "/records/[clientId]"
      via: "row click opens new tab"
      pattern: "window\\.open\\(.*_blank|target=.\\_blank."
---

<objective>
Rename the sidebar's never-built "Records" entry to "CRM" (NAV-01) and build the CRM dashboard itself: a full-page client data table at `/records` with name/country/credit-status/responsible-employee columns, a name search box, an "over limit only" toggle, and a simple "Add client" modal — replacing the compact in-project block as the primary way to browse clients (D-01 through D-04). Row click opens the client's detail page in a new tab (NAV-02), using the route built in Plan 03.

Purpose: Give users a dedicated home for CRM data, satisfying NAV-01/NAV-02 and the dashboard-side portions of CLI-01/02/03 (the columns surface address/notes/responsible-employee status at a glance; full edit happens on the detail page from Plan 03).
Output: `/records` page (dashboard), renamed sidebar nav item, reusable `AddClientModal` component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-CONTEXT.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-UI-SPEC.md
@.planning/phases/02-crm-dashboard-navigation-client-detail/02-PATTERNS.md

@apps/workspaces/src/components/layout/WorkspaceSidebar.tsx
@apps/workspaces/src/components/projectPage/CrmClientsBlock.tsx
@apps/workspaces/src/lib/hooks/useCrm.ts
@apps/workspaces/src/lib/api/crm.api.ts
</context>

<interfaces>
<!-- Existing hooks/types this plan consumes as-is, no changes needed to them -->

From apps/workspaces/src/lib/hooks/useCrm.ts:
```typescript
export function useClients(): UseQueryResult<CrmClient[]>
export function useCreateClient(): UseMutationResult<CrmClient, unknown, CreateClientInput>
```

From apps/workspaces/src/lib/api/crm.api.ts:
```typescript
export interface CrmClient { id: string; company_id: string; name: string; country: string; vat_id: string | null; email: string | null; credit_limit: number; outstanding_balance: number; default_rate_eur: number | null; notes: string | null; address: string | null; responsible_employee_id: string | null; created_at: string; updated_at: string; }
export interface CreateClientInput { name: string; country: string; vat_id?: string | null; email?: string | null; credit_limit?: number; default_rate_eur?: number | null; notes?: string | null; address?: string | null; responsible_employee_id?: string | null; }
```

Credit-status 3-tier logic to reuse exactly (source: CrmClientsBlock.tsx `CreditBar`, lines 17-20):
```typescript
const pct = client.credit_limit > 0 ? Math.min(100, (client.outstanding_balance / client.credit_limit) * 100) : 100;
const over = client.outstanding_balance >= client.credit_limit;
const color = over ? 'bg-red-500' : pct >= 70 ? 'bg-amber-500' : 'bg-emerald-500';
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename sidebar nav item + extract shared AddClientModal</name>
  <files>apps/workspaces/src/components/layout/WorkspaceSidebar.tsx, apps/workspaces/src/components/projectPage/AddClientModal.tsx</files>
  <read_first>apps/workspaces/src/components/layout/WorkspaceSidebar.tsx, apps/workspaces/src/components/projectPage/CrmClientsBlock.tsx</read_first>
  <action>
In `apps/workspaces/src/components/layout/WorkspaceSidebar.tsx`, change the `ITEMS` array entry `{ name: 'Records', href: '/records', icon: Boxes, module: 'records' }` to `{ name: 'CRM', href: '/records', icon: Boxes, module: 'records' }` (per D-01/NAV-01). Do not change `href` or `module` — `enabled_modules` gating is unaffected (confirmed in RESEARCH.md).

Create `apps/workspaces/src/components/projectPage/AddClientModal.tsx` — a new standalone modal component extracting `CrmClientsBlock.tsx`'s inline add-client form (lines 42-58, 75-91) into a reusable dialog per RESEARCH.md's Open Question 2 recommendation (extract shared component to avoid drift between the in-project block and the new dashboard). Props: `{ open: boolean; onClose: () => void }`. Internal state: `name`, `country` (default `'DE'`), `vatId`, `limit` (default `'10000'`), `rate` — same field set, same defaults, same `COUNTRIES` array (`['SI','DE','AT','IT','HR','HU','FR','NL','BE','PL','CZ','SK','ES','RS','CH','GB','TR']`) as `CrmClientsBlock.tsx`. Use `useCreateClient()` from `@/lib/hooks/useCrm` for the mutation; on success, call `onClose()` and reset form fields. Modal shell per UI-SPEC: centered overlay (`fixed inset-0 z-40 bg-black/40 flex items-center justify-center` backdrop, click closes), `saas-card` surface `max-w-md w-full mx-4`, close via backdrop click or an `X` icon button (`lucide-react`) in the top-right. Submit button label "Add client" (disabled while `create.isPending` or name is empty), matching UI-SPEC Copywriting Contract exactly.

Do NOT modify `CrmClientsBlock.tsx` in this task — it keeps its own inline form as-is (out of scope for this plan; only extract a new shared modal for the dashboard's use, since CrmClientsBlock's existing behavior must not regress per the roadmap's Phase 1 success criteria).
  </action>
  <acceptance_criteria>
    <source>apps/workspaces/src/components/layout/WorkspaceSidebar.tsx contains "name: 'CRM'" and does not contain "name: 'Records'"</source>
    <source>apps/workspaces/src/components/layout/WorkspaceSidebar.tsx still contains "href: '/records'" and "module: 'records'"</source>
    <source>apps/workspaces/src/components/projectPage/AddClientModal.tsx exports a component accepting "open" and "onClose" props and calls "useCreateClient"</source>
    <behavior>Visiting any workspace page with the sidebar visible shows "CRM" as the nav label, still routing to /records on click</behavior>
  </acceptance_criteria>
  <verify>
    <automated>MISSING — no test framework in repo (confirmed in RESEARCH.md Validation Architecture). Manual: `npm run dev` in apps/workspaces, visually confirm sidebar shows "CRM" linking to /records</automated>
  </verify>
  <done>Sidebar shows "CRM" label at the existing /records href; AddClientModal component exists, compiles, and submits via useCreateClient without touching CrmClientsBlock.tsx.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the /records CRM dashboard page</name>
  <files>apps/workspaces/src/app/records/page.tsx</files>
  <read_first>apps/workspaces/src/app/projects/page.tsx, apps/workspaces/src/components/projectPage/CrmClientsBlock.tsx, apps/workspaces/src/lib/hooks/useCrm.ts, apps/workspaces/src/components/projectPage/AddClientModal.tsx</read_first>
  <action>
Create `apps/workspaces/src/app/records/page.tsx` as a new client component (`'use client'`) — this route does not exist yet (confirmed in RESEARCH.md). Structure per UI-SPEC "CRM Dashboard" contract:

- Page container: `p-6` padding, page heading "CRM Dashboard" styled per UI-SPEC Display typography role (`text-3xl font-semibold`, 28px).
- Toolbar row (`flex items-center gap-3 mb-4`): search input (`saas-input`/`input-field` style, ~280px width, placeholder "Search clients by name…" per Copywriting Contract) bound to local `search` state; "Over limit only" toggle (pill/switch) bound to local `overLimitOnly` boolean state, label text "Over limit only"; "Add client" button (primary accent `bg-primary-600 hover:bg-primary-700 text-white`) that opens `AddClientModal` via local `addOpen` boolean state.
- Data: `const { data: clients, isLoading } = useClients();` Filter client-side (per RESEARCH.md Alternative Considered A2): filter by `name.toLowerCase().includes(search.toLowerCase())` AND (if `overLimitOnly`) `outstanding_balance >= credit_limit`.
- Table (flatter bordered table per UI-SPEC, not `saas-card` grid): `w-full border-collapse`, `thead` with columns in fixed order — Name, Country, Credit status, Responsible employee — `px-4 py-4` cell padding (16px vertical, grid-aligned per UI-SPEC spacing scale). Name cell: `font-semibold`. Country cell: badge style `text-[10px] font-bold px-1.5 py-0.5 rounded bg-gray-100 text-gray-500` (same as CrmClientsBlock's country tag). Credit-status cell: condensed badge — colored dot (`h-2 w-2 rounded-full` using the exact 3-tier color logic from the Interfaces section above) + percentage text, NOT the full bar (per UI-SPEC: "condensed to fit a table cell"). Responsible-employee cell: render `client.responsible_employee_id` — since this plan does not yet have a team-name lookup wired in, render literal `"Unassigned"` in `text-gray-400 italic` when null, and when non-null render the raw id string for now UNLESS a `useTeam()` lookup is trivially available (it is — reuse `apps/workspaces/src/lib/hooks/useTeam.ts`'s `useTeam()` to map `responsible_employee_id` to a team member's `first_name last_name`; build a `Map<id, name>` from `useTeam().data` and look up per row, falling back to "Unassigned" only when the id itself is null).
- Row: `cursor-pointer hover:bg-gray-50 dark:hover:bg-slate-800/60`, `onClick={() => window.open(\`/records/${client.id}\`, '_blank', 'noopener,noreferrer')}` (NAV-02 — same-app new-tab nav; explicitly NOT `crossAppUrl`, per RESEARCH.md's correction).
- Loading state: centered `Loader2` spinner + "Loading…" text, matching existing patterns (`PageBlockView.tsx`'s loading blocks).
- Empty state (zero clients, not zero after filtering): centered icon (`Building2` from `lucide-react`) + heading "No clients yet" + body "Add your first customer to start tracking credit limits and activity." + inline "Add client" button, per Copywriting Contract.
- Empty state (zero results after search/filter but clients exist): simple centered text, e.g. "No clients match your search." (Claude's discretion on exact wording per CONTEXT.md).
- Render `<AddClientModal open={addOpen} onClose={() => setAddOpen(false)} />` at the bottom of the component tree.
  </action>
  <acceptance_criteria>
    <source>apps/workspaces/src/app/records/page.tsx contains "useClients" and "window.open" and "AddClientModal"</source>
    <source>apps/workspaces/src/app/records/page.tsx does not import "crossAppUrl"</source>
    <source>apps/workspaces/src/app/records/page.tsx contains "Search clients by name" and "Over limit only" matching the Copywriting Contract exactly</source>
    <behavior>Visiting /records with zero clients shows the "No clients yet" empty state with a working "Add client" CTA</behavior>
    <behavior>Visiting /records with seeded clients shows a table row per client; typing in the search box filters rows client-side by name substring; toggling "Over limit only" filters to only rows where outstanding_balance >= credit_limit</behavior>
    <behavior>Clicking a client row opens a new browser tab at /records/{client.id} (Plan 03 builds the target page; this plan only needs the link/window.open call to fire correctly)</behavior>
  </acceptance_criteria>
  <verify>
    <automated>MISSING — no test framework in repo. Manual: npm run dev in apps/workspaces, navigate to /records, seed at least 2 clients (one over-limit, one under), verify table renders, search filters, toggle filters, row click opens new tab (target page may 404 until Plan 03 lands — that is expected and acceptable at this plan's completion)</automated>
  </verify>
  <done>/records renders a full-page client table with working search, over-limit filter, and Add client modal; row clicks fire window.open targeting /records/{id} in a new tab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → existing crm API (GET /clients, POST /clients) | Already authenticated via existing middleware from Phase 1; this plan adds no new endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering (XSS) | AddClientModal form inputs | accept | All fields render as plain React-controlled inputs (no dangerouslySetInnerHTML); React's default escaping is sufficient — no free-text HTML rendering introduced by this plan |
| T-02-05 | Information Disclosure | /records dashboard table | accept | Data already scoped server-side by company_id via existing `useClients()`/`GET /clients` (Phase 1 behavior, unchanged) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — reuses lucide-react, @tanstack/react-query already in package.json |
</threat_model>

<verification>
1. Sidebar shows "CRM" at `/records`, unchanged href/module.
2. `/records` renders a full-page table (not cards) with the four D-02 columns in order: Name, Country, Credit status, Responsible employee.
3. Search box filters by name substring; "Over limit only" toggle filters to over-limit clients only; both filters compose (AND).
4. "Add client" opens a modal with the same field set as the existing in-project form; submitting creates a client and the new client appears in the table after the mutation's query invalidation.
5. Row click opens `/records/{clientId}` in a new browser tab via `window.open`, not `crossAppUrl`.
</verification>

<success_criteria>
NAV-01 and NAV-02 fully satisfied; CLI-01/02/03 partially satisfied at the dashboard-glance level (full edit capability lands in Plan 03's detail page). D-01 through D-04 implemented exactly as decided in CONTEXT.md.
</success_criteria>

<output>
Create `.planning/phases/02-crm-dashboard-navigation-client-detail/02-02-SUMMARY.md` when done
</output>
