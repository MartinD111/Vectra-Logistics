---
phase: 28-security-tenancy-capabilities-foundation
plan: 04
type: execute
wave: 3
depends_on: ["28-02", "28-03"]
files_modified:
  - apps/api/src/domains/projects
  - apps/api/src/domains/folders
  - apps/api/src/domains/records
  - apps/api/src/domains/crm
  - apps/api/src/domains/integrations
  - apps/api/src/domains/pod
  - docs/architecture
  - docs/specs
autonomous: true
requirements: [SECCTX-03, SECCTX-04, SECCTX-05]

must_haves:
  truths:
    - "Cross-tenant negative tests must cover the pilot read/write paths named in the requirement set: folders, projects/pages/programs, records/views, CRM, marketplace/POD, and integrations (SECCTX-03)"
    - "Tests should fail if company scope is accidentally dropped from pilot repository/service paths, especially around raw `findById` helpers and public-edge tenant resolution seams"
    - "Phase 28 closes with operator-facing contract docs for tenant isolation, public trust, and explicit capability/demo behavior so later phases inherit one truth source instead of oral tradition"
  artifacts:
    - path: "apps/api/src/domains/projects"
      provides: "Negative tests pinning project/page/program tenant isolation"
      contains: "test"
    - path: "apps/api/src/domains/records"
      provides: "Negative tests pinning record/view tenant isolation"
      contains: "test"
    - path: "docs/architecture"
      provides: "Durable Phase 28 contract docs for request context, public trust, and demo/capability policy"
      contains: "tenant"
  key_links:
    - from: "apps/api/src/domains/projects/projects.repository.ts"
      to: "apps/api/src/domains/projects/*.test.ts"
      via: "Negative tests must fail if raw-id helpers or write paths lose company scoping"
      pattern: "WHERE .*company_id|findProject|findPage|findProgram"
    - from: "apps/api/src/domains/records/records.repository.ts"
      to: "apps/api/src/domains/records/*.test.ts"
      via: "Collection/record/view queries already scope by company and should be pinned against regression"
      pattern: "company_id"
    - from: "apps/api/src/domains/yard/gate.controller.ts"
      to: "docs/architecture"
      via: "The final docs should explain the canonical trust posture and the remaining migration path for public token routes"
      pattern: "public|trust"
---

<objective>
Prove and document the Phase 28 contract: add cross-tenant negative regression coverage for the pilot paths and publish durable architecture docs describing the new request, capability, and public-trust rules that later phases must obey.

Purpose: Phase 28 should end with enforceable tests and a visible operator/developer contract, not only code refactors.
Output: negative tests for the named pilot paths plus architecture/spec documentation for tenant isolation, public-edge trust, and explicit capability/demo behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-security-tenancy-capabilities-foundation/28-CONTEXT.md
@.planning/phases/28-security-tenancy-capabilities-foundation/28-RESEARCH.md
@.planning/phases/28-security-tenancy-capabilities-foundation/28-02-PLAN.md
@.planning/phases/28-security-tenancy-capabilities-foundation/28-03-PLAN.md
@docs/architecture/current-state-truth-matrix.md
@apps/api/src/domains/projects/projects.repository.ts
@apps/api/src/domains/records/records.repository.ts
@apps/api/src/domains/crm/crm.repository.ts
@apps/api/src/domains/integrations/integrations.repository.ts
@apps/api/src/domains/pod/pod.repository.ts
</context>

<interfaces>
The test output must make tenant-isolation regressions explicit. Suitable shapes include repository or service tests that assert:

```typescript
await assert.rejects(() => service.getProject(foreignProjectId, companyA));
await assert.equal(await repository.findRecord(recordId, companyA), null);
```

The exact harness may vary by domain, but the suite must cover both read and write paths where practical and must specifically guard against losing `company_id` scoping in SQL or service ownership checks.

The doc output should give one canonical Phase 28 contract summary for:
- shared RequestContext
- capability-evaluation boundary
- public-edge trust family
- explicit demo/degraded behavior policy
- deferred items, especially assignment-based visibility and remaining public-token migration work
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cross-tenant negative tests for the pilot read/write paths</name>
  <files>apps/api/src/domains/projects/*, apps/api/src/domains/folders/*, apps/api/src/domains/records/*, apps/api/src/domains/crm/*, apps/api/src/domains/integrations/*, apps/api/src/domains/pod/*</files>
  <read_first>
    apps/api/src/domains/projects/projects.repository.ts
    apps/api/src/domains/records/records.repository.ts
    apps/api/src/domains/crm/crm.repository.ts
    apps/api/src/domains/integrations/integrations.repository.ts
    apps/api/src/domains/pod/pod.repository.ts
    existing *.test.ts files in apps/api/src/domains/records and apps/api/src/domains/outlook for local test style
  </read_first>
  <action>
    Add negative regression coverage proving that cross-tenant access fails on the pilot read/write paths named in SECCTX-03: folders, projects/pages/programs, records/views, CRM, marketplace/POD, and integrations. Where the repo already has repository tests, extend those patterns; where it does not, add focused service/repository tests rather than broad end-to-end scaffolding.

    Prioritize tests that would fail if a developer later removed `company_id` constraints from SQL or bypassed service-level ownership checks. Include at least one representative write-path denial where practical, not only reads.
  </action>
  <acceptance_criteria>
    - Cross-tenant negative tests exist for every pilot path family named in SECCTX-03
    - The tests would fail if company scoping were removed from the protected path
    - Read and write seams are both represented where practical
    - The harness fits the repo's current Node test style
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npm test</automated>
  </verify>
  <done>The pilot tenant-isolation contract is pinned by regression tests instead of depending on memory.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Publish the Phase 28 contract docs and migration path</name>
  <files>docs/architecture/*, docs/specs/*</files>
  <read_first>
    docs/architecture/current-state-truth-matrix.md
    docs/specs/architecture-steering.md
    docs/specs/modules/team-permissions.md
    docs/specs/modules/yard-pod-fieldops.md
    outputs from Plans 28-01 through 28-03
  </read_first>
  <action>
    Add or update durable docs that explain the final Phase 28 contract:
    - what RequestContext contains
    - which capability questions are now canonical
    - which public trust patterns are canonical in this phase
    - what explicit demo/degraded behavior means
    - what remains deferred, especially assignment-based visibility enforcement and remaining public-token route migration

    The documentation should be concrete enough that Phase 29 and 30 implementers can read it and know what they must reuse rather than improvising new request/trust/capability patterns.
  </action>
  <acceptance_criteria>
    - The repo has one visible Phase 28 contract doc or doc set covering request context, capabilities, public trust, and demo policy
    - Deferred items are named explicitly so later phases do not accidentally assume they already shipped
    - The docs describe the migration path for remaining public token routes such as POD
  </acceptance_criteria>
  <verify>
    <automated>if (Get-Command rg -ErrorAction SilentlyContinue) { rg -n "RequestContext|capabil|public trust|demo|degraded|POD|assignment" docs/architecture docs/specs } else { Get-ChildItem -Recurse docs/architecture,docs/specs | Select-String -Pattern "RequestContext|capabil|public trust|demo|degraded|POD|assignment" }</automated>
  </verify>
  <done>The Phase 28 contract is durable and visible to later implementers in docs, not only in code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenant-scoped service/repository path -> data access | Regression tests must catch accidental cross-tenant leakage when SQL or ownership checks change |
| Code contract -> future phase implementers | Docs must make the canonical Phase 28 contract explicit so future work does not fork request/trust/capability behavior again |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-10 | Information Disclosure | Pilot domain data access | mitigate | Negative tests explicitly pin company-scoped reads and writes across the Phase 28 pilot surfaces |
| T-28-11 | Repudiation | Architecture contract drift | mitigate | Publish one visible Phase 28 contract doc naming what shipped versus what remains deferred |
| T-28-12 | Tampering | Future public-edge changes | mitigate | Document the canonical trust family and remaining migration path so new public routes cannot claim ambiguity |
</threat_model>

<verification>
- `cd apps/api && npm test` passes with the new negative tests included.
- Search across docs shows the shipped Phase 28 contract terms are explicitly documented.
- The doc set clearly separates shipped contract from deferred assignment-visibility and remaining public-token migration work.
</verification>

<success_criteria>
- SECCTX-03 is enforceable through regression coverage.
- SECCTX-04 and SECCTX-05 close with durable contract docs, not only code changes.
- Phase 29 and 30 inherit one explicit foundation contract.
</success_criteria>

<output>
Create `.planning/phases/28-security-tenancy-capabilities-foundation/28-04-SUMMARY.md` when done
</output>
