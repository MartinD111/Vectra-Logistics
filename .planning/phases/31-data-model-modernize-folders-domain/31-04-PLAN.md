---
phase: 31
plan: 04
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - apps/api/src/domains/folders/folders.types.ts
  - apps/api/src/domains/folders/dto/folder.dto.ts
  - apps/api/src/domains/folders/folders.repository.ts
  - apps/api/src/domains/folders/folders.repository.test.ts
autonomous: true
requirements: [HIER-02, HIER-03, HIER-07]

must_haves:
  truths:
    - "A folder's ancestor chain can be read directly off the row (ancestor_ids) with no additional queries"
    - "Creating or moving a folder computes ancestor_ids correctly from the new parent, and patches every existing descendant's ancestor_ids in the same operation"
    - "A folder can be archived or unarchived at the repository layer, and every folder within an archived subtree can be bulk-archived in one query"
  artifacts:
    - path: "apps/api/src/domains/folders/folders.types.ts"
      provides: "Folder.ancestor_ids and .archived_at fields"
      contains: "ancestor_ids"
    - path: "apps/api/src/domains/folders/dto/folder.dto.ts"
      provides: "ArchiveFolderSchema/UnarchiveFolderSchema for the empty-body archive/unarchive requests"
      contains: "ArchiveFolderSchema"
    - path: "apps/api/src/domains/folders/folders.repository.ts"
      provides: "descendantFolderIds, moveFolder (ancestor_ids-aware), archiveFolderSubtree, unarchiveFolder"
      contains: "descendantFolderIds"
  key_links:
    - from: "apps/api/src/domains/folders/folders.repository.ts"
      to: "folders.ancestor_ids GIN index (plan 31-01)"
      via: "containment query"
      pattern: "ancestor_ids @>"
---

<objective>
Rewrite `folders.repository.ts`'s data-access layer (plus the small type/DTO additions it depends on) to use the new `ancestor_ids` column, add ancestor-index maintenance on create/move (HIER-07), add the bulk subtree-archive query (supports HIER-03/HIER-04), and expose all the new methods the folders-domain service rewrite (plan 31-05) will orchestrate.

Purpose: This is the "define contracts" step for the folders-domain rewrite — plan 31-05's service layer is written against the exact repository method signatures created here, so that plan can focus purely on RequestContext/capability/event_outbox/cascade orchestration without also inventing repository SQL.
Output: `folders.repository.ts` with ancestor-index-aware CRUD, `folders.types.ts`/`dto/folder.dto.ts` extended to match.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-data-model-modernize-folders-domain/31-CONTEXT.md
@.planning/phases/31-data-model-modernize-folders-domain/31-RESEARCH.md
@.planning/phases/31-data-model-modernize-folders-domain/31-PATTERNS.md
@apps/api/src/domains/folders/folders.types.ts
@apps/api/src/domains/folders/folders.repository.ts
@apps/api/src/domains/folders/dto/folder.dto.ts
</context>

<interfaces>
Current `Folder` (folders.types.ts): `id`, `company_id`, `parent_id`, `name`, `icon`, `color`, `sort_order`, `created_by`, `created_at`, `updated_at`. Add `ancestor_ids: string[]` and `archived_at: Date | null` (both columns exist post-plan-31-01).

Current `folders.repository.ts` full file (67 lines) — methods to preserve as-is in shape but extend: `listFolders`, `findFolder`, `findFolderForCompany`, `createFolder`, `updateFolder`. Method to remove: `deleteFolder` (raw hard `DELETE`, replaced by archive per D-01). Method to extend: `moveFolder` (currently just sets `parent_id`, must also recompute/patch `ancestor_ids`).

Current `dto/folder.dto.ts`: `CreateFolderSchema`, `UpdateFolderSchema`, `MoveFolderSchema` (all `z.object`). `ManualRunSchema` in `apps/api/src/domains/workflows/dto/workflows.dto.ts` is the precedent for a near-empty action-request schema: `z.object({ idempotency_key: z.string().trim().min(1).max(200).optional() })` — the new Archive/Unarchive schemas here need no fields at all, so use `z.object({})`.

RESEARCH.md Pattern 2 (ancestor-ID array): depth = `COALESCE(array_length(ancestor_ids,1), 0) + 1`; on move, `NEW.ancestor_ids = parent.ancestor_ids || parent.id` (or `'{}'` for root); existing descendants of the moved node need their stored `ancestor_ids` prefix rewritten in the same operation, bounded to at most 2 levels of descendants given max depth 3.

`folders_ancestor_ids_gin_idx` (from plan 31-01) supports the `ancestor_ids @> ARRAY[$1]::uuid[]` containment query for "all descendants of folder X" used by `descendantFolderIds`.

`folders_prevent_cycle_and_depth_trg` (from plan 31-01) is the DB-level backstop for cycle/depth — this plan's repository methods must still pass correct data (correctly computed `ancestor_ids`) so the trigger's independent recomputation agrees; the repository does not need to duplicate the trigger's validation logic, only supply correct writes.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, DTOs, and ancestor-index-aware create/read</name>
  <files>apps/api/src/domains/folders/folders.types.ts, apps/api/src/domains/folders/dto/folder.dto.ts, apps/api/src/domains/folders/folders.repository.ts, apps/api/src/domains/folders/folders.repository.test.ts</files>
  <read_first>
    - apps/api/src/domains/folders/folders.types.ts
    - apps/api/src/domains/folders/dto/folder.dto.ts
    - apps/api/src/domains/folders/folders.repository.ts
    - apps/api/src/domains/workflows/dto/workflows.dto.ts (ManualRunSchema — near-empty-body precedent)
  </read_first>
  <behavior>
    - Test 1: `foldersRepository.createFolder` given `ancestorIds: []` inserts a row with `ancestor_ids = []` (verify via the SQL params passed to a mocked `db.query`).
    - Test 2: `foldersRepository.createFolder` given `ancestorIds: ['a', 'p']` inserts the new row with that exact array as a query param.
    - Test 3: `foldersRepository.descendantFolderIds(companyId, folderId)` issues exactly one `db.query` call containing `ancestor_ids @>` and returns whatever id list the mocked query resolves with.
  </behavior>
  <action>
    In `folders.types.ts`: add `ancestor_ids: string[];` and `archived_at: Date | null;` to `Folder`.

    In `dto/folder.dto.ts`: add `export const ArchiveFolderSchema = z.object({});` and `export const UnarchiveFolderSchema = z.object({});` with `export type ArchiveFolderDto = z.infer<typeof ArchiveFolderSchema>;` / `UnarchiveFolderDto` — empty-body schemas since archive/unarchive take no payload beyond the `:id` route param.

    In `folders.repository.ts`: change `createFolder`'s signature to also accept `ancestorIds: string[]` as a 4th parameter (the caller — the service layer in plan 31-05 — computes this from the parent's own `ancestor_ids`, since the repository holds no business logic per this codebase's layering convention): `async createFolder(companyId: string, createdBy: string | null, data: {...}, ancestorIds: string[]): Promise<Folder>`, and change the INSERT to `INSERT INTO folders (company_id, parent_id, name, icon, color, ancestor_ids, created_by) VALUES ($1, $2, $3, $4, $5, $6, $7) RETURNING *` passing `ancestorIds` as `$6`.

    Add `async descendantFolderIds(companyId: string, folderId: string): Promise<string[]>`: `SELECT id FROM folders WHERE company_id = $1 AND (id = $2 OR ancestor_ids @> ARRAY[$2]::uuid[])`, params `[companyId, folderId]`, return `rows.map(r => r.id)` — a single query returning the folder itself plus every descendant, used by the cascade-archive orchestration in plan 31-05.

    Add `async findFoldersByIds(companyId: string, ids: string[]): Promise<Folder[]>`: `SELECT * FROM folders WHERE company_id = $1 AND id = ANY($2::uuid[])` — used to fetch a parent's current `ancestor_ids` before computing a child's, and by later phases (32+) for breadcrumb name resolution.

    Create `folders.repository.test.ts` (new file, `node:test` + `mock.method` on `db.query`, matching `folders.service.test.ts`'s existing style) covering the 3 Behavior cases above.
  </action>
  <acceptance_criteria>
    - `Folder` interface includes `ancestor_ids: string[]` and `archived_at: Date | null`
    - `ArchiveFolderSchema`/`UnarchiveFolderSchema` exist as empty `z.object({})` schemas
    - `foldersRepository.createFolder` accepts and persists an `ancestorIds` parameter
    - `foldersRepository.descendantFolderIds` exists, issues a single `ancestor_ids @>`-based query, and returns the folder's own id plus all descendants
    - `foldersRepository.findFoldersByIds` exists
    - `apps/api/src/domains/folders/folders.repository.test.ts` exists and its 3 test cases pass
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && node --require ts-node/register --test src/domains/folders/folders.repository.test.ts</automated>
  </verify>
  <done>Folder repository types/DTOs updated; ancestor-index-aware create and O(1) descendant lookup exist and are unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move (ancestor_ids recompute + descendant patch) and archive/unarchive</name>
  <files>apps/api/src/domains/folders/folders.repository.ts, apps/api/src/domains/folders/folders.repository.test.ts</files>
  <read_first>
    - apps/api/src/domains/folders/folders.repository.ts (as edited by Task 1)
    - apps/api/src/domains/folders/folders.repository.test.ts (as created by Task 1 — extend it)
    - .planning/phases/31-data-model-modernize-folders-domain/31-RESEARCH.md (Pattern 2's "on move" paragraph, Pattern 4)
  </read_first>
  <behavior>
    - Test 1: `moveFolder(id, parentId, newAncestorIds)` updates `parent_id` and `ancestor_ids` on the moved row in one `UPDATE ... RETURNING *`.
    - Test 2: `patchDescendantAncestors(client, folderId, oldPrefix, newPrefix, companyId)` rewrites the `ancestor_ids` prefix for every descendant of `folderId` in one query (array replace, not per-row).
    - Test 3: `archiveFolderSubtree(client, folderIds, companyId)` only returns/archives rows with `archived_at IS NULL`.
    - Test 4: `unarchiveFolder(id, companyId)` clears `archived_at` and returns `null` if the folder doesn't belong to that company.
  </behavior>
  <action>
    Extend `moveFolder`'s signature to `async moveFolder(id: string, parentId: string | null, ancestorIds: string[]): Promise<Folder | null>`, changing the query to `UPDATE folders SET parent_id = $2, ancestor_ids = $3, updated_at = NOW() WHERE id = $1 RETURNING *` (caller in plan 31-05 computes `ancestorIds` the same way `createFolder` does: `parent ? [...parent.ancestor_ids, parent.id] : []`).

    Add `async patchDescendantAncestors(client: PoolClient, folderId: string, oldAncestorIds: string[], newAncestorIds: string[], companyId: string): Promise<void>`: for every descendant of `folderId` (found via the same `ancestor_ids @> ARRAY[$folderId]` containment check), replace the `oldAncestorIds` prefix (which always ends in `folderId`) with `newAncestorIds` (which also ends in `folderId`) inside each descendant's own `ancestor_ids` array. Implement as: `UPDATE folders SET ancestor_ids = $3 || ancestor_ids[array_position(ancestor_ids, $4::uuid):], updated_at = NOW() WHERE company_id = $1 AND ancestor_ids @> ARRAY[$4]::uuid[]`, params `[companyId, folderId, newAncestorIds, folderId]` — this replaces everything in each descendant's `ancestor_ids` array up to and including `folderId`'s position with the new prefix, preserving anything below `folderId` in the chain (bounded to at most 2 extra levels given max depth 3, so this is cheap). Import `PoolClient` from `'pg'`.

    Add `async archiveFolderSubtree(client: PoolClient, folderIds: string[], companyId: string): Promise<Folder[]>`: `UPDATE folders SET archived_at = NOW(), updated_at = NOW() WHERE company_id = $1 AND id = ANY($2::uuid[]) AND archived_at IS NULL RETURNING *`, params `[companyId, folderIds]`.

    Add `async unarchiveFolder(id: string, companyId: string): Promise<Folder | null>`: `UPDATE folders SET archived_at = NULL, updated_at = NOW() WHERE id = $1 AND company_id = $2 RETURNING *`, return `rows[0] ?? null`.

    Remove `deleteFolder` (raw hard `DELETE`) entirely — archive/unarchive is now the only removal path per D-01/Open-Question-1's archive-only recommendation.

    Also update `listFolders`/`findFolderForCompany` `WHERE` clauses to add `AND archived_at IS NULL` so archived folders are excluded from default tree reads (matching the same "archived = hidden from default view" convention applied in plan 31-03 to projects/programs) — but not `findFolder` (unscoped internal lookup, still needed to fetch an already-archived row for unarchive).

    Extend `folders.repository.test.ts` with the 4 Behavior cases above.
  </action>
  <acceptance_criteria>
    - `foldersRepository.moveFolder` persists both `parent_id` and `ancestor_ids` in one query
    - `foldersRepository.patchDescendantAncestors` exists, takes a `PoolClient`, and rewrites descendant `ancestor_ids` in one query (no loop over individual descendant rows)
    - `foldersRepository.archiveFolderSubtree` exists, takes a `PoolClient`, filters `archived_at IS NULL`
    - `foldersRepository.unarchiveFolder` exists and is company-scoped
    - `foldersRepository.deleteFolder` no longer exists (`grep -c "deleteFolder" apps/api/src/domains/folders/folders.repository.ts` returns 0)
    - `foldersRepository.listFolders`/`findFolderForCompany` exclude archived rows
    - `node --require ts-node/register --test src/domains/folders/folders.repository.test.ts` passes all 7 cumulative test cases (3 from Task 1 + 4 from this task)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && node --require ts-node/register --test src/domains/folders/folders.repository.test.ts</automated>
  </verify>
  <done>Move recomputes and patches ancestor_ids in one transaction-ready operation; archive/unarchive exist at the repository layer; hard-delete is fully removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `folders.repository.ts` internal queries | Repository-layer SQL is the last line of defense before the DB-level composite FK/trigger from plan 31-01; incorrect `ancestor_ids` computation here would not be a security hole (the trigger independently re-validates), but would produce wrong breadcrumbs/cascade scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02 | Denial of Service | `patchDescendantAncestors`, `descendantFolderIds` | mitigate | Both queries are GIN-index-backed (`ancestor_ids @>`) and bounded by max depth 3 (D-04) — no unbounded recursion or fan-out is possible regardless of tree size |
| T-31-SC | Tampering (supply chain) | N/A — no new packages | accept | No new npm dependency introduced by this plan |
</threat_model>

<verification>
- `cd apps/api && node --require ts-node/register --test src/domains/folders/folders.repository.test.ts`
- `cd apps/api && npx tsc --noEmit -p tsconfig.json`
</verification>

<success_criteria>
- `folders.repository.ts` exposes ancestor-index-aware create/move/archive/unarchive/descendant-lookup methods, fully replacing the old per-request recursive-walk-adjacent hard-delete data layer (HIER-07 repository half, HIER-03 repository-side support).
- No repository method issues a recursive CTE or a per-row loop of sequential queries for tree traversal.
</success_criteria>

<output>
Create `.planning/phases/31-data-model-modernize-folders-domain/31-04-SUMMARY.md` when done
</output>
