---
phase: 31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/028_folder_hierarchy_invariants.sql
autonomous: true
requirements: [HIER-01, HIER-02, HIER-03, HIER-04, HIER-07]

must_haves:
  truths:
    - "A data_collections row can store a folder_id pointing at a folder in the same company"
    - "An UPDATE that sets a folder/project/program/collection/page row's parent-pointer to a different company's node is rejected by a database constraint, not just application code"
    - "An UPDATE that would move a folder into its own descendant is rejected by a database trigger before the row is written"
    - "Every folder/project/program/data_collection/project_page row has an archived_at column available for the cascade-archive feature built in later plans"
    - "A folder's full ancestor chain can be read in a single indexed query instead of a per-request recursive walk"
  artifacts:
    - path: "database/migrations/028_folder_hierarchy_invariants.sql"
      provides: "archived_at columns, folders.ancestor_ids, data_collections.folder_id, composite (id, company_id) FK invariants, cycle+depth trigger"
      contains: "folders_prevent_cycle_and_depth"
  key_links:
    - from: "folders.parent_id"
      to: "folders(id, company_id)"
      via: "composite foreign key"
      pattern: "FOREIGN KEY \\(parent_id, company_id\\) REFERENCES folders \\(id, company_id\\)"
    - from: "data_collections.folder_id"
      to: "folders(id, company_id)"
      via: "composite foreign key"
      pattern: "FOREIGN KEY \\(folder_id, company_id\\) REFERENCES folders \\(id, company_id\\)"
---

<objective>
Create the single new idempotent SQL migration (`028_folder_hierarchy_invariants.sql`) that gives the folder/project/program/collection/page hierarchy a tenant-safe, cycle-safe, archive-capable schema: `data_collections.folder_id` (HIER-01), composite `(id, company_id)` FK invariants across the tree so no row can be reparented cross-tenant (HIER-02), a DB trigger rejecting cycles and depth>3 (HIER-03, D-04), `archived_at` columns everywhere the cascade-archive feature needs them (HIER-04), and `folders.ancestor_ids` with a GIN index so tree/breadcrumb lookups never need a recursive CTE again (HIER-07).

Purpose: Every later plan in this phase (records/projects/folders domain code) and every later phase (32-34) builds directly on this schema. Getting the invariants wrong here (constraint names, off-by-one depth, missing backfill) breaks everything downstream.
Output: `database/migrations/028_folder_hierarchy_invariants.sql`, applied via the existing `npm run migrate` runner (`apps/api/src/scripts/migrate.ts`), which tracks applied files in `schema_migrations` and runs each file inside its own transaction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-data-model-modernize-folders-domain/31-CONTEXT.md
@.planning/phases/31-data-model-modernize-folders-domain/31-RESEARCH.md
@database/migrations/006_folders.sql
@database/migrations/004_projects_and_programs.sql
@database/migrations/025_records_views.sql
@database/migrations/009_project_pages.sql
@database/migrations/012_page_hierarchy.sql
@database/migrations/026_event_outbox.sql
@apps/api/src/scripts/migrate.ts
</context>

<interfaces>
<!-- Current schema this migration extends. Executor should not re-derive these from scratch. -->

`folders` (006_folders.sql): `id UUID PK`, `company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE`, `parent_id UUID REFERENCES folders(id) ON DELETE CASCADE` (inline, default constraint name `folders_parent_id_fkey`), `name`, `icon`, `color`, `sort_order`, `created_by`, `created_at`, `updated_at`. No `archived_at`, no `ancestor_ids` yet.

`projects` (004_projects_and_programs.sql): `id UUID PK`, `company_id UUID NOT NULL`, `name`, `description`, `color`, `created_by`, `created_at`, `updated_at`. `folder_id` added later in 006_folders.sql: `ALTER TABLE projects ADD COLUMN IF NOT EXISTS folder_id UUID REFERENCES folders(id) ON DELETE SET NULL` (default constraint name `projects_folder_id_fkey`). No `archived_at` yet.

`programs` (004_projects_and_programs.sql): `id UUID PK`, `company_id UUID NOT NULL`, `project_id UUID REFERENCES projects(id) ON DELETE SET NULL` (default constraint name `programs_project_id_fkey`), `folder_id` added in 006_folders.sql (default constraint name `programs_folder_id_fkey`), `name`, `description`, `type`, `status`, `config`, `created_by`, `created_at`, `updated_at`. No `archived_at` yet.

`data_collections` (025_records_views.sql): `id UUID PK`, `company_id UUID NOT NULL`, `project_id UUID REFERENCES projects(id) ON DELETE SET NULL`, `name`, `schema JSONB`, `created_by`, `created_at`, `updated_at`. No `folder_id`, no `archived_at` yet. Header comment: "Schema is locked in docs/specs/core/workspace-blocks.md §3.3 — do not redesign" — only ADD `folder_id`/`archived_at`, never touch `project_id`/`schema`.

`project_pages` (009_project_pages.sql + 012_page_hierarchy.sql): `id UUID PK`, `company_id UUID NOT NULL`, `project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE` (default constraint name `project_pages_project_id_fkey`), `parent_page_id UUID REFERENCES project_pages(id) ON DELETE CASCADE` (added in 012, default constraint name `project_pages_parent_page_id_fkey`), `title`, `icon`, `is_default`, `sort_order`, `config`, `created_by`, `created_at`, `updated_at`. No `archived_at` yet.

`event_outbox` (026_event_outbox.sql, unchanged this plan): `tenant_id UUID NOT NULL REFERENCES companies(id)`, `event_id`, `event_name`, `object_type`, `object_id`, `project_id UUID REFERENCES projects(id) ON DELETE SET NULL`, `payload JSONB`. Referenced here only so later plans know it already exists — this plan does not touch it.

Migration runner (`apps/api/src/scripts/migrate.ts`): applies every file in `database/migrations/` matching `/^\d+_[\w-]+\.sql$/` in sorted filename order, inside its own `BEGIN`/`COMMIT`, tracked in `schema_migrations(filename)`. New file must be named `028_folder_hierarchy_invariants.sql` to sort after `027_workflows.sql`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Additive columns, indexes, and ancestor_ids backfill</name>
  <files>database/migrations/028_folder_hierarchy_invariants.sql</files>
  <read_first>
    - database/migrations/006_folders.sql
    - database/migrations/004_projects_and_programs.sql
    - database/migrations/025_records_views.sql
    - database/migrations/009_project_pages.sql
    - database/migrations/026_event_outbox.sql (for idempotent-migration header-comment convention)
  </read_first>
  <action>
    Create `database/migrations/028_folder_hierarchy_invariants.sql` with a header comment matching the `026_event_outbox.sql` style ("Migration: Folder hierarchy invariants. Apply after 027. Idempotent." plus a short purpose paragraph noting per Pitfall 2 from RESEARCH.md that `data_collections` only gets an added `folder_id`/`archived_at`, never a `project_id`/`schema` change, because that schema is locked in `docs/specs/core/workspace-blocks.md` §3.3).

    Add these statements, all `IF NOT EXISTS`/idempotent:
    - `ALTER TABLE folders ADD COLUMN IF NOT EXISTS archived_at TIMESTAMPTZ;`
    - `ALTER TABLE folders ADD COLUMN IF NOT EXISTS ancestor_ids UUID[] NOT NULL DEFAULT '{}';`
    - `ALTER TABLE projects ADD COLUMN IF NOT EXISTS archived_at TIMESTAMPTZ;`
    - `ALTER TABLE programs ADD COLUMN IF NOT EXISTS archived_at TIMESTAMPTZ;`
    - `ALTER TABLE data_collections ADD COLUMN IF NOT EXISTS archived_at TIMESTAMPTZ;`
    - `ALTER TABLE project_pages ADD COLUMN IF NOT EXISTS archived_at TIMESTAMPTZ;`
    - `CREATE INDEX IF NOT EXISTS folders_ancestor_ids_gin_idx ON folders USING GIN (ancestor_ids);`

    Add `data_collections.folder_id` as a new column referencing the folders composite key directly (the composite unique constraint is created in Task 2 of this same file, so order this `ALTER TABLE data_collections ADD COLUMN IF NOT EXISTS folder_id UUID;` here in Task 1 without the FK yet, then add the FK constraint itself in Task 2 alongside the other composite FKs — this avoids a forward reference to a UNIQUE constraint that doesn't exist yet in file order). Also add `CREATE INDEX IF NOT EXISTS data_collections_folder_idx ON data_collections (folder_id);`.

    Backfill `ancestor_ids` for every existing folder row using a one-time `DO $$ ... $$` block with a `WITH RECURSIVE` query INSIDE the migration (this is explicitly allowed — RESEARCH.md's Anti-Pattern warning is about a recursive CTE running on every request/read, not a one-time backfill script). The backfill must: (a) compute each folder's ordered ancestor chain from root to immediate parent using `parent_id`, (b) `UPDATE folders SET ancestor_ids = computed_chain WHERE id = computed.id`, (c) for any folder whose computed depth (`array_length(ancestor_ids,1) + 1`) exceeds 3, emit `RAISE NOTICE 'Folder % exceeds depth 3 after backfill (pre-existing, not truncated): %', id, array_length(ancestor_ids,1)+1` per RESEARCH.md's Backfill note — do NOT reject, truncate, or reparent these rows; the depth-3 guard only applies to new creates/moves going forward (Task 2's trigger).
  </action>
  <acceptance_criteria>
    - File `database/migrations/028_folder_hierarchy_invariants.sql` exists and starts with a header comment containing "Idempotent"
    - File contains `ADD COLUMN IF NOT EXISTS archived_at` for folders, projects, programs, data_collections, and project_pages (5 occurrences)
    - File contains `ancestor_ids UUID[] NOT NULL DEFAULT '{}'`
    - File contains `folders_ancestor_ids_gin_idx` and `USING GIN (ancestor_ids)`
    - File contains a `WITH RECURSIVE` block used only inside a one-time backfill `DO $$` block (not inside any function intended for per-request use)
    - File contains a `RAISE NOTICE` referencing depth exceeding 3
    - Running the file twice against the same database produces no error (idempotency) — verify by applying via `npm run migrate` (apps/api) twice in a row inside `docker compose exec api sh -c "npm run build && npm run migrate"` or the dev equivalent, confirming the second run logs "No pending migrations."
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Wave 0: apply via `cd apps/api && npm run build && npm run migrate` against the running dev Postgres (docker-compose service `postgres` on port 5433), then re-run `npm run migrate` and confirm it prints "No pending migrations." for 028</automated>
  </verify>
  <done>Migration file created with all additive columns/indexes/backfill; applying it against a running dev database succeeds and is idempotent on re-run.</done>
</task>

<task type="auto">
  <name>Task 2: Composite (id, company_id) FK invariants + cycle/depth trigger</name>
  <files>database/migrations/028_folder_hierarchy_invariants.sql</files>
  <read_first>
    - database/migrations/028_folder_hierarchy_invariants.sql (as written by Task 1 — append to it, do not overwrite Task 1's statements)
    - .planning/phases/31-data-model-modernize-folders-domain/31-RESEARCH.md (Pattern 3, Pattern 4, Pitfall 1, Pitfall 4, Open Question 2 — trigger folds in depth check)
  </read_first>
  <action>
    Append to the same migration file, after Task 1's statements:

    1. Add composite `UNIQUE (id, company_id)` constraints, idempotently, on exactly three tables (do NOT add on programs or data_collections — nothing references their `id` as a parent-pointer target in this hierarchy): `folders`, `projects`, `project_pages`. Use `ALTER TABLE ... ADD CONSTRAINT ... UNIQUE (id, company_id)` wrapped in a `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN NULL; END $$;` block for idempotency (constraint names: `folders_id_company_uniq`, `projects_id_company_uniq`, `project_pages_id_company_uniq`).

    2. Drop and re-add these FKs as composite, each wrapped in `DO $$ BEGIN ALTER TABLE ... DROP CONSTRAINT IF EXISTS <assumed_default_name>; EXCEPTION WHEN undefined_object THEN NULL; END $$;` (per Pitfall 1, `DROP CONSTRAINT IF EXISTS` already makes this safe even if the assumed name is wrong or already dropped) followed by `ALTER TABLE ... ADD CONSTRAINT ... FOREIGN KEY (...) REFERENCES ... (id, company_id) ON DELETE ...` preserving each column's existing `ON DELETE` behavior (`CASCADE` for `folders.parent_id` and `project_pages.parent_page_id`; `SET NULL` for `projects.folder_id`, `programs.folder_id`, `programs.project_id`, `project_pages.project_id` — wait, `project_pages.project_id` is currently `ON DELETE CASCADE` per 009_project_pages.sql line 11, keep it `CASCADE`):
       - `folders.parent_id` → drop `folders_parent_id_fkey`, add `FOREIGN KEY (parent_id, company_id) REFERENCES folders (id, company_id) ON DELETE CASCADE`
       - `projects.folder_id` → drop `projects_folder_id_fkey`, add `FOREIGN KEY (folder_id, company_id) REFERENCES folders (id, company_id) ON DELETE SET NULL`
       - `programs.folder_id` → drop `programs_folder_id_fkey`, add `FOREIGN KEY (folder_id, company_id) REFERENCES folders (id, company_id) ON DELETE SET NULL`
       - `programs.project_id` → drop `programs_project_id_fkey`, add `FOREIGN KEY (project_id, company_id) REFERENCES projects (id, company_id) ON DELETE SET NULL`
       - `project_pages.project_id` → drop `project_pages_project_id_fkey`, add `FOREIGN KEY (project_id, company_id) REFERENCES projects (id, company_id) ON DELETE CASCADE`
       - `project_pages.parent_page_id` → drop `project_pages_parent_page_id_fkey`, add `FOREIGN KEY (parent_page_id, company_id) REFERENCES project_pages (id, company_id) ON DELETE CASCADE`
       - `data_collections.folder_id` (new column from Task 1, no existing constraint to drop) → add `FOREIGN KEY (folder_id, company_id) REFERENCES folders (id, company_id) ON DELETE SET NULL` directly
       - `data_collections.project_id` (existing column) → drop the existing single-column FK (default name `data_collections_project_id_fkey`), add `FOREIGN KEY (project_id, company_id) REFERENCES projects (id, company_id) ON DELETE SET NULL`

    3. Create the cycle+depth trigger function `folders_prevent_cycle_and_depth()`: `RETURNS TRIGGER`, `LANGUAGE plpgsql`. Logic: if `NEW.parent_id IS NULL`, `RETURN NEW`. If `NEW.parent_id = NEW.id`, `RAISE EXCEPTION 'Folder cannot be its own parent' USING ERRCODE = 'check_violation'`. `SELECT ancestor_ids INTO parent_ancestors FROM folders WHERE id = NEW.parent_id`. If `NEW.id = ANY(parent_ancestors)`, `RAISE EXCEPTION 'Cannot move a folder into its own descendant' USING ERRCODE = 'check_violation'`. Depth check (per Pitfall 4's off-by-one fix — top-level folder = depth 1): compute `new_depth := COALESCE(array_length(parent_ancestors,1), 0) + 1 + 1` (parent's depth + 1 for the new/moved node); if `new_depth > 3`, `RAISE EXCEPTION 'Folder nesting cannot exceed depth 3' USING ERRCODE = 'check_violation'`. `RETURN NEW`.

    4. Create the trigger: `CREATE TRIGGER folders_prevent_cycle_and_depth_trg BEFORE INSERT OR UPDATE OF parent_id ON folders FOR EACH ROW EXECUTE FUNCTION folders_prevent_cycle_and_depth();` — use `CREATE OR REPLACE FUNCTION` for the function (already idempotent) and drop-then-create for the trigger (`DROP TRIGGER IF EXISTS folders_prevent_cycle_and_depth_trg ON folders;` then `CREATE TRIGGER ...`) for idempotency.
  </action>
  <acceptance_criteria>
    - File contains `UNIQUE (id, company_id)` exactly 3 times (folders, projects, project_pages) — not on programs or data_collections
    - File contains 8 `FOREIGN KEY (...) REFERENCES ... (id, company_id)` composite FK definitions (folders.parent_id, projects.folder_id, programs.folder_id, programs.project_id, project_pages.project_id, project_pages.parent_page_id, data_collections.folder_id, data_collections.project_id)
    - File contains function `folders_prevent_cycle_and_depth` with both a cycle check (`NEW.id = ANY(parent_ancestors)`) and a depth check (`> 3`)
    - File contains `CREATE TRIGGER folders_prevent_cycle_and_depth_trg ... BEFORE INSERT OR UPDATE OF parent_id ON folders`
    - Applying the migration against the running dev Postgres succeeds with no errors (constraint-drop `DO` blocks tolerate a mismatched assumed name via `IF EXISTS`)
    - Manual verification query after migration: attempting `UPDATE folders SET parent_id = '<a-known-descendant-id>' WHERE id = '<ancestor-id>'` in `psql` raises an exception containing "descendant"; attempting to insert a 4th-level folder raises an exception containing "depth 3"
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Wave 0: after applying via `npm run migrate`, run a manual `psql -h localhost -p 5433 -U vectra_user -d vectra_db -c "..."` cycle-attempt and depth-attempt query per acceptance_criteria; a proper `node:test` integration test for this is created in plan 31-06</automated>
  </verify>
  <done>Composite FK invariants and cycle/depth trigger are live in the migration file; cross-tenant reparenting and cycle/depth-4 attempts are rejected at the database level when manually tested against the dev database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| API service → PostgreSQL | Any future code path (service, worker, script, direct SQL) writing `parent_id`/`folder_id`/`project_id`/`parent_page_id` crosses into a boundary where tenant isolation must be enforced independent of the caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering / Elevation of Privilege | `folders.parent_id`, `projects.folder_id`, `programs.folder_id`, `programs.project_id`, `data_collections.folder_id`, `data_collections.project_id`, `project_pages.project_id`, `project_pages.parent_page_id` | mitigate | Composite `(id, company_id)` FK invariant added in Task 2 — cross-tenant reparenting is rejected by PostgreSQL regardless of which code path attempts the write |
| T-31-02 | Denial of Service | `folders.parent_id` (self-referential chain) | mitigate | `folders_prevent_cycle_and_depth_trg` BEFORE INSERT OR UPDATE trigger rejects any write that would create a cycle or exceed depth 3, added in Task 2 |
| T-31-SC | Tampering (supply chain) | N/A — this plan introduces no new npm/pip/cargo packages | accept | No package installs in this plan; Package Legitimacy Gate not applicable (RESEARCH.md "Package Legitimacy Audit" section confirms no new dependencies) |
</threat_model>

<verification>
- Migration file exists, is idempotent (re-running produces "No pending migrations."), and applies cleanly against the running dev Postgres via `npm run migrate` (apps/api).
- Manual `psql` cycle-attempt, depth-attempt, and cross-tenant-attempt queries (see Task 2 acceptance_criteria) all raise the expected exceptions.
- `grep -c "ADD COLUMN IF NOT EXISTS" database/migrations/028_folder_hierarchy_invariants.sql` returns at least 7 (5 archived_at + ancestor_ids + folder_id).
</verification>

<success_criteria>
- `data_collections.folder_id` column exists and is queryable (HIER-01 schema half).
- Composite `(id, company_id)` FK invariant exists on every cross-tenant-reparentable relationship (HIER-02).
- Cycle and depth-4 attempts are rejected at the DB level (HIER-03, D-04).
- `archived_at` exists on folders/projects/programs/data_collections/project_pages (HIER-04 schema half).
- `folders.ancestor_ids` exists, is backfilled for all existing rows, and is GIN-indexed (HIER-07 schema half).
</success_criteria>

<output>
Create `.planning/phases/31-data-model-modernize-folders-domain/31-01-SUMMARY.md` when done
</output>
