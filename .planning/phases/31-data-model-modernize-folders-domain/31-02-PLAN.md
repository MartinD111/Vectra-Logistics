---
phase: 31
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - apps/api/src/domains/records/records.types.ts
  - apps/api/src/domains/records/records.repository.ts
  - apps/api/src/domains/records/records.service.ts
  - apps/api/src/domains/records/dto/create-collection.dto.ts
  - apps/api/src/domains/records/dto/update-collection.dto.ts
  - apps/api/src/domains/records/records.repository.test.ts
autonomous: true
requirements: [HIER-01, HIER-04]

must_haves:
  truths:
    - "A data_collections row can be created or updated with a folder_id pointing at a folder owned by the same company"
    - "Attempting to set folder_id to a folder owned by a different company is rejected before any write"
    - "Archiving a folder can bulk-archive every data_collection directly filed in that folder (and any of its descendant folders) inside the same transaction the caller controls"
  artifacts:
    - path: "apps/api/src/domains/records/records.types.ts"
      provides: "DataCollectionRow.folder_id and .archived_at fields"
      contains: "folder_id"
    - path: "apps/api/src/domains/records/records.repository.ts"
      provides: "archiveCollectionsInFolders / archiveCollectionsInProjects / unarchiveCollection methods usable from the folders-domain cascade transaction"
      contains: "archiveCollectionsInFolders"
  key_links:
    - from: "apps/api/src/domains/records/records.service.ts"
      to: "apps/api/src/domains/folders/folders.repository.ts"
      via: "assertOwnedFolder-style ownership check before accepting folder_id"
      pattern: "findFolderForCompany|folder_id"
---

<objective>
Add `folder_id` to `data_collections` end-to-end (types, repository, DTOs, service ownership check) per HIER-01, and add the bulk archive/unarchive repository methods the folders-domain cascade (plan 31-05) will call to archive collections when a folder or project is archived, per HIER-04.

Purpose: `data_collections.folder_id` mirrors the existing `projects.folder_id`/`programs.folder_id` pattern exactly — this closes the one attachment gap named in the phase's Success Criterion 1. The bulk archive methods are added here (not in 31-05) because they belong to the records domain's own repository, following the same "each domain owns its own table's mutations" boundary already used by `projectsRepository`/`recordsRepository` in the existing `createCollectionWithDefaultView` transaction pattern.
Output: `data_collections` rows can be filed into folders; `recordsRepository` exposes transaction-safe (`PoolClient`-taking) bulk archive methods for later cascade use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-data-model-modernize-folders-domain/31-CONTEXT.md
@.planning/phases/31-data-model-modernize-folders-domain/31-RESEARCH.md
@apps/api/src/domains/records/records.types.ts
@apps/api/src/domains/records/records.repository.ts
@apps/api/src/domains/records/records.service.ts
@apps/api/src/domains/records/dto/create-collection.dto.ts
@apps/api/src/domains/records/dto/update-collection.dto.ts
@apps/api/src/domains/folders/folders.repository.ts
@apps/api/src/core/events/outbox.ts
</context>

<interfaces>
`DataCollectionRow` (records.types.ts, current): `id`, `company_id`, `project_id: string | null`, `name`, `schema: CollectionPropertyDef[]`, `created_by`, `created_at`, `updated_at`. Add `folder_id: string | null` and `archived_at: Date | null` (both now exist as DB columns per plan 31-01's migration).

`recordsRepository.updateCollection(id, companyId, patch)` (records.repository.ts lines 73-88) already uses a dynamic `sets`/`params` patch pattern that ignores `undefined` fields — extend `patch`'s type to accept `folder_id?: string | null` the same way, no structural change needed to the loop itself.

`recordsRepository.createCollectionWithDefaultView(companyId, d)` (records.repository.ts lines 7-59) is the existing transaction-owning method; its `INSERT INTO data_collections (company_id, project_id, name, schema, created_by) VALUES (...)` must gain `folder_id` as a 6th column/value.

`folders.repository.ts` `findFolderForCompany(id, companyId): Promise<Folder | null>` (folders.repository.ts lines 18-24) is the existing company-scoped folder lookup to reuse for the new ownership check — do not write a second copy of this query in the records domain; import `foldersRepository` and call `findFolderForCompany` directly, mirroring the try/catch shape of `projects.service.ts`'s `assertOwnedFolder` (projects.service.ts lines 199-203). Note: `assertOwnedFolder` itself throws 403 (not 404) for a folder that exists but belongs to another company — this plan's new check should throw a clean 404 instead, since `findFolderForCompany` returns `null` for both "not found" and "wrong tenant" and there is no separate existence check to leak from.

`insertDurableEvent(client: PoolClient, event: DurableEventEnvelope)` and `createDurableEventEnvelope(input)` (core/events/outbox.ts, already imported in records.repository.ts line 2 via `'../../core/events'`) — reuse for the new bulk-archive event emission, one call per archived row, `eventName: 'data_collection.archived'`, `objectType: 'data_collection'`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: data_collections.folder_id — types, repository, DTOs, service ownership check</name>
  <files>apps/api/src/domains/records/records.types.ts, apps/api/src/domains/records/records.repository.ts, apps/api/src/domains/records/records.service.ts, apps/api/src/domains/records/dto/create-collection.dto.ts, apps/api/src/domains/records/dto/update-collection.dto.ts</files>
  <read_first>
    - apps/api/src/domains/records/records.types.ts
    - apps/api/src/domains/records/records.repository.ts
    - apps/api/src/domains/records/records.service.ts
    - apps/api/src/domains/records/dto/create-collection.dto.ts
    - apps/api/src/domains/records/dto/update-collection.dto.ts
    - apps/api/src/domains/folders/folders.repository.ts (findFolderForCompany)
    - apps/api/src/domains/projects/projects.service.ts (assertOwnedFolder pattern, lines 199-203)
  </read_first>
  <behavior>
    - Test 1: `CreateCollectionSchema.safeParse({ name: 'X', folder_id: '<valid-uuid>' })` succeeds; parsed data includes `folder_id`.
    - Test 2: `CreateCollectionSchema.safeParse({ name: 'X', folder_id: 'not-a-uuid' })` fails validation.
    - Test 3: `recordsService.createCollection` given a `folder_id` belonging to a different company throws `AppError` with status 404 ("Folder not found") — `findFolderForCompany` returns `null` for both "no such folder" and "wrong tenant", so a plain 404 is the correct behavior with no existence leak, regardless of any other domain's status-code choice.
  </behavior>
  <action>
    In `records.types.ts`: add `folder_id: string | null;` and `archived_at: Date | null;` to `DataCollectionRow`.

    In `dto/create-collection.dto.ts` and `dto/update-collection.dto.ts`: add `folder_id: z.string().uuid().nullable().optional()` to both `CreateCollectionSchema` and `UpdateCollectionSchema` (same `.nullable().optional()` shape already used for other optional FK-like fields in this codebase, e.g. `folders/dto/folder.dto.ts`'s `parent_id`).

    In `records.repository.ts`: extend `createCollectionWithDefaultView`'s `d` parameter with `folderId?: string | null`, add `folder_id` as the 3rd column in the `INSERT INTO data_collections (company_id, project_id, folder_id, name, schema, created_by) VALUES ($1, $2, $3, $4, $5, $6)` (renumber params accordingly, `d.folderId ?? null` in the values array). Extend `updateCollection`'s `patch` parameter type with `folder_id?: string | null` — no loop change needed, the existing dynamic `sets`/`params` construction (lines 76-82) already handles any new key in `patch` generically since it iterates `Object.entries(patch)`.

    In `records.service.ts`: import `foldersRepository` from `'../folders/folders.repository'`. Add a private `assertOwnedFolder(id: string, companyId: string): Promise<void>` method mirroring `projects.service.ts`'s existing one exactly (throw `AppError(404, 'Folder not found')` if `foldersRepository.findFolderForCompany(id, companyId)` returns null — reuse `findFolderForCompany`, not `findFolder`, so the company check happens in one query rather than two). In `createCollection`, after Zod parse succeeds and before calling the repository, if `parsed.data.folder_id` is set call `await this.assertOwnedFolder(parsed.data.folder_id, companyId)`; pass `folderId: parsed.data.folder_id` through to `recordsRepository.createCollectionWithDefaultView`. In `updateCollection`, apply the same ownership check when `parsed.data.folder_id` is set, and pass `folder_id: parsed.data.folder_id` through to `recordsRepository.updateCollection`'s patch object.
  </action>
  <acceptance_criteria>
    - `DataCollectionRow` in records.types.ts contains `folder_id: string | null` and `archived_at: Date | null`
    - `CreateCollectionSchema` and `UpdateCollectionSchema` both accept an optional nullable `folder_id` UUID field
    - `recordsRepository.createCollectionWithDefaultView` INSERT statement includes `folder_id` as a column
    - `recordsService.createCollection` and `updateCollection` both call an ownership check on `folder_id` before writing, throwing `AppError(404, 'Folder not found')` for a folder owned by a different company
    - `node --require ts-node/register --test src/domains/records/records.service.test.ts` passes, including new folder_id ownership test cases
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && node --require ts-node/register --test src/domains/records/records.service.test.ts</automated>
  </verify>
  <done>data_collections can be created/updated with a company-scoped folder_id; cross-tenant folder_id is rejected with 404 before any write.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bulk archive/unarchive repository methods for the folders-domain cascade</name>
  <files>apps/api/src/domains/records/records.repository.ts, apps/api/src/domains/records/records.repository.test.ts</files>
  <read_first>
    - apps/api/src/domains/records/records.repository.ts (as edited by Task 1)
    - apps/api/src/core/events/outbox.ts (createDurableEventEnvelope, insertDurableEvent, EventOutboxRow)
    - .planning/phases/31-data-model-modernize-folders-domain/31-RESEARCH.md (Pitfall 5 — two-pass cascade: folder-filed collections in pass 1, project-filed collections in pass 2)
  </read_first>
  <behavior>
    - Test 1: `archiveCollectionsInFolders(client, folderIds, companyId)` returns only rows whose `folder_id` is in `folderIds`, `company_id` matches, and `archived_at` was previously `NULL`; sets `archived_at = NOW()`.
    - Test 2: `archiveCollectionsInProjects(client, projectIds, companyId)` returns only rows whose `project_id` is in `projectIds` and `company_id` matches (independent of `folder_id`, so a collection attached via `project_id` only is still caught in pass 2 per Pitfall 5).
    - Test 3: `unarchiveCollection(id, companyId)` sets `archived_at = NULL` and returns the updated row, or `null` if not found for that company.
  </behavior>
  <action>
    Add three new methods to `RecordsRepository` in `records.repository.ts`, all taking an explicit `client: PoolClient` for the first two (so they run inside the folders-domain cascade transaction owned by `foldersService.archiveFolder`, per RESEARCH.md's Pattern 1 "repository methods take `client` as first arg" convention already used in `workflowsRepository`):

    - `async archiveCollectionsInFolders(client: PoolClient, folderIds: string[], companyId: string): Promise<DataCollectionRow[]>` — `UPDATE data_collections SET archived_at = NOW(), updated_at = NOW() WHERE company_id = $1 AND folder_id = ANY($2::uuid[]) AND archived_at IS NULL RETURNING *`, params `[companyId, folderIds]`. Return `rows`.
    - `async archiveCollectionsInProjects(client: PoolClient, projectIds: string[], companyId: string): Promise<DataCollectionRow[]>` — same shape, `WHERE company_id = $1 AND project_id = ANY($2::uuid[]) AND archived_at IS NULL RETURNING *`.
    - `async unarchiveCollection(id: string, companyId: string): Promise<DataCollectionRow | null>` — non-transactional (uses module-level `db`, not a passed client, since this is a standalone single-row restore per D-03, not part of a cascade) — `UPDATE data_collections SET archived_at = NULL, updated_at = NOW() WHERE id = $1 AND company_id = $2 RETURNING *`, return `rows[0] ?? null`.

    Import `PoolClient` from `'pg'` at the top of the file (alongside the existing `db` import).

    Create `records.repository.test.ts` (new file, `node:test` + `mock.method` on `db`/a fake `PoolClient`, matching the existing `records.service.test.ts`/`folders.service.test.ts` style) covering the 3 Behavior cases above using a minimal fake client object `{ query: mock.fn(async (sql, params) => ({ rows: [...] })) }` passed directly (no real DB connection needed for this unit-level test — a real-DB integration test for the full cascade is added in plan 31-06).
  </action>
  <acceptance_criteria>
    - `recordsRepository.archiveCollectionsInFolders` and `.archiveCollectionsInProjects` both accept a `PoolClient` as first argument and filter on `archived_at IS NULL` so already-archived rows are not re-touched/re-eventing
    - `recordsRepository.unarchiveCollection` exists and restores `archived_at` to `NULL`
    - `apps/api/src/domains/records/records.repository.test.ts` exists and its 3 test cases pass
    - `node --require ts-node/register --test src/domains/records/records.repository.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && node --require ts-node/register --test src/domains/records/records.repository.test.ts</automated>
  </verify>
  <done>recordsRepository exposes transaction-safe bulk archive methods (by folder set and by project set) plus a single-row unarchive method, all unit-tested against a fake PoolClient.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client → `POST/PATCH /records/collections` | `folder_id` is caller-supplied input; must be validated as belonging to the caller's own company before being persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering / Elevation of Privilege | `recordsService.createCollection`/`updateCollection` `folder_id` input | mitigate | `assertOwnedFolder` service-layer check (404 for cross-tenant/missing) is defense-in-depth on top of the DB-level composite FK from plan 31-01 — both layers reject a cross-tenant `folder_id` |
| T-31-SC | Tampering (supply chain) | N/A — no new packages | accept | No new npm dependency introduced by this plan |
</threat_model>

<verification>
- `cd apps/api && node --require ts-node/register --test src/domains/records/records.service.test.ts src/domains/records/records.repository.test.ts`
- `npm test` (apps/api full suite) stays green after this plan.
</verification>

<success_criteria>
- `data_collections` rows can be filed into a folder via `folder_id`, validated against the caller's own company (HIER-01).
- `recordsRepository` exposes the bulk archive primitives the folders-domain cascade (plan 31-05) needs to archive collections as part of a folder/project archive (HIER-04).
</success_criteria>

<output>
Create `.planning/phases/31-data-model-modernize-folders-domain/31-02-SUMMARY.md` when done
</output>
