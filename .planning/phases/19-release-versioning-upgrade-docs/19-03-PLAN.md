---
phase: 19-release-versioning-upgrade-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - scripts/list-release-migrations.sh
  - docs/DEPLOYMENT.md
autonomous: true
requirements: [REL-02, REL-03]

must_haves:
  truths:
    - "CHANGELOG.md exists at repo root with one section per shipped release (v1.0, v2.0, v3.0), each listing its migration files"
    - "A reusable script can regenerate the migration-filename list for a release given the previous release's git tag"
    - "docs/DEPLOYMENT.md's upgrade guidance is a single 5-step procedure using npm run migrate, with zero remaining manual psql -f database/migrations instructions"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "One ## section per release (v1.0, v2.0, v3.0) with a migration list per section"
      contains: "## v1.0"
    - path: "scripts/list-release-migrations.sh"
      provides: "git diff <prev-tag>..HEAD --name-only -- database/migrations/ wrapper, filtered to the NNN_description.sql convention"
    - path: "docs/DEPLOYMENT.md"
      provides: "5-step upgrade procedure replacing the old per-file psql section"
      contains: "## Upgrading a running install"
  key_links:
    - from: "docs/DEPLOYMENT.md upgrade procedure"
      to: "apps/api/src/scripts/migrate.ts"
      via: "step 3: docker compose run --rm api npm run migrate"
      pattern: "npm run migrate"
    - from: "CHANGELOG.md migration lists"
      to: "scripts/list-release-migrations.sh"
      via: "generated via git diff <prev-tag>..HEAD --name-only -- database/migrations/"
      pattern: "database/migrations/"
---

<objective>
Give Vectra a real changelog (REL-02) and replace the outdated manual per-file `psql` upgrade instructions with the migration-runner-based 5-step procedure (REL-03).

Purpose: An operator upgrading a running install needs one place that says what changed (CHANGELOG.md) and one procedure to follow (docs/DEPLOYMENT.md) — not 8 scattered `psql -f` call-outs.
Output: `CHANGELOG.md` at repo root, `scripts/list-release-migrations.sh` (the generator RESEARCH.md recommends for future releases), and a rewritten `docs/DEPLOYMENT.md` upgrade section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-release-versioning-upgrade-docs/19-RESEARCH.md
@.planning/phases/19-release-versioning-upgrade-docs/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CHANGELOG.md + migration-list generator script</name>
  <files>CHANGELOG.md, scripts/list-release-migrations.sh</files>
  <read_first>
    apps/api/src/scripts/migrate.ts
  </read_first>
  <action>
    Create `scripts/list-release-migrations.sh` (repo root `scripts/` dir, new) — a bash script taking one positional arg, `$1` = the previous release's git tag (e.g. `v2.0`). It runs `git diff "$1"..HEAD --name-only -- database/migrations/` and filters the output through `grep -E '^database/migrations/[0-9]+_[A-Za-z0-9_-]+\.sql$'` (the same filename convention `migrate.ts`'s discovery regex `^\d+_[\w-]+\.sql$` encodes, documented in CLAUDE.md's "SQL Conventions" as `NNN_description.sql`). If no arg is given, print a usage message to stderr (`Usage: scripts/list-release-migrations.sh <previous-release-git-tag>`) and exit 1. Make the script executable (`chmod +x`) is not required in this environment but the shebang `#!/usr/bin/env bash` must be the first line.

    Create `CHANGELOG.md` at repo root with exactly 3 `##` sections, newest first:
    - `## v3.0 - On-Premise GA - Unreleased` — one line summarizing the milestone goal (dispatchers/on-prem GA, per ROADMAP.md's v3.0 goal), and a `### Migrations` subsection stating no new migration files have shipped since `v2.0` (verified via `git diff v2.0..HEAD --name-only -- database/migrations/`, which is empty as of this phase).
    - `## v2.0 - 2026-07-12 - Workspace Engine — Engine Unification` — one line summary, `### Migrations` subsection stating no new migration files shipped since `v1.0` (verified via `git diff v1.0..v2.0 --name-only -- database/migrations/`, empty).
    - `## v1.0 - 2026-07-06 - CRM Rework` — one line summary, `### Migrations` subsection listing all 23 migration files present at the `v1.0` tag, from `002_realtime_and_documents.sql` through `024_kpi_target_client.sql` (full ordered list: 002, 003, 004, 005, 006, 007, 008, 009, 010, 011, 012, 013, 014, 015, 016, 017, 018, 019, 020, 021, 022, 023, 024 — do not include a `001` entry, it does not exist in this repo).

    Add a one-line note near the top of `CHANGELOG.md` (below the `# Changelog` title, above the first `##` section) explaining how future sections are generated: `Migration lists per release are generated via ` + "`scripts/list-release-migrations.sh <previous-tag>`" + `.`
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -c "^## " CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## " CHANGELOG.md` returns exactly `3`
    - `grep -n "024_kpi_target_client.sql" CHANGELOG.md` matches (last v1.0 migration present)
    - `grep -n "001_" CHANGELOG.md` returns no matches (no such migration file exists)
    - `scripts/list-release-migrations.sh` exists, first line is `#!/usr/bin/env bash`, and contains the string `database/migrations/`
    - Running `bash scripts/list-release-migrations.sh` (no arg) exits non-zero and prints a `Usage:` message
  </acceptance_criteria>
  <done>CHANGELOG.md has 3 dated sections each with a Migrations subsection; scripts/list-release-migrations.sh reproduces the git diff pattern verified in RESEARCH.md</done>
</task>

<task type="auto">
  <name>Task 2: Replace docs/DEPLOYMENT.md's manual migration section with a 5-step upgrade procedure</name>
  <files>docs/DEPLOYMENT.md</files>
  <read_first>
    docs/DEPLOYMENT.md
  </read_first>
  <action>
    In `docs/DEPLOYMENT.md`, replace the entire `## Database migrations` section — from the `## Database migrations` heading (line 93) through the last `psql -f database/migrations/021_crm_extensions.sql` code block, immediately before the `## Outlook / Microsoft 365 integration` heading (line 174) — with one new section titled `## Upgrading a running install` (this becomes the new heading at that position; do NOT leave the old `## Database migrations` heading in place).

    The new section must contain exactly 5 numbered steps, each with a runnable command:
    1. Pull the new release: `git fetch --tags` then `git checkout vX.Y.Z` (reference `CHANGELOG.md` for the latest tag/section).
    2. Rebuild the images stamped with the new version: export the `VERSION` env var from the repo's `VERSION` file, then `docker compose -f docker-compose.prod.yml build` (this now passes `VERSION` as a build arg into all 4 images per Plan 02 of this phase).
    3. Run pending migrations before restarting: `docker compose -f docker-compose.prod.yml run --rm api npm run migrate` — note that this is the same runner used on first-run (Phase 15's `apps/api/src/scripts/migrate.ts`), idempotent, and safe to run even when there are zero pending files.
    4. Restart the stack on the new images: `docker compose -f docker-compose.prod.yml up -d`.
    5. Confirm the upgrade: `curl https://<your-api-host>/health` and check the returned `version` field matches the tag checked out in step 1.

    Do not reference any `psql -f database/migrations/0NN_*.sql` command anywhere in the replacement section or elsewhere in the file.
  </action>
  <verify>
    <automated>! grep -q "psql -f database/migrations" docs/DEPLOYMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "psql -f database/migrations" docs/DEPLOYMENT.md` finds NO matches (command exits non-zero)
    - `grep -n "## Upgrading a running install" docs/DEPLOYMENT.md` matches exactly once
    - `grep -n "## Database migrations" docs/DEPLOYMENT.md` returns no matches (old heading removed)
    - `grep -n "npm run migrate" docs/DEPLOYMENT.md` matches at least once
    - The file still contains `## Outlook / Microsoft 365 integration` immediately after the new section (structure preserved, section order unchanged)
  </acceptance_criteria>
  <done>docs/DEPLOYMENT.md's upgrade guidance is a single 5-step procedure; zero manual per-file psql instructions remain anywhere in the file</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Documentation and a local read-only git-inspection script; no runtime trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | `scripts/list-release-migrations.sh` | accept | Read-only `git diff` wrapper against the local repo; takes no untrusted network input, writes nothing, operator-invoked only |
</threat_model>

<verification>
`grep -c "^## " CHANGELOG.md` returns 3.
`grep -q "psql -f database/migrations" docs/DEPLOYMENT.md` finds no matches (exit code 1).
`bash scripts/list-release-migrations.sh v2.0` runs without error and prints an empty result (no new migrations since v2.0, consistent with RESEARCH.md's verified `git diff v2.0..HEAD` output).
</verification>

<success_criteria>
- `CHANGELOG.md` exists with 3 sections (v1.0, v2.0, v3.0-unreleased), each with a migration list
- `scripts/list-release-migrations.sh` reproduces the verified `git diff <tag>..HEAD --name-only -- database/migrations/` pattern
- `docs/DEPLOYMENT.md` contains a single `## Upgrading a running install` 5-step section
- Zero occurrences of `psql -f database/migrations` remain in `docs/DEPLOYMENT.md`
</success_criteria>

<output>
Create `.planning/phases/19-release-versioning-upgrade-docs/19-03-SUMMARY.md` when done
</output>
