---
phase: 27-baseline-truth-roadmap-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/current-state-truth-matrix.md
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03, BASE-04]

must_haves:
  truths:
    - "D-02: status assignment uses repository-visible evidence only: code, scripts, migrations, routes, package metadata, and committed docs in this repo; no tribal knowledge or assumed runtime behavior"
    - "D-03: capabilities with real code but mock data, stub connectors, disabled workers, fake runtime behavior, or incomplete end-to-end contracts are classified as partial rather than shipped"
    - "D-05: the truth artifact must inventory core runtime surfaces plus operational entrypoints: apps, packages, services, route domains, migrations/tables, queues/workers, public endpoints, and build/install/migrate/local-run scripts"
    - "D-06 and D-07: demo/stub caveats stay inline with the affected grouped surface so the inventory remains structural, grouped, and maintainable rather than a giant exhaustive dump"
    - "Phase 27 is a repo-truth audit, not a cleanup sprint: command failures, missing scripts, demo paths, and schema uncertainty must be documented rather than opportunistically fixed"
    - "The primary output is a hardened docs/architecture/current-state-truth-matrix.md that covers grouped inventory, imported roadmap status, baseline commands, and ADR gaps in one audit-grade artifact"
    - "Status calls use the strict evidence bar from CONTEXT.md D-01 through D-04: real code plus real product wiring plus repo-visible proof for shipped; ambiguity biases downward"
  artifacts:
    - path: "docs/architecture/current-state-truth-matrix.md"
      provides: "Phase 27 truth artifact with grouped inventory, imported roadmap mapping, baseline command matrix, and ADR-gap register"
      contains: "## Grouped Repository Inventory"
    - path: "docs/architecture/current-state-truth-matrix.md"
      provides: "Operator-visible command truth for lint, typecheck, tests, builds, migrations, and local boot"
      contains: "## Baseline Command Matrix"
    - path: "docs/architecture/current-state-truth-matrix.md"
      provides: "Named decision gaps that block architecture-changing work in Phases 28-30"
      contains: "## ADR Gaps Before Architecture-Changing PRs"
  key_links:
    - from: "docs/architecture/current-state-truth-matrix.md"
      to: ".planning/codebase/STRUCTURE.md"
      via: "Grouped inventory reuses codebase seams, then tightens them with current repo evidence"
      pattern: "apps|packages|services|workers|migrations"
    - from: "docs/architecture/current-state-truth-matrix.md"
      to: "package.json"
      via: "Baseline command matrix records real script entrypoints and missing root surfaces"
      pattern: "\"scripts\""
    - from: "docs/architecture/current-state-truth-matrix.md"
      to: "apps/api/src/server.ts"
      via: "Public endpoints and active worker wiring are verified against actual bootstrap code"
      pattern: "app\\.use|app\\.get\\(\"/health\"|start.*Worker"
---

<objective>
Harden `docs/architecture/current-state-truth-matrix.md` into the single audit-grade Phase 27 baseline artifact: a grouped inventory of runtime and operational repo surfaces, a strict imported-roadmap truth matrix, a command baseline that documents real pass/fail/not-available outcomes without opportunistic fixes, and an ADR-gap section that names the decisions required before Phases 28-30 proceed.

Purpose: v5.0 foundation work should start from repository truth, not from inherited assumptions or stale planning notes.
Output: one reconciled `docs/architecture/current-state-truth-matrix.md` that satisfies BASE-01 through BASE-04 directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-baseline-truth-roadmap-reconciliation/27-CONTEXT.md
@.planning/phases/27-baseline-truth-roadmap-reconciliation/27-RESEARCH.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@docs/specs/architecture-steering.md
@docs/architecture/current-state-truth-matrix.md
</context>

<interfaces>
From `.planning/ROADMAP.md` Phase 27 success criteria (must all be visibly satisfiable from the final artifact):
```text
1. docs/architecture/current-state-truth-matrix.md maps every imported roadmap phase to shipped, partial, demo, absent, or deferred status.
2. Inventory covers apps, packages, services, migrations/tables, queues/workers, route domains, and public endpoints.
3. Baseline commands for lint, typecheck, tests, builds, migrations, and local boot are documented with pass/fail status and not mass-fixed opportunistically.
4. ADR gap list names the decisions required before architecture-changing PRs.
```

From `package.json` + app manifests (actual command surfaces, not assumptions):
```json
// root
{
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "migrate": "npm run migrate --workspace @vectra/api",
    "install:on-prem": "npm run install:on-prem --workspace @vectra/api"
  }
}

// apps/api
{
  "scripts": {
    "build": "tsc",
    "test": "node --require ts-node/register --test src/**/*.test.ts",
    "migrate": "node dist/scripts/migrate.js"
  }
}

// apps/workspaces, apps/cmr, apps/marketplace
{
  "scripts": {
    "build": "next build",
    "lint": "next lint"
  }
}
```

From `apps/api/src/server.ts` and worker files (status proof must reflect actual bootstrap wiring):
```typescript
app.use("/api/webhooks", webhookRoutes);
app.get("/health", async (req, res) => { ... });
// bootstrap starts matching/email workers; telematics worker is not started here
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build the grouped repository inventory inside the truth matrix</name>
  <files>docs/architecture/current-state-truth-matrix.md</files>
  <read_first>
    docs/architecture/current-state-truth-matrix.md
    .planning/codebase/STRUCTURE.md
    .planning/codebase/INTEGRATIONS.md
    apps/api/src/server.ts
    apps/api/src/workers/telematics.worker.ts
    database/migrations/*.sql
  </read_first>
  <action>
    Edit `docs/architecture/current-state-truth-matrix.md` to add a new `## Grouped Repository Inventory` section near the top of the document. Per D-05 through D-07, keep it grouped, not exhaustive, and keep demo/stub caveats inline with the affected surface.

    Cover, at minimum:
    - Apps (`api`, `workspaces`, `cmr`, `marketplace`)
    - Shared packages (`ui`, `api-client`, `auth`, `types`, `data`, `config`)
    - Standalone services (`matching-engine`)
    - Database migrations/tables (visible migration range and notable schema caveats)
    - Queue/worker surfaces (matching, email-sync, telematics) with inline truth about whether the worker only exists or is actually bootstrap-wired
    - Route domains and public endpoints, explicitly separating authenticated `/api/v1/*` domains from public `/health`, `/api/webhooks/*`, and public POD token routes
    - Operational entrypoints (root scripts, app scripts, Docker Compose surfaces)

    Attach demo/stub/partial caveats inline to the affected surface rather than in a detached caveat appendix. If a surface is known to depend on simulated behavior, disabled wiring, or incomplete contracts, say so in-place.
  </action>
  <acceptance_criteria>
    - `docs/architecture/current-state-truth-matrix.md` contains a `## Grouped Repository Inventory` heading
    - The inventory explicitly names apps, packages, services, migrations/tables, queues/workers, route domains, and public endpoints
    - Public edges are called out separately from authenticated `/api/v1/*` domains
    - Telematics worker truth is documented honestly (worker file exists, bootstrap wiring status stated)
    - Demo/stub caveats appear inline with the relevant surface, not only in a disconnected risk list
  </acceptance_criteria>
  <verify>
    <automated>rg -n "## Grouped Repository Inventory|queues|workers|public endpoints|route domains|migrations" docs/architecture/current-state-truth-matrix.md</automated>
  </verify>
  <done>The truth matrix has an explicit grouped inventory that satisfies BASE-01 and stays aligned with CONTEXT.md D-05 through D-07.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tighten the imported roadmap truth matrix using strict evidence</name>
  <files>docs/architecture/current-state-truth-matrix.md</files>
  <read_first>
    docs/architecture/current-state-truth-matrix.md
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/27-baseline-truth-roadmap-reconciliation/27-CONTEXT.md
    docs/specs/architecture-steering.md
  </read_first>
  <action>
    Revise the existing imported-roadmap mapping in `docs/architecture/current-state-truth-matrix.md` so every imported phase is classified using the strict evidence bar from D-01 through D-04.

    For each imported phase row:
    - Keep or revise the status to one of `shipped`, `partial`, `demo`, `absent`, or `deferred`
    - Tighten the evidence column so it cites real repo-visible proof, not broad narrative claims
    - Preserve the v5 routing/decision column, but make sure it clearly reflects whether the work is reopened in v5, deferred to a later milestone, or blocked pending foundation decisions

    Where evidence is mixed, bias downward rather than upward. Avoid labels like `partial/shipped foundation` if a single stricter label plus evidence text can carry the truth more cleanly.
  </action>
  <acceptance_criteria>
    - Every imported roadmap phase row has a status expressed in the allowed rubric
    - Evidence statements are repo-visible and concrete rather than aspirational
    - Mixed/demo/incomplete contracts are not overstated as shipped
    - The matrix still explains how imported roadmap work maps into v5+ execution
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Imported Roadmap Phase Mapping|shipped|partial|demo|absent|deferred" docs/architecture/current-state-truth-matrix.md</automated>
  </verify>
  <done>The imported roadmap matrix satisfies BASE-02 with a stricter evidence bar and a clean handoff into v5+ planning.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record baseline command truth and ADR gaps without fixing them</name>
  <files>docs/architecture/current-state-truth-matrix.md</files>
  <read_first>
    package.json
    apps/api/package.json
    apps/workspaces/package.json
    apps/cmr/package.json
    apps/marketplace/package.json
    docker-compose.yml
    docker-compose.prod.yml
    docs/architecture/current-state-truth-matrix.md
    .planning/phases/27-baseline-truth-roadmap-reconciliation/27-RESEARCH.md
  </read_first>
  <behavior>
    - The final artifact distinguishes between `pass`, `fail`, and `not available` rather than collapsing them into one status.
    - If a command fails or a script is missing, the doc records that exact outcome and a concise likely cause; it does not turn into a remediation sprint.
    - The ADR-gap list is phrased as a precondition register for Phases 28-30, not a vague backlog.
  </behavior>
  <action>
    Add two new sections to `docs/architecture/current-state-truth-matrix.md`:

    1. `## Baseline Command Matrix`
       - Document the concrete commands used to assess:
         - lint
         - typecheck
         - tests
         - builds
         - migrations
         - local boot
       - Prefer the real repo surfaces that actually exist. If no root command exists, say so and record the closest per-surface commands explicitly.
       - During execution, run the commands and record status as `pass`, `fail`, or `not available`, plus a short evidence note.
       - Do not fix failures as part of this plan.

    2. `## ADR Gaps Before Architecture-Changing PRs`
       - Name the concrete decisions that must be locked before Phases 28-30 proceed.
       - Cover at least: request-context/capability boundary, public endpoint trust model, demo-vs-production behavior, schema truth for credential/runtime tables, event envelope/outbox contract, workflow persistence/run contract, and matching-engine/service contract reconciliation.
       - For each gap, explain why later phases depend on it.
  </action>
  <acceptance_criteria>
    - `docs/architecture/current-state-truth-matrix.md` contains both `## Baseline Command Matrix` and `## ADR Gaps Before Architecture-Changing PRs`
    - The command matrix includes lint, typecheck, tests, builds, migrations, and local boot
    - Missing scripts or failures are recorded explicitly rather than hidden
    - The ADR section names concrete decisions and ties them to upcoming phases
  </acceptance_criteria>
  <verify>
    <automated>rg -n "## Baseline Command Matrix|## ADR Gaps Before Architecture-Changing PRs|lint|typecheck|tests|build|migrations|local boot" docs/architecture/current-state-truth-matrix.md</automated>
  </verify>
  <done>The truth matrix now satisfies BASE-03 and BASE-04 and is explicit about both command reality and architectural decision debt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository evidence -> maintainer documentation | Phase 27 turns code/manifests/routes/scripts into a durable truth artifact that later phases will trust |
| baseline command execution -> future planning decisions | The documented pass/fail/not-available outcomes will influence whether later work is safe to start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | Truth matrix status rows | mitigate | Require repo-visible evidence for every status call; bias downward on ambiguity; never label demo/incomplete paths as shipped without direct proof |
| T-27-02 | Repudiation | Baseline command section | mitigate | Record exact commands and explicit `pass` / `fail` / `not available` outcomes so missing scripts and failing surfaces cannot be hand-waved later |
| T-27-03 | Information Integrity | ADR-gap register | mitigate | Phrase gaps as concrete preconditions tied to Phases 28-30 so future implementation packets cannot claim the decisions were already made when they were not |
</threat_model>

<verification>
- `rg -n "## Grouped Repository Inventory|## Baseline Command Matrix|## ADR Gaps Before Architecture-Changing PRs" docs/architecture/current-state-truth-matrix.md` returns all three headings.
- Review the imported roadmap table and confirm every row has an allowed status and concrete evidence.
- Review the command matrix and confirm lint, typecheck, tests, builds, migrations, and local boot all have an explicit documented outcome.
- Review the ADR-gap section and confirm each listed gap names both the decision and the downstream phase pressure it blocks.
</verification>

<success_criteria>
- `docs/architecture/current-state-truth-matrix.md` directly satisfies all four Phase 27 roadmap success criteria
- The artifact is maintainable and grouped, not a raw dump
- Command failures and missing scripts are preserved as truth, not repaired in this phase
- The ADR-gap list gives Phase 28-30 a clear precondition register
</success_criteria>

<output>
Create `.planning/phases/27-baseline-truth-roadmap-reconciliation/27-01-SUMMARY.md` when done
</output>
