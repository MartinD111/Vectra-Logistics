---
phase: 34-drag-to-reorder-reparent-create-rename-archive-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workspaces/src/lib/api/folders.api.ts
  - apps/workspaces/src/lib/api/projects.api.ts
  - apps/workspaces/src/lib/hooks/useFolders.ts
  - apps/workspaces/src/lib/hooks/useProjects.ts
  - apps/workspaces/src/components/tree/treeDragUtils.ts
  - apps/workspaces/src/components/tree/treeArchiveCount.ts
autonomous: true
requirements: [TREEOPS-01, TREEOPS-02, TREEOPS-03, TREEOPS-04, TREEOPS-05, TREEOPS-06]

must_haves:
  truths:
    - "Frontend has working API wrappers that call the already-shipped Phase 32 archive/unarchive/reorder/moveNode endpoints with correct payload shapes"
    - "Every folder/project mutation (existing and new) invalidates the full-tree cache so the sidebar reflects changes without a manual page refresh or ~60s stale wait"
    - "Pure, dependency-free tree-walk utilities exist for flattening the visible tree for drag-and-drop and for counting archive descendants by node type"
  artifacts:
    - path: "apps/workspaces/src/lib/api/folders.api.ts"
      provides: "archive/unarchive/reorder/moveNode wrappers on foldersApi"
    - path: "apps/workspaces/src/lib/api/projects.api.ts"
      provides: "archive/unarchive wrappers on projectsApi"
    - path: "apps/workspaces/src/lib/hooks/useFolders.ts"
      provides: "useArchiveFolder/useUnarchiveFolder/useReorderTree/useMoveTreeNode hooks + fullTree cache invalidation fix"
    - path: "apps/workspaces/src/lib/hooks/useProjects.ts"
      provides: "useArchiveProject/useUnarchiveProject hooks invalidating the shared fullTree key"
    - path: "apps/workspaces/src/components/tree/treeDragUtils.ts"
      provides: "flattenVisibleTree + computeDropZone pure functions"
    - path: "apps/workspaces/src/components/tree/treeArchiveCount.ts"
      provides: "countDescendants DFS walk by node_type"
  key_links:
    - from: "apps/workspaces/src/lib/hooks/useFolders.ts"
      to: "apps/workspaces/src/lib/api/folders.api.ts"
      via: "mutationFn: foldersApi.archive/unarchive/reorder/moveNode"
      pattern: "foldersApi\\.(archive|unarchive|reorder|moveNode)"
    - from: "apps/workspaces/src/lib/hooks/useProjects.ts"
      to: "apps/workspaces/src/lib/api/projects.api.ts"
      via: "mutationFn: projectsApi.archive/unarchive"
      pattern: "projectsApi\\.(archive|unarchive)"
---

<objective>
Build the missing frontend API/hook foundation and pure tree-walk utilities that every subsequent Phase 34 plan depends on. The Phase 32 backend already exposes `POST /folders/:id/archive`, `POST /folders/:id/unarchive`, `POST /folders/tree/reorder`, `POST /folders/tree/move`, and the equivalent project archive/unarchive routes — none of them are wrapped on the frontend today (verified by direct read in RESEARCH.md). This is Wave 1 (the hard prerequisite RESEARCH.md calls "Wave 0 work") — no UI component in this phase can be built correctly without it.

Purpose: Unblock every later plan in this phase (context menu, archive dialog, drag-and-drop) with a correct, typed, cache-coherent data layer.
Output: Extended `folders.api.ts`/`projects.api.ts`/`useFolders.ts`/`useProjects.ts`, plus two new pure utility modules (`treeDragUtils.ts`, `treeArchiveCount.ts`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-drag-to-reorder-reparent-create-rename-archive-flows/34-CONTEXT.md
@.planning/phases/34-drag-to-reorder-reparent-create-rename-archive-flows/34-RESEARCH.md
@.planning/phases/34-drag-to-reorder-reparent-create-rename-archive-flows/34-PATTERNS.md
</context>

<interfaces>
<!-- Backend contracts this plan's wrappers must match exactly. -->

From `apps/api/src/domains/folders/folders.routes.ts`:
- `POST /api/v1/folders/:id/archive` — capability `workspace.admin`, no body, returns `{ folder: Folder }`
- `POST /api/v1/folders/:id/unarchive` — same, returns `{ folder: Folder }`
- `POST /api/v1/folders/tree/reorder` — body validated by `ReorderNodesSchema`, returns `{ node_type, parent_id, project_id, ordered_ids }`
- `POST /api/v1/folders/tree/move` — body validated by `MoveNodeSchema`, returns the updated node (untyped on the client, `unknown`)

From `apps/api/src/domains/folders/dto/tree.dto.ts`:
- `NodeType = z.enum(['folder', 'project', 'program', 'data_collection'])` — reorder/move endpoints accept these four node types only (NOT `project_page`)
- `ReorderNodesSchema = { node_type, parent_id: string | null, project_id?: string | null, ordered_ids: string[] (min 1) }`
- `MoveNodeSchema = { node_type, node_id, new_parent_id: string | null, project_id?: string | null }`

From `apps/api/src/domains/projects/projects.routes.ts`:
- `POST /api/v1/projects/:id/archive` — capability `workspace.admin`, no body, returns `{ project: Project }`
- `POST /api/v1/projects/:id/unarchive` — same, returns `{ project: Project }`

From `packages/api-client/src/client.ts`:
- `ApiError extends Error { status: number }` — thrown by `apiFetch` on non-2xx; `.message` carries the server's exact `{ error: string }` body text (or a friendly 401/fallback message)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend folders.api.ts and projects.api.ts with the missing archive/unarchive/reorder/moveNode wrappers</name>
  <files>apps/workspaces/src/lib/api/folders.api.ts, apps/workspaces/src/lib/api/projects.api.ts</files>
  <read_first>
    apps/workspaces/src/lib/api/folders.api.ts
    apps/workspaces/src/lib/api/projects.api.ts
    apps/api/src/domains/folders/dto/tree.dto.ts
    apps/api/src/domains/folders/folders.routes.ts
  </read_first>
  <action>
    In `apps/workspaces/src/lib/api/folders.api.ts`, add four methods to the existing `foldersApi` object (after `getFullTree`, same object, same `BASE`/`apiFetch` conventions already used by `move`/`remove`): `archive(id: string)` → `POST ${BASE}/${id}/archive`, no body, unwrap `{ folder: Folder }` to `Folder`; `unarchive(id: string)` → `POST ${BASE}/${id}/unarchive`, same unwrap; `reorder(data)` → `POST ${BASE}/tree/reorder` with body `data`, where `data` is typed `{ node_type: 'folder' | 'project' | 'program' | 'data_collection'; parent_id: string | null; project_id?: string | null; ordered_ids: string[] }`, return type `{ node_type: string; parent_id: string | null; project_id: string | null; ordered_ids: string[] }` (no unwrap, response is already flat); `moveNode(data)` → `POST ${BASE}/tree/move` with body `data`, same input type as `reorder` minus `ordered_ids` plus `node_id: string` and `new_parent_id: string | null` in place of `parent_id`, return type `unknown` (callers don't need the shape, only success/failure). Do not add new imports — `apiFetch` and `BASE` are already in scope.

    In `apps/workspaces/src/lib/api/projects.api.ts`, add two methods to the existing `projectsApi` object (after `remove`, before `stats`, same `BASE`/`apiFetch` conventions as `create`/`update`): `archive(id: string)` → `POST ${BASE}/${id}/archive`, no body, unwrap `{ project: Project }` to `Project`; `unarchive(id: string)` → `POST ${BASE}/${id}/unarchive`, same unwrap.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    <source>`grep -c "archive:" apps/workspaces/src/lib/api/folders.api.ts` returns 1 or more (archive + unarchive both match "archive:" substring, so expect at least 2)</source>
    <source>`grep -c "reorder:" apps/workspaces/src/lib/api/folders.api.ts` returns 1</source>
    <source>`grep -c "moveNode:" apps/workspaces/src/lib/api/folders.api.ts` returns 1</source>
    <source>`grep -c "archive:" apps/workspaces/src/lib/api/projects.api.ts` returns at least 2 (archive + unarchive)</source>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </acceptance_criteria>
  <done>foldersApi exposes archive/unarchive/reorder/moveNode; projectsApi exposes archive/unarchive; both compile clean with no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extend useFolders.ts and useProjects.ts with mutation hooks and fix the fullTree cache-invalidation gap</name>
  <files>apps/workspaces/src/lib/hooks/useFolders.ts, apps/workspaces/src/lib/hooks/useProjects.ts</files>
  <read_first>
    apps/workspaces/src/lib/hooks/useFolders.ts
    apps/workspaces/src/lib/hooks/useProjects.ts
    apps/workspaces/src/lib/api/folders.api.ts
  </read_first>
  <action>
    In `apps/workspaces/src/lib/hooks/useFolders.ts`: patch `invalidateFolderAffectedQueries` (currently invalidates `qk.folders`, `['projects']`, `['programs']`) to add a fourth call `qc.invalidateQueries({ queryKey: qk.fullTree })` — this fixes the existing gap (RESEARCH.md Pitfall 4) for `useCreateFolder`/`useUpdateFolder`/`useMoveFolder`/`useDeleteFolder` too, not just the new hooks. Add four new exported hooks following the exact `useMoveFolder` shape (mutationFn + `onSuccess: () => invalidateFolderAffectedQueries(qc)`): `useArchiveFolder()` (mutationFn: `foldersApi.archive`), `useUnarchiveFolder()` (mutationFn: `foldersApi.unarchive`), `useReorderTree()` (mutationFn: `foldersApi.reorder`), `useMoveTreeNode()` (mutationFn: `foldersApi.moveNode`).

    In `apps/workspaces/src/lib/hooks/useProjects.ts`: this file has no `qk.fullTree` constant today (it only imports from `projects.api.ts`) — add the literal invalidation key `['folders', 'tree', 'full'] as const` (matching `useFolders.ts`'s `qk.fullTree` value exactly, since projects also drive the tree sidebar via `useFullTree()`). Add `useArchiveProject()` and `useUnarchiveProject()` mutation hooks (mutationFn: `projectsApi.archive`/`projectsApi.unarchive`) whose `onSuccess` invalidates both `qk.projects` and the new `['folders', 'tree', 'full']` key.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    <source>`grep -c "export function useArchiveFolder\|export function useUnarchiveFolder\|export function useReorderTree\|export function useMoveTreeNode" apps/workspaces/src/lib/hooks/useFolders.ts` returns 4</source>
    <source>`grep -A3 "function invalidateFolderAffectedQueries" apps/workspaces/src/lib/hooks/useFolders.ts | grep -c "qk.fullTree"` returns 1</source>
    <source>`grep -c "export function useArchiveProject\|export function useUnarchiveProject" apps/workspaces/src/lib/hooks/useProjects.ts` returns 2</source>
    <source>`grep -c "folders', 'tree', 'full'" apps/workspaces/src/lib/hooks/useProjects.ts` returns at least 1</source>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </acceptance_criteria>
  <done>All 6 new mutation hooks exist and invalidate the full-tree query key on success; the existing folder mutation hooks also now invalidate it via the shared helper.</done>
</task>

<task type="auto">
  <name>Task 3: Create treeDragUtils.ts (flatten + drop-zone geometry) and treeArchiveCount.ts (descendant DFS count)</name>
  <files>apps/workspaces/src/components/tree/treeDragUtils.ts, apps/workspaces/src/components/tree/treeArchiveCount.ts</files>
  <read_first>
    apps/workspaces/src/components/shared/treeFindPath.ts
    apps/workspaces/src/components/tree/treeFilters.ts
    apps/workspaces/src/lib/api/folders.api.ts
  </read_first>
  <action>
    Create `apps/workspaces/src/components/tree/treeDragUtils.ts` as a pure, dependency-free module (mirrors `treeFilters.ts`'s shape, imports only `type { TreeNode } from '@/lib/api/folders.api'`) exporting: `interface FlatTreeRow { id: string; node_type: 'folder' | 'project'; parentKey: string; depth: number }`; `function flattenVisibleTree(nodes: TreeNode[], expanded: Set<string>, depth = 0, parentKey = 'root'): FlatTreeRow[]` — filters nodes to `node_type === 'folder' || node_type === 'project'` only (D-03: only these two types are drag targets), and for folder nodes only recurses into `n.children` when `expanded.has(n.id)`, passing `n.id` as the child rows' `parentKey`; `function computeDropZone(pointerY: number, overRect: DOMRect, overIsFolder: boolean): 'before' | 'after' | 'into'` — computes `relative = (pointerY - overRect.top) / overRect.height`, returns `'into'` only when `overIsFolder && relative > 0.25 && relative < 0.75`, otherwise `relative < 0.5 ? 'before' : 'after'`.

    Create `apps/workspaces/src/components/tree/treeArchiveCount.ts` as a pure DFS module mirroring `treeFindPath.ts`'s exact shape (same header-comment style: "Pure DFS ... over the client-cached full tree ... No mutation of input, no dependencies", imports only `type { TreeNode } from '@/lib/api/folders.api'`) exporting: `interface DescendantCounts { folder: number; project: number; program: number; data_collection: number; project_page: number }`; `function countDescendants(node: TreeNode): DescendantCounts` — recursively walks `node.children` (never counts `node` itself), incrementing the tally keyed by each descendant's `node_type`, starting from an object with all five keys initialized to 0.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    <source>`grep -c "export function flattenVisibleTree" apps/workspaces/src/components/tree/treeDragUtils.ts` returns 1</source>
    <source>`grep -c "export function computeDropZone" apps/workspaces/src/components/tree/treeDragUtils.ts` returns 1</source>
    <source>`grep -c "export function countDescendants" apps/workspaces/src/components/tree/treeArchiveCount.ts` returns 1</source>
    <source>`grep -c "folder: 0\|folder:0" apps/workspaces/src/components/tree/treeArchiveCount.ts` returns at least 1 (initial counts object present)</source>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </acceptance_criteria>
  <done>Both utility modules exist, export the specified pure functions with no side effects and no new dependencies, and compile clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser client → `apps/api` folders/projects domain routes | Every new wrapper (archive/unarchive/reorder/moveNode) crosses this boundary; the server re-validates tenant ownership and `workspace.admin` capability on every call — the client wrappers add no new trust, they are thin transport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-34-01 | Elevation of Privilege | New `foldersApi.reorder`/`moveNode`/`archive`/`unarchive` wrappers | accept | Server-side `requireCapability('workspace.admin')` already gates all four endpoints (Phase 32); wrappers add no client-side bypass since they perform no local authorization decisions |
| T-34-02 | Tampering | `useProjects.ts`'s new literal `['folders', 'tree', 'full']` invalidation key | accept | Read-only cache-invalidation key, not a data-write path; a typo here only causes stale UI, not a security issue |
</threat_model>

<verification>
Run `npx tsc --noEmit -p apps/workspaces/tsconfig.json` from the repo root after all three tasks — must exit 0 with no new type errors in the six touched/created files.
</verification>

<success_criteria>
- `foldersApi` and `projectsApi` expose all 6 new methods with correct request/response typing matching the Phase 32 backend contracts.
- `useFolders.ts` and `useProjects.ts` expose 6 new mutation hooks, and every folder/project mutation (new and pre-existing) invalidates the shared full-tree query key.
- `treeDragUtils.ts` and `treeArchiveCount.ts` exist as pure, dependency-free modules ready for Wave 2+ plans to consume without further foundation work.
</success_criteria>

<output>
Create `.planning/phases/34-drag-to-reorder-reparent-create-rename-archive-flows/34-01-SUMMARY.md` when done
</output>
