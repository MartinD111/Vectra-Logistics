---
phase: 34-drag-to-reorder-reparent-create-rename-archive-flows
plan: 05
type: execute
wave: 3
depends_on: ["34-03", "34-04"]
files_modified:
  - apps/workspaces/src/components/tree/TreeSection.tsx
autonomous: false
requirements: [TREEOPS-05, TREEOPS-06]

must_haves:
  truths:
    - "Selecting Archive on a folder/project row opens the ArchiveConfirmDialog built in 34-03 against the correct node, and confirming archives it"
    - "After archiving a node with zero descendants, an Undo toast appears and clicking it restores the node; after archiving a node with descendants, no Undo toast appears"
    - "An archived folder/project disappears from the default tree view immediately after the mutation succeeds (no manual refresh or stale-cache wait)"
  artifacts:
    - path: "apps/workspaces/src/components/tree/TreeSection.tsx"
      provides: "ArchiveConfirmDialog mount against archiveTarget state; TreeUndoToast mount + handleUndo dispatch"
  key_links:
    - from: "apps/workspaces/src/components/tree/TreeSection.tsx"
      to: "apps/workspaces/src/components/tree/ArchiveConfirmDialog.tsx"
      via: "conditional render when archiveTarget is set"
      pattern: "ArchiveConfirmDialog"
    - from: "apps/workspaces/src/components/tree/TreeSection.tsx"
      to: "apps/workspaces/src/lib/hooks/useFolders.ts / useProjects.ts"
      via: "useUnarchiveFolder()/useUnarchiveProject() dispatch from handleUndo"
      pattern: "useUnarchive(Folder|Project)\\(\\)"
---

<objective>
Complete the archive flow (TREEOPS-05, TREEOPS-06) by mounting the standalone `ArchiveConfirmDialog` and `TreeUndoToast` components (built in 34-03) into `TreeSection.tsx`, against the `archiveTarget` state that 34-04 already declared and wired as the `onArchive` prop. This plan only touches `TreeSection.tsx` — `TreeNodeRow.tsx`'s "Archive" menu item already calls `onArchive(node)` (34-04), which sets `archiveTarget`; this plan renders the dialog for that state and handles the post-archive Undo flow.

Purpose: Close the loop on the archive-confirmation UX so archiving is safe (descendant-count breakdown before the irreversible-without-cascade action) and reversible when safe (D-07's zero-descendant Undo).
Output: `TreeSection.tsx` renders `ArchiveConfirmDialog` and `TreeUndoToast` at the correct moments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-drag-to-reorder-reparent-create-rename-archive-flows/34-CONTEXT.md
@.planning/phases/34-drag-to-reorder-reparent-create-rename-archive-flows/34-UI-SPEC.md
@.planning/phases/34-drag-to-reorder-reparent-create-rename-archive-flows/34-03-SUMMARY.md
@.planning/phases/34-drag-to-reorder-reparent-create-rename-archive-flows/34-04-SUMMARY.md
</context>

<interfaces>
<!-- Contracts from 34-03, already built. -->

```
function ArchiveConfirmDialog(props: {
  node: TreeNode;
  onClose: () => void;
  onArchived: (node: TreeNode, totalDescendants: number) => void;
}): JSX.Element

function TreeUndoToast(props: {
  nodeName: string;
  onUndo: () => void;
  onDismiss: () => void;
}): JSX.Element
```

From 34-04's `TreeSection.tsx`: `const [archiveTarget, setArchiveTarget] = useState<TreeNode | null>(null);` already exists and is already wired as every root `TreeNodeRow`'s `onArchive` prop — this plan adds the dialog render against it, it does not redeclare it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Mount ArchiveConfirmDialog against archiveTarget and handle the post-archive Undo toast</name>
  <files>apps/workspaces/src/components/tree/TreeSection.tsx</files>
  <read_first>
    apps/workspaces/src/components/tree/TreeSection.tsx
    apps/workspaces/src/components/tree/ArchiveConfirmDialog.tsx
    apps/workspaces/src/components/tree/TreeUndoToast.tsx
    apps/workspaces/src/lib/hooks/useFolders.ts
    apps/workspaces/src/lib/hooks/useProjects.ts
  </read_first>
  <action>
    Import `ArchiveConfirmDialog` from `./ArchiveConfirmDialog` and `TreeUndoToast` from `./TreeUndoToast`. Add state: `const [undoTarget, setUndoTarget] = useState<TreeNode | null>(null);`. Add `const unarchiveFolder = useUnarchiveFolder(); const unarchiveProject = useUnarchiveProject();`.

    Render `{archiveTarget && <ArchiveConfirmDialog node={archiveTarget} onClose={() => setArchiveTarget(null)} onArchived={(node, totalDescendants) => { if (totalDescendants === 0) setUndoTarget(node); }} />}` after the existing root-list render block. The dialog itself already closes on confirm/cancel (34-03's contract calls `onClose()` internally before `onArchived`), so this handler only needs to decide whether to show the Undo toast — it does not need to also call `setArchiveTarget(null)`.

    Render `{undoTarget && <TreeUndoToast nodeName={undoTarget.name} onUndo={handleUndo} onDismiss={() => setUndoTarget(null)} />}` where `handleUndo` is defined as: dispatch `unarchiveFolder.mutate(undoTarget.id)` when `undoTarget.node_type === 'folder'`, else `unarchiveProject.mutate(undoTarget.id)` when `'project'`, then `setUndoTarget(null)` immediately (do not wait for the mutation to resolve before dismissing the toast — the mutation's own `onSuccess` already invalidates the full-tree cache per 34-01, so the row reappearing is handled by the existing cache-invalidation path, not by this toast's dismiss timing).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    <source>`grep -c "ArchiveConfirmDialog" apps/workspaces/src/components/tree/TreeSection.tsx` returns at least 2 (import + JSX usage)</source>
    <source>`grep -c "TreeUndoToast" apps/workspaces/src/components/tree/TreeSection.tsx` returns at least 2 (import + JSX usage)</source>
    <source>`grep -c "totalDescendants === 0" apps/workspaces/src/components/tree/TreeSection.tsx` returns 1</source>
    <source>`grep -c "useUnarchiveFolder\|useUnarchiveProject" apps/workspaces/src/components/tree/TreeSection.tsx` returns 2</source>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </acceptance_criteria>
  <done>Archiving a node from the context menu opens ArchiveConfirmDialog with the correct descendant breakdown; confirming archives it and hides the dialog; an Undo toast appears only when the archived node had zero descendants, and clicking Undo restores it via the correct unarchive mutation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify archive confirmation, descendant counts, and Undo</name>
  <what-built>Archive confirmation dialog with per-type descendant-count breakdown, wired to the folder/project Archive menu action; zero-descendant Undo toast.</what-built>
  <action>Manually walk through the archive confirmation and Undo flows in the running app per how-to-verify below; no code changes in this task.</action>
  <how-to-verify>
    1. Right-click a folder with known children (some projects/programs nested under it) → Archive — confirm the dialog shows a per-type breakdown matching the actual tree contents (e.g. "This will also archive 2 projects, 3 pages.") with zero-count types omitted.
    2. Confirm the archive — confirm the folder and its listed descendants disappear from the default tree view immediately (no manual refresh needed).
    3. Right-click a leaf project with no children → Archive → confirm — confirm the dialog shows "This folder/project has no contents and will be archived." and, after confirming, an Undo toast appears bottom-right.
    4. Click Undo on that toast — confirm the project reappears in the tree.
    5. Repeat step 1-2 for a folder WITH descendants — confirm no Undo toast appears after confirming.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser client → `POST /folders/:id/archive`, `POST /folders/:id/unarchive`, `POST /projects/:id/archive`, `POST /projects/:id/unarchive` | All four archive/unarchive dispatch paths cross this boundary; server re-validates tenant ownership + `workspace.admin` capability on every call (Phase 31/32, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-34-08 | Repudiation | Undo restores a node without re-confirming intent | accept | Undo is scoped to zero-descendant archives only (D-07) — the blast radius of an accidental Undo click is a single node re-appearing, not a cascade; the archive itself already required an explicit confirm click |
| T-34-09 | Information Disclosure | Descendant-count breakdown reveals node names/counts the archiving user might not otherwise see in one place | accept | The user already has read access to the full tree (`GET /folders/tree/full` is scoped to their own tenant, Phase 31 `HIER-02`); the breakdown reveals nothing beyond what `useFullTree()` already returned to this client |
</threat_model>

<verification>
`npx tsc --noEmit -p apps/workspaces/tsconfig.json` exits 0. Manual click-through per the checkpoint task above.
</verification>

<success_criteria>
Archive confirmation with accurate descendant-count breakdown is wired end-to-end; archived nodes vanish from the default tree view immediately; Undo works for zero-descendant archives and is correctly absent otherwise (TREEOPS-05, TREEOPS-06).
</success_criteria>

<output>
Create `.planning/phases/34-drag-to-reorder-reparent-create-rename-archive-flows/34-05-SUMMARY.md` when done
</output>
