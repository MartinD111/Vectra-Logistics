---
phase: 26-additional-view-types
plan: 05
type: execute
wave: 3
depends_on: ["26-02", "26-03", "26-04"]
files_modified:
  - apps/workspaces/src/components/projectPage/BoardBlock.tsx
autonomous: true
requirements: [VIEW-02, VIEW-03, VIEW-04, VIEW-05]

must_haves:
  truths:
    - "The same data_collection can be viewed as List, Calendar, Gallery, or Timeline via the same collection-view block, all reading the same collection_records — no new block kind, no data duplication"
    - "Switching between all 6 view types (Board, Table, List, Calendar, Gallery, Timeline) on the same collection never creates duplicate records"
  artifacts:
    - path: "apps/workspaces/src/components/projectPage/BoardBlock.tsx"
      provides: "view.type branch covering all 6 view types, mounting the correct renderer with filtered/sorted records"
      contains: "'timeline'"
  key_links:
    - from: "apps/workspaces/src/components/projectPage/BoardBlock.tsx"
      to: "apps/workspaces/src/components/projectPage/collectionList/CollectionListView.tsx"
      via: "conditional render when view.type === 'list'"
      pattern: "<CollectionListView"
    - from: "apps/workspaces/src/components/projectPage/BoardBlock.tsx"
      to: "apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx"
      via: "conditional render when view.type === 'calendar'"
      pattern: "<CollectionCalendarView"
    - from: "apps/workspaces/src/components/projectPage/BoardBlock.tsx"
      to: "apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx"
      via: "conditional render when view.type === 'gallery'"
      pattern: "<CollectionGalleryView"
    - from: "apps/workspaces/src/components/projectPage/BoardBlock.tsx"
      to: "apps/workspaces/src/components/projectPage/collectionTimeline/CollectionTimelineView.tsx"
      via: "conditional render when view.type === 'timeline'"
      pattern: "<CollectionTimelineView"
---

<objective>
Wire the four new view renderers (List, Calendar, Gallery, Timeline — built in Plans 26-02/26-03/26-04) into `BoardBlock.tsx`'s existing `view.type` branch, alongside the already-shipped Board and Table paths, completing Phase 26's goal: the same `collection-view` block renders all 6 view types over the same `collection_records`, with zero new block kind and zero data duplication.

Purpose: This is the phase's single integration point — every renderer plan up to now has been isolated and unwired; this plan makes all four functionally reachable through the live app.
Output: `BoardBlock.tsx` extended with a full `view.type` branch chain and the additional `view.config`-derived props (`calendarDateProperty`, `galleryCoverProperty`, `timelineStartProperty`, `timelineEndProperty`) each new renderer needs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-additional-view-types/26-CONTEXT.md
@.planning/phases/26-additional-view-types/26-PATTERNS.md
@.planning/phases/26-additional-view-types/26-UI-SPEC.md
@.planning/phases/26-additional-view-types/26-01-SUMMARY.md
@.planning/phases/26-additional-view-types/26-02-SUMMARY.md
@.planning/phases/26-additional-view-types/26-03-SUMMARY.md
@.planning/phases/26-additional-view-types/26-04-SUMMARY.md
</context>

<interfaces>
Read the actual exported prop signatures from each of the following before wiring — they are the source of truth, not this summary (component internals may have settled on minor signature details during their own plan's execution):

- `apps/workspaces/src/components/projectPage/collectionList/CollectionListView.tsx` — expects `{ collection, records, collectionId, titlePropId, cardProperties }`
- `apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx` — expects `{ collection, records, collectionId, titlePropId, calendarDateProperty, cardProperties }`
- `apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx` — expects `{ collection, records, collectionId, titlePropId, cardProperties, galleryCoverProperty }`
- `apps/workspaces/src/components/projectPage/collectionTimeline/CollectionTimelineView.tsx` — expects `{ collection, records, collectionId, timelineStartProperty, timelineEndProperty, cardProperties }`

From `apps/workspaces/src/lib/hooks/useRecords.ts` (existing, unmodified): `export function useCreateRecord(collectionId: string): UseMutationResult<CollectionRecord, unknown, CreateRecordInput>;` — Calendar needs an instance passed in (its own plan already imports and calls this itself per Plan 26-03's Task 2 — confirm at read time whether `CollectionCalendarView` constructs its own `useCreateRecord(collectionId)` internally (in which case `BoardBlock.tsx` does not need to pass one in) or expects it as a prop; wire accordingly based on the actual file, not an assumption).

Current `BoardBlock.tsx` render branch (before this plan, lines 196-218):
```typescript
{view.type === 'table' ? (
  <CollectionTableView collection={collection} records={sorted} updateRecord={updateRecord} />
) : isFilteredEmpty ? (
  <BoardEmptyFilterState />
) : (
  <DndContext ...>{/* board columns */}</DndContext>
)}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Branch BoardBlock.tsx's view.type render on all 6 view types</name>
  <files>apps/workspaces/src/components/projectPage/BoardBlock.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/BoardBlock.tsx (full file, current state — lines 138-150 compute `cardProperties`/`aggregation`; lines 196-218 are the existing `view.type` ternary to extend)
    apps/workspaces/src/components/projectPage/collectionList/CollectionListView.tsx (this phase's Plan 26-02 output — exact prop signature)
    apps/workspaces/src/components/projectPage/collectionCalendar/CollectionCalendarView.tsx (this phase's Plan 26-03 output — exact prop signature, and whether it constructs its own `useCreateRecord` internally)
    apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx (this phase's Plan 26-02 output — exact prop signature)
    apps/workspaces/src/components/projectPage/collectionTimeline/CollectionTimelineView.tsx (this phase's Plan 26-04 output — exact prop signature)
  </read_first>
  <action>
    In `BoardBlock.tsx`, import `CollectionListView` from `./collectionList/CollectionListView`, `CollectionCalendarView` from `./collectionCalendar/CollectionCalendarView`, `CollectionGalleryView` from `./collectionGallery/CollectionGalleryView`, and `CollectionTimelineView` from `./collectionTimeline/CollectionTimelineView`.

    After the existing `cardProperties`/`aggregation` computation (around line 148-150), add four new derived `view.config` reads, matching the existing `String(view.config.groupBy ?? '')` convention:
    ```
    const calendarDateProperty = String(view.config.calendarDateProperty ?? '');
    const galleryCoverProperty = String(view.config.galleryCoverProperty ?? '');
    const timelineStartProperty = String(view.config.timelineStartProperty ?? '');
    const timelineEndProperty = String(view.config.timelineEndProperty ?? '');
    ```

    Replace the existing `{view.type === 'table' ? (...) : isFilteredEmpty ? (...) : (...)}` ternary with a chain (nested ternaries or an equivalent if/else-returning helper — match the file's existing style) covering, in this order:
    1. `view.type === 'table'` — unchanged `<CollectionTableView collection={collection} records={sorted} updateRecord={updateRecord} />`
    2. `view.type === 'list'` — `<CollectionListView collection={collection} records={sorted} collectionId={block.collectionId as string} titlePropId={titlePropId} cardProperties={cardProperties} />`
    3. `view.type === 'calendar'` — `<CollectionCalendarView collection={collection} records={sorted} collectionId={block.collectionId as string} titlePropId={titlePropId} calendarDateProperty={calendarDateProperty} cardProperties={cardProperties} />` (add any additional props the actual file requires, per `read_first`)
    4. `view.type === 'gallery'` — `<CollectionGalleryView collection={collection} records={sorted} collectionId={block.collectionId as string} titlePropId={titlePropId} cardProperties={cardProperties} galleryCoverProperty={galleryCoverProperty} />`
    5. `view.type === 'timeline'` — `<CollectionTimelineView collection={collection} records={sorted} collectionId={block.collectionId as string} timelineStartProperty={timelineStartProperty} timelineEndProperty={timelineEndProperty} cardProperties={cardProperties} />`
    6. else `isFilteredEmpty` — unchanged `<BoardEmptyFilterState />`
    7. else — unchanged `<DndContext>` board-columns JSX (the default/fallback path for `view.type === 'board'`)

    Do not construct a second `useUpdateAnyRecord`/`useRecords`/`useCollection`/`useView` instance for any new branch — every new renderer receives the same `sorted` array and (where relevant) the same `updateRecord` instance already computed once at the top of `BoardBlock`. Do not touch `ViewSwitcher`'s or `ViewSettingsMenu`'s existing mount points (`<ViewSwitcher .../>`, `<ViewSettingsMenu .../>`) — both already support all 6 types after Plan 26-01, with unchanged call signatures.
  </action>
  <acceptance_criteria>
    - `grep -c "import { CollectionListView }" apps/workspaces/src/components/projectPage/BoardBlock.tsx` returns >= 1, and likewise for `CollectionCalendarView`, `CollectionGalleryView`, `CollectionTimelineView`
    - `grep -c "view.type === 'list'" apps/workspaces/src/components/projectPage/BoardBlock.tsx` returns >= 1, and likewise for `'calendar'`, `'gallery'`, `'timeline'`
    - `grep -c "view.type === 'table'" apps/workspaces/src/components/projectPage/BoardBlock.tsx` returns >= 1 (unchanged existing branch still present)
    - `grep -c "<DndContext" apps/workspaces/src/components/projectPage/BoardBlock.tsx` returns >= 1 (fallback board-columns branch still present, unchanged)
    - Only one `useUpdateAnyRecord(` call exists in the file: `grep -c "useUpdateAnyRecord(" apps/workspaces/src/components/projectPage/BoardBlock.tsx` returns exactly 1
    - `grep -c "calendarDateProperty" apps/workspaces/src/components/projectPage/BoardBlock.tsx` returns >= 2 (derived-const + prop pass-through), likewise for `galleryCoverProperty`, `timelineStartProperty`, `timelineEndProperty`
    - `BoardBlock`'s export name and `{ block, onChange }` prop signature are unchanged
    - `cd apps/workspaces && npx tsc --noEmit` reports no new errors in `BoardBlock.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep "BoardBlock.tsx" || echo "no errors in BoardBlock.tsx"</automated>
  </verify>
  <done>BoardBlock.tsx renders the correct component for all 6 view.type values, passing the correct view.config-derived props to each of the four new renderers, with the existing Board/Table paths and hook instances unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> BoardBlock render branch | `view.type`/`view.config` values read here are already trusted, company-scoped data fetched via the existing `useView`/`useCollection` hooks — no new fetch, no new endpoint, purely a client-side render-branch extension |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-26-08 | Repudiation / Elevation of Privilege | Duplicate-record risk across all 6 view-type switches | mitigate | `ViewSwitcher`'s create-or-reuse logic (Plan 26-01, unchanged by this plan) never calls `createRecord`/`useCreateRecord` — this plan only adds render branches, it does not touch the switching mechanism, so the existing "never duplicates collection_records" guarantee from Phase 25 extends unchanged to all 4 new types |
| T-26-SC | Tampering | npm installs | accept | Zero new packages this plan |
</threat_model>

<verification>
- `cd apps/workspaces && npx tsc --noEmit` passes with no new errors in `BoardBlock.tsx`.
- Manual/live smoke test: on a single collection, use ViewSwitcher to cycle through all 6 view types (Board -> Table -> List -> Calendar -> Gallery -> Timeline -> back to Board), confirming each renders correctly and no duplicate `collection_records` are created (via `listRecords` count, unchanged before/after) and exactly 6 `collection_views` rows exist after the first full cycle (via `listViews`, not growing on repeated toggles).
- Confirm Calendar/Timeline's prev/next nav buttons both carry `aria-label="Previous month"`/`aria-label="Next month"` when inspected in the rendered DOM (UI-SPEC checker follow-up item).
</verification>

<success_criteria>
- VIEW-02, VIEW-03, VIEW-04, VIEW-05 all satisfied end-to-end: List, Calendar, Gallery, and Timeline are all reachable via the same `collection-view` block, reading the same `collection_records`, with no new block kind and no data duplication.
- All 6 view types (Board, Table, List, Calendar, Gallery, Timeline) are switchable on the same collection without creating duplicate records — the full VIEWX-04/D-10 create-once-then-remember guarantee now covers every view type this milestone ships.
</success_criteria>

<output>
Create `.planning/phases/26-additional-view-types/26-05-SUMMARY.md` when done
</output>
