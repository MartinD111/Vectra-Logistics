---
phase: 26-additional-view-types
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - apps/workspaces/src/components/projectPage/collectionList/CollectionListView.tsx
  - apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx
autonomous: true
requirements: [VIEW-02, VIEW-04]

must_haves:
  truths:
    - "User can view a collection as a flat list — single-column rows, not a grid — with title left and card-face properties as inline chips right (D-09)"
    - "User can view a collection as a gallery (card grid) with an optional cover image per card, chosen from a files-type schema property (D-04)"
    - "When a Gallery card has no cover image (property unset, no property chosen, or empty value), it shows a plain placeholder block, never a broken-image icon (D-05)"
  artifacts:
    - path: "apps/workspaces/src/components/projectPage/collectionList/CollectionListView.tsx"
      provides: "Flat single-column list view renderer, reusing formatCardPropertyValue for inline chips"
      contains: "formatCardPropertyValue"
    - path: "apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx"
      provides: "Card-grid gallery view renderer with optional per-record cover image"
      contains: "galleryCoverProperty"
  key_links:
    - from: "apps/workspaces/src/components/projectPage/collectionList/CollectionListView.tsx"
      to: "apps/workspaces/src/components/projectPage/board/BoardCard.tsx"
      via: "import { formatCardPropertyValue }"
      pattern: "formatCardPropertyValue"
    - from: "apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx"
      to: "apps/workspaces/src/components/projectPage/board/BoardCard.tsx"
      via: "import { formatCardPropertyValue }"
      pattern: "formatCardPropertyValue"
---

<objective>
Ship VIEW-02 (List) and VIEW-04 (Gallery) — two new sibling view-type renderer components for the `collection-view` block, each living in its own subfolder (`collectionList/`, `collectionGallery/`) following `CollectionTableView.tsx`'s established precedent, both reusing `formatCardPropertyValue` (exported in Plan 26-01) instead of reimplementing person/select/multi-select label resolution.

Purpose: List and Gallery are both read-only, card-face-preview-driven renderers with no inline editing or drag-and-drop — the simplest pair of the four new view types, grouped into one plan.
Output: `CollectionListView.tsx` (new), `CollectionGalleryView.tsx` (new). Neither is wired into `BoardBlock.tsx` yet — that happens in Plan 26-05 once all four renderers exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-additional-view-types/26-CONTEXT.md
@.planning/phases/26-additional-view-types/26-PATTERNS.md
@.planning/phases/26-additional-view-types/26-UI-SPEC.md
@.planning/phases/26-additional-view-types/26-01-SUMMARY.md
</context>

<interfaces>
From `apps/workspaces/src/components/projectPage/board/BoardCard.tsx` (Plan 26-01):
```typescript
export function formatCardPropertyValue(
  value: unknown,
  property: CollectionPropertyDef,
  personNames: Map<string, string>,
): string;
```

From `apps/workspaces/src/components/projectPage/collectionTable/CollectionTableView.tsx` (existing, unmodified — container/empty-state precedent):
```typescript
const CONTAINER_CLASS = 'border border-gray-200 dark:border-slate-700 rounded-lg overflow-hidden';
function CollectionTableEmptyState() { /* "No records match these filters" / "Try removing a filter condition..." */ }
```

From `apps/workspaces/src/lib/hooks/useTeam.ts` (existing, unmodified):
```typescript
export function useTeam(): UseQueryResult<{ id: string; first_name: string; last_name: string }[]>;
```

From `apps/workspaces/src/lib/api/records.api.ts` (existing, unmodified):
```typescript
export interface CollectionPropertyDef { id: string; name: string; type: 'text' | 'number' | 'date' | 'select' | 'multi-select' | 'checkbox' | 'person' | 'url' | 'email' | 'phone' | 'files' | 'relation'; options?: { id: string; label: string }[]; }
export interface DataCollection { id: string; schema: CollectionPropertyDef[]; /* ... */ }
export interface CollectionRecord { id: string; props: Record<string, unknown>; /* ... */ }
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create CollectionListView.tsx (D-09, VIEW-02)</name>
  <files>apps/workspaces/src/components/projectPage/collectionList/CollectionListView.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/collectionTable/CollectionTableView.tsx (full file — container/empty-state shell to copy verbatim; note this component's `<table>` cell-editing pattern is NOT reused here)
    apps/workspaces/src/components/projectPage/board/BoardCard.tsx (title-rendering convention lines 146-148, exported `formatCardPropertyValue`)
    apps/workspaces/src/lib/hooks/useTeam.ts (`useTeam` signature)
    .planning/phases/26-additional-view-types/26-CONTEXT.md (D-09 — List is structurally distinct from Table: single-column rows, not a grid)
    .planning/phases/26-additional-view-types/26-UI-SPEC.md (Interaction Contract row "List rows (D-09)" — exact row layout, chip styling, typography tiers)
  </read_first>
  <action>
    Create `apps/workspaces/src/components/projectPage/collectionList/CollectionListView.tsx`, a `'use client'` component exporting `CollectionListView({ collection, records, collectionId, titlePropId, cardProperties }: { collection: DataCollection; records: CollectionRecord[]; collectionId: string; titlePropId: string; cardProperties: CollectionPropertyDef[] })`. Import `formatCardPropertyValue` from `'../board/BoardCard'` and `useTeam` from `'@/lib/hooks/useTeam'`. Build `personNames` the same way `BoardCard.tsx` does: `new Map(team.map((m) => [m.id, \`${m.first_name} ${m.last_name}\`.trim()]))`.

    Reuse the exact container/empty-state shell from `CollectionTableView.tsx` (`CONTAINER_CLASS` border/rounded wrapper; empty-state copy `"No records match these filters"` / `"Try removing a filter condition or adjusting a value to see more records."`, verbatim) when `records.length === 0`.

    Otherwise render one row per record: `<div className="flex items-center justify-between gap-3 px-2.5 py-2 border-t border-gray-100 dark:border-slate-800" ...>` — the FIRST row (`index === 0`) must omit `border-t border-gray-100 dark:border-slate-800` (no top border on the first row, matching the container's own top border). Each row's `onClick` opens `window.open(\`/collections/${collectionId}/records/${record.id}\`, '_blank', 'noopener,noreferrer')` (verbatim pattern from `BoardCard.tsx` line 142) and the row has `cursor-pointer`.

    Left side: the record's title (`String(record.props[titlePropId] ?? '')`), rendered at Body tier size/weight (`text-sm font-semibold text-gray-800 dark:text-gray-200`, or `text-gray-400 italic` + `"Untitled"` when empty — matching `BoardCard`'s muted-empty-title convention; do NOT use the 16px Display tier, which is reserved for Gallery per the UI-SPEC Typography table).

    Right side: `(cardProperties ?? []).length > 0` renders `<div className="inline-flex items-center gap-1 flex-wrap justify-end">`, one chip per property: `<div className="rounded-full bg-gray-100 dark:bg-slate-800 px-2 py-0.5 text-xs text-gray-600 dark:text-gray-300">{formatCardPropertyValue(record.props[property.id], property, personNames)}</div>`.
  </action>
  <acceptance_criteria>
    - `grep -c "export function CollectionListView" apps/workspaces/src/components/projectPage/collectionList/CollectionListView.tsx` returns exactly 1, with props `{ collection, records, collectionId, titlePropId, cardProperties }`
    - `grep -c "import { formatCardPropertyValue }" apps/workspaces/src/components/projectPage/collectionList/CollectionListView.tsx` returns >= 1 (no duplicated label-resolution logic)
    - `grep -c "window.open(\`/collections/\${collectionId}/records/\${record.id}\`" apps/workspaces/src/components/projectPage/collectionList/CollectionListView.tsx` returns >= 1
    - `grep -c "No records match these filters" apps/workspaces/src/components/projectPage/collectionList/CollectionListView.tsx` returns >= 1 (empty state copy verbatim)
    - The component is a single-column stacked-row layout (no `<table>`, no CSS grid) — `grep -c "<table" apps/workspaces/src/components/projectPage/collectionList/CollectionListView.tsx` returns 0
    - `cd apps/workspaces && npx tsc --noEmit` reports no new errors in this file
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep "CollectionListView.tsx" || echo "no errors in CollectionListView.tsx"</automated>
  </verify>
  <done>CollectionListView.tsx exists, renders single-column rows (title left, card-property chips right), reuses formatCardPropertyValue verbatim, and compiles cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create CollectionGalleryView.tsx (D-04, D-05, VIEW-04)</name>
  <files>apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/board/BoardCard.tsx (lines 137-159 — card surface classes and non-editing card-body layout to reuse near-verbatim per UI-SPEC's "identical to today's BoardCard body layout" instruction; exported `formatCardPropertyValue`)
    apps/workspaces/src/components/projectPage/collectionTable/CollectionTableView.tsx (empty-state shell to reuse)
    apps/workspaces/src/components/records/PropertyField.tsx (lines 73-77 — confirms `files`-type property values are stored as `string[]` of freeform strings/URLs, not `{url, name}` objects)
    .planning/phases/26-additional-view-types/26-CONTEXT.md (D-04 — cover image is user-chosen per view via a files-type property; D-05 — missing cover renders a plain placeholder, no broken-image icon)
    .planning/phases/26-additional-view-types/26-UI-SPEC.md (Interaction Contract row "Gallery card grid (D-04, D-05)" — grid columns, cover block sizing, placeholder styling)
  </read_first>
  <action>
    Create `apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx`, a `'use client'` component exporting `CollectionGalleryView({ collection, records, collectionId, titlePropId, cardProperties, galleryCoverProperty }: { collection: DataCollection; records: CollectionRecord[]; collectionId: string; titlePropId: string; cardProperties: CollectionPropertyDef[]; galleryCoverProperty: string })`. Import `formatCardPropertyValue` from `'../board/BoardCard'` and `useTeam` from `'@/lib/hooks/useTeam'`, building `personNames` the same way `CollectionListView.tsx`/`BoardCard.tsx` do.

    When `records.length === 0`, render the same centered empty-state shell/copy as `CollectionTableView.tsx`'s `CollectionTableEmptyState` ("No records match these filters" / "Try removing a filter condition or adjusting a value to see more records.").

    Otherwise render a grid container `<div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4">`, one card per record: `<div className="rounded-md bg-white dark:bg-slate-900 border border-gray-200 dark:border-slate-700 shadow-sm overflow-hidden cursor-pointer" onClick={() => window.open(\`/collections/${collectionId}/records/${record.id}\`, '_blank', 'noopener,noreferrer')}>` (same surface classes as `BoardCard.tsx` line 144, minus `cursor-grab`/`touch-none`/drag attributes — Gallery cards are never draggable, no `useSortable`/`@dnd-kit` import needed).

    Cover block, `h-32` fixed height, above the card body: if `galleryCoverProperty` is truthy AND `Array.isArray(record.props[galleryCoverProperty])` AND that array's first entry is a truthy string, render `<img src={(record.props[galleryCoverProperty] as string[])[0]} alt="" className="w-full h-32 object-cover" />`. Otherwise (property unset, no property chosen for the view, or empty for this record — D-05) render `<div className="w-full h-32 bg-gray-100 dark:bg-slate-800" />` — a plain solid-fill placeholder block, no icon, no broken-image glyph.

    Below the cover, in a `p-2.5` padded body: the title at Display tier (`text-base font-semibold text-gray-800 dark:text-gray-200`, or `text-gray-400 italic` + `"Untitled"` when empty), then — when `(cardProperties ?? []).length > 0` — `<div className="mt-1.5 space-y-1">` with one `<div className="text-xs text-gray-500 dark:text-gray-400 truncate">{formatCardPropertyValue(record.props[property.id], property, personNames)}</div>` per property, matching `BoardCard.tsx`'s body layout exactly (lines 149-157).
  </action>
  <acceptance_criteria>
    - `grep -c "export function CollectionGalleryView" apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx` returns exactly 1, with props including `galleryCoverProperty: string`
    - `grep -c "grid-cols-2 sm:grid-cols-3 lg:grid-cols-4" apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx` returns >= 1
    - `grep -c "h-32" apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx` returns >= 2 (both the `<img>` and placeholder branch)
    - No broken-image icon import: `grep -cE "ImageOff|BrokenImage" apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx` returns 0
    - No `useSortable`/`@dnd-kit` import: `grep -c "dnd-kit" apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx` returns 0
    - `grep -c "import { formatCardPropertyValue }" apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx` returns >= 1
    - `grep -c "window.open(\`/collections/\${collectionId}/records/\${record.id}\`" apps/workspaces/src/components/projectPage/collectionGallery/CollectionGalleryView.tsx` returns >= 1
    - `cd apps/workspaces && npx tsc --noEmit` reports no new errors in this file
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep "CollectionGalleryView.tsx" || echo "no errors in CollectionGalleryView.tsx"</automated>
  </verify>
  <done>CollectionGalleryView.tsx exists, renders a responsive card grid with optional per-record cover image and a plain placeholder when absent, reuses formatCardPropertyValue/BoardCard's card-body layout, and compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> `<img src>` (Gallery cover) | `galleryCoverProperty` values are user-entered freeform strings (`files`-type property, stored as `string[]`) rendered directly as an `<img src>` — same trust level as `PropertyField.tsx`'s existing `StringArrayChips` freeform files/relation input, no new sanitization surface (browsers do not execute arbitrary `<img src>` values as script) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-26-03 | Information Disclosure | Gallery cover `<img src>` | accept | Same existing company-scoped `collection_records.props` read path already used by every other view (Board/Table/List) — no new endpoint, no new data exposed beyond what the record's own schema already contains |
| T-26-04 | Tampering | List/Gallery click-through navigation | accept | `window.open` target is always `/collections/${collectionId}/records/${record.id}` built from the same trusted `collectionId`/`record.id` values already used identically by `BoardCard.tsx` — no new URL-construction surface |
| T-26-SC | Tampering | npm installs | accept | Zero new packages this plan |
</threat_model>

<verification>
- `cd apps/workspaces && npx tsc --noEmit` passes with no new errors in the two new files.
- Manual/live smoke test (once wired in Plan 26-05): switch a board's view to List, confirm single-column rows with title left / chips right; switch to Gallery, confirm a responsive card grid with placeholder covers (no `galleryCoverProperty` chosen yet), then choose a files-type cover property via ViewSettingsMenu and confirm cover images render for records with a value.
</verification>

<success_criteria>
- VIEW-02 satisfied: user can view a collection as a flat list, structurally distinct from Table.
- VIEW-04 satisfied: user can view a collection as a gallery card grid with optional cover images, placeholder-safe when absent.
</success_criteria>

<output>
Create `.planning/phases/26-additional-view-types/26-02-SUMMARY.md` when done
</output>
