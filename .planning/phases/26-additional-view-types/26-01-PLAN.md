---
phase: 26-additional-view-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workspaces/src/components/projectPage/board/BoardCard.tsx
  - apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx
  - apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx
autonomous: true
requirements: [VIEW-02, VIEW-03, VIEW-04, VIEW-05]

must_haves:
  truths:
    - "ViewSwitcher shows a dropdown (not a segmented control) listing all 6 view types (Board, Table, List, Calendar, Gallery, Timeline); selecting one uses the existing create-once-then-remember mechanism unchanged (D-10)"
    - "ViewSettingsMenu offers a Calendar date-property picker, a Gallery cover-image-property picker, and Timeline start/end date-property pickers, each shown only for the matching view.type and autosaving to view.config (D-01, D-04, D-06)"
    - "formatCardPropertyValue's person/select/multi-select label-resolution logic is reusable by other view renderers without duplication"
  artifacts:
    - path: "apps/workspaces/src/components/projectPage/board/BoardCard.tsx"
      provides: "Exported formatCardPropertyValue, reused verbatim by List/Calendar/Gallery view renderers in later plans"
      contains: "export function formatCardPropertyValue"
    - path: "apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx"
      provides: "6-type dropdown view switcher"
      contains: "'timeline'"
    - path: "apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx"
      provides: "Property pickers for calendar/gallery/timeline view.config keys"
      contains: "calendarDateProperty"
  key_links:
    - from: "apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx"
      to: "apps/workspaces/src/lib/hooks/useRecords.ts"
      via: "useViews/useCreateView create-once-then-remember + pendingType guard, unchanged"
      pattern: "pendingType"
    - from: "apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx"
      to: "apps/workspaces/src/lib/hooks/useRecords.ts"
      via: "useUpdateView(view.id).mutate({ config: { ...view.config, <key>: value } })"
      pattern: "updateView\\.mutate"
---

<objective>
Ship the shared foundation Phase 26's four new view renderers (List, Calendar, Gallery, Timeline) depend on: (1) export `BoardCard.tsx`'s private `formatCardPropertyValue` so it can be reused verbatim instead of being duplicated four times, (2) rewrite `ViewSwitcher.tsx` from a 2-button segmented control into a 6-type dropdown menu per D-10, and (3) extend `ViewSettingsMenu.tsx` with the per-view-type property pickers (Calendar's date property D-01, Gallery's cover-image property D-04, Timeline's start/end date properties D-06) that later plans' renderers consume via `view.config`.

Purpose: Interface-first ordering — this plan defines the contracts (widened `type` union, exported function, new `view.config` keys) that Wave 2's four parallel renderer plans consume without needing to re-derive them or modify shared files themselves.
Output: `BoardCard.tsx` (export change only, no behavior change), `ViewSwitcher.tsx` (rewritten dropdown, 6 types), `ViewSettingsMenu.tsx` (extended with 3 new conditional sections).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-additional-view-types/26-CONTEXT.md
@.planning/phases/26-additional-view-types/26-PATTERNS.md
@.planning/phases/26-additional-view-types/26-UI-SPEC.md
</context>

<interfaces>
<!-- Contracts this plan produces for Wave 2 plans (26-02, 26-03, 26-04) to consume directly. -->

From `apps/workspaces/src/components/projectPage/board/BoardCard.tsx` (after this plan):
```typescript
export function formatCardPropertyValue(
  value: unknown,
  property: CollectionPropertyDef,
  personNames: Map<string, string>,
): string;
```
Import path for downstream plans: `import { formatCardPropertyValue } from '../board/BoardCard';`

From `apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx` (after this plan) — new `view.config` keys downstream renderer plans read at render time via their own `view` prop (BoardBlock passes `view` through, wired in Plan 26-05):
- `view.config.calendarDateProperty: string` (schema property id of a `date`-type property, or `''`/`undefined` if unset)
- `view.config.galleryCoverProperty: string` (schema property id of a `files`-type property, or `''`/`undefined` if unset)
- `view.config.timelineStartProperty: string` / `view.config.timelineEndProperty: string` (schema property ids of `date`-type properties, independently settable)

From `apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx` (after this plan):
```typescript
export function ViewSwitcher({
  collectionId, currentView, onSwitch,
}: {
  collectionId: string;
  currentView: CollectionView;
  onSwitch: (viewId: string) => void;
}): JSX.Element;
```
Signature unchanged from today — only the internal `type` union and rendered markup change. `BoardBlock.tsx` (Plan 26-05) does not need to change how it calls `ViewSwitcher`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Export formatCardPropertyValue from BoardCard.tsx</name>
  <files>apps/workspaces/src/components/projectPage/board/BoardCard.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/board/BoardCard.tsx (full file, current state — lines 24-56 define the private `formatCardPropertyValue`)
  </read_first>
  <action>
    In `BoardCard.tsx`, change the function declaration at line 24 from `function formatCardPropertyValue(` to `export function formatCardPropertyValue(`. Make no other changes to the function body, its signature, or any other part of the file — this is a pure visibility change so Plans 26-02/26-03/26-04 can `import { formatCardPropertyValue } from '../board/BoardCard'` instead of duplicating the person/select/multi-select label-resolution logic four times.
  </action>
  <acceptance_criteria>
    - `grep -c "export function formatCardPropertyValue" apps/workspaces/src/components/projectPage/board/BoardCard.tsx` returns exactly 1
    - `grep -c "export function BoardCard" apps/workspaces/src/components/projectPage/board/BoardCard.tsx` returns exactly 1 (unchanged)
    - The function body (switch on `property.type`, cases `checkbox`/`person`/`select`/`multi-select`/`files`/`relation`/`default`) is byte-identical to before except the `export` keyword
    - `cd apps/workspaces && npx tsc --noEmit` reports no new errors in `BoardCard.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep "BoardCard.tsx" || echo "no errors in BoardCard.tsx"</automated>
  </verify>
  <done>formatCardPropertyValue is exported from BoardCard.tsx with zero behavior change, ready for reuse by later plans.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite ViewSwitcher.tsx as a 6-type dropdown menu (D-10)</name>
  <files>apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx (full file, current 2-button segmented control)
    apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx (lines 71-84 — the hand-rolled popover shell: `fixed inset-0 z-20` backdrop + `absolute z-30` panel — to replicate here, left-aligned instead of right-aligned)
    .planning/phases/26-additional-view-types/26-UI-SPEC.md (Interaction Contract row "ViewSwitcher dropdown (D-10)" — exact trigger/menu/row styling)
    .planning/phases/26-additional-view-types/26-CONTEXT.md (D-10 — replaces, not supplements, the segmented control; create-once-then-remember mechanism carries over unchanged)
  </read_first>
  <action>
    Widen the `VIEW_TYPES` array from 2 to 6 entries: `{ type: 'board', label: 'Board', icon: LayoutGrid }`, `{ type: 'table', label: 'Table', icon: Table2 }`, `{ type: 'list', label: 'List', icon: List }`, `{ type: 'calendar', label: 'Calendar', icon: Calendar }`, `{ type: 'gallery', label: 'Gallery', icon: Image }`, `{ type: 'timeline', label: 'Timeline', icon: GanttChartSquare }`. Import `List`, `Calendar`, `Image`, `GanttChartSquare`, `ChevronDown` from `lucide-react` alongside the existing `LayoutGrid`, `Table2` — if `GanttChartSquare` does not exist in this project's installed `lucide-react` 0.294.0 (check by attempting the import and running `tsc`), use `BarChart3` instead (per 26-UI-SPEC.md's documented fallback) and note the substitution in a one-line code comment.

    Widen the `type` union everywhere it currently reads `'board' | 'table'` (the `VIEW_TYPES` array's type annotation, `pendingType` state, and the `switchTo` parameter type) to `'board' | 'table' | 'list' | 'calendar' | 'gallery' | 'timeline'`.

    Preserve the `switchTo` function's create-once-then-remember logic verbatim (the `if (type === currentView.type) return`, the `pendingType`/`createView.isPending` in-flight guard, the `viewsQuery.data?.find((v) => v.type === type)` existing-sibling lookup, and the `createView.mutateAsync(...).then(...).catch(...).finally(...)` chain) — only change the inline `name:` value from the current ternary (`type === 'board' ? 'Board' : 'Table'`) to a lookup: `VIEW_TYPES.find((v) => v.type === type)?.label ?? type`.

    Add local `const [editing, setEditing] = useState(false)` for the dropdown's open/close state. Replace the segmented-button markup entirely with: a trigger `<button>` showing the current view's icon + label + a `ChevronDown` icon (`min-h-[32px]`, toggles `editing` on click); when `editing` is true, render `<div className="fixed inset-0 z-20" onClick={() => setEditing(false)} />` followed by `<div className="absolute left-0 top-full mt-1 z-30 ...">` (left-aligned, since `ViewSwitcher` is first in the toolbar row per UI-SPEC) containing 6 stacked menu rows, each `min-h-[32px] px-2 py-1.5` with the row's icon + label, `hover:bg-gray-100 dark:hover:bg-slate-800`, and — for the row matching `currentView.type` — `text-primary-600` plus `bg-primary-50 dark:bg-primary-900/20`. Clicking a row calls `switchTo(type)` then `setEditing(false)`.
  </action>
  <acceptance_criteria>
    - `grep -c "'timeline'" apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx` returns >= 1
    - `grep -c "ChevronDown" apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx` returns >= 1
    - `grep -c "pendingType" apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx` returns >= 1 (in-flight guard preserved)
    - `grep -c "fixed inset-0 z-20" apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx` returns >= 1
    - `grep -c "export function ViewSwitcher" apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx` returns exactly 1, with the same `{ collectionId, currentView, onSwitch }` prop signature as before
    - The 6-entry `VIEW_TYPES` array includes all of `'board'`, `'table'`, `'list'`, `'calendar'`, `'gallery'`, `'timeline'`
    - `cd apps/workspaces && npx tsc --noEmit` reports no new errors in `ViewSwitcher.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep "ViewSwitcher.tsx" || echo "no errors in ViewSwitcher.tsx"</automated>
  </verify>
  <done>ViewSwitcher.tsx renders a 6-type dropdown menu, preserves the create-once-then-remember + pendingType guard mechanism unchanged, and compiles cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend ViewSettingsMenu.tsx with Calendar/Gallery/Timeline property pickers</name>
  <files>apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx (full file, current state — lines 33-44's local-state buffering pattern and lines 102-124's aggregation-picker pattern are the direct precedent)
    .planning/phases/26-additional-view-types/26-CONTEXT.md (D-01 calendar date property, D-04 gallery cover property, D-06 timeline start/end properties — each persists to a distinct additive `view.config` key, no schema/migration change)
    .planning/phases/26-additional-view-types/26-UI-SPEC.md (Interaction Contract rows "Calendar date-property picker (D-01)", "Gallery cover-property picker (D-04)", "Timeline start/end-property pickers (D-06)")
  </read_first>
  <action>
    Add three new local-state pairs alongside the existing `localCardProperties`/`localAggregation`, each synced in the existing `useEffect(() => {...}, [view.id])`: `localCalendarDateProperty` (from `view.config.calendarDateProperty as string | undefined`), `localGalleryCoverProperty` (from `view.config.galleryCoverProperty`), `localTimelineStartProperty` and `localTimelineEndProperty` (from `view.config.timelineStartProperty`/`view.config.timelineEndProperty`).

    Add three new setter functions following the exact shape of `setAggregationProp` (local-state update + `updateView.mutate({ config: { ...view.config, <key>: value } })`, one `mutate` call per field, never merged): `setCalendarDateProperty(propId: string)` writing `calendarDateProperty`, `setGalleryCoverProperty(propId: string)` writing `galleryCoverProperty`, `setTimelineStartProperty(propId: string)` writing `timelineStartProperty`, and `setTimelineEndProperty(propId: string)` writing `timelineEndProperty` — the last two are two independent `updateView.mutate` calls, matching the existing `setAggregationType`/`setAggregationProp` split (D-06's per-field autosave note).

    Below the existing "Card properties" section (and before or after "Column aggregation" — Claude's discretion on ordering), add three new conditional sections using the same `<div><p className="text-[10px] font-bold uppercase tracking-wider text-gray-400 mb-1.5">...</p><select className="saas-input !py-1.5 text-xs w-full">...</select></div>` shape as the existing aggregation picker:
    - Gated on `view.type === 'calendar'`: one `<select>` labeled "Calendar date property", options from `collection.schema.filter((p) => p.type === 'date')`, value `localCalendarDateProperty ?? ''`, `onChange` calls `setCalendarDateProperty`.
    - Gated on `view.type === 'gallery'`: one `<select>` labeled "Cover image property", options from `collection.schema.filter((p) => p.type === 'files')`, value `localGalleryCoverProperty ?? ''`, `onChange` calls `setGalleryCoverProperty`.
    - Gated on `view.type === 'timeline'`: two `<select>` elements labeled "Start date property" and "End date property" respectively, both sourced from `collection.schema.filter((p) => p.type === 'date')`, values `localTimelineStartProperty ?? ''` / `localTimelineEndProperty ?? ''`, `onChange` calling `setTimelineStartProperty` / `setTimelineEndProperty` independently.

    Each `<select>` includes a placeholder `<option value="">Select a date property…</option>` (or "Select a files property…" for Gallery), matching the existing aggregation picker's placeholder-option convention.
  </action>
  <acceptance_criteria>
    - `grep -c "calendarDateProperty" apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx` returns >= 2 (read + write)
    - `grep -c "galleryCoverProperty" apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx` returns >= 2
    - `grep -c "timelineStartProperty" apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx` returns >= 2 and `grep -c "timelineEndProperty" apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx` returns >= 2 (independent keys, not a combined object)
    - `grep -c "view.type === 'calendar'" apps/workspaces/src/components/projectPage/board/ViewSettingsMenu.tsx` returns >= 1, same for `'gallery'` and `'timeline'` (each new section only renders for its matching view type)
    - Each new setter calls `updateView.mutate({ config: { ...view.config, ` — no separate "Save" button introduced
    - `cd apps/workspaces && npx tsc --noEmit` reports no new errors in `ViewSettingsMenu.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep "ViewSettingsMenu.tsx" || echo "no errors in ViewSettingsMenu.tsx"</automated>
  </verify>
  <done>ViewSettingsMenu.tsx offers Calendar/Gallery/Timeline property pickers, each conditional on view.type, each autosaving independently to its own view.config key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> `PATCH /api/v1/records/views/:id` | New `view.config` keys (`calendarDateProperty`, `galleryCoverProperty`, `timelineStartProperty`, `timelineEndProperty`) flow through the existing authenticated, company-scoped `useUpdateView` mutation — same endpoint already used by `cardProperties`/`columnAggregation`, no new backend surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-26-01 | Tampering | `ViewSettingsMenu` new property-picker `<select>` values | accept | Values are always one of `collection.schema`'s own property ids (rendered from `<option>` elements sourced from the schema array itself, never free-text input) — same trust level as the existing `cardProperties`/`aggregation` pickers already in production |
| T-26-02 | Tampering | `useUpdateView` config write (new keys) | accept | Same existing `PATCH /views/:id` endpoint, already gated by `authenticateToken` + `company_id` scoping in `records.repository.ts` — additive JSONB keys introduce no new validation gap |
| T-26-SC | Tampering | npm/lucide-react icon imports | accept | `GanttChartSquare`/`BarChart3` are existing exports of the already-installed `lucide-react` 0.294.0 dependency — zero new packages this plan |
</threat_model>

<verification>
- `cd apps/workspaces && npx tsc --noEmit` passes with no new errors in the three touched files.
- Manual/live smoke test: open a board, click the ViewSwitcher trigger, confirm all 6 types are listed with the current type highlighted; switch to a type with no existing sibling view and confirm exactly one new `collection_views` row is created (via `listViews`), then switch away and back and confirm no duplicate row is created. Open ViewSettingsMenu on a Calendar/Gallery/Timeline view and confirm the matching property picker(s) appear and persist across a page refresh.
</verification>

<success_criteria>
- ViewSwitcher offers all 6 view types via a dropdown (D-10), preserving create-once-then-remember behavior.
- ViewSettingsMenu offers Calendar/Gallery/Timeline property pickers (D-01, D-04, D-06), each autosaving to a distinct `view.config` key.
- `formatCardPropertyValue` is exported and ready for reuse without duplication.
</success_criteria>

<output>
Create `.planning/phases/26-additional-view-types/26-01-SUMMARY.md` when done
</output>
