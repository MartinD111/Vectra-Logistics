---
phase: 32-aggregated-tree-read-api-reorder-move-endpoints
plan: 03
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - apps/api/src/domains/folders/folders.service.ts
  - apps/api/src/domains/folders/folders.controller.ts
  - apps/api/src/domains/folders/folders.routes.ts
  - apps/api/src/domains/folders/folders.service.test.ts
  - apps/api/src/domains/folders/folders.integration.test.ts
autonomous: true
requirements: [TREEAPI-01]

must_haves:
  truths:
    - "A single GET request returns the entire tenant's folder/project/program/collection/page tree"
    - "The tree read never leaks another tenant's rows"
  artifacts:
    - path: "apps/api/src/domains/folders/folders.controller.ts"
      provides: "getFullTree handler"
      contains: "getFullTree"
    - path: "apps/api/src/domains/folders/folders.routes.ts"
      provides: "GET /tree/full route registered before /:id"
      contains: "tree/full"
  key_links:
    - from: "apps/api/src/domains/folders/folders.routes.ts"
      to: "apps/api/src/domains/folders/folders.controller.ts"
      via: "getFullTree import"
      pattern: "getFullTree"
    - from: "apps/api/src/domains/folders/folders.service.ts"
      to: "apps/api/src/domains/projects/projects.repository.ts"
      via: "Promise.all of listProjects/listPrograms/listAllPages"
      pattern: "Promise\\.all"
---

<objective>
Build `GET /folders/tree/full` — a single tenant-scoped endpoint that returns the entire workspace tree (folders + projects + programs + collections + pages) via exactly 5 parallel flat queries and in-memory nesting, with no per-node fan-out.

Purpose: TREEAPI-01's core deliverable. Later UI phases (33/34) get one endpoint to call instead of aggregating 5 separate list endpoints client-side.
Output: `foldersService.getFullTree`, `GET /tree/full` wired ahead of the existing `/:id` catch-all route, unit + live-DB integration coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-RESEARCH.md
@.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-PATTERNS.md
@.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-01-PLAN.md

<interfaces>
<!-- Existing folders.service.ts / controller / routes this plan extends. Executor should use these directly. -->

From apps/api/src/domains/folders/folders.service.ts (listFolderTree + buildTree — the aggregate-read shape and in-memory-nesting technique to extend):
```typescript
async listFolderTree(ctx: RequestContext): Promise<FolderTree[]> {
  const tenantId = requireCompanyId(ctx);
  const flat = await foldersRepository.listFolders(tenantId);
  return this.buildTree(flat, null);
}

private buildTree(flat: Folder[], parentId: string | null): FolderTree[] {
  return flat
    .filter((f) => f.parent_id === parentId)
    .map((f) => ({ ...f, children: this.buildTree(flat, f.id) }));
}
```

From apps/api/src/domains/projects/projects.repository.ts: `listProjects(companyId)`, `listPrograms(companyId, projectId?)` (no `projectId` arg → ALL programs for tenant), `listAllPages(companyId)` — all already company-scoped, single-query, no fan-out.

From apps/api/src/domains/records/records.repository.ts: `listCollections(companyId)` — company-scoped, single-query.

From apps/api/src/domains/folders/folders.controller.ts (existing GET-handler shape to mirror):
```typescript
export const listFolders = asyncHandler(async (req: RequestWithContext, res: Response) => {
  res.json({ folders: await foldersService.listFolderTree(requireRequestContext(req)) });
});
```

From apps/api/src/domains/folders/folders.routes.ts (full file, current state — route registration order matters: `/:id` is a catch-all that will swallow `/tree/full` if registered first):
```typescript
router.get('/', listFolders);
router.post('/', requireCapability('workspace.admin'), createFolder);
router.get('/:id', getFolder);
router.patch('/:id', requireCapability('workspace.admin'), updateFolder);
router.patch('/:id/move', requireCapability('workspace.admin'), moveFolder);
router.post('/:id/archive', requireCapability('workspace.admin'), archiveFolder);
router.post('/:id/unarchive', requireCapability('workspace.admin'), unarchiveFolder);
```

From apps/api/src/domains/folders/folders.integration.test.ts: `adminCtx(companyId)` factory + `before`/`after` fixture scaffolding provisioning throwaway `company_id`s (`company1Id`, `company2Id`, `company3Id`) — reuse this scaffolding rather than building new fixtures.

From apps/api/src/domains/folders/folders.types.ts (TreeNode type, built in 32-01): discriminated union with `node_type`, `id`, `company_id`, `name`, `children: TreeNode[]`, `raw`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getFullTree + assembleTree (folders.service.ts)</name>
  <files>apps/api/src/domains/folders/folders.service.ts, apps/api/src/domains/folders/folders.service.test.ts</files>
  <read_first>
    apps/api/src/domains/folders/folders.service.ts (existing listFolderTree + buildTree to extend)
    apps/api/src/domains/folders/folders.types.ts (TreeNode type from 32-01)
    apps/api/src/domains/projects/projects.repository.ts (listProjects/listPrograms/listAllPages signatures)
    apps/api/src/domains/records/records.repository.ts (listCollections signature)
    apps/api/src/domains/folders/folders.service.test.ts (existing mock.method(foldersRepository, ...) conventions and the `ctx()`/`folder()` fixture builders already defined in this file)
  </read_first>
  <behavior>
    - `getFullTree(ctx)`: calls `requireCompanyId(ctx)`, then `Promise.all` of exactly 5 repository calls — `foldersRepository.listFolders(tenantId)`, `projectsRepository.listProjects(tenantId)`, `projectsRepository.listPrograms(tenantId)` (no second arg), `projectsRepository.listAllPages(tenantId)`, `recordsRepository.listCollections(tenantId)` — then returns `this.assembleTree(folders, projects, programs, pages, collections)`. No `assertCapability` call (matches `listFolderTree`'s capability-free read).
    - `assembleTree(folders, projects, programs, pages, collections)`: returns `TreeNode[]` rooted at folders with `parent_id === null` PLUS root-level projects/programs/collections with no `folder_id`. Each folder node's `children` includes nested child folders (recursive, `buildTree`-style) PLUS projects whose `folder_id` equals that folder's id PLUS programs whose `folder_id` equals that folder's id and `project_id` is null (folder-filed programs) PLUS collections whose `folder_id` equals that folder's id and `project_id` is null. Each project node's `children` includes programs whose `project_id` equals that project's id, root pages whose `project_id` equals that project's id and `parent_page_id` is null (nested pages nest recursively under their `parent_page_id`, same technique), and collections whose `project_id` equals that project's id.
    - Implementation builds a `Map` keyed by resolved parent id (or a `'root'` sentinel) in a single pass over each of the 5 input arrays, then nests recursively — do NOT write a new per-node `.filter()` loop across the full array set (that reintroduces the per-node fan-out anti-pattern in application memory even though it avoids extra DB queries).
    - Cases: empty tenant (all 5 lists empty) → returns `[]`; a folder → project → program 3-level fixture produces `TreeNode[]` with `node_type` discriminants `'folder'`, `'project'`, `'program'` at each level; a root-level project (`folder_id: null`) appears at the top level, not nested under any folder; a collection filed under a folder and a collection filed under a project both land in the correct parent's `children`; calling `getFullTree` issues exactly 5 repository calls total (assert via mock call-count on each of the 5 methods).
  </behavior>
  <action>
    Add `getFullTree` and a private `assembleTree` helper to `FoldersService`, placed after `listFolderTree`. Import `Project, Program, ProjectPage` from '../projects/projects.types' and `DataCollectionRow` from '../records/records.types' (extend the existing import block). Extend folders.service.test.ts with the cases from `<behavior>`, using `mock.method(foldersRepository, 'listFolders', ...)`, `mock.method(projectsRepository, 'listProjects', ...)`, `mock.method(projectsRepository, 'listPrograms', ...)`, `mock.method(projectsRepository, 'listAllPages', ...)`, `mock.method(recordsRepository, 'listCollections', ...)`, asserting each mock's `.mock.calls.length === 1`.
  </action>
  <acceptance_criteria>
    - `foldersService.getFullTree(ctx)` calls each of the 5 repository methods exactly once
    - A folder→project→program 3-level fixture produces `tree[0].node_type === 'folder'`, `tree[0].children[0].node_type === 'project'`, `tree[0].children[0].children[0].node_type === 'program'`
    - A root-level project (`folder_id: null`) appears in the top-level array, not nested under any folder node
    - `getFullTree` does not call `assertCapability` (test with `ctx({ roles: [] })` and assert no throw)
    - `node --require ts-node/register --test src/domains/folders/folders.service.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && node --require ts-node/register --test src/domains/folders/folders.service.test.ts</automated>
  </verify>
  <done>getFullTree assembles the full cross-entity tree from exactly 5 flat queries with correct nesting and no capability gate.</done>
</task>

<task type="auto">
  <name>Task 2: Wire GET /folders/tree/full + live tenant-isolation integration test</name>
  <files>apps/api/src/domains/folders/folders.controller.ts, apps/api/src/domains/folders/folders.routes.ts, apps/api/src/domains/folders/folders.integration.test.ts</files>
  <read_first>
    apps/api/src/domains/folders/folders.controller.ts (existing listFolders handler shape)
    apps/api/src/domains/folders/folders.routes.ts (existing route table — route-ordering hazard: `/:id` would greedily match `/tree/*`)
    apps/api/src/domains/folders/folders.integration.test.ts (adminCtx factory + before/after fixture scaffolding to extend)
  </read_first>
  <action>
    Add `export const getFullTree = asyncHandler(async (req: RequestWithContext, res: Response) => { res.json({ tree: await foldersService.getFullTree(requireRequestContext(req)) }); });` to folders.controller.ts, placed after `listFolders`. In folders.routes.ts, add `getFullTree` to the controller import list and register `router.get('/tree/full', getFullTree);` immediately after `router.get('/', listFolders);` and strictly before `router.get('/:id', getFolder);` — Express matches routes in registration order, so `/tree/full` must come before the `/:id` catch-all or `:id` will capture the literal string `"tree"`. Add a new integration test to folders.integration.test.ts asserting `foldersService.getFullTree(adminCtx(company1Id))` returns a tree containing `folderA` (the existing `company1Id` fixture) with `node_type === 'folder'`, and that no node in the result has an `id` matching any `company2Id`/`company3Id` fixture object (cross-tenant isolation proof, live DB).
  </action>
  <acceptance_criteria>
    - folders.routes.ts registers `GET /tree/full` at a line number lower than `GET /:id`'s registration line
    - `GET /tree/full` inherits `router.use(authenticateToken)` but has no `requireCapability` call (capability-free read, matching `GET /`)
    - New integration test proves cross-tenant isolation: `company1Id`'s tree result contains none of `company2Id`'s or `company3Id`'s fixture ids
    - `node --require ts-node/register --test src/domains/folders/folders.integration.test.ts` exits 0 (requires reachable Postgres)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && node --require ts-node/register --test src/domains/folders/folders.integration.test.ts</automated>
  </verify>
  <done>GET /folders/tree/full is live, correctly ordered ahead of /:id, capability-free, and proven tenant-scoped by a live-DB integration test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GET /folders/tree/full | Authenticated but capability-free read; the entire response must be scoped to the caller's `company_id` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03-01 | Information Disclosure | `folders.routes.ts` route table | mitigate | `/:id` (a greedy catch-all) must be registered AFTER `/tree/full` or Express will treat "tree" as a folder id, returning a 404 instead of the tree and, in a hypothetically different route ordering, could misroute a request — enforced via explicit registration-order acceptance criterion |
| T-32-03-02 | Information Disclosure | `foldersService.getFullTree` | mitigate | Every one of the 5 underlying repository calls (`listFolders`, `listProjects`, `listPrograms`, `listAllPages`, `listCollections`) is already `company_id`-scoped — reused verbatim, not reimplemented; proven by the live cross-tenant integration test in Task 2 |
| T-32-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this plan |

</threat_model>

<verification>
Run `cd apps/api && node --require ts-node/register --test src/domains/folders/folders.service.test.ts src/domains/folders/folders.integration.test.ts`. `getFullTree` must issue exactly 5 queries, nest correctly, stay capability-free, and never leak cross-tenant rows.
</verification>

<success_criteria>
- GET /folders/tree/full returns the full tenant tree in one request
- Query count is exactly 5, independent of tree size/depth
- Cross-tenant isolation proven by a live-DB integration test
</success_criteria>

<output>
Create `.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-03-SUMMARY.md` when done
</output>
