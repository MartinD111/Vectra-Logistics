---
phase: 32-aggregated-tree-read-api-reorder-move-endpoints
plan: 04
type: execute
wave: 3
depends_on: ["32-02", "32-03"]
files_modified:
  - apps/api/src/domains/folders/folders.service.ts
  - apps/api/src/domains/folders/folders.controller.ts
  - apps/api/src/domains/folders/folders.routes.ts
  - apps/api/src/domains/folders/folders.service.test.ts
  - apps/api/src/domains/folders/folders.reorder-concurrency.integration.test.ts
autonomous: true
requirements: [TREEAPI-02, TREEAPI-04]

must_haves:
  truths:
    - "Reordering siblings requires workspace.admin"
    - "Two concurrent reorder requests against the same sibling set never produce a lost update"
    - "A stale reorder request (sibling set changed since read) is rejected with 409, not silently applied"
  artifacts:
    - path: "apps/api/src/domains/folders/folders.service.ts"
      provides: "reorderSiblings — capability-gated, transactional, dispatch-by-node_type"
      contains: "assertCapability(ctx, 'workspace.admin')"
    - path: "apps/api/src/domains/folders/folders.routes.ts"
      provides: "POST /tree/reorder route"
      contains: "tree/reorder"
    - path: "apps/api/src/domains/folders/folders.reorder-concurrency.integration.test.ts"
      provides: "live two-transaction proof of no lost update"
  key_links:
    - from: "apps/api/src/domains/folders/folders.routes.ts"
      to: "apps/api/src/core/capabilities/index.ts"
      via: "requireCapability('workspace.admin') gate on POST /tree/reorder"
      pattern: "requireCapability\\('workspace\\.admin'\\), reorderNodes"
    - from: "apps/api/src/domains/folders/folders.service.ts"
      to: "apps/api/src/domains/projects/projects.repository.ts"
      via: "dispatch-by-node_type calling reorderProjects/reorderPrograms"
      pattern: "reorderProjects|reorderPrograms"
---

<objective>
Wire the lock-safe reorder primitives built in 32-02 into a capability-gated, transactional `POST /folders/tree/reorder` endpoint, and close VALIDATION.md's Wave 0 gap by proving lock-safety with a real two-`PoolClient` concurrent-transaction test against the running harness built in 32-01.

Purpose: TREEAPI-02's full endpoint delivery — a reorder that is server-authoritative, lock-safe, and workspace.admin-gated, with the "no lost updates under concurrent reorder" claim proven against a live database rather than by code inspection alone.
Output: `foldersService.reorderSiblings`, `POST /tree/reorder`, and a passing live concurrency test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-RESEARCH.md
@.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-PATTERNS.md
@.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-01-PLAN.md
@.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-02-PLAN.md

<interfaces>
<!-- What 32-02/32-01 produced, and the existing transaction/dispatch shapes this plan wires together. -->

From apps/api/src/domains/folders/folders.service.ts (archiveFolder — the db.connect()/BEGIN/COMMIT/ROLLBACK transaction skeleton to reuse verbatim):
```typescript
const client = await db.connect();
try {
  await client.query('BEGIN');
  // ... work using `client`, each write followed by insertDurableEvent(client, ...) ...
  await client.query('COMMIT');
  return updated;
} catch (error) {
  await client.query('ROLLBACK');
  throw error;
} finally {
  client.release();
}
```

From apps/api/src/domains/folders/folders.service.ts (moveFolder — capability-gate + Zod-parse opening shape to mirror):
```typescript
async moveFolder(ctx: RequestContext, id: string, body: unknown): Promise<Folder> {
  assertCapability(ctx, 'workspace.admin');
  const tenantId = requireCompanyId(ctx);
  const folder = await this.assertOwnedFolder(id, tenantId);
  const parsed = MoveFolderSchema.safeParse(body);
  if (!parsed.success) throw new AppError(400, parsed.error.issues[0].message);
  // ...
}
```

From apps/api/src/domains/folders/dto/tree.dto.ts (built in 32-01): `ReorderNodesSchema = z.object({ node_type: NodeType, parent_id: z.string().uuid().nullable(), ordered_ids: z.array(z.string().uuid()).min(1) })`.

From apps/api/src/domains/projects/projects.repository.ts and apps/api/src/domains/folders/folders.repository.ts and apps/api/src/domains/records/records.repository.ts (built in 32-02): `reorderProjects(client, companyId, folderId, orderedIds)`, `reorderPrograms(client, companyId, { folderId, projectId }, orderedIds)`, `reorderFolders(client, companyId, parentId, orderedIds)`, `reorderCollections(client, companyId, folderId, orderedIds)` — all throw `AppError(409, ...)` on a stale sibling set.

From apps/api/src/core/testUtils/concurrentRace.ts (built in 32-01): `raceLockedTransactions<A, B>(pool, txnA, txnB): Promise<{ resultA, resultB, aFinishedAt, bFinishedAt, bStartedAt }>` — a DB-agnostic two-`PoolClient` orchestration wrapper; the caller supplies the actual `BEGIN`/`FOR UPDATE`/`COMMIT` logic inside `txnA`/`txnB`.

From apps/api/src/core/events/outbox.ts: `createDurableEventEnvelope(input)` / `insertDurableEvent(client, envelope)` — the durable-event emission pair used throughout `archiveFolder`.

From apps/api/src/domains/folders/folders.controller.ts (moveFolder handler — mutation-handler shape to mirror):
```typescript
export const moveFolder = asyncHandler(async (req: RequestWithContext, res: Response) => {
  res.json({ folder: await foldersService.moveFolder(requireRequestContext(req), req.params.id, req.body) });
});
```

From apps/api/src/domains/folders/folders.routes.ts (state after 32-03 — `/tree/full` already registered before `/:id`; this plan adds `/tree/reorder` in the same position band).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reorderSiblings service dispatch (folders.service.ts)</name>
  <files>apps/api/src/domains/folders/folders.service.ts, apps/api/src/domains/folders/folders.service.test.ts</files>
  <read_first>
    apps/api/src/domains/folders/folders.service.ts (archiveFolder's transaction skeleton, moveFolder's capability+Zod-parse opening, the existing `fakeClient()` test helper already defined in folders.service.test.ts)
    apps/api/src/domains/folders/dto/tree.dto.ts (ReorderNodesSchema from 32-01)
    apps/api/src/domains/projects/projects.repository.ts, apps/api/src/domains/folders/folders.repository.ts, apps/api/src/domains/records/records.repository.ts (reorderProjects/reorderPrograms/reorderFolders/reorderCollections from 32-02)
    apps/api/src/core/events/outbox.ts (createDurableEventEnvelope/insertDurableEvent signatures)
  </read_first>
  <behavior>
    - `reorderSiblings(ctx, body)`: `assertCapability(ctx, 'workspace.admin')` first (before any parsing or DB work), then `requireCompanyId(ctx)`, then `ReorderNodesSchema.safeParse(body)` (400 on failure). Opens `db.connect()`, `BEGIN`, dispatches to the matching repository method by `node_type`: `'folder'` → `foldersRepository.reorderFolders(client, tenantId, parent_id, ordered_ids)`; `'project'` → `projectsRepository.reorderProjects(client, tenantId, parent_id, ordered_ids)`; `'program'` → `projectsRepository.reorderPrograms(client, tenantId, { folderId: parent_id, projectId: null }, ordered_ids)`; `'data_collection'` → `recordsRepository.reorderCollections(client, tenantId, parent_id, ordered_ids)`.
    - Known scope-narrowing (document with an inline `// TODO:` comment, same convention as `integrations.service.ts`'s existing TODOs for incomplete features): `'program'` reorder via this endpoint covers only folder-filed program siblings (`folder_id`-scoped) in this phase, not project-filed ones (`project_id`-scoped) — `ReorderNodesSchema`'s single `parent_id` field cannot unambiguously carry both scopes, and RESEARCH.md flagged this as an open design question rather than a resolved requirement.
    - On success, exactly ONE `insertDurableEvent(client, createDurableEventEnvelope({ eventName: 'tree.<node_type>.reordered', tenantId, actorId: userId, objectType: node_type, objectId: parent_id ?? 'root', correlationId: ctx.requestId, payloadVersion: 1, payload: { ordered_ids } }))` call per reorder — a single batched event, a documented exception to Phase 31's per-row archive-event convention (RESEARCH.md Open Question 1), not one event per sibling. `COMMIT`, return `{ node_type, parent_id, ordered_ids }`. On any thrown error (including the repository's `AppError(409)`), `ROLLBACK` then rethrow; `finally` releases the client — identical shape to `archiveFolder`.
    - Cases: ctx without `workspace.admin` → throws 403 before any repository/db call; invalid body (`ordered_ids: []`) → throws 400 before `db.connect()` is called; a repository-level 409 → the transaction is rolled back (assert `ROLLBACK` is the fakeClient's last captured call) and the 409 propagates; a successful reorder for each of the 4 node types dispatches to the correct repository method with `tenantId` among its arguments; exactly one `insertDurableEvent` call per successful reorder (not one per sibling), with `eventName` matching `/^tree\..+\.reordered$/`.
  </behavior>
  <action>
    Add `reorderSiblings` to `FoldersService`, placed after `moveFolder` and before `archiveFolder`. Import `ReorderNodesSchema` from './dto/tree.dto' (extend the existing dto import line). Extend folders.service.test.ts with the cases from `<behavior>`, reusing the existing `fakeClient()` helper and `mock.method(db, 'connect', async () => fakeClient())`, plus `mock.method(projectsRepository, 'reorderProjects', ...)` / `reorderPrograms` / `mock.method(foldersRepository, 'reorderFolders', ...)` / `mock.method(recordsRepository, 'reorderCollections', ...)` as needed per dispatch case.
  </action>
  <acceptance_criteria>
    - `reorderSiblings` throws AppError `.status === 403` when `ctx.roles` lacks `admin`, with zero repository calls (assert via mock call-count 0)
    - `reorderSiblings` throws AppError `.status === 400` on `{ node_type: 'project', parent_id: null, ordered_ids: [] }` before `db.connect()` is called
    - A dispatch test for each of `folder`/`project`/`program`/`data_collection` asserts the matching repository method is called exactly once with `tenantId` as an argument
    - A propagated repository 409 results in `ROLLBACK` being the fakeClient's last captured `query` call before the error is rethrown
    - Exactly one `insertDurableEvent` call happens per successful reorder, `eventName` matching `/^tree\..+\.reordered$/`
    - `node --require ts-node/register --test src/domains/folders/folders.service.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && node --require ts-node/register --test src/domains/folders/folders.service.test.ts</automated>
  </verify>
  <done>reorderSiblings is capability-gated, validated, transactional, dispatches correctly by node_type, rolls back on conflict, and emits exactly one batched durable event per successful reorder.</done>
</task>

<task type="auto">
  <name>Task 2: Wire POST /folders/tree/reorder + live concurrent-lock integration test</name>
  <files>apps/api/src/domains/folders/folders.controller.ts, apps/api/src/domains/folders/folders.routes.ts, apps/api/src/domains/folders/folders.reorder-concurrency.integration.test.ts</files>
  <read_first>
    apps/api/src/domains/folders/folders.controller.ts (moveFolder handler shape)
    apps/api/src/domains/folders/folders.routes.ts (route table state after 32-03 — `/tree/full` already registered before `/:id`)
    apps/api/src/core/testUtils/concurrentRace.ts (raceLockedTransactions harness from 32-01)
    apps/api/src/domains/folders/folders.integration.test.ts (adminCtx/before/after fixture scaffolding to mirror in a new dedicated test file)
    apps/api/src/core/db/index.ts (db Pool export)
  </read_first>
  <action>
    Add `export const reorderNodes = asyncHandler(async (req: RequestWithContext, res: Response) => { res.json({ ok: true, ...(await foldersService.reorderSiblings(requireRequestContext(req), req.body)) }); });` to folders.controller.ts. In folders.routes.ts, add `reorderNodes` to the import list and register `router.post('/tree/reorder', requireCapability('workspace.admin'), reorderNodes);` immediately after the `/tree/full` route (before `/:id`). Create apps/api/src/domains/folders/folders.reorder-concurrency.integration.test.ts (a new file, not an extension of folders.integration.test.ts — this test needs its own dedicated `before`/`after` fixture: a throwaway `company_id` with one folder that has exactly 3 child folders as the sibling set to race against). Use `raceLockedTransactions(db, txnA, txnB)` where `txnA`: opens `BEGIN` on its client, calls `foldersRepository.reorderFolders(clientA, companyId, parentFolderId, [childId2, childId3, childId1])`, waits ~300ms (simulating a slow client holding the lock), then `COMMIT`s; `txnB`: started concurrently by the harness, attempts `BEGIN` + `foldersRepository.reorderFolders(clientB, companyId, parentFolderId, [childId3, childId1, childId2])` + `COMMIT` — because `txnA` holds the row lock via `FOR UPDATE`, `txnB`'s call must block until `txnA` commits.
  </action>
  <acceptance_criteria>
    - folders.routes.ts registers `POST /tree/reorder` with `requireCapability('workspace.admin')`, positioned before `/:id`
    - New file `folders.reorder-concurrency.integration.test.ts` imports `raceLockedTransactions` from `../../core/testUtils/concurrentRace`
    - Test asserts `bFinishedAt >= aFinishedAt` (B's transaction cannot complete before A's commits, proving the lock blocked it)
    - Test asserts the final `sort_order` values for the 3 sibling folder rows exactly match `txnB`'s submitted order (last-committed-wins, not a merge/interleave of A and B's orderings — proving no lost update)
    - `node --require ts-node/register --test src/domains/folders/folders.reorder-concurrency.integration.test.ts` exits 0 (requires reachable Postgres)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && node --require ts-node/register --test src/domains/folders/folders.reorder-concurrency.integration.test.ts</automated>
  </verify>
  <done>POST /folders/tree/reorder is live, workspace.admin-gated, and proven lock-safe under real concurrent transactions with no lost update — closing VALIDATION.md's Wave 0 gap for TREEAPI-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /folders/tree/reorder | Capability-gated (workspace.admin); untrusted `node_type`/`parent_id`/`ordered_ids` cross into a transactional write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04-01 | Elevation of Privilege | `POST /tree/reorder` route + service | mitigate | `requireCapability('workspace.admin')` at the route AND `assertCapability` in the service — redundant by design, matching the existing `move`/`archive`/`unarchive` route pattern; this is the exact gap class Phase 31's own threat model flagged for its new routes |
| T-32-04-02 | Tampering | Concurrent reorder race | mitigate | `SELECT ... FOR UPDATE` (from 32-02) serializes concurrent reorders against the same sibling set; proven with a real two-`PoolClient` integration test in Task 2, not just code inspection |
| T-32-04-03 | Information Disclosure | `node_type` confusion (client claims a `node_type` that doesn't match the referenced row) | accept | Each dispatch branch's repository query is `company_id`-scoped, so a mismatched id naturally locks zero rows (`0 !== orderedIds.length`) → `AppError(409)`, not a silent wrong-table write; no additional type-confusion check needed |
| T-32-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this plan |

</threat_model>

<verification>
Run `cd apps/api && node --require ts-node/register --test src/domains/folders/folders.service.test.ts src/domains/folders/folders.reorder-concurrency.integration.test.ts`. Reorder must be capability-gated, transactional, and proven lock-safe against a live concurrent race.
</verification>

<success_criteria>
- POST /folders/tree/reorder requires workspace.admin
- No lost updates under concurrent reorder, proven by a live two-transaction test
- Stale sibling-set submissions are rejected with 409, not silently applied
</success_criteria>

<output>
Create `.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-04-SUMMARY.md` when done
</output>
