---
phase: 32-aggregated-tree-read-api-reorder-move-endpoints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/029_tree_sort_order.sql
  - apps/api/src/domains/folders/dto/tree.dto.ts
  - apps/api/src/domains/folders/folders.types.ts
  - apps/api/src/core/testUtils/concurrentRace.ts
  - apps/api/src/core/testUtils/concurrentRace.test.ts
autonomous: true
requirements: [TREEAPI-01, TREEAPI-02, TREEAPI-03]

must_haves:
  truths:
    - "A migration exists that adds sort_order to projects/programs/data_collections without breaking existing data"
    - "Reorder and move request bodies are validated against a strict node_type enum before touching the database"
    - "The reorder/move DTOs can disambiguate project-scoped programs (project_id set) from folder-scoped programs (project_id null/omitted)"
  artifacts:
    - path: "database/migrations/029_tree_sort_order.sql"
      provides: "sort_order columns + indexes for projects/programs/data_collections"
      contains: "sort_order INTEGER NOT NULL DEFAULT 0"
    - path: "apps/api/src/domains/folders/dto/tree.dto.ts"
      provides: "ReorderNodesSchema/MoveNodeSchema Zod DTOs, including the optional project_id program-scope disambiguator"
      exports: ["ReorderNodesSchema", "MoveNodeSchema"]
    - path: "apps/api/src/core/testUtils/concurrentRace.ts"
      provides: "reusable two-transaction race harness for TREEAPI-02 lock-safety tests"
      exports: ["raceLockedTransactions"]
  key_links:
    - from: "apps/api/src/domains/folders/dto/tree.dto.ts"
      to: "apps/api/src/domains/folders/folders.service.ts"
      via: "ReorderNodesSchema/MoveNodeSchema import (consumed in later plans)"
      pattern: "from '\\./dto/tree\\.dto'"
---

<objective>
Lay the foundation Phase 32's write endpoints need before any endpoint code is written: the additive `sort_order` migration for `projects`/`programs`/`data_collections`, the Zod DTOs that gate the two new mutation endpoints' request bodies (including the `project_id` scope disambiguator needed because `programs` rows can be filed under either a folder OR a project), the `TreeNode` discriminated-union type the aggregated read endpoint will return, and a reusable two-transaction concurrency-race test harness (VALIDATION.md's Wave 0 gap — no prior art for genuinely-concurrent testing exists anywhere in this codebase).

Purpose: Every later Phase 32 plan (aggregated read, reorder, move) depends on one or more of these artifacts. Building them first, in isolation, means the write-endpoint plans can focus entirely on business logic instead of re-deriving schema/validation/test-infra along the way.
Output: `029_tree_sort_order.sql`, `dto/tree.dto.ts`, an extended `folders.types.ts`, and `core/testUtils/concurrentRace.ts` + its unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-RESEARCH.md
@.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-PATTERNS.md

<interfaces>
<!-- Current state of files this plan extends. Executor should use these directly. -->

From apps/api/src/domains/folders/folders.types.ts (full file, current state):
```typescript
export interface Folder {
  id: string;
  company_id: string;
  parent_id: string | null;
  name: string;
  icon: string | null;
  color: string | null;
  sort_order: number;
  created_by: string | null;
  created_at: Date;
  updated_at: Date;
  ancestor_ids: string[];
  archived_at: Date | null;
}

export interface FolderTree extends Folder {
  children: FolderTree[];
}
```

From apps/api/src/domains/folders/dto/folder.dto.ts (Zod convention to mirror):
```typescript
import { z } from 'zod';

export const MoveFolderSchema = z.object({
  parent_id: z.string().uuid().nullable(),
});

export type MoveFolderDto = z.infer<typeof MoveFolderSchema>;
```

From apps/api/src/domains/projects/projects.types.ts: `Project`, `Program`, `ProjectPage` interfaces (id, company_id, folder_id/project_id/parent_page_id parent-pointer fields, name/title). Note `Program` carries BOTH `folder_id` and `project_id` — a program can be filed directly under a folder OR nested under a project (never both at once).

From apps/api/src/domains/records/records.types.ts: `DataCollectionRow` interface (id, company_id, project_id, folder_id, name).

From apps/api/src/core/events/outbox.ts, lines 114-141 (the only existing `FOR UPDATE` precedent in this codebase — `SKIP LOCKED` queue-claim semantics, NOT the blocking semantics reorder needs):
```typescript
const { rows } = await db.query<EventOutboxRow>(
  `WITH due AS (
     SELECT id FROM event_outbox
     WHERE attempts < max_attempts AND (...)
     ORDER BY created_at ASC
     FOR UPDATE SKIP LOCKED
     LIMIT $1
   )
   UPDATE event_outbox e SET status = 'publishing', ... FROM due WHERE e.id = due.id RETURNING e.*`,
  [limit, workerId],
);
```

From apps/api/src/core/db/index.ts (full file):
```typescript
import { Pool } from 'pg';
const db = new Pool({ connectionString: process.env.DATABASE_URL });
export { db };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 029 — sort_order columns on projects/programs/data_collections</name>
  <files>database/migrations/029_tree_sort_order.sql</files>
  <read_first>
    database/migrations/028_folder_hierarchy_invariants.sql (idempotency + header-comment convention, immediately-preceding migration in this arc)
    apps/api/src/domains/projects/projects.types.ts (Project/Program shapes gaining sort_order)
    apps/api/src/domains/records/records.types.ts (DataCollectionRow shape gaining sort_order)
  </read_first>
  <action>
    Create database/migrations/029_tree_sort_order.sql. Header comment: "Migration: Tree node sort_order columns. Apply after 028. Idempotent." followed by prose explaining folders and project_pages already have sort_order (006_folders.sql, 009_project_pages.sql) but projects/programs/data_collections do not, and this migration adds it additively so the TREEAPI-02 sibling reorder endpoint can cover all node types the TREEAPI-01 aggregated tree exposes.
    Add exactly these three statements: `ALTER TABLE projects ADD COLUMN IF NOT EXISTS sort_order INTEGER NOT NULL DEFAULT 0;`, `ALTER TABLE programs ADD COLUMN IF NOT EXISTS sort_order INTEGER NOT NULL DEFAULT 0;`, `ALTER TABLE data_collections ADD COLUMN IF NOT EXISTS sort_order INTEGER NOT NULL DEFAULT 0;`.
    Then add three indexes: `CREATE INDEX IF NOT EXISTS projects_folder_sort_idx ON projects (folder_id, sort_order);`, `CREATE INDEX IF NOT EXISTS programs_parent_sort_idx ON programs (folder_id, project_id, sort_order);`, `CREATE INDEX IF NOT EXISTS data_collections_folder_sort_idx ON data_collections (folder_id, sort_order);`.
    Every statement must use `IF NOT EXISTS` per project CLAUDE.md's idempotent-migration convention — no bare `ALTER TABLE ... ADD COLUMN` without the guard.
  </action>
  <acceptance_criteria>
    - File database/migrations/029_tree_sort_order.sql exists, matches the `NNN_description.sql` naming convention
    - Contains exactly 3 `ADD COLUMN IF NOT EXISTS sort_order INTEGER NOT NULL DEFAULT 0` statements (projects, programs, data_collections)
    - Contains 3 `CREATE INDEX IF NOT EXISTS` statements named projects_folder_sort_idx, programs_parent_sort_idx, data_collections_folder_sort_idx
    - Running the file twice in a row against a live Postgres instance succeeds both times with exit code 0 (idempotent-rerun proof); if Postgres is unreachable in this environment, every ALTER/CREATE statement uses `IF NOT EXISTS` as a static substitute proof
  </acceptance_criteria>
  <verify>
    <automated>psql "$DATABASE_URL" -f database/migrations/029_tree_sort_order.sql && psql "$DATABASE_URL" -f database/migrations/029_tree_sort_order.sql</automated>
  </verify>
  <done>Migration file created, idempotent, adds sort_order + supporting indexes to projects/programs/data_collections.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tree DTOs + TreeNode discriminated-union type</name>
  <files>apps/api/src/domains/folders/dto/tree.dto.ts, apps/api/src/domains/folders/folders.types.ts</files>
  <read_first>
    apps/api/src/domains/folders/dto/folder.dto.ts (Zod convention to mirror exactly)
    apps/api/src/domains/folders/folders.types.ts (current Folder/FolderTree)
    apps/api/src/domains/projects/projects.types.ts (Project, Program, ProjectPage — note Program has both folder_id and project_id)
    apps/api/src/domains/records/records.types.ts (DataCollectionRow)
  </read_first>
  <action>
    Create apps/api/src/domains/folders/dto/tree.dto.ts. Define `const NodeType = z.enum(['folder', 'project', 'program', 'data_collection'])` with a code comment noting `'page'` is intentionally excluded from move/reorder scope this phase (per RESEARCH.md Open Question 2 — RESOLVED: page reparenting stays on the existing `PATCH /projects/pages/:pageId`, which has no cross-project cycle risk today).
    Export `ReorderNodesSchema = z.object({ node_type: NodeType, parent_id: z.string().uuid().nullable(), project_id: z.string().uuid().nullable().optional(), ordered_ids: z.array(z.string().uuid()).min(1) })` and `MoveNodeSchema = z.object({ node_type: NodeType, node_id: z.string().uuid(), new_parent_id: z.string().uuid().nullable(), project_id: z.string().uuid().nullable().optional() })`.
    The optional `project_id` field is the scope disambiguator for `node_type: 'program'` only — `programs` rows carry both `folder_id` and `project_id` columns (a program is filed directly under a folder OR nested under a project, never both), so a single `parent_id`/`new_parent_id` field cannot unambiguously address both scopes. Add a code comment documenting this: when `project_id` is a non-null UUID, the request targets a project-scoped program (siblings/destination share `project_id`, `folder_id` is null); when `project_id` is omitted or null, the request targets a folder-scoped program (siblings/destination share `folder_id`, `project_id` is null) — the default, matching the other three node types where `project_id` is unused. This disambiguator is threaded through by the reorder/move dispatch logic built in 32-04/32-05, ensuring project-filed programs are fully reorderable/movable through these endpoints, not just folder-filed ones.
    Export inferred types `ReorderNodesDto` and `MoveNodeDto` via `z.infer<>`, matching folder.dto.ts's convention.
    In folders.types.ts, import `Project, Program, ProjectPage` from '../projects/projects.types' and `DataCollectionRow` from '../records/records.types'. Append a `TreeNode` discriminated-union-shaped interface after the existing `FolderTree` interface: fields `node_type: 'folder' | 'project' | 'program' | 'data_collection' | 'project_page'`, `id: string`, `company_id: string`, `name: string` (populated from `name` for folder/project/program/data_collection, or `title` for project_page), `children: TreeNode[]`, and `raw: Folder | Project | Program | ProjectPage | DataCollectionRow` holding the original row untouched.
  </action>
  <acceptance_criteria>
    - `apps/api/src/domains/folders/dto/tree.dto.ts` exports `ReorderNodesSchema`, `MoveNodeSchema`, `ReorderNodesDto`, `MoveNodeDto`
    - `ReorderNodesSchema.safeParse({ node_type: 'project', parent_id: null, ordered_ids: [] })` returns `success: false` (min(1) violated)
    - `MoveNodeSchema.safeParse({ node_type: 'page', node_id: '11111111-1111-4111-8111-111111111111', new_parent_id: null })` returns `success: false` ('page' is not in the NodeType enum)
    - `ReorderNodesSchema.safeParse({ node_type: 'program', parent_id: null, project_id: '11111111-1111-4111-8111-111111111111', ordered_ids: ['22222222-2222-4222-8222-222222222222'] })` returns `success: true` with `project_id` present in `.data` (project-scoped program disambiguator round-trips)
    - `MoveNodeSchema.safeParse({ node_type: 'program', node_id: '11111111-1111-4111-8111-111111111111', new_parent_id: null, project_id: '22222222-2222-4222-8222-222222222222' })` returns `success: true` with `project_id` present in `.data`
    - `ReorderNodesSchema.safeParse({ node_type: 'project', parent_id: null, ordered_ids: ['11111111-1111-4111-8111-111111111111'] })` (no `project_id` field) returns `success: true` — `project_id` is optional and does not break non-program node types
    - `folders.types.ts` exports `TreeNode` with a `node_type` discriminant field and a recursive `children: TreeNode[]`
    - `tsc --noEmit` passes for apps/api
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit</automated>
  </verify>
  <done>tree.dto.ts (including the project_id program-scope disambiguator) and the TreeNode type exist, are importable, and type-check cleanly against the rest of the folders domain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Two-transaction concurrency race harness (Wave 0 infra for TREEAPI-02)</name>
  <files>apps/api/src/core/testUtils/concurrentRace.ts, apps/api/src/core/testUtils/concurrentRace.test.ts</files>
  <read_first>
    apps/api/src/core/events/outbox.ts (the only existing FOR UPDATE lock precedent in this codebase — claimDueEvents, lines 114-141; note it uses SKIP LOCKED, which reorder must NOT use)
    apps/api/src/core/db/index.ts (Pool export this harness wraps)
  </read_first>
  <behavior>
    - `raceLockedTransactions(pool, txnA, txnB)` opens two `PoolClient`s via `pool.connect()` BEFORE invoking either callback (so both connections exist prior to either transaction starting, avoiding pool-exhaustion false-blocking on a small pool).
    - Records `bStartedAt = Date.now()` immediately before invoking `txnB(clientB)`.
    - Runs `txnA(clientA)` and `txnB(clientB)` concurrently via `Promise.all`, recording `Date.now()` as `aFinishedAt`/`bFinishedAt` when each resolves.
    - Releases both clients in a `finally` block regardless of success/failure.
    - Returns `{ resultA, resultB, aFinishedAt, bFinishedAt, bStartedAt }`.
    - The actual `BEGIN` / `SELECT ... FOR UPDATE` / hold-delay / `COMMIT` sequencing is the CALLER's responsibility inside `txnA`/`txnB` — this harness is a DB-agnostic orchestration wrapper only, it contains no SQL.
    - Test cases (using fake pool-like stubs — no live DB required): (1) both `connect()` calls happen before either `txnA`/`txnB` body starts executing, proven via a shared call-order array; (2) `bStartedAt` is captured before `txnB`'s callback resolves even when `txnA` is deliberately slower via a controlled `setTimeout`-wrapped delay, proving both run concurrently rather than sequentially; (3) `resultA`/`resultB` in the returned object exactly equal each callback's resolved value; (4) both fake clients' `release()` is called exactly once each, even when one callback throws.
  </behavior>
  <action>
    Create apps/api/src/core/testUtils/concurrentRace.ts exporting `raceLockedTransactions<A, B>(pool: Pool, txnA: (client: PoolClient) => Promise<A>, txnB: (client: PoolClient) => Promise<B>): Promise<{ resultA: A; resultB: B; aFinishedAt: number; bFinishedAt: number; bStartedAt: number }>` implementing the behavior above. Create apps/api/src/core/testUtils/concurrentRace.test.ts with the 4 cases from `<behavior>`, using fake pool objects exposing a `connect` method that returns a fake `PoolClient` stub (`{ query: mock.fn(), release: mock.fn() }`) — no real Postgres connection required for this test. This harness is Wave 0 infrastructure: it will be exercised against a live Postgres connection (real `BEGIN`/`FOR UPDATE`/`COMMIT`) by the reorder-lock-safety integration test built in a later plan (32-04) — this task only proves the harness's own orchestration contract.
  </action>
  <acceptance_criteria>
    - `apps/api/src/core/testUtils/concurrentRace.ts` exports `raceLockedTransactions`
    - Unit test in `concurrentRace.test.ts` passes without a live DB connection (fake pool/client stubs only)
    - Test asserts `bStartedAt` is recorded before `txnB`'s callback resolves even when `txnA` is deliberately slower, proving concurrent (not sequential) execution
    - Test asserts both fake clients' `release()` is called exactly once even when one callback throws
    - `node --require ts-node/register --test src/core/testUtils/concurrentRace.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && node --require ts-node/register --test src/core/testUtils/concurrentRace.test.ts</automated>
  </verify>
  <done>Reusable two-transaction race harness exists, is unit-tested independent of a live database, and is ready for the 32-04 reorder plan's real FOR UPDATE lock-safety integration test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Request body → Zod DTO | Reorder/move request bodies (built by later plans' routes) cross from untrusted client input into typed, validated data via the DTOs this plan defines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01-01 | Tampering | `dto/tree.dto.ts` NodeType enum | mitigate | `node_type` is a strict Zod `z.enum([...])`, not a free string — an unrecognized node type fails validation at the DTO boundary before any dispatch logic runs (enforced when the DTOs are consumed in 32-04/32-05) |
| T-32-01-02 | Tampering | `dto/tree.dto.ts` `ordered_ids`/`node_id`/`project_id` fields | mitigate | Every id field is `z.string().uuid()`, rejecting non-UUID payloads before any SQL is built with them |
| T-32-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this plan (zod already a dependency) — package legitimacy gate not applicable |

</threat_model>

<verification>
Run `cd apps/api && npx tsc --noEmit` (types + DTOs compile) and `node --require ts-node/register --test src/core/testUtils/concurrentRace.test.ts` (harness unit test green). Apply `029_tree_sort_order.sql` twice against a reachable Postgres instance to prove idempotency; if unreachable in this environment, confirm every statement uses `IF NOT EXISTS` by static review.
</verification>

<success_criteria>
- Migration 029 exists, is idempotent, and adds sort_order to projects/programs/data_collections
- ReorderNodesSchema/MoveNodeSchema exist, reject invalid node_type and empty/malformed id fields, and carry the optional project_id program-scope disambiguator
- TreeNode type exists and type-checks
- raceLockedTransactions harness exists, is unit-tested, and is ready for live-DB use in 32-04
</success_criteria>

<output>
Create `.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-01-SUMMARY.md` when done
</output>
