---
phase: 32-aggregated-tree-read-api-reorder-move-endpoints
plan: 05
type: execute
wave: 4
depends_on: ["32-04"]
files_modified:
  - apps/api/src/domains/folders/folders.service.ts
  - apps/api/src/domains/folders/folders.controller.ts
  - apps/api/src/domains/folders/folders.routes.ts
  - apps/api/src/domains/folders/folders.service.test.ts
  - apps/api/src/domains/folders/folders.integration.test.ts
autonomous: true
requirements: [TREEAPI-03, TREEAPI-04]

must_haves:
  truths:
    - "Moving a node validates tenant ownership on the destination before writing"
    - "Cross-tenant move attempts and cycle attempts fail with distinguishable error codes"
    - "Every tree mutation endpoint requires workspace.admin"
  artifacts:
    - path: "apps/api/src/domains/folders/folders.service.ts"
      provides: "moveNode — dispatch by node_type, delegates folder cycle-check to existing moveFolder"
      contains: "MoveNodeSchema.safeParse"
    - path: "apps/api/src/domains/folders/folders.routes.ts"
      provides: "POST /tree/move route"
      contains: "tree/move"
  key_links:
    - from: "apps/api/src/domains/folders/folders.service.ts"
      to: "apps/api/src/domains/folders/folders.service.ts"
      via: "moveNode delegates node_type 'folder' straight to moveFolder, not a reimplementation"
      pattern: "case 'folder':"
    - from: "apps/api/src/domains/folders/folders.service.ts"
      to: "apps/api/src/domains/projects/projects.repository.ts"
      via: "moveNode calls updateProject/updateProgram with folder_id"
      pattern: "updateProject\\(node_id"
---

<objective>
Build `POST /folders/tree/move` — a single, workspace.admin-gated endpoint that reparents any of folder/project/program/data_collection, validating destination tenant ownership and rejecting illegal moves (cross-tenant destinations, and — for folders only — cycles) with a specific, distinguishable error per reason.

Purpose: TREEAPI-03's full delivery, closing out Phase 32. This is a thin dispatcher over already-existing, already-tested per-domain update paths — it must not reimplement cross-tenant checks or Phase 31's folder cycle detection, only orchestrate them.
Output: `foldersService.moveNode`, `POST /tree/move`, unit + live-DB coverage distinguishing cross-tenant-destination (404) from folder-cycle (400) rejections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-RESEARCH.md
@.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-PATTERNS.md
@.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-04-PLAN.md

<interfaces>
<!-- Existing per-domain reparenting paths this plan dispatches to, unchanged. Executor should use these directly. -->

From apps/api/src/domains/folders/folders.service.ts (moveFolder — the existing, Phase-31-built folder cycle/depth check, to be delegated to verbatim, never reimplemented):
```typescript
async moveFolder(ctx: RequestContext, id: string, body: unknown): Promise<Folder> {
  assertCapability(ctx, 'workspace.admin');
  const tenantId = requireCompanyId(ctx);
  const folder = await this.assertOwnedFolder(id, tenantId);
  const parsed = MoveFolderSchema.safeParse(body);
  if (!parsed.success) throw new AppError(400, parsed.error.issues[0].message);
  const { parent_id: parentId } = parsed.data;
  if (parentId) {
    const parent = await this.assertOwnedFolder(parentId, tenantId);
    if (parent.id === id || parent.ancestor_ids.includes(id)) {
      throw new AppError(400, 'Cannot move a folder into its own descendant');
    }
    // depth check, ancestor_ids rewrite, descendant patch ...
  }
  // ...
}

private async assertOwnedFolder(id: string, companyId: string): Promise<Folder> {
  const f = await foldersRepository.findFolderForCompany(id, companyId);
  if (!f) throw new AppError(404, 'Folder not found');
  return f;
}
```

From apps/api/src/domains/projects/projects.repository.ts: `updateProject(id, { folder_id })`, `updateProgram(id, { folder_id })` — existing COALESCE-update paths, return `null` if the row's `id` isn't found (repository queries are not company-scoped on the update itself, so ownership of `node_id` must be pre-checked via `findProjectForCompany`/`findProgramForCompany` before dispatch if not already implied — see action notes).

From apps/api/src/domains/records/records.repository.ts: `updateCollection(id, companyId, patch)` — note this method's signature takes `companyId` as its second positional argument (unlike `updateProject`/`updateProgram`), and is already company-scoped in its own WHERE clause.

From apps/api/src/domains/folders/dto/tree.dto.ts (built in 32-01): `MoveNodeSchema = z.object({ node_type: NodeType, node_id: z.string().uuid(), new_parent_id: z.string().uuid().nullable() })`.

From apps/api/src/domains/folders/folders.controller.ts (moveFolder handler — mutation-handler shape to mirror):
```typescript
export const moveFolder = asyncHandler(async (req: RequestWithContext, res: Response) => {
  res.json({ folder: await foldersService.moveFolder(requireRequestContext(req), req.params.id, req.body) });
});
```

From apps/api/src/domains/folders/folders.integration.test.ts: existing HIER-02 (cross-tenant FK rejection) / HIER-03 (folder cycle rejection) live-DB tests — this plan adds parallel cases for non-folder node types, proving TREEAPI-03's "specific error distinguishing the reason" (404 for cross-tenant vs. 400 for folder cycle).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: moveNode service dispatch (folders.service.ts)</name>
  <files>apps/api/src/domains/folders/folders.service.ts, apps/api/src/domains/folders/folders.service.test.ts</files>
  <read_first>
    apps/api/src/domains/folders/folders.service.ts (moveFolder to delegate to verbatim, assertOwnedFolder private helper)
    apps/api/src/domains/folders/dto/tree.dto.ts (MoveNodeSchema)
    apps/api/src/domains/projects/projects.repository.ts (updateProject/updateProgram/findProjectForCompany/findProgramForCompany signatures)
    apps/api/src/domains/records/records.repository.ts (updateCollection/findCollection signatures)
  </read_first>
  <behavior>
    - `moveNode(ctx, body)`: `assertCapability(ctx, 'workspace.admin')`, `requireCompanyId(ctx)`, `MoveNodeSchema.safeParse(body)` (400 on failure), then switch on `node_type`:
      - `'folder'`: `return this.moveFolder(ctx, node_id, { parent_id: new_parent_id })` — delegate verbatim, reusing Phase 31's cycle/depth check; no new cycle-check code path is written inside `moveNode` itself.
      - `'project'`: first `await this.assertOwnedFolder(node_id ... )` is NOT correct here — instead first confirm the project itself belongs to this tenant via `projectsRepository.findProjectForCompany(node_id, tenantId)` (404 `'Project not found'` if null); if `new_parent_id` is non-null, `await this.assertOwnedFolder(new_parent_id, tenantId)` (404 if the destination folder doesn't belong to this tenant); then `const updated = await projectsRepository.updateProject(node_id, { folder_id: new_parent_id })`; if `!updated`, throw `AppError(404, 'Project not found')`; return `updated`.
      - `'program'`: same shape as `'project'`, using `projectsRepository.findProgramForCompany`/`updateProgram(node_id, { folder_id: new_parent_id })` — this only reparents folder-filed programs (setting `folder_id`), matching the same scope-narrowing documented in 32-04's reorder dispatch (project-filed program reparenting via `project_id` stays out of scope this phase, same `// TODO:` convention).
      - `'data_collection'`: confirm ownership via `recordsRepository.findCollection(node_id, tenantId)` (404 if null); validate destination via `assertOwnedFolder` when `new_parent_id` is non-null; then `recordsRepository.updateCollection(node_id, tenantId, { folder_id: new_parent_id })`; if the result is null, throw 404.
    - Cases: ctx without `workspace.admin` → 403 before any repository call; move a `project` into a folder belonging to a different tenant → 404 (via `assertOwnedFolder`'s existing cross-tenant 404); move a `folder` into its own descendant → delegates to `moveFolder` and inherits its existing 400 "Cannot move a folder into its own descendant" (test asserts the underlying `foldersRepository.moveFolder` mock is never invoked when the cycle check fails, proving `moveFolder`'s own logic ran, not a duplicate); move a `data_collection` with `new_parent_id: null` (un-filing it) → `updateCollection` called with `folder_id: null`; move a `project`/`program`/`data_collection` whose `node_id` doesn't belong to the caller's tenant → 404 from the ownership pre-check, before any destination validation.
  </behavior>
  <action>
    Add `moveNode` to `FoldersService`, placed after `moveFolder`. Import `MoveNodeSchema` from './dto/tree.dto' (extend the existing dto import). Extend folders.service.test.ts with the cases in `<behavior>`, mocking `projectsRepository.findProjectForCompany`/`findProgramForCompany`/`updateProject`/`updateProgram`, `recordsRepository.findCollection`/`updateCollection`, and `foldersRepository.findFolderForCompany` (for `assertOwnedFolder`'s destination check) as needed per case.
  </action>
  <acceptance_criteria>
    - `moveNode` throws AppError `.status === 403` when `ctx.roles` lacks `admin`, with zero repository calls
    - `moveNode({ node_type: 'folder', ... })` results in `foldersRepository.moveFolder` being invoked through the existing `moveFolder` method (no separate/duplicate cycle-detection code exists inside `moveNode` — verified both by test and by source inspection of the `case 'folder':` branch containing only a single delegating call)
    - `moveNode({ node_type: 'project', node_id: <owned-project>, new_parent_id: <cross-tenant-folder-id> })` throws AppError `.status === 404`
    - `moveNode({ node_type: 'data_collection', node_id: <missing>, new_parent_id: null })` throws AppError `.status === 404` when the ownership pre-check returns null
    - `node --require ts-node/register --test src/domains/folders/folders.service.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && node --require ts-node/register --test src/domains/folders/folders.service.test.ts</automated>
  </verify>
  <done>moveNode dispatches correctly by node_type, delegates folder cycle protection to the existing moveFolder rather than duplicating it, and rejects cross-tenant destinations and missing rows with the correct existing error codes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire POST /folders/tree/move + cross-tenant/capability integration coverage</name>
  <files>apps/api/src/domains/folders/folders.controller.ts, apps/api/src/domains/folders/folders.routes.ts, apps/api/src/domains/folders/folders.integration.test.ts</files>
  <read_first>
    apps/api/src/domains/folders/folders.controller.ts (moveFolder handler)
    apps/api/src/domains/folders/folders.routes.ts (route table state after 32-03/32-04 — `/tree/full` and `/tree/reorder` already registered before `/:id`)
    apps/api/src/domains/folders/folders.integration.test.ts (existing HIER-02/HIER-03 cross-tenant/cycle DB-level tests, adminCtx/before/after fixtures to extend)
  </read_first>
  <action>
    Add `export const moveNode = asyncHandler(async (req: RequestWithContext, res: Response) => { res.json({ node: await foldersService.moveNode(requireRequestContext(req), req.body) }); });` to folders.controller.ts. In folders.routes.ts, add `moveNode` to the import list and register `router.post('/tree/move', requireCapability('workspace.admin'), moveNode);` after `/tree/reorder` and before `/:id`. Extend folders.integration.test.ts with two new live-DB tests: (1) a TREEAPI-03 test creating a `project` row in `company1Id`, then calling `foldersService.moveNode(adminCtx(company1Id), { node_type: 'project', node_id: <that project's id>, new_parent_id: folderB.id })` (folderB belongs to `company2Id`) and asserting it throws an AppError with `.status === 404` — distinct from the existing folder-cycle test's 400, proving TREEAPI-03's "specific error distinguishing the reason"; (2) a TREEAPI-04 test asserting both `foldersService.moveNode` and `foldersService.reorderSiblings` throw `.status === 403` when called with a non-admin `adminCtx`-derived context (`{ ...adminCtx(company1Id), roles: ['member'] }`).
  </action>
  <acceptance_criteria>
    - folders.routes.ts registers `POST /tree/move` with `requireCapability('workspace.admin')`, positioned before `/:id`
    - New integration test proves a project move to a cross-tenant folder destination throws a 404-status AppError (not the folder-move's 400 cycle error)
    - New integration test proves both `reorderSiblings` and `moveNode` reject a non-admin ctx with 403
    - `node --require ts-node/register --test src/domains/folders/folders.integration.test.ts` exits 0
    - Full suite: `npm test` (apps/api) exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && node --require ts-node/register --test src/domains/folders/folders.integration.test.ts && npm test</automated>
  </verify>
  <done>POST /folders/tree/move is live, workspace.admin-gated, distinguishes cross-tenant-destination (404) from folder-cycle (400) rejections, and the full apps/api test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /folders/tree/move | Capability-gated (workspace.admin); untrusted `node_id`/`new_parent_id`/`node_type` cross into a reparenting write spanning 4 different domains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05-01 | Tampering / Elevation of Privilege | `moveNode`'s destination validation | mitigate | Reuses existing `assertOwnedFolder`/`findProjectForCompany`-style 404-on-cross-tenant checks (already `company_id`-scoped, established pre-Phase-31/Phase-31) before dispatching any write; the composite `(id, company_id)` FK invariants (Phase 31) are the DB-level backstop |
| T-32-05-02 | Elevation of Privilege | `POST /tree/move` route + service | mitigate | `requireCapability('workspace.admin')` at the route AND `assertCapability` in the service — redundant by design, matching `move`/`archive`/`unarchive` |
| T-32-05-03 | Tampering | Folder cycle-detection drift | mitigate | `moveNode`'s `'folder'` branch is a single delegating call to the existing `moveFolder` — no duplicate cycle-check implementation exists, eliminating the risk of two cycle-check implementations drifting apart |
| T-32-05-04 | Tampering | `node_type` confusion (e.g. `node_type: 'project'` with a `node_id` that is actually a program's UUID) | accept | Each dispatch branch looks the row up in its own type-specific, `company_id`-scoped table via existing `find*ForCompany`/`findCollection` helpers — a UUID that doesn't exist in the claimed table's tenant-scoped rows naturally 404s, no extra type-confusion check needed |
| T-32-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this plan |

</threat_model>

<verification>
Run `cd apps/api && npm test` (full suite). `POST /folders/tree/move` must be workspace.admin-gated, reject cross-tenant destinations with 404, delegate folder cycle protection to the existing moveFolder, and leave the full apps/api suite green — this is the final plan in Phase 32.
</verification>

<success_criteria>
- POST /folders/tree/move requires workspace.admin
- Cross-tenant destination and folder-cycle rejections are distinguishable (404 vs 400)
- Folder cycle detection is delegated, not duplicated
- Full apps/api test suite green
</success_criteria>

<output>
Create `.planning/phases/32-aggregated-tree-read-api-reorder-move-endpoints/32-05-SUMMARY.md` when done
</output>
