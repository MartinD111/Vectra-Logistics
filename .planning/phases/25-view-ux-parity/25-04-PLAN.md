---
phase: 25-view-ux-parity
plan: 04
type: execute
wave: 4
depends_on: ["25-03"]
files_modified:
  - apps/workspaces/src/components/projectPage/collectionTable/CollectionTableView.tsx
  - apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx
  - apps/workspaces/src/components/projectPage/BoardBlock.tsx
autonomous: true
requirements: [VIEWX-04]

must_haves:
  truths:
    - "User can switch a collection-view block between view types on the same collection without creating duplicate records"
    - "D-05: switching changes which collection_views row (and its type) the block points at, creating a sibling view row once and reusing it thereafter — never re-materializes collection_records"
  artifacts:
    - path: "apps/workspaces/src/components/projectPage/collectionTable/CollectionTableView.tsx"
      provides: "Table render path for view.type === 'table', reusing PropertyField per cell"
      contains: "PropertyField"
    - path: "apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx"
      provides: "Board/Table segmented control that creates-once-then-remembers the sibling view"
      contains: "useViews"
    - path: "apps/workspaces/src/components/projectPage/BoardBlock.tsx"
      provides: "Branches render on view.type between board columns and CollectionTableView"
      contains: "view.type === 'table'"
  key_links:
    - from: "apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx"
      to: "apps/workspaces/src/lib/hooks/useRecords.ts"
      via: "useViews(collectionId) find-by-type, else useCreateView(collectionId).mutateAsync"
      pattern: "useCreateView\\(collectionId\\)"
    - from: "apps/workspaces/src/components/projectPage/BoardBlock.tsx"
      to: "apps/workspaces/src/components/projectPage/collectionTable/CollectionTableView.tsx"
      via: "conditional render when view.type === 'table'"
      pattern: "<CollectionTableView"
---

<objective>
Ship VIEWX-04: a Board/Table segmented control (D-05) on the `collection-view` block's chrome that switches which saved `collection_views` row (and its `type`) the block points at -- creating a second view row only once, then remembering it on later toggles -- plus a new `CollectionTableView` render path that shows the same `collection_records` as a schema-driven, directly-editable table, never re-materializing data.

Purpose: Completes this phase's parity bar -- a view is a lens over one dataset, not a data copy; switching lenses must never duplicate `collection_records`.
Output: `CollectionTableView.tsx` (new, in its own `collectionTable/` folder per the Pitfall 2 naming-collision avoidance), `ViewSwitcher.tsx` (new), `BoardBlock.tsx` extended to branch its render on `view.type` and mount `ViewSwitcher` in its toolbar row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-view-ux-parity/25-CONTEXT.md
@.planning/phases/25-view-ux-parity/25-RESEARCH.md
@.planning/phases/25-view-ux-parity/25-PATTERNS.md
@.planning/phases/25-view-ux-parity/25-UI-SPEC.md
@.planning/phases/25-view-ux-parity/25-01-SUMMARY.md
@.planning/phases/25-view-ux-parity/25-02-SUMMARY.md
@.planning/phases/25-view-ux-parity/25-03-SUMMARY.md
</context>

<interfaces>
Contracts from Plan 25-01/25-02/25-03 and existing hooks this plan consumes.

From apps/workspaces/src/lib/hooks/useRecords.ts (existing + Plan 25-01):
- `export function useViews(collectionId: string): UseQueryResult<CollectionView[]>;` (existing, unmodified -- lists all views for a collection)
- `export function useCreateView(collectionId: string): UseMutationResult<CollectionView, unknown, CreateViewInput>;` (existing, unmodified)
- `export function useUpdateAnyRecord(collectionId: string): UseMutationResult<CollectionRecord, unknown, { id: string; data: UpdateRecordInput }>;` (existing, unmodified -- already used by BoardBlock/BoardCard for record edits)

From apps/workspaces/src/lib/projectPage/viewFilters.ts (Plan 25-01):
- `export function applyFilters(records: CollectionRecord[], filters: FilterCondition[], schema: CollectionPropertyDef[]): CollectionRecord[];`
- `export function applySorts(records: CollectionRecord[], sorts: SortCondition[], schema: CollectionPropertyDef[]): CollectionRecord[];`

From apps/workspaces/src/components/records/PropertyField.tsx (existing, unmodified):
- `export function PropertyField({ property, value, onCommit }: { property: CollectionPropertyDef; value: unknown; onCommit: (value: unknown) => void; }): JSX.Element;` (editable, 12-branch switch on property.type)

From apps/workspaces/src/lib/projectPage/blocks.ts (existing, unmodified):
- `export interface CollectionViewBlock extends PageBlockBase { kind: 'collection-view'; title?: string; collectionId: string | null; viewId: string | null; }`

Existing unrelated static table block (do NOT reuse its cell-editing pattern, name collision to avoid per Pitfall 2):
- `apps/workspaces/src/components/projectPage/TableBlock.tsx` exports `TableView`/`TableEditor` -- a different block kind (`kind: 'table'`), raw contentEditable cells, unrelated to collection-view. `CollectionTableView` must NOT be named `TableView`/`TableBlock` and must live in its own `collectionTable/` subfolder.

BoardBlock.tsx current signature and onChange contract (unchanged by this plan except internal branching):
- `export function BoardBlock({ block, onChange }: { block: CollectionViewBlock; onChange?: (block: CollectionViewBlock) => void }): JSX.Element;`
- `onChange?.({ ...block, collectionId, viewId })` is the existing pattern (see the D-04 auto-provisioning effect, lines 97-100) for updating which view/collection the block points at -- ViewSwitcher must call this same `onChange` prop, not invent a second mechanism.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build CollectionTableView.tsx</name>
  <files>apps/workspaces/src/components/projectPage/collectionTable/CollectionTableView.tsx</files>
  <read_first>
    apps/workspaces/src/components/records/PropertyPanel.tsx (lines 46-55 -- schema-driven property rendering loop, the cell-semantics template)
    apps/workspaces/src/components/projectPage/TableBlock.tsx (lines 55-82 -- table/thead/tbody DOM structure and Tailwind class conventions ONLY; do not copy its Cell component's contentEditable pattern)
    apps/workspaces/src/lib/api/records.api.ts (CollectionRecord, CollectionPropertyDef, DataCollection shapes)
    apps/workspaces/src/lib/hooks/useRecords.ts (useUpdateAnyRecord signature)
    .planning/phases/25-view-ux-parity/25-UI-SPEC.md (Interaction Contract row "Table view (CollectionTableView, new)" -- columns = collection.schema in schema order, cells editable via PropertyField, header uses Label typography tier, empty state shares Board's copy)
  </read_first>
  <action>
    Create apps/workspaces/src/components/projectPage/collectionTable/CollectionTableView.tsx, a 'use client' component accepting `{ collection: DataCollection; records: CollectionRecord[]; updateRecord: UseMutationResult<CollectionRecord, unknown, { id: string; data: UpdateRecordInput }> }` (import `UseMutationResult` from `@tanstack/react-query`, `UpdateRecordInput` from `@/lib/api/records.api`). The `records` prop is the already-filtered-and-sorted array (BoardBlock computes this before passing down, same as it does for the board's columns) -- this component does not call applyFilters/applySorts itself.

    Render a `<table>` following TableBlock.tsx's CONTAINER_CLASS/HEADER_ROW_CLASS/CELL_CLASS Tailwind conventions (border/rounded container, `bg-gray-50 dark:bg-slate-800` header row with `text-[11px] font-bold uppercase tracking-wider text-gray-400` -- UI-SPEC actually specifies 12px semibold for Label tier; use `text-xs font-semibold uppercase tracking-wider` to match this phase's declared typography, not the pre-existing TableBlock's slightly different `text-[11px] font-bold`). `<thead>` renders one `<th>` per `collection.schema` entry (`property.name`), in schema order (no reordering UI this phase, per D-05's discretion note). `<tbody>` renders one `<tr>` per record in `records`, with `key={record.id}`; each `<td>` renders `<PropertyField property={property} value={record.props[property.id]} onCommit={(v) => updateRecord.mutate({ id: record.id, data: { props: { ...record.props, [property.id]: v } } })} />` -- editable, not read-only (table view is a first-class editing surface per UI-SPEC, unlike the card-face preview). If `records.length === 0`, render the same empty-state copy pair used by the board ("No records match these filters" / "Try removing a filter condition or adjusting a value to see more records.") centered in the table body area, matching BoardShellError's text styling.

    Export as `export function CollectionTableView(...)`.
  </action>
  <acceptance_criteria>
    - Component file is at apps/workspaces/src/components/projectPage/collectionTable/CollectionTableView.tsx (new subfolder, not projectPage/ root)
    - Exported component name is CollectionTableView, not TableView or TableBlock (grep confirms no naming collision: grep -c "export function TableView" in this file returns 0)
    - Table columns are derived from collection.schema (one <th> per schema property, in schema order)
    - Each cell renders via <PropertyField property=... value=... onCommit=... /> wired to the updateRecord mutation passed in as a prop (not a new mutation hook created inside this component)
    - Zero records after filtering renders the shared empty-state copy, not a blank table
    - cd apps/workspaces and npx tsc --noEmit reports no new errors in this file
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep "CollectionTableView.tsx" || echo "no errors in CollectionTableView.tsx"</automated>
  </verify>
  <done>CollectionTableView.tsx exists in its own collectionTable/ folder, compiles, renders collection.schema as columns with editable PropertyField cells, and does not collide with the existing TableBlock.tsx naming.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build ViewSwitcher.tsx</name>
  <files>apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx</files>
  <read_first>
    apps/workspaces/src/lib/hooks/useRecords.ts (useViews, useCreateView signatures, lines 81-88 and 105-111)
    apps/workspaces/src/components/projectPage/BoardBlock.tsx (lines 97-100 -- the existing recordsApi.createView(...).then((view) => onChange?.({ ...block, collectionId, viewId: view.id })) pattern to mirror for the create-path)
    apps/workspaces/src/components/records/PropertyField.tsx (lines 111-146 -- PersonField popover shell, template for this control's visual chrome if rendered as a dropdown; UI-SPEC calls for a segmented control instead, see below)
    .planning/phases/25-view-ux-parity/25-CONTEXT.md (D-05 -- switching changes which collection_views row/viewId the block points at; never re-materializes collection_records)
    .planning/phases/25-view-ux-parity/25-RESEARCH.md (Open Question 1 -- create-once-then-remember recommendation)
    .planning/phases/25-view-ux-parity/25-UI-SPEC.md (Interaction Contract row "View switcher (D-05)" -- segmented control, LayoutGrid/Table2 icons, primary-600 accent for selected state)
  </read_first>
  <action>
    Create apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx, a 'use client' component accepting `{ collectionId: string; currentView: CollectionView; onSwitch: (viewId: string) => void }` (import `CollectionView` from `@/lib/api/records.api`). Internally call `useViews(collectionId)` (existing hook) and `useCreateView(collectionId)` (existing hook).

    Render a segmented control: two buttons, "Board" (`LayoutGrid` icon from lucide-react) and "Table" (`Table2` icon from lucide-react), side by side in a single rounded container (border, `rounded-lg`, matching the toolbar's existing button sizing -- 32px min height). The button matching `currentView.type` is visually selected (`bg-primary-600 text-white` or equivalent per the UI-SPEC's accent-for-selected-state rule); the other is plain (`text-gray-500`/`hover:bg-gray-100`).

    On clicking the non-selected button (target type `t`, either `'board'` or `'table'`): if `t === currentView.type`, no-op. Otherwise, look up `useViews(collectionId).data?.find((v) => v.type === t)`. If found, call `onSwitch(found.id)` directly (no API call -- reuses the existing sibling view row). If not found, call `useCreateView(collectionId).mutateAsync({ name: t === 'board' ? 'Board' : 'Table', type: t, config: {} })` and, on resolution, call `onSwitch(created.id)` (this is the "create-once" half -- once created, it will be found by the lookup on every subsequent switch, satisfying "then-remember"). This create-or-reuse logic must never call `createRecord`/`updateRecord` -- confirm no record-mutation import exists in this file.

    Export as `export function ViewSwitcher(...)`.
  </action>
  <acceptance_criteria>
    - Component exports ViewSwitcher accepting { collectionId, currentView, onSwitch }
    - Renders exactly two buttons (Board/Table) with LayoutGrid/Table2 icons; the one matching currentView.type shows the primary-600 accent as selected
    - Switching to a type with an existing sibling view (found via useViews(collectionId)) calls onSwitch(found.id) with NO createView call
    - Switching to a type with no existing sibling view calls useCreateView(collectionId).mutateAsync({ type: t, ... }) exactly once, then calls onSwitch(created.id)
    - File contains no import of createRecord/useCreateRecord or any record-mutation hook (grep confirms: grep -c "createRecord" ViewSwitcher.tsx returns 0)
    - cd apps/workspaces and npx tsc --noEmit reports no new errors in this file
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep "ViewSwitcher.tsx" || echo "no errors in ViewSwitcher.tsx"</automated>
  </verify>
  <done>ViewSwitcher.tsx exists, compiles, and implements create-once-then-remember switching between board and table views without ever creating or duplicating collection_records.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Branch BoardBlock.tsx render on view.type and mount ViewSwitcher</name>
  <files>apps/workspaces/src/components/projectPage/BoardBlock.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/BoardBlock.tsx (full file, current state after Plans 25-02/25-03's edits)
    apps/workspaces/src/components/projectPage/collectionTable/CollectionTableView.tsx (this plan's Task 1 output)
    apps/workspaces/src/components/projectPage/board/ViewSwitcher.tsx (this plan's Task 2 output)
    apps/workspaces/src/lib/projectPage/registry.tsx (lines 247-250 -- confirms entry() takes exactly one component per block kind; BoardBlock must branch internally, registry.tsx is NOT touched by this task)
  </read_first>
  <action>
    In BoardBlock.tsx, import `CollectionTableView` from `./collectionTable/CollectionTableView` and `ViewSwitcher` from `./board/ViewSwitcher`. Mount `<ViewSwitcher collectionId={block.collectionId as string} currentView={view} onSwitch={(viewId) => onChange?.({ ...block, viewId })} />` in the same header row as `FilterSortToolbar`/`ViewSettingsMenu` (from Plans 25-02/25-03) -- position it first in that row (leftmost), per the UI-SPEC's "next to the Filter/Sort/... toolbar" placement.

    Branch the body render on `view.type`: keep the existing `<DndContext>`/`columns.map(...)`/`<AddColumnControl>` JSX as the `view.type !== 'table'` (board) path; add a second path rendering `<CollectionTableView collection={collection} records={sorted} updateRecord={updateRecord} />` when `view.type === 'table'` (reusing the same `filtered`/`sorted` derived arrays and the same `updateRecord = useUpdateAnyRecord(...)` instance already computed earlier in the component -- do not create a second mutation hook instance). Do NOT rename the `BoardBlock` export or touch `registry.tsx` -- `entry()` takes exactly one component per block kind/context, confirmed unchanged.
  </action>
  <acceptance_criteria>
    - BoardBlock.tsx imports and renders ViewSwitcher in its toolbar header row, positioned before FilterSortToolbar/ViewSettingsMenu
    - BoardBlock.tsx renders <CollectionTableView ... /> when view.type === 'table' and the existing DndContext/board-columns JSX otherwise
    - The same updateRecord (useUpdateAnyRecord) instance is passed to CollectionTableView -- no second useUpdateAnyRecord(...) call is added
    - registry.tsx is unmodified (git diff shows no changes to that file from this task)
    - The BoardBlock export name and its { block, onChange } props signature are unchanged
    - cd apps/workspaces and npx tsc --noEmit reports no new errors in BoardBlock.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit 2>&1 | grep "BoardBlock.tsx" || echo "no errors in BoardBlock.tsx"</automated>
  </verify>
  <done>BoardBlock.tsx branches its render on view.type, mounts ViewSwitcher in its toolbar, and switching view types shows the correct render path without any new collection_records being created -- verified by manual/live smoke test (no FE test runner available per 25-RESEARCH.md).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> `POST /api/v1/records/collections/:id/views` | Sibling-view creation on first switch flows through the existing authenticated, company-scoped createView endpoint (already used by Phase 24's board provisioning) -- no new backend surface |
| Browser -> `PATCH /api/v1/records/records/:id` | Table view's editable cells flow through the existing `useUpdateAnyRecord` hook, same endpoint BoardCard/BoardColumn already use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-25-07 | Tampering | ViewSwitcher's createView call | accept | Same existing `POST /collections/:id/views` endpoint already used by BoardBlock's D-04 auto-provisioning effect, gated by `authenticateToken` + `company_id` scoping in `records.repository.ts` -- no new validation gap |
| T-25-08 | Tampering | CollectionTableView cell edits | accept | Reuses the existing `useUpdateAnyRecord` mutation and `PropertyField` input controls already proven safe in `PropertyPanel`/`BoardCard` -- no new data-access path, no new endpoint |
| T-25-09 | Repudiation / Elevation of Privilege | Duplicate-record risk on view switching | mitigate | ViewSwitcher's create-or-reuse logic never calls `createRecord`/`useCreateRecord` by construction (enforced via the Task 2 acceptance criterion grepping for absence of that import) -- switching view type only ever creates a `collection_views` row, never a `collection_records` row |
| T-25-SC | Tampering | npm installs | accept | Zero new packages this plan |
</threat_model>

<verification>
- `cd apps/workspaces && npx tsc --noEmit` passes with no new errors in the three touched/created files.
- Manual/live smoke test: switch a board to Table view, confirm the same records render as rows with editable cells; switch back to Board, confirm columns/cards are unchanged and no new `collection_records` were created (verify via `listRecords` count before/after, or via `listViews` showing exactly 2 views for the collection after the first switch, not growing on repeated toggles).
</verification>

<success_criteria>
- VIEWX-04 satisfied: user can switch a `collection-view` block between Board and Table view types on the same collection without creating duplicate `collection_records`, and the switch is remembered (no new `collection_views` row created on repeated toggling back and forth).
- All four VIEWX-01..04 requirements are now fully shipped across Plans 25-01 through 25-04.
</success_criteria>

<output>
Create `.planning/phases/25-view-ux-parity/25-04-SUMMARY.md` when done
</output>
