---
phase: 06-credit-risk-kpi-evaluator-semaphore
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workspaces/src/components/projectPage/PodTrackerBlock.tsx
autonomous: true
requirements: [RSK-02, RSK-03]

must_haves:
  truths:
    - "Selecting an over-limit client in the assign-load dropdown shows a red frosted-glass warning in the form immediately, before the dispatcher clicks Create"
    - "The warning is purely visual client-side UX — submitting anyway still hits the existing backend 403 from assertCreditOk(), which remains the sole enforcement path"
    - "The on-submit 403 error message (create.isError) is restyled with the same frosted-glass red treatment as the pre-submit warning, so both look like one consistent semaphore"
    - "PodTrackerBlock.tsx reads client data via useCrm's useClients, not useBilling's — the stale pre-Phase-1 import is gone"
  artifacts:
    - path: "apps/workspaces/src/components/projectPage/PodTrackerBlock.tsx"
      provides: "Frosted-glass over-limit warning shown pre-submit and on 403, sourced from useCrm client data"
      contains: "backdrop-blur"
  key_links:
    - from: "apps/workspaces/src/components/projectPage/PodTrackerBlock.tsx"
      to: "apps/workspaces/src/lib/hooks/useCrm.ts"
      via: "useClients import"
      pattern: "from '@/lib/hooks/useCrm'"
---

<objective>
Add the dispatcher-facing red "frosted glass" credit-risk semaphore (RSK-02) to the load-assignment form in `PodTrackerBlock.tsx` — the only load-assignment UI in the app — shown as soon as an over-limit client is selected in the dropdown, computed client-side from data already fetched via `useClients`. Confirm and preserve that the existing backend 403 (`assertCreditOk`) remains the sole enforcement path (RSK-03) — this plan is UI-only, no new backend call, no new override path. Fold in the pre-existing `useBilling` → `useCrm` stale-hook fix (D-09) since this phase is already editing this exact file.

Purpose: Today `PodTrackerBlock.tsx` shows the 403 as plain red text only after a failed submit attempt — there is no warning before that point, and the component reads client data through the wrong (pre-CRM-domain) hook. This plan closes both gaps in one pass.
Output: `PodTrackerBlock.tsx` updated with a `useClients` import from `useCrm`, an inline over-limit computation reusing `CrmClientsBlock.tsx`'s existing utilization logic, and a frosted-glass warning component shown both pre-submit and on 403.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-credit-risk-kpi-evaluator-semaphore/06-CONTEXT.md

@apps/workspaces/src/components/projectPage/PodTrackerBlock.tsx
@apps/workspaces/src/components/projectPage/CrmClientsBlock.tsx
@apps/workspaces/src/lib/hooks/useCrm.ts
@apps/workspaces/src/lib/hooks/useBilling.ts
@apps/workspaces/src/lib/api/crm.api.ts
</context>

<interfaces>
<!-- Existing shapes this plan must respect -->

From apps/workspaces/src/components/projectPage/PodTrackerBlock.tsx (current — the file being edited):
```tsx
import { useClients } from '@/lib/hooks/useBilling'; // WRONG — swap to useCrm
// ...
export function PodTrackerView({ block }: { block: PodTrackerBlockType }) {
  const { data: clients } = useClients();
  const [clientId, setClientId] = useState('');
  // ... form with <select value={clientId} onChange={...}>
  {create.isError && (
    <p className="text-[11px] text-red-600 dark:text-red-400">{(create.error as Error)?.message ?? 'Could not create the request.'}</p>
  )}
}
```

From apps/workspaces/src/lib/hooks/useCrm.ts (the correct hook — already default-exports the right shape):
```typescript
export function useClients() {
  const { user } = useAuth();
  return useQuery({ queryKey: qk.clients, queryFn: crmApi.listClients, enabled: !!user?.company_id });
}
```
Returns `CrmClient[]` (from `crm.api.ts`): `{ id, company_id, name, country, vat_id, email, credit_limit, outstanding_balance, default_rate_eur, notes, address, responsible_employee_id, created_at, updated_at }` — a superset of `useBilling`'s client shape (same `credit_limit`/`outstanding_balance`/`name`/`country` fields the form already uses), so the swap is a drop-in replacement for every field `PodTrackerBlock.tsx` currently reads.

From apps/workspaces/src/components/projectPage/CrmClientsBlock.tsx (`CreditBar`, lines 17-37 — reuse this exact over-limit logic, do not reinvent):
```tsx
const pct = client.credit_limit > 0 ? Math.min(100, (client.outstanding_balance / client.credit_limit) * 100) : 100;
const over = client.outstanding_balance >= client.credit_limit;
```

From apps/workspaces/src/lib/api/crm.api.ts (`CrmClient` type — confirms field names to use):
```typescript
export interface CrmClient {
  id: string; company_id: string; name: string; country: string; vat_id: string | null; email: string | null;
  credit_limit: number; outstanding_balance: number; default_rate_eur: number | null; notes: string | null;
  address: string | null; responsible_employee_id: string | null; created_at: string; updated_at: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Swap to useCrm + add frosted-glass over-limit semaphore</name>
  <files>apps/workspaces/src/components/projectPage/PodTrackerBlock.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/PodTrackerBlock.tsx
    apps/workspaces/src/components/projectPage/CrmClientsBlock.tsx
    apps/workspaces/src/lib/hooks/useCrm.ts
  </read_first>
  <action>
    Change the import at the top of `PodTrackerBlock.tsx` from `import { useClients } from '@/lib/hooks/useBilling';` to `import { useClients } from '@/lib/hooks/useCrm';` and remove the now-unused `import type { PodRequest } from '@/lib/api/pod.api';`-adjacent `useBilling` reference if any other symbol from `useBilling` was imported (there is only the one `useClients` import in this file — confirm no other `useBilling` symbols are used before removing the import line entirely).
    Inside `PodTrackerView`, after the existing `const { data: clients } = useClients();` line, add a derived value: `const selectedClient = (clients ?? []).find((c) => c.id === clientId);` and `const isOverLimit = !!selectedClient && selectedClient.outstanding_balance >= selectedClient.credit_limit;` (exact same `>=` comparison `CreditBar` uses in `CrmClientsBlock.tsx:19`, so the semaphore and the existing dashboard bar never disagree on what counts as "over limit").
    Add a new local component in the same file (mirroring the file's existing pattern of small helper components like `Column`/`PendingCard`/`DeliveredCard` defined below the main export): `function CreditWarningBanner({ clientName }: { clientName: string })` rendering a frosted-glass panel: `<div className="mb-2 rounded-lg border border-red-300/50 dark:border-red-500/30 bg-red-50/70 dark:bg-red-950/40 backdrop-blur-sm px-3 py-2 flex items-center gap-2"><AlertTriangle className="w-4 h-4 text-red-600 dark:text-red-400 flex-shrink-0" /><p className="text-xs font-semibold text-red-700 dark:text-red-300">{clientName} is over their credit limit — this load will be blocked.</p></div>` (import `AlertTriangle` from `lucide-react` alongside the file's existing icon imports).
    In the `showForm` block, immediately after the client `<select>` element (before the rate input row), render `{isOverLimit && selectedClient && <CreditWarningBanner clientName={selectedClient.name} />}` so the warning appears the instant an over-limit client is selected, before the dispatcher touches Create.
    Restyle the existing on-submit error block (`{create.isError && (...)}`) to use the same frosted-glass treatment instead of plain text: replace the `<p className="text-[11px] text-red-600 dark:text-red-400">...</p>` with a `<div>` using the identical classes as `CreditWarningBanner` (`border-red-300/50 dark:border-red-500/30 bg-red-50/70 dark:bg-red-950/40 backdrop-blur-sm`) wrapping the existing message text and an `AlertTriangle` icon, keeping the existing fallback message `(create.error as Error)?.message ?? 'Could not create the request.'`.
    Do not add any new API call, hook, or client-side blocking of the Create button — `isOverLimit` only controls whether the warning renders; the button remains clickable and the existing `assertCreditOk` 403 (unchanged, untouched by this plan) is what actually blocks the assignment server-side.
  </action>
  <verify>
    <automated>cd apps/workspaces && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from '@/lib/hooks/useBilling'" apps/workspaces/src/components/projectPage/PodTrackerBlock.tsx` returns 0.
    - `grep -c "from '@/lib/hooks/useCrm'" apps/workspaces/src/components/projectPage/PodTrackerBlock.tsx` returns 1.
    - `grep -c "backdrop-blur" apps/workspaces/src/components/projectPage/PodTrackerBlock.tsx` returns at least 2 (pre-submit warning + restyled on-submit error).
    - `grep -c "outstanding_balance >= .*\.credit_limit" apps/workspaces/src/components/projectPage/PodTrackerBlock.tsx` returns at least 1 (matches CreditBar's exact over-limit comparison).
    - `npx tsc --noEmit` (run from `apps/workspaces`) exits 0.
    - Manual/browser check: selecting an over-limit client (one whose outstanding_balance >= credit_limit in seeded/demo data) shows the red frosted-glass banner before clicking Create; clicking Create anyway still surfaces the 403 in the same frosted-glass style.
  </acceptance_criteria>
  <done>PodTrackerBlock.tsx imports useClients from useCrm (not useBilling); selecting an over-limit client shows a red frosted-glass warning immediately; the on-submit 403 error uses the same visual treatment; the existing assertCreditOk 403 is untouched and remains the sole enforcement path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side over-limit warning -> backend enforcement | The frontend warning is purely advisory; it reads the same client-visible credit_limit/outstanding_balance already exposed via GET /crm/clients, and must not be mistaken for or replace the backend's assertCreditOk() 403 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | Client-side isOverLimit check | accept | This is a UX warning only — a dispatcher bypassing or spoofing the client-side check (e.g. via devtools) still hits the unchanged, untouched backend assertCreditOk() 403 on submit; no new trust is placed in the client-side computation |
</threat_model>

<verification>
Run `npx tsc --noEmit` in `apps/workspaces` to confirm no type regressions. Manually load the app, open a project page with a POD tracker block, select a client known to be over their credit limit (or create one via the CRM dashboard with a small credit limit and a large outstanding balance), and confirm the frosted-glass warning appears before submit, and the 403 on submit is styled consistently.
</verification>

<success_criteria>
- Dispatcher sees a red frosted-glass visual warning before assigning a load to an over-limit client (RSK-02).
- The existing hard 403 block remains completely untouched and is the only real enforcement path — this plan adds zero new blocking logic (RSK-03).
- PodTrackerBlock.tsx reads client data through the correct, current CRM domain hook (D-09 stale-hook fix folded in).
</success_criteria>

<output>
Create `.planning/phases/06-credit-risk-kpi-evaluator-semaphore/06-02-SUMMARY.md` when done
</output>
