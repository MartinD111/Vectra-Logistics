---
phase: 24-board-view-legacy-kanban-migration
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - apps/workspaces/src/lib/projectPage/blocks.ts
  - apps/workspaces/src/lib/projectPage/registry.tsx
  - apps/workspaces/src/lib/projectPage/slashMenu.ts
  - apps/workspaces/src/components/projectPage/BoardBlock.tsx
  - apps/workspaces/src/components/projectPage/board/BoardColumn.tsx
  - apps/workspaces/src/components/projectPage/board/BoardCard.tsx
  - apps/workspaces/src/components/projectPage/board/AddColumnControl.tsx
autonomous: true
requirements: [BOARD-01, BOARD-02, BOARD-03]

must_haves:
  truths:
    - "Inserting a new Board block via the slash menu auto-provisions a data_collection (Title text property, Status select property with To Do/In Progress/Done options) and a board-type view, with zero picker UI (D-04)"
    - "A collection-view page block renders one column per live option value of the view's groupBy select property — never hand-authored columns"
    - "User can drag a card to a different column (its groupBy prop value updates) and reorder cards within a column (sort_order updates), committed via a single PATCH /records/:id call"
    - "An empty column (zero cards, true for every brand-new board's default 3 columns per D-04) is still a valid drop target — dragging a card into it succeeds"
    - "A '+ New' control in a column creates a card pre-set to that column's groupBy value, entering an inline-editable title state on the card face (not navigating away)"
    - "A '+ Add column' control appends a new select option to the groupBy property's schema; column headers are inline-renamable; deleting a non-empty column is blocked"
  artifacts:
    - path: "apps/workspaces/src/lib/projectPage/blocks.ts"
      provides: "CollectionViewBlock interface + 'collection-view' PageBlockKind member + PAGE_BLOCK_REGISTRY entries (kanban hidden, collection-view visible)"
      contains: "kind: 'collection-view'"
    - path: "apps/workspaces/src/lib/projectPage/registry.tsx"
      provides: "'collection-view' registry entry wired to BoardBlock"
      contains: "'collection-view': entry('collection-view'"
    - path: "apps/workspaces/src/components/projectPage/BoardBlock.tsx"
      provides: "board renderer: D-04 provisioning effect, column grouping, DndContext root"
      min_lines: 60
    - path: "apps/workspaces/src/components/projectPage/board/BoardColumn.tsx"
      provides: "single column: useDroppable column-level drop target, SortableContext for card reordering, inline rename, blocked delete, + New control"
      min_lines: 30
    - path: "apps/workspaces/src/components/projectPage/board/BoardCard.tsx"
      provides: "useSortable card with inline-editable title on creation, click-to-open"
      min_lines: 30
    - path: "apps/workspaces/src/components/projectPage/board/AddColumnControl.tsx"
      provides: "+ Add column control, appends a select option via schema PATCH"
      min_lines: 15
  key_links:
    - from: "apps/workspaces/src/lib/projectPage/registry.tsx"
      to: "apps/workspaces/src/components/projectPage/BoardBlock.tsx"
      via: "'collection-view' entry renderer/editor"
      pattern: "BoardBlock block=\\{block as CollectionViewBlock\\}"
    - from: "apps/workspaces/src/components/projectPage/BoardBlock.tsx"
      to: "apps/workspaces/src/lib/hooks/useRecords.ts"
      via: "useCollection/useView/useRecords/useCreateCollection/useCreateView/useUpdateAnyRecord"
      pattern: "useCollection\\(|useRecords\\(|useView\\("
    - from: "apps/workspaces/src/components/projectPage/board/BoardColumn.tsx"
      to: "@dnd-kit/core useDroppable"
      via: "column-level droppable region distinct from per-card useSortable registrations"
      pattern: "useDroppable\\(\\{\\s*id:\\s*column\\.id"
    - from: "apps/workspaces/src/components/projectPage/board/BoardCard.tsx"
      to: "apps/workspaces/src/app/collections/[collectionId]/records/[recordId]/page.tsx"
      via: "window.open on click"
      pattern: "window\\.open\\(`/collections/"
---

<objective>
Build the real drag-and-drop board: a new `collection-view` page block that renders live columns from a select property's option values, supports cross-column drag + within-column reorder via `@dnd-kit/sortable` (genuinely new ground in this codebase), lets a user add/rename columns and create cards inline within a column, and auto-provisions sensible defaults when inserted fresh (no "pick a collection" picker). This satisfies BOARD-01, BOARD-02, and BOARD-03 in full.

Purpose: Turn Phase 22's schema-driven records/collections/views API into a real, usable Trello-style board UI — the first UI consumer of that data model besides Phase 23's record detail page.
Output: `collection-view` block kind registered end-to-end (type, registry, slash menu), a working `BoardBlock` + `board/` component family with drag-and-drop (including empty-column drop targets), column management, and inline card creation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-board-view-legacy-kanban-migration/24-CONTEXT.md
@.planning/phases/24-board-view-legacy-kanban-migration/24-RESEARCH.md
@.planning/phases/24-board-view-legacy-kanban-migration/24-PATTERNS.md
@.planning/phases/24-board-view-legacy-kanban-migration/24-UI-SPEC.md
</context>

<interfaces>
<!-- Types and hooks this plan consumes, from Plan 24-01 (already shipped) and existing code. -->

From `apps/workspaces/src/lib/api/records.api.ts` (extended by Plan 24-01 — `UpdateRecordInput` now includes `sort_order?: number` alongside `props?`/`body?`):
```typescript
export interface CollectionPropertyDef {
  id: string; name: string;
  type: 'text' | 'number' | 'date' | 'select' | 'multi-select' | 'checkbox'
    | 'person' | 'url' | 'email' | 'phone' | 'files' | 'relation';
  options?: { id: string; label: string }[];
}
export interface DataCollection { id: string; company_id: string; project_id: string | null; name: string; schema: CollectionPropertyDef[]; created_by: string | null; created_at: string; updated_at: string; }
export interface CollectionRecord { id: string; company_id: string; collection_id: string; parent_record_id: string | null; props: Record<string, unknown>; body: Record<string, unknown>; sort_order: number; created_by: string | null; created_at: string; updated_at: string; }
export interface CollectionView { id: string; collection_id: string; name: string; type: string; config: Record<string, unknown>; created_at: string; updated_at: string; }
export interface UpdateRecordInput { props?: Record<string, unknown>; body?: Record<string, unknown>; sort_order?: number; }
```

From `apps/workspaces/src/lib/hooks/useRecords.ts` (extended by Plan 24-01) — use these, do not hand-roll `apiFetch`:
```typescript
useCollection(id): { data: DataCollection | undefined, isLoading, isError }
useRecords(collectionId): { data: CollectionRecord[] | undefined, isLoading, isError }
useView(id): { data: CollectionView | undefined, isLoading, isError }
useUpdateCollectionSchema(id): UseMutationResult<DataCollection, unknown, CollectionPropertyDef[]>
useCreateCollection(): UseMutationResult<{ collection: DataCollection; view: CollectionView }, unknown, CreateCollectionInput>
useCreateView(collectionId): UseMutationResult<CollectionView, unknown, CreateViewInput>
useCreateRecord(collectionId): UseMutationResult<CollectionRecord, unknown, CreateRecordInput>
useUpdateAnyRecord(collectionId): UseMutationResult<CollectionRecord, unknown, { id: string; data: UpdateRecordInput }>
```

From `apps/workspaces/src/lib/projectPage/blocks.ts` (existing, read before editing):
```typescript
export type PageBlockKind = /* ...existing members... */ | 'kanban' | /* insert 'collection-view' here */;
interface PageBlockBase { id: string; kind: PageBlockKind; span: BlockSpan; }
export const uid = (): string => Math.random().toString(36).slice(2, 10);
```

From `apps/workspaces/src/components/projectPage/SubPageBlock.tsx` — the exact create-on-mount async-provisioning pattern to clone for D-04 (placeholder `null` id -> effect creates real resource -> `onUpdate` swaps it in):
```typescript
const creatingRef = useRef(false);
useEffect(() => {
  if (block.pageId !== null) return;
  if (creatingRef.current) return;
  creatingRef.current = true;
  projectsApi.createPage(...).then((created) => onUpdate({ ...block, pageId: created.id })).catch(() => setFailed(true));
}, [block.pageId, ctx.projectId]);
```

From `apps/workspaces/src/app/(routes)/dispatch/page.tsx` — `@dnd-kit/core` sensor/DndContext wiring to reuse verbatim (lines 553-555, 596-601):
```typescript
const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }));
<DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={handleDragEnd}>
```

From `@dnd-kit/core`'s `useDroppable` — needed in `BoardColumn.tsx` because `SortableContext`'s `items` array is empty for a column with zero cards (true for every brand-new board's default columns, D-04); an empty `SortableContext` registers no `useSortable` element, so there is nothing for `@dnd-kit` to hit-test a drop against unless the column container itself is also a droppable region:
```typescript
import { useDroppable } from '@dnd-kit/core';
const { setNodeRef, isOver } = useDroppable({ id: column.id });
// setNodeRef goes on the column's card-list container div (the SAME div that wraps <SortableContext>),
// distinct from each card's own useSortable registration in BoardCard.tsx.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Register collection-view block kind + provisioning + static board rendering</name>
  <files>apps/workspaces/src/lib/projectPage/blocks.ts, apps/workspaces/src/lib/projectPage/registry.tsx, apps/workspaces/src/lib/projectPage/slashMenu.ts, apps/workspaces/src/components/projectPage/BoardBlock.tsx</files>
  <read_first>
    apps/workspaces/src/lib/projectPage/blocks.ts
    apps/workspaces/src/lib/projectPage/registry.tsx
    apps/workspaces/src/lib/projectPage/slashMenu.ts
    apps/workspaces/src/components/projectPage/SubPageBlock.tsx
    apps/workspaces/src/components/projectPage/KanbanBlock.tsx
    apps/workspaces/src/lib/hooks/useRecords.ts
  </read_first>
  <action>
    In `blocks.ts`: insert `'collection-view'` into the `PageBlockKind` union immediately after `'kanban'` (line ~40). Add a new interface immediately after `KanbanBlock` (line ~244):
    `export interface CollectionViewBlock extends PageBlockBase { kind: 'collection-view'; title?: string; collectionId: string | null; viewId: string | null; }`
    — `collectionId`/`viewId` are `null` in the placeholder state before D-04's provisioning effect runs, mirroring `SubPageBlock.pageId`'s convention exactly. Add `CollectionViewBlock` to the `PageBlock` union (line ~381, alongside `KanbanBlock`).

    In `PAGE_BLOCK_REGISTRY` (line ~561-572): change the existing `kanban` entry's `available: true` to `available: false` (hides it from the palette; the entry itself stays registered so already-inserted legacy blocks keep rendering/migrating — do not delete or rename this entry, do not touch its `create()`). Immediately after it, add a new entry: `{ kind: 'collection-view', group: 'widget', title: 'Board', icon: 'Kanban', description: 'A drag-and-drop board grouped by a select property, backed by a real data collection.', available: true, create: () => ({ id: uid(), kind: 'collection-view', span: 'full', title: 'Board', collectionId: null, viewId: null }) }`.

    In `slashMenu.ts`'s `EXTRA_KEYWORDS` map, add: `'collection-view': ['board', 'columns', 'cards', 'view', 'database']`.

    In `registry.tsx`: import `CollectionViewBlock` from `./blocks` and `BoardBlock` from `@/components/projectPage/BoardBlock`. Add to the `entries` Record: `'collection-view': entry('collection-view', ({ block, ctx }) => <BoardBlock block={block as CollectionViewBlock} onChange={ctx.onChange} />, ({ block, onUpdate }) => <BoardBlock block={block as CollectionViewBlock} onChange={onUpdate} />)`.

    Create `apps/workspaces/src/components/projectPage/BoardBlock.tsx` (new file, `'use client'`). Export `BoardBlock({ block, onChange }: { block: CollectionViewBlock; onChange?: (block: CollectionViewBlock) => void })`. At the top of the component body, construct both mutation hooks unconditionally (Rules of Hooks — hooks are never called conditionally or nested inside effects/callbacks): `const createCollection = useCreateCollection();` and `const createView = useCreateView(block.collectionId ?? '');` (the empty-string fallback is only used as `useCreateView`'s construction-time argument for its own `onSuccess` cache-invalidation key while `block.collectionId` is still `null` — harmless, since nothing subscribes to that placeholder key). Then implement the D-04 provisioning effect as a `useEffect` that only SEQUENCES the two mutations' `.mutateAsync(...)` calls — it does not call the hooks themselves (clone `SubPageBlockEditor`'s `creatingRef`/`useEffect` shape exactly): when `block.collectionId === null`, call `createCollection.mutateAsync({ name: block.title ?? 'Board', schema: [ { id: uid(), name: 'Title', type: 'text' }, { id: statusId, name: 'Status', type: 'select', options: [{ id: uid(), label: 'To Do' }, { id: uid(), label: 'In Progress' }, { id: uid(), label: 'Done' }] } ] })` (capture `statusId = uid()` before building schema so it can be reused for the view's `groupBy`), then await `createView.mutateAsync({ name: 'Board', type: 'board', config: { groupBy: statusId } })`, then `onChange?.({ ...block, collectionId: collection.id, viewId: view.id })`. Guard with a ref (`creatingRef = useRef(false)`) so the mutateAsync sequence fires exactly once even under React StrictMode double-invoke. Show a `Loader2`-spinner "Creating board…" state (same shell/copy convention as `SubPageBlockEditor`'s "Creating page…") while `collectionId === null`.

    When `block.collectionId` and `block.viewId` are both set: call `useCollection(block.collectionId)`, `useView(block.viewId)`, `useRecords(block.collectionId)`. Show the loading spinner while any is loading; show the UI-SPEC two-line error shell ("Couldn't load this board." / "Check your connection and try again.") if any errors. Once loaded, derive the groupBy property: `collection.schema.find((p) => p.id === view.config.groupBy)`. Write a pure `groupRecordsByColumn(records, schema, groupByPropId)` helper (module-level function, not a hook) that maps the property's `options` array into `{ id, title: label, cards }` objects, filtering `records` where `props[groupByPropId] === option.id`, sorted by `sort_order` ascending — this is the exact function from RESEARCH.md's "Grouping records into board columns" code example. Render the `saas-card !p-4` wrapper (block.title if set, `text-sm font-bold` per UI-SPEC) containing a `flex gap-3 overflow-x-auto pb-1` row, one `w-56 flex-shrink-0 rounded-lg bg-gray-50 dark:bg-slate-800/60 p-2` column div per group (inline for this task — no drag yet, no `BoardColumn`/`BoardCard` sub-components yet), each card rendered as a `rounded-md bg-white dark:bg-slate-900 border border-gray-200 dark:border-slate-700 px-2.5 py-2 text-sm shadow-sm` div showing `record.props[collection.schema[0].id]` as its title (per Phase 23's D-01 convention: title = first schema property) with an `onClick={() => window.open(\`/collections/${block.collectionId}/records/${record.id}\`, '_blank', 'noopener,noreferrer')}` handler. This task satisfies BOARD-01 (live columns, no hand-authoring) and D-04 (auto-provisioning) but not yet drag-and-drop or column/card creation controls — those are Task 2 and Task 3.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    <source>blocks.ts: `PageBlockKind` union includes `'collection-view'`; `CollectionViewBlock` interface exists with `collectionId: string | null` and `viewId: string | null`; `PageBlock` union includes `CollectionViewBlock`</source>
    <source>PAGE_BLOCK_REGISTRY: the `kanban` entry has `available: false`; a new `collection-view` entry exists with `available: true` and `create()` returning `collectionId: null, viewId: null`</source>
    <source>registry.tsx: `entries['collection-view']` is defined and its renderer/editor both reference `BoardBlock`</source>
    <source>BoardBlock.tsx: `useCreateCollection()` and `useCreateView(...)` are called at the top level of the component body (unconditionally, not inside the `useEffect` or any callback); only `.mutateAsync(...)` calls on the resulting mutation objects appear inside the ref-gated `useEffect`</source>
    <source>BoardBlock.tsx: contains a `useEffect` gated by a ref that only fires when `block.collectionId === null`, sequencing `createCollection.mutateAsync` then `createView.mutateAsync` then `onChange`</source>
    <behavior>Inserting a "Board" block from the slash menu shows "Creating board…" briefly, then renders 3 columns (To Do / In Progress / Done) with zero cards, no picker UI ever shown</behavior>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </acceptance_criteria>
  <done>collection-view block kind fully registered end-to-end; BoardBlock renders live columns from real collection/view/records data with D-04 auto-provisioning; kanban hidden from palette but still renders for existing pages.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Drag-and-drop — cross-column move + within-column reorder + empty-column drop target</name>
  <files>apps/workspaces/src/components/projectPage/BoardBlock.tsx, apps/workspaces/src/components/projectPage/board/BoardColumn.tsx, apps/workspaces/src/components/projectPage/board/BoardCard.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/BoardBlock.tsx
    apps/workspaces/src/app/(routes)/dispatch/page.tsx
    apps/workspaces/src/lib/hooks/useRecords.ts
  </read_first>
  <behavior>
    - Dragging a card from column A to column B and dropping fires exactly one `useUpdateAnyRecord(collectionId).mutate({ id: cardId, data: { props: { [groupByPropId]: columnB.id }, sort_order: dropIndex } })`.
    - Dragging a card to a different position within the same column fires the same mutation with an unchanged groupBy prop but a new `sort_order` matching the drop index.
    - A card that is not dragged (pointer moves less than the 8px activation distance) triggers its `onClick` (open in new tab) instead of a drag.
    - Dragging a card into a column that currently has zero cards succeeds — the column's card-list container is itself a registered `useDroppable` region (`id: column.id`), independent of any per-card `useSortable` registration, so `over.id` resolves to the column even when `SortableContext`'s `items` array is empty.
  </behavior>
  <action>
    Extract the inline column/card markup from Task 1's `BoardBlock.tsx` into two new files. `apps/workspaces/src/components/projectPage/board/BoardCard.tsx`: export `BoardCard({ record, titlePropId }: { record: CollectionRecord; titlePropId: string })`, using `useSortable({ id: record.id })` from `@dnd-kit/sortable` (not `useDraggable` — within-column reordering requires the sortable primitive) with `CSS.Transform.toString(transform)` + `transition` from `@dnd-kit/utilities`, spreading `{...attributes} {...listeners}` on the card's root div (whole-card drag surface, no separate grip icon, per UI-SPEC). Keep the same visual shell and click-to-open handler from Task 1.

    `apps/workspaces/src/components/projectPage/board/BoardColumn.tsx`: export `BoardColumn({ column, titlePropId }: { column: { id: string; title: string; cards: CollectionRecord[] }; titlePropId: string })`. Import `useDroppable` from `@dnd-kit/core` in addition to `SortableContext`/`verticalListSortingStrategy` from `@dnd-kit/sortable`. Call `const { setNodeRef, isOver } = useDroppable({ id: column.id })` — this registers the COLUMN ITSELF as a drop target, distinct from and in addition to each card's own `useSortable` registration in `BoardCard.tsx`; this is required because when `column.cards` is empty (true for every brand-new board's default columns per D-04), `<SortableContext items={[]}>` has no sortable children for `@dnd-kit` to hit-test against, so without this column-level `useDroppable` a card can never be dropped into an empty column. Attach `ref={setNodeRef}` to the card-list container div (the SAME div that wraps `<SortableContext>` — not a separate wrapper), and apply a visual drop-hover affordance when `isOver` is true (e.g. `ring-2 ring-blue-400` or equivalent per UI-SPEC's existing hover conventions). Wrap the card list in `<SortableContext items={column.cards.map((c) => c.id)} strategy={verticalListSortingStrategy}>` and map `column.cards` to `<BoardCard key={card.id} record={card} titlePropId={titlePropId} />`. Keep the same `w-56 flex-shrink-0 rounded-lg bg-gray-50 dark:bg-slate-800/60 p-2` shell, header row (title + count badge), from Task 1.

    In `BoardBlock.tsx`: wrap the column row in a single board-root `<DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={handleDragEnd}>`, using the exact sensor config from `dispatch/page.tsx` (`useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }))`). Implement `handleDragEnd(event: DragEndEvent)`: find `active.id` (the dragged record's id) and locate its record in the current `records` array; determine the target column by finding which column's `cards` array (post drag, from `over.id`) contains `over.id` as a card id, OR — when `over.id` matches a `column.id` directly (the column-level `useDroppable` region fired, meaning the card was dropped on/into a column rather than on another card, which is the ONLY path for an empty column) — use that column directly as the target; compute the target index within that column's cards (position of the matching card's id in that column's `cards` array when `over.id` was a card id, or `0` when `over.id` was a column id, since dropping directly on an empty/near-empty column places the card at the top). Call `updateRecord.mutate({ id: activeId, data: { props: { [groupByPropId]: targetColumnId }, sort_order: targetIndex } })` via `useUpdateAnyRecord(block.collectionId!)`. Render `<BoardColumn key={col.id} column={col} titlePropId={collection.schema[0].id} />` per column inside the `DndContext`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    <source>BoardCard.tsx uses `useSortable` from `@dnd-kit/sortable`, not `useDraggable` from `@dnd-kit/core`</source>
    <source>BoardColumn.tsx calls `useDroppable({ id: column.id })` from `@dnd-kit/core`, distinct from the per-card `useSortable` calls in BoardCard.tsx, and attaches its `setNodeRef` to the same container div that wraps `<SortableContext>`</source>
    <source>BoardColumn.tsx wraps its card list in `<SortableContext items={...} strategy={verticalListSortingStrategy}>`</source>
    <source>BoardBlock.tsx's `handleDragEnd` resolves the target column via either a matching card id inside a column's `cards` array OR a direct match against `column.id` (the empty-column case) — grep confirms both branches exist</source>
    <source>BoardBlock.tsx's `handleDragEnd` calls `updateRecord.mutate` (or `.mutateAsync`) exactly once per drag end, passing both `props` (groupBy) and `sort_order` in one call — not two separate PATCH calls</source>
    <source>BoardBlock.tsx's `DndContext` uses `PointerSensor` with `activationConstraint: { distance: 8 }`, matching `dispatch/page.tsx`'s existing sensor config</source>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </acceptance_criteria>
  <done>Cards are draggable between and within columns, including into columns with zero cards via the column-level useDroppable region; drag end commits exactly one PATCH with both the new groupBy value and sort_order; click-to-open still works when not dragging.</done>
</task>

<task type="auto">
  <name>Task 3: Column management (add/rename/blocked-delete) + inline card creation</name>
  <files>apps/workspaces/src/components/projectPage/board/AddColumnControl.tsx, apps/workspaces/src/components/projectPage/board/BoardColumn.tsx, apps/workspaces/src/components/projectPage/board/BoardCard.tsx</files>
  <read_first>
    apps/workspaces/src/components/projectPage/board/BoardColumn.tsx
    apps/workspaces/src/components/projectPage/board/BoardCard.tsx
    apps/workspaces/src/components/records/AddPropertyModal.tsx
    apps/workspaces/src/components/records/PropertyField.tsx
    apps/workspaces/src/lib/hooks/useRecords.ts
  </read_first>
  <action>
    Create `apps/workspaces/src/components/projectPage/board/AddColumnControl.tsx` (new file). Export `AddColumnControl({ collection, groupByPropId }: { collection: DataCollection; groupByPropId: string })`. Render as a narrow `w-56 flex-shrink-0` ghost column (`border-2 border-dashed border-gray-200 dark:border-slate-700 rounded-lg`, centered, per UI-SPEC) that on click becomes an inline text input for the new column label; on blur/Enter with non-empty text, call `useUpdateCollectionSchema(collection.id).mutateAsync(nextSchema)` where `nextSchema = collection.schema.map((prop) => prop.id === groupByPropId ? { ...prop, options: [...(prop.options ?? []), { id: uid(), label: trimmedLabel }] } : prop)` — clone the append-option state shape from `AddPropertyModal.tsx`'s `addOption`. This is the "+ Add column" control (D-05); the schema PATCH must fully resolve (`await`) before the control clears/resets, per the two-sequential-request convention (no dependent record write here, but the schema write itself must not be treated as fire-and-forget).

    In `BoardColumn.tsx`: add inline rename — clicking the column title turns it into a text `<input>` (same `text-xs font-semibold uppercase tracking-wider` weight per UI-SPEC) that on blur/Enter calls `useUpdateCollectionSchema` to PATCH the matching option's `label` only (never its `id` — mutate `collection.schema.find((p) => p.id === groupByPropId).options.find((o) => o.id === column.id).label`, keeping every other field including `id` untouched, per RESEARCH.md Pitfall 4). Escape cancels without committing. Add a delete affordance (trash icon, `lucide-react`'s existing icon set) that appears on column-header hover: `disabled` with `opacity-40 cursor-not-allowed` whenever `column.cards.length > 0` (D-06 — do not implement reassignment logic, just block); when empty and clicked, show the destructive confirmation copy from UI-SPEC ("Delete column? This removes it from the board. Cards must be moved out first.") before removing the option from schema via the same `useUpdateCollectionSchema` mutation (filter out the matching option). Add a "+ New" control at the bottom of the card list (same visual weight as `AddColumnControl`'s trigger, `min-h-[32px]`) that on click calls `useCreateRecord(collectionId).mutate({ props: { [titlePropId]: '', [groupByPropId]: column.id } })` and, on success, marks the newly created record's id as "entering inline-edit" (local component state, e.g. `const [editingNewCardId, setEditingNewCardId] = useState<string | null>(null)`, set to the created record's id). Keep the column-level `useDroppable` region and its `setNodeRef`/`isOver` wiring from Task 2 unchanged — the "+ New" control and delete affordance are additive UI within the same column shell, not a replacement for the droppable container.

    In `BoardCard.tsx`: accept an optional `autoFocusEdit?: boolean` prop (or equivalent signal from the parent's `editingNewCardId === record.id` check). When true, render the title as an editable `<input autoFocus>` instead of static text — clone `PropertyField.tsx`'s `DebouncedTextField` 800ms-debounce/blur-flush shape, committing via `useUpdateAnyRecord(collectionId).mutate({ id: record.id, data: { props: { ...record.props, [titlePropId]: nextValue } } })`. Blur or Enter commits and exits edit mode (parent clears `editingNewCardId`); Escape exits edit mode WITHOUT deleting the record (it is already persisted) — leaving it titled "Untitled" (placeholder shown when the title prop is empty, per UI-SPEC) until edited again. The card must NOT auto-navigate to the record detail page on creation — stays on the board (D-07 explicit).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    <source>AddColumnControl.tsx exists, calls `useUpdateCollectionSchema` with a schema array that appends `{ id: uid(), label: trimmedLabel }` to the groupBy property's `options`</source>
    <source>BoardColumn.tsx's column-rename PATCH mutates only the matching option's `label` field — the option's `id` value in the constructed next-schema payload is identical to the original `column.id`, never regenerated</source>
    <source>BoardColumn.tsx's delete-column control has `disabled` (or equivalent `opacity-40 cursor-not-allowed` non-interactive state) whenever `column.cards.length > 0`</source>
    <source>BoardColumn.tsx still calls `useDroppable({ id: column.id })` and attaches `setNodeRef` to the card-list container after this task's edits — the empty-column drop target from Task 2 is preserved, not removed by the new column-management UI</source>
    <source>BoardCard.tsx's inline-edit input, on Escape, exits edit mode without calling any delete/remove mutation — grep confirms no `deleteRecord`/`removeRecord` call exists anywhere in board/ components</source>
    <behavior>Clicking "+ New" in a column creates a record with that column's groupBy value pre-set, the new card immediately shows an autofocused inline-editable title, and no navigation occurs</behavior>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </acceptance_criteria>
  <done>Users can add/rename columns and are blocked from deleting non-empty ones; inline "+ New" card creation pre-sets groupBy and enters inline-edit-on-card-face state without navigating away; the empty-column drop target from Task 2 remains functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (BoardBlock/BoardColumn/AddColumnControl) → API (records domain) | Client constructs full next-`schema` arrays and `props`/`sort_order` payloads sent as PATCH bodies; server validates types but not select-option-id membership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Tampering | Column rename (`BoardColumn.tsx`) | mitigate | Rename mutation always preserves the option's existing `id`, only replaces `label` — prevents an accidental id-drift that would silently orphan every card in that column (RESEARCH.md Pitfall 4) |
| T-24-04 | Denial of Service (client-side) | `AddColumnControl.tsx` schema PATCH | accept | Backend already caps `schema.length` at 100 properties (`CreateCollectionSchema`/`UpdateCollectionSchema` `.max(100)`, Phase 22 T-22-01) — repeated column-adds are bounded by that existing server-side limit, no new mitigation needed |
| T-24-05 | Tampering | Drag-and-drop `PATCH /records/:id` (props + sort_order) | accept | Same company-scoped ownership check as every other record write (Phase 22, inherited) — a user can only move cards within collections/records their own `company_id` owns; cross-tenant moves are already rejected server-side |

No new backend endpoints are introduced this plan — all requests go through Phase 22's already-validated, already-company-scoped routes.
</threat_model>

<verification>
Run `npx tsc --noEmit -p apps/workspaces/tsconfig.json` after each task. Manual dev-server pass: insert a new Board block (confirm 3 default columns, no picker), drag a card between columns and reload (confirm persisted), drag a card into an empty column (confirm it succeeds, not just non-empty columns), reorder within a column and reload (confirm persisted), add a column, rename a column, attempt to delete a non-empty column (confirm blocked), click "+ New" (confirm inline-edit title, no navigation).
</verification>

<success_criteria>
- BOARD-01: `collection-view` block renders columns purely from `collection.schema[groupByPropId].options`, never hand-authored.
- BOARD-02: drag between/within columns — including into empty columns via the column-level `useDroppable` region — commits one PATCH with both `props` and `sort_order`.
- BOARD-03: "+ New" creates a card pre-set to its column, entering inline-edit without navigation.
- D-04, D-05, D-06, D-07 all implemented per 24-CONTEXT.md.
- `npx tsc --noEmit -p apps/workspaces/tsconfig.json` passes with zero errors after every task.
</success_criteria>

<output>
Create `.planning/phases/24-board-view-legacy-kanban-migration/24-02-SUMMARY.md` when done
</output>
</output>
