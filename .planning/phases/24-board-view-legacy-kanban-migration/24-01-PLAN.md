---
phase: 24-board-view-legacy-kanban-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workspaces/src/lib/api/records.api.ts
  - apps/workspaces/src/lib/hooks/useRecords.ts
autonomous: true
requirements: [BOARD-01, BOARD-02, BOARD-03]

must_haves:
  truths:
    - "recordsApi exposes listCollections, createCollection, listRecords, listViews, createView, and getView functions that call the already-shipped Phase 22 endpoints"
    - "recordsApi.createRecord sends collection_id inside the POST body (not just the URL), so the backend's required-field validation (CreateRecordSchema) succeeds on first call"
    - "useRecords.ts exposes hooks to list collections/records/views, create a collection, create a view, and update an arbitrary record by id (not fixed at hook-construction time) with optimistic list-cache reconciliation"
  artifacts:
    - path: "apps/workspaces/src/lib/api/records.api.ts"
      provides: "extended recordsApi object (10 functions total) + CollectionView/CreateCollectionInput/CreateViewInput types"
      contains: "listCollections: ()"
    - path: "apps/workspaces/src/lib/hooks/useRecords.ts"
      provides: "useCollections, useRecords, useViews, useView, useCreateCollection, useCreateView, useUpdateAnyRecord hooks"
      contains: "useUpdateAnyRecord"
  key_links:
    - from: "apps/workspaces/src/lib/hooks/useRecords.ts"
      to: "apps/workspaces/src/lib/api/records.api.ts"
      via: "recordsApi.* calls inside queryFn/mutationFn"
      pattern: "recordsApi\\.(listCollections|createCollection|listRecords|listViews|createView|getView)"
    - from: "apps/workspaces/src/lib/api/records.api.ts createRecord"
      to: "apps/api/src/domains/records/dto/create-record.dto.ts CreateRecordSchema"
      via: "POST body must include collection_id"
      pattern: "collection_id:\\s*collectionId"
---

<objective>
Extend the Phase 22 records API client (`records.api.ts`) and hooks (`useRecords.ts`) with the list/create/get functions Phase 24's board view needs — none of which exist today (Phase 23 only needed single-collection/single-record reads). Also fix a latent bug in `createRecord`: the backend's `CreateRecordSchema` requires `collection_id` inside the request body, but the current client function never sends it, so the very first record creation call (migration, D-04 board provisioning, or inline card creation) would 400 with "Required" until this is fixed.

Purpose: Establish the data-layer contract every other Phase 24 plan (board rendering, drag-and-drop, legacy migration) depends on, so downstream plans consume typed, already-tested functions instead of hand-rolling `apiFetch` calls.
Output: `recordsApi` with 10 total functions (5 existing + 5 new + `getView`), `useRecords.ts` with 5 new hooks, `createRecord` bug fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-board-view-legacy-kanban-migration/24-CONTEXT.md
@.planning/phases/24-board-view-legacy-kanban-migration/24-RESEARCH.md
@.planning/phases/24-board-view-legacy-kanban-migration/24-PATTERNS.md
</context>

<interfaces>
<!-- Backend contract this plan's new client functions must match exactly. -->

From `apps/api/src/domains/records/records.routes.ts` (all already shipped, already tested per Phase 22's 22-VERIFICATION.md):
```
GET    /api/v1/records/collections               -> { collections: DataCollectionRow[] }
POST   /api/v1/records/collections                -> { collection, view }   (body: { name, schema })
GET    /api/v1/records/collections/:id/records    -> { records: CollectionRecordRow[] }
GET    /api/v1/records/collections/:id/views      -> { views: CollectionViewRow[] }
POST   /api/v1/records/collections/:id/views      -> { view }               (body: { name, type, config })
GET    /api/v1/records/views/:id                  -> { view }
```

From `apps/api/src/domains/records/dto/create-record.dto.ts` (`CreateRecordSchema`) — `collection_id` is REQUIRED in the body, the URL's `:id` param is never read by the controller:
```typescript
export const CreateRecordSchema = z.object({
  collection_id: z.string().uuid(),
  parent_record_id: z.string().uuid().nullable().optional(),
  props: z.record(z.string(), z.unknown()).optional(),
  body: PageConfigSchema.optional(),
});
```

From `apps/api/src/domains/records/dto/create-view.dto.ts` (`CreateViewSchema`) — `config` is intentionally opaque, `type` enum includes `'board'`:
```typescript
export const CreateViewSchema = z.object({
  name: z.string().min(1).max(160),
  type: z.enum(['board', 'table', 'calendar', 'gallery', 'list', 'timeline']).default('table'),
  config: z.record(z.string(), z.unknown()).optional(),
});
```

From `apps/api/src/domains/records/dto/create-collection.dto.ts` (`CreateCollectionSchema`) — only `name` + `schema`, NO `project_id` field (D-03: migrated/new collections stay company-wide, never project-scoped):
```typescript
export const CreateCollectionSchema = z.object({
  name: z.string().min(1).max(160),
  schema: z.array(PropertyDefSchema).max(100).optional(),
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend records.api.ts — list/create/get functions + createRecord bug fix</name>
  <files>apps/workspaces/src/lib/api/records.api.ts</files>
  <read_first>
    apps/workspaces/src/lib/api/records.api.ts
    apps/api/src/domains/records/records.routes.ts
    apps/api/src/domains/records/records.controller.ts
    apps/api/src/domains/records/dto/create-record.dto.ts
    apps/api/src/domains/records/dto/create-view.dto.ts
    apps/api/src/domains/records/dto/create-collection.dto.ts
  </read_first>
  <action>
    In `apps/workspaces/src/lib/api/records.api.ts`, add three new exported interfaces above the `BASE`/`recordsApi` block, following the existing interface style exactly:
    - `CollectionView { id: string; collection_id: string; name: string; type: string; config: Record<string, unknown>; created_at: string; updated_at: string }`
    - `CreateCollectionInput { name: string; schema: CollectionPropertyDef[] }` (no `project_id` field — backend's `CreateCollectionSchema` does not accept one; D-03 keeps collections company-wide)
    - `CreateViewInput { name: string; type: string; config: Record<string, unknown> }`

    Add these functions inside the `recordsApi` object literal, in the same `apiFetch<...>(...).then((r) => r.X)` style as the existing five:
    - `listCollections: () => apiFetch<{ collections: DataCollection[] }>(\`${BASE}/collections\`).then((r) => r.collections)`
    - `createCollection: (data: CreateCollectionInput) => apiFetch<{ collection: DataCollection; view: CollectionView }>(\`${BASE}/collections\`, 'POST', data)` — return the RAW `{ collection, view }` object unwrapped (caller needs both pieces; do not `.then((r) => r.collection)` here)
    - `listRecords: (collectionId: string) => apiFetch<{ records: CollectionRecord[] }>(\`${BASE}/collections/${collectionId}/records\`).then((r) => r.records)`
    - `listViews: (collectionId: string) => apiFetch<{ views: CollectionView[] }>(\`${BASE}/collections/${collectionId}/views\`).then((r) => r.views)`
    - `createView: (collectionId: string, data: CreateViewInput) => apiFetch<{ view: CollectionView }>(\`${BASE}/collections/${collectionId}/views\`, 'POST', data).then((r) => r.view)`
    - `getView: (id: string) => apiFetch<{ view: CollectionView }>(\`${BASE}/views/${id}\`).then((r) => r.view)`

    Fix the existing `createRecord` function: it currently POSTs `data: CreateRecordInput` (only `{ props?, body? }`) to `/collections/${collectionId}/records` without ever including `collection_id` in the payload. The backend's `CreateRecordSchema` requires `collection_id: z.string().uuid()` in the body — the controller never reads `req.params.id`. Change the call to send `{ ...data, collection_id: collectionId }` as the body so the required field is always present. Do not change the function's public signature (`createRecord(collectionId: string, data: CreateRecordInput)`) — only its internal request body construction.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    <source>`recordsApi` object in records.api.ts exports exactly these keys: getCollection, updateCollection, getRecord, updateRecord, createRecord, listCollections, createCollection, listRecords, listViews, createView, getView (11 total)</source>
    <source>createRecord's request body includes `collection_id: collectionId` — grep confirms `collection_id: collectionId` appears in the createRecord implementation</source>
    <source>createCollection's return type is `{ collection: DataCollection; view: CollectionView }` (not unwrapped) — no `.then((r) => r.collection)` chained on that call</source>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </acceptance_criteria>
  <done>All 6 new/fixed functions exist, typecheck passes, createRecord's body includes collection_id.</done>
</task>

<task type="auto">
  <name>Task 2: Extend useRecords.ts — list/create hooks + generic record-update mutation</name>
  <files>apps/workspaces/src/lib/hooks/useRecords.ts</files>
  <read_first>
    apps/workspaces/src/lib/hooks/useRecords.ts
    apps/workspaces/src/lib/api/records.api.ts
  </read_first>
  <action>
    In `apps/workspaces/src/lib/hooks/useRecords.ts`, extend the `qk` query-key object (currently just `collection`/`record`) with three new key factories: `collections: () => ['records-collections'] as const`, `records: (collectionId: string) => ['records-list', collectionId] as const`, `views: (collectionId: string) => ['records-views', collectionId] as const`.

    Add these hooks, following the exact `enabled: !!user?.company_id && !!id` guard convention already used by `useCollection`/`useRecord`:
    - `useCollections()` — `useQuery` on `qk.collections()`, `queryFn: () => recordsApi.listCollections()`, `enabled: !!user?.company_id`
    - `useRecords(collectionId: string)` — `useQuery` on `qk.records(collectionId)`, `queryFn: () => recordsApi.listRecords(collectionId)`, `enabled: !!user?.company_id && !!collectionId`
    - `useViews(collectionId: string)` — `useQuery` on `qk.views(collectionId)`, `queryFn: () => recordsApi.listViews(collectionId)`, `enabled: !!user?.company_id && !!collectionId`
    - `useView(id: string)` — `useQuery` on a new `qk.view = (id: string) => ['records-view', id] as const` key, `queryFn: () => recordsApi.getView(id)`, `enabled: !!user?.company_id && !!id`
    - `useCreateCollection()` — `useMutation`, `mutationFn: (data: CreateCollectionInput) => recordsApi.createCollection(data)`, no onSuccess cache write needed (caller uses the returned `{ collection, view }` directly)
    - `useCreateView(collectionId: string)` — `useMutation`, `mutationFn: (data: CreateViewInput) => recordsApi.createView(collectionId, data)`, `onSuccess: () => qc.invalidateQueries({ queryKey: qk.views(collectionId) })`

    Add a new `useUpdateAnyRecord(collectionId: string)` hook — the board needs to update many different record ids from one component (drag-and-drop across cards), unlike `useUpdateRecord(id)` which binds one fixed id at construction time. Signature: `useMutation({ mutationFn: ({ id, data }: { id: string; data: UpdateRecordInput }) => recordsApi.updateRecord(id, data), onSuccess: (updated, variables) => { qc.setQueryData(qk.record(variables.id), updated); qc.setQueryData(qk.records(collectionId), (prev: CollectionRecord[] | undefined) => prev ? prev.map((r) => r.id === updated.id ? updated : r) : prev); } })`. This patches both the single-record cache entry AND splices the updated record into the collection's records-list cache entry (`qk.records(collectionId)`) so the board's column grouping re-renders from the mutation response without an extra fetch.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    <source>qk object has 5 total key factories: collection, record, collections, records, views, view (6 actually — count includes original 2 plus 4 new)</source>
    <source>useUpdateAnyRecord's onSuccess updates both `qk.record(variables.id)` and splices into `qk.records(collectionId)` cache — grep confirms both `qc.setQueryData(qk.record(` and `qc.setQueryData(qk.records(` appear inside useUpdateAnyRecord's body</source>
    <behavior>Calling `useUpdateAnyRecord(collectionId).mutate({ id: recordX, data: { props: {...} } })` resolves to the updated CollectionRecord and does not require a component-level id at hook construction</behavior>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </acceptance_criteria>
  <done>All 7 new hooks exist (useCollections, useRecords, useViews, useView, useCreateCollection, useCreateView, useUpdateAnyRecord), typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (workspaces app) → API (records domain) | Client-constructed request bodies (collection schema, record props, view config) cross into `apps/api`'s already-validated Zod DTOs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | `recordsApi.listCollections`/`listRecords`/`listViews` | accept | Inherited from Phase 22 — every repository query is scoped by `company_id` server-side (verified in `22-VERIFICATION.md`); these new client functions call the same already-scoped endpoints, no new exposure surface |
| T-24-02 | Tampering | `recordsApi.createRecord` | mitigate | This task's fix ensures `collection_id` always comes from the caller-supplied `collectionId` parameter (server-trusted param, not raw user text), not an arbitrary client-supplied field name that could be spoofed |

No new endpoints are added this task — all mitigations for input validation, authentication, and company-scoping are inherited unchanged from Phase 22's already-shipped/tested backend.
</threat_model>

<verification>
Run `npx tsc --noEmit -p apps/workspaces/tsconfig.json` after each task — must exit 0. Manually confirm (read-only inspection, no live server needed) that `recordsApi.createRecord`'s constructed body includes `collection_id`.
</verification>

<success_criteria>
- `recordsApi` exports 11 functions total (5 pre-existing + 6 new/fixed), matching the Phase 22 backend contract exactly.
- `useRecords.ts` exports 5 pre-existing + 7 new hooks.
- `npx tsc --noEmit -p apps/workspaces/tsconfig.json` passes with zero errors.
- No plan in this phase's later waves needs to hand-roll an `apiFetch` call for collections/records/views — everything routes through `recordsApi`/`useRecords.ts`.
</success_criteria>

<output>
Create `.planning/phases/24-board-view-legacy-kanban-migration/24-01-SUMMARY.md` when done
</output>
