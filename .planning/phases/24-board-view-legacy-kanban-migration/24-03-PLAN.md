---
phase: 24-board-view-legacy-kanban-migration
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - apps/workspaces/src/lib/projectPage/kanbanMigration.ts
  - apps/workspaces/src/components/projectPage/KanbanMigrationGate.tsx
  - apps/workspaces/src/lib/projectPage/registry.tsx
autonomous: true
requirements: [BOARD-04]

must_haves:
  truths:
    - "Opening a page with an existing kanban block and making any edit (add/remove/move a card) auto-migrates it to a collection-view/board block, with every existing card and column preserved"
    - "Migration is silent and lazy — it never runs on page load/view, only on the first edit action, and never fires twice for the same block"
    - "A one-time toast ('Board upgraded to the new view engine.') appears exactly once when migration runs, auto-dismissing without user action"
    - "The drafts-kanban block kind is never touched by migration logic — only kind === 'kanban' blocks migrate"
  artifacts:
    - path: "apps/workspaces/src/lib/projectPage/kanbanMigration.ts"
      provides: "pure buildMigrationPlan(kanban) transform + migrateOnFirstEdit(kanban) async orchestration"
      contains: "export function buildMigrationPlan"
    - path: "apps/workspaces/src/components/projectPage/KanbanMigrationGate.tsx"
      provides: "editor-path wrapper: ref-gated migration trigger + one-time toast + block-kind swap"
      contains: "migratingRef"
  key_links:
    - from: "apps/workspaces/src/lib/projectPage/registry.tsx"
      to: "apps/workspaces/src/components/projectPage/KanbanMigrationGate.tsx"
      via: "'kanban' entry's editor arrow"
      pattern: "KanbanMigrationGate block=\\{block as KanbanBlock\\}"
    - from: "apps/workspaces/src/components/projectPage/KanbanMigrationGate.tsx"
      to: "apps/workspaces/src/lib/projectPage/kanbanMigration.ts"
      via: "migrateOnFirstEdit call on first onUpdate"
      pattern: "migrateOnFirstEdit\\("
    - from: "apps/workspaces/src/lib/projectPage/kanbanMigration.ts"
      to: "apps/workspaces/src/lib/api/records.api.ts"
      via: "recordsApi.createCollection / createView / createRecord sequential calls"
      pattern: "recordsApi\\.(createCollection|createView|createRecord)"
---

<objective>
Implement BOARD-04: auto-migrate any legacy `kanban` block to a `collection-view`/board on its first edit, with zero data loss and a one-time toast, without ever touching the page-load path or the unrelated `drafts-kanban` block kind.

Purpose: Every page authored before this milestone with a `kanban` block must keep working and gain the new database-backed board engine transparently, the moment a user interacts with it — matching `workspace-blocks.md`'s explicit "no flag day" migration contract.
Output: `kanbanMigration.ts` (pure transform + orchestration), `KanbanMigrationGate.tsx` (the ref-gated trigger + toast), `registry.tsx`'s `'kanban'` editor entry rewired to use the gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-board-view-legacy-kanban-migration/24-CONTEXT.md
@.planning/phases/24-board-view-legacy-kanban-migration/24-RESEARCH.md
@.planning/phases/24-board-view-legacy-kanban-migration/24-PATTERNS.md
@.planning/phases/24-board-view-legacy-kanban-migration/24-UI-SPEC.md
</context>

<interfaces>
<!-- Source shape being migrated (unchanged, do not edit these types). -->

From `apps/workspaces/src/lib/projectPage/blocks.ts`:
```typescript
export interface KanbanCard { id: string; text: string; }
export interface KanbanColumn { id: string; title: string; cards: KanbanCard[]; }
export interface KanbanBlock extends PageBlockBase { kind: 'kanban'; title?: string; columns: KanbanColumn[]; }
export interface CollectionViewBlock extends PageBlockBase { kind: 'collection-view'; title?: string; collectionId: string | null; viewId: string | null; }
export interface DraftsKanbanBlock extends PageBlockBase { kind: 'drafts-kanban'; title?: string; } // DO NOT TOUCH — structurally unrelated, derives columns from live shipment data
```

From `apps/workspaces/src/lib/api/records.api.ts` (Plan 24-01) / `apps/workspaces/src/lib/hooks/useRecords.ts` — use these directly, do not hand-roll `apiFetch`:
```typescript
recordsApi.createCollection(data: { name: string; schema: CollectionPropertyDef[] }): Promise<{ collection: DataCollection; view: CollectionView }>
recordsApi.createView(collectionId: string, data: { name: string; type: string; config: Record<string, unknown> }): Promise<CollectionView>
recordsApi.createRecord(collectionId: string, data: { props?: Record<string, unknown>; body?: Record<string, unknown> }): Promise<CollectionRecord>
```

From `apps/workspaces/src/lib/projectPage/registry.tsx` (current `'kanban'` entry, to be modified):
```typescript
'kanban': entry('kanban',
  ({ block, ctx }) => <KanbanBoardView block={block as KanbanBlock} onChange={ctx.onChange} />,
  ({ block, onUpdate }) => <KanbanBoardView block={block as KanbanBlock} onChange={onUpdate} />,
),
```
The **view-mode** (read-only) renderer must stay pointed at `KanbanBoardView` unchanged — migration only triggers from an edit action, never from viewing. Only the **editor** arrow (second argument) is rewired to `KanbanMigrationGate`.

From `apps/workspaces/src/app/profile/page.tsx` (lines 281-288) — toast visual pattern to reuse for D-02:
```typescript
<div className="flex items-center gap-3 p-4 rounded-xl text-sm border bg-green-50 dark:bg-green-900/20 border-green-200 dark:border-green-800 text-green-800 dark:text-green-300">
  <CheckCircle2 className="h-5 w-5 shrink-0" /> {msg.text}
</div>
```
Per UI-SPEC: positioned `fixed bottom-6 right-6 z-50`, auto-dismiss after 4000ms, no manual dismiss control, copy is exactly "Board upgraded to the new view engine." (do not rephrase).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure migration transform + orchestration function</name>
  <files>apps/workspaces/src/lib/projectPage/kanbanMigration.ts</files>
  <read_first>
    apps/workspaces/src/lib/projectPage/blocks.ts
    apps/workspaces/src/lib/api/records.api.ts
    apps/workspaces/src/components/records/AddPropertyModal.tsx
  </read_first>
  <behavior>
    - `buildMigrationPlan(kanban)` given a `KanbanBlock` with 2 columns (one with 2 cards, one with 0 cards) returns a plan whose `schema` has exactly 2 properties (`Title` text, `Status` select with 2 options), whose `statusOptions[i].id` is IDENTICAL to `kanban.columns[i].id` (never a freshly generated id — RESEARCH.md Pitfall 4), and whose `records` array has exactly 2 entries (matching the 2 total cards across both columns), each with `sort_order` reset to its position within its own column (0-based, not global).
    - `buildMigrationPlan` is a pure function: calling it twice with the same input produces two structurally-equal plans except for the freshly generated `titlePropId`/`statusPropId` (random per call) — it performs zero network/API calls.
    - `migrateOnFirstEdit(kanban)` awaits `createCollection` before calling `createView`, and awaits `createView` before issuing any `createRecord` calls (two-sequential-request rule — schema-defining calls must resolve before any call that references ids from that schema).
  </behavior>
  <action>
    Create `apps/workspaces/src/lib/projectPage/kanbanMigration.ts` (new file). Export `buildMigrationPlan(kanban: KanbanBlock)`: generate `titlePropId = uid()` and `statusPropId = uid()` (import `uid` from `./blocks`). Build `statusOptions = kanban.columns.map((col) => ({ id: col.id, label: col.title }))` — MUST reuse `col.id` as the option's `id`, never derive a new id from `col.title` (Pitfall 4: this makes column rename in the new board free/stable, since grouping matches by id not label). Build `schema: CollectionPropertyDef[] = [{ id: titlePropId, name: 'Title', type: 'text' }, { id: statusPropId, name: 'Status', type: 'select', options: statusOptions }]`. Build `records = kanban.columns.flatMap((col) => col.cards.map((card, cardIdx) => ({ props: { [titlePropId]: card.text, [statusPropId]: col.id }, sort_order: cardIdx })))` — `sort_order` resets per-column (0-based within each column, matching the card's original position in `col.cards`). Return `{ titlePropId, statusPropId, schema, records }`.

    Export `async function migrateOnFirstEdit(kanban: KanbanBlock): Promise<CollectionViewBlock>` (import `CollectionViewBlock` type and `recordsApi` from `../api/records.api`). Call `buildMigrationPlan(kanban)`, then `const { collection } = await recordsApi.createCollection({ name: kanban.title ?? 'Board', schema: plan.schema })`, then `const view = await recordsApi.createView(collection.id, { name: 'Board', type: 'board', config: { groupBy: plan.statusPropId } })`, then `await Promise.all(plan.records.map((r) => recordsApi.createRecord(collection.id, r)))` (card creates can run in parallel — they don't depend on each other, only on the already-resolved collection/view). Return `{ id: kanban.id, kind: 'collection-view', span: kanban.span, title: kanban.title, collectionId: collection.id, viewId: view.id }`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    <source>buildMigrationPlan's returned `schema[1].options` array has `id` values that exactly match `kanban.columns[i].id` for every `i` — no `uid()` call inside the `statusOptions` map</source>
    <source>buildMigrationPlan contains zero references to `recordsApi`, `apiFetch`, `fetch`, or any async/await — verified by grep showing no network-call identifiers in the function body</source>
    <source>migrateOnFirstEdit awaits `createCollection` and destructures/uses its `collection.id` before calling `createView`, and awaits `createView` before the `Promise.all(...createRecord...)` line — sequential ordering confirmed by reading top-to-bottom statement order</source>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </acceptance_criteria>
  <done>buildMigrationPlan is pure and testable, correctly preserves column identity via option id reuse; migrateOnFirstEdit orchestrates the 3 API calls in the correct sequential order and returns a valid CollectionViewBlock.</done>
</task>

<task type="auto">
  <name>Task 2: Migration gate component + one-time toast + registry wiring</name>
  <files>apps/workspaces/src/components/projectPage/KanbanMigrationGate.tsx, apps/workspaces/src/lib/projectPage/registry.tsx</files>
  <read_first>
    apps/workspaces/src/lib/projectPage/kanbanMigration.ts
    apps/workspaces/src/lib/projectPage/registry.tsx
    apps/workspaces/src/components/projectPage/KanbanBlock.tsx
    apps/workspaces/src/components/projectPage/SubPageBlock.tsx
    apps/workspaces/src/app/profile/page.tsx
  </read_first>
  <action>
    Create `apps/workspaces/src/components/projectPage/KanbanMigrationGate.tsx` (new file, `'use client'`). Export `KanbanMigrationGate({ block, onUpdate }: { block: KanbanBlock; onUpdate: (block: PageBlock) => void })`. This component renders `<KanbanBoardView block={block} onChange={handleChange} />` where `handleChange` is a wrapper around the legacy block's own `onChange` (which currently just mutates `columns` locally via `onUpdate`). Guard migration with a `migratingRef = useRef(false)` (module pattern cloned from `SubPageBlockEditor`'s `creatingRef`): the FIRST time `handleChange` fires (i.e., the first edit — add/remove/move card), check `if (migratingRef.current) return` then set `migratingRef.current = true`, call `await migrateOnFirstEdit(nextKanbanBlock)` (import from `../lib/projectPage/kanbanMigration`, where `nextKanbanBlock` is the block AFTER applying the user's edit — the migration must preserve the edit the user just made, not just the pre-edit state), then call `onUpdate(migratedBlock)` (swapping the block's `kind` to `'collection-view'` in the same call site) and set local state to show the one-time toast. On migration failure (network error), keep rendering the legacy `KanbanBoardView` with the local edit still applied (do not lose the user's in-progress edit), reset `migratingRef.current = false` so the next edit retries, and show no toast.

    Render the toast conditionally (local `const [showToast, setShowToast] = useState(false)`): a `fixed bottom-6 right-6 z-50` div using the `bg-green-50 dark:bg-green-900/20 border-green-200 dark:border-green-800 text-green-800 dark:text-green-300` success-banner classes (cloned from `profile/page.tsx`), containing a `CheckCircle2` icon (`lucide-react`) and the exact copy "Board upgraded to the new view engine." — set a `setTimeout(() => setShowToast(false), 4000)` on mount of the toast state, no manual dismiss button.

    In `registry.tsx`: import `KanbanMigrationGate` from `@/components/projectPage/KanbanMigrationGate`. Change ONLY the `'kanban'` entry's editor arrow (second argument to `entry(...)`) from `({ block, onUpdate }) => <KanbanBoardView block={block as KanbanBlock} onChange={onUpdate} />` to `({ block, onUpdate }) => <KanbanMigrationGate block={block as KanbanBlock} onUpdate={onUpdate} />`. Leave the renderer (first argument, view-mode) untouched — it must keep using `KanbanBoardView` directly, since migration must never fire from a read-only view. Do NOT modify the `'drafts-kanban'` entry — confirm it is untouched by this task's diff.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    <source>KanbanMigrationGate.tsx contains a ref (e.g. `migratingRef`) checked-and-set synchronously before the first `migrateOnFirstEdit` call — grep confirms `migratingRef.current` appears at least twice (a read-check and a write-set)</source>
    <source>registry.tsx: the `'kanban'` entry's editor arrow references `KanbanMigrationGate`; the renderer (view-mode) arrow still references `KanbanBoardView` directly, unchanged</source>
    <source>registry.tsx: the `'drafts-kanban'` entry is byte-identical to its state before this task (no accidental match/edit) — grep confirms `DraftsKanbanView` still appears exactly where it did in the pre-task read</source>
    <source>Toast copy is the exact string "Board upgraded to the new view engine." — no rephrasing, no punctuation changes</source>
    <behavior>Opening a page with an existing kanban block and viewing it (no edit) does NOT trigger migration — the block stays kind:'kanban' and renders via the unchanged KanbanBoardView renderer</behavior>
    <behavior>Making a single edit (e.g. adding a card) to an existing kanban block triggers exactly one migration: the block swaps to kind:'collection-view', the toast appears once and auto-dismisses after 4000ms, and the newly added card is present among the migrated records</behavior>
    <automated>npx tsc --noEmit -p apps/workspaces/tsconfig.json</automated>
  </acceptance_criteria>
  <done>Legacy kanban blocks migrate exactly once, only on first edit (never on view), with the user's triggering edit preserved in the migrated data, a one-time auto-dismissing toast, and drafts-kanban left completely untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (KanbanMigrationGate) → API (records domain) | Client orchestrates 3 sequential/parallel writes (createCollection, createView, N×createRecord) with no server-side transaction wrapping them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06 | Tampering | `KanbanMigrationGate`'s migration trigger | mitigate | `migratingRef` synchronous ref-gate prevents a fast typist from double-firing `migrateOnFirstEdit` before the first async chain resolves and the block kind swaps (RESEARCH.md Pitfall 2 — duplicate `data_collections` rows) |
| T-24-07 | Denial of Service (partial failure) | `migrateOnFirstEdit`'s multi-call sequence | mitigate | On any step's failure, the legacy block stays `kind: 'kanban'` (never partially swapped) and `migratingRef` resets so the next edit retries — no orphaned `collection-view` block pointing at a nonexistent/incomplete collection |
| T-24-08 | Repudiation | Migration is silent/automatic (D-01) | accept | Explicit locked decision (D-01/D-02) — a one-time toast is the only user-facing signal, by design; no audit-log requirement was scoped into this phase's requirements |

No new backend endpoints are introduced — all writes go through Phase 22's already-validated, already-company-scoped routes (`createCollection`/`createView`/`createRecord`).
</threat_model>

<verification>
Run `npx tsc --noEmit -p apps/workspaces/tsconfig.json` after each task. Manual dev-server pass: open a page with an existing `kanban` block, confirm no migration on load; add/move/remove a card, confirm exactly one migration (toast appears once, block swaps to collection-view, all original cards + the triggering edit present with correct column/status mapping); open a page with a `drafts-kanban` block, confirm it is completely unaffected.
</verification>

<success_criteria>
- BOARD-04: legacy kanban blocks auto-migrate to collection-view on first edit, zero data loss, one-time toast, drafts-kanban untouched.
- D-01, D-02, D-03 (from 24-CONTEXT.md) fully implemented.
- `npx tsc --noEmit -p apps/workspaces/tsconfig.json` passes with zero errors after every task.
- All 4 phase requirements (BOARD-01..04) are now satisfied across plans 24-01, 24-02, 24-03.
</success_criteria>

<output>
Create `.planning/phases/24-board-view-legacy-kanban-migration/24-03-SUMMARY.md` when done
</output>
