---
phase: 8
plan: 08-01
wave: 1
depends_on: [07-01]
requirements: [RND-01, RND-03]
files_modified:
  - apps/workspaces/src/components/projectPage/pageBlockViews.tsx
  - apps/workspaces/src/components/projectPage/PageBlockView.tsx
  - apps/workspaces/src/lib/projectPage/registry.tsx
autonomous: true
---

# Phase 8 — Page Read-Rendering → Registry

**Goal:** `PageBlockView` dispatches through `pageBlockRegistry`; the 30-case switch is gone; output and persisted JSON unchanged. (RND-01, RND-03)

## Tasks
- **08-01-01** Extract the per-kind local view functions + helpers out of `PageBlockView.tsx` into new `components/projectPage/pageBlockViews.tsx` (exported); add `DividerView`. Avoids the registry↔PageBlockView import cycle (registry imports views; PageBlockView imports registry).
- **08-01-02** Repoint each `pageBlockRegistry` entry's `renderer` at its specific view via adapter arrows mapping `PageCtx`→props, mirroring the old switch (`projectId as string`, `onChange`, `clientId`).
- **08-01-03** Reduce `PageBlockView.tsx` to `return <>{pageBlockRegistry.render(block, { projectId, clientId, onChange })}</>`. External signature unchanged → `DragOverlay`, `PageView`, `BlockEditor` default arm need no edits.

## Verification
- `npx tsc --noEmit` → exit 0; `npx next build` → exit 0 (incl. `/projects/[id]/pages/[pageId]`, `/records/[clientId]`, `/programs/[id]`).
- `grep -c "switch (block.kind)" PageBlockView.tsx` → 0.

## must_haves
- No `switch(block.kind)` in `PageBlockView.tsx`.
- Every kind renders via the registry; no persisted-JSON change.
