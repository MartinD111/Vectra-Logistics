{
  "name": "@vectra/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Shared auth: AuthProvider, useAuth, cookie-based SSO session, RequireRole/RequireWorkspace guards.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "peerDependencies": {
    "react": "^18",
    "react-dom": "^18"
  },
  "dependencies": {
    "@vectra/types": "*"
  }
}
