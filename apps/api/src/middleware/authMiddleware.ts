// LEGACY — re-exports from core/auth/middleware. Do not add new logic here.
export { authenticateToken, requireRole, requireVerified } from '../core/auth/middleware';
export type { AuthRequest } from '../core/auth/middleware';
