// Workspace Engine — public surface. Generic block-plugin contract + registry.
// Domains (project pages, mini programs) build a registry instance from their
// own block type and render context; see lib/projectPage/registry.tsx and
// lib/miniProgram/registry.tsx.

export * from './types';
export * from './registry';
